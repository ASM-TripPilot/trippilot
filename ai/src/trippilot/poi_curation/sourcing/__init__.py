"""m7/sourcing — TourAPI 소싱 파이프라인 (TRIP-246, U6-05).

한국관광공사 TourAPI(공공데이터, 저장 자유)에서 지역·타입별 POI를 수집해
수집 게이트(5단 — 스키마·실재·중복·신뢰·정책) 통과분을 **등록 제안 JSON**으로
직렬화한다.

**INV-1 부기**: 이 파이프라인의 산출물은 "등록 제안"일 뿐이다 — POI 정본은
backend C7(place-data)이 단일 소유하므로(PR #76), 산출물이 후보 풀에 직접
들어가지 않는다. 백엔드 정본 등록을 거친 뒤에만 closed-set 후보가 된다.
"""
