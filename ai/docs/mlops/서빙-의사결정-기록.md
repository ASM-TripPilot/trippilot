# 서빙 의사결정 기록 — 무엇을 재고, 무엇을 안 쓰기로 했나

**2026-09-26 · 대상 기간 2026-09-21~26 · 근거 PR [#657](https://github.com/ASM-TripPilot/trippilot/pull/657)**

이 문서 하나만 읽으면 "이 서비스의 모델 서빙을 어떻게 결정했는가"가 끝나도록 쓴다. 수치는
전부 재현 명령과 함께 적고, **못 잰 것은 못 쟀다고 적는다.**

---

## 1. 한 줄 요약

자체 호스팅 모델 2종을 놓고 서빙 엔진·오토스케일링·캐시를 검토해, **셋 중 하나만 켜고
둘은 근거를 남기고 접었다.** 접은 쪽의 근거가 이 문서의 본론이다.

---

## 2. 대상 — 우리가 가중치를 들고 있는 것

| | M-1 임베딩 (KURE-v1) | M-2 리마인드 문구 (파인튜닝 Qwen3-4B) |
|---|---|---|
| 호출 | 사용자 요청 경로, **직렬 3회/요청** | 발송 며칠 전 배치, 여행 1건당 몇 회 |
| 지연 요구 | 요청 예산 안(상한 5초) | 없음 — 콜드스타트 수 분 허용 |
| 상주 | 항상 | **0 으로 내려가도 됨** |
| 실패 시 | 검색 강등 | 백엔드 상수 문구로 폴백 |
| 현재 서빙 | 프로세스 안 CPU | AWS Bedrock Custom Model Import |

**이 표가 모든 결정을 가른다.** 동시성 압력이 없는 워크로드에 동시성을 위한 도구를 붙이면
비용만 남는다.

---

## 3. 결정 — 채택 1, 기각 1, 보류 2

| # | 검토 대상 | 결정 | 근거 |
|---|---|---|---|
| D-1 | 임베딩 정확 캐시 | **채택·가동** | 닫힌 질의 28가지가 프로세스 수명 내내 반복됐다 |
| D-2 | Triton Inference Server | **기각** | 이득이 서버가 아니라 엔진 몫이었다 (§4) |
| D-3 | 인프로세스 ONNX Runtime | 구현·기본 꺼짐 | amd64 13~15% 확인. 컨테이너 실측 후 기본값 전환 |
| D-4 | 오토스케일링(HPA) | 구현·기본 꺼짐 | `metrics-server` 부재 — 켜면 안 되는 이유가 §5 |

---

## 4. D-2 Triton 기각 — 판단이 두 번 뒤집힌 기록

### ① 첫 측정은 "3.5배 느림" 이었다

| 백엔드 | 지연(38건 인코딩) | 비 |
|---|---:|---:|
| 현행 sentence-transformers | 983 ms | 1.00× |
| Triton (ONNX Runtime) | 3,453 ms | 3.51× |

여기서 멈췄으면 "Triton 은 느리다"로 끝났을 것이다. **그런데 서버측 분해가 그 결론과
맞지 않았다** — queue 1.8ms · compute_input 0.05ms · compute_output 0.58ms 로 Triton 자신의
몫은 소수점이고 `compute_infer` 만 3,673ms 였다. 전송도 직렬화도 아니고 순수 연산이다.
즉 느린 것은 **서버가 아니라 그 안의 엔진이 도는 환경**이다.

### ② 교란을 제거하니 순위가 뒤집혔다

같은 ORT 버전·같은 그래프·같은 사전 토크나이즈 입력·같은 4스레드로 2×2 를 쟀다.

| | 네이티브 macOS | 컨테이너 linux/arm64 |
|---|---:|---:|
| torch | 992 ms | 2,406 ms |
| onnxruntime | 767 ms | **3,845 ms** |

**Docker Desktop VM 벌점이 백엔드마다 달랐다**(torch 2.42× · ORT 5.01×). 네이티브에서는
ORT 가 1.29배 빠르고 컨테이너에서는 1.60배 느리다. 개발 맥 숫자로는 어느 쪽도 결론이 안
된다는 뜻이고, 그래서 **배포 대상 아키텍처(amd64)에서 다시 쟀다.**

### ③ amd64 에서는 ONNX 가 이긴다

| 측정 | 현행 | ONNX Runtime | 비 |
|---|---:|---:|---:|
| 1차 | 8,652 ms | 7,538 ms | 0.87× |
| 2차 | 9,636 ms | 8,226 ms | 0.85× |

동등성은 세 환경 전부에서 통과했다 — **문서벡터 코사인 최소 1.00000, top-4 순위 6/6 질의
24/24 위치 동일.** 재적재 없이 전환 가능하다는 뜻이다.

### 결론

이득은 **엔진(ONNX Runtime) 몫이고 서버(Triton) 몫이 아니다.** 그래서 같은 이득을 컨테이너
없이 가져가는 쪽을 골랐다.

| 안 | 속도 | 대가 |
|---|---|---|
| 현행 유지 | 기준 | 없음 |
| **인프로세스 ONNX** ← 채택 | 13~15% | 모델 변환 단계 하나 |
| Triton 별도 컨테이너 | 13~15% − 네트워크 왕복 | 컨테이너 1개 + 모델 2.1GB 상주(현 임베딩 파드 6Gi 와 **별도**) |

**Triton 이 값을 하는 조건은 여러 모델을 한 제어면에 모을 때다.** 우리 둘은 CPU 임베딩과
서버리스 LLM 이라 공유할 제어면이 없다. 코드 경로는 플래그 뒤에 남겼다 — 모델이 늘면 그때
같은 하네스로 다시 잰다.

---

## 5. D-4 오토스케일링을 기본 꺼짐으로 둔 이유

보수적 선택이 아니라 **선행 조건 부재**다. `metrics-server` 가 클러스터에 없다(리포 전체
검색 0건). 없이 켜면 이렇게 된다.

```
HPA 생성됨 (에러 없음)
  → TARGETS <unknown>/70%
  → Deployment 는 replicas 를 렌더하지 않는다 (HPA 가 소유권을 가져갔으므로)
  → 아무도 수를 정하지 않는 상태
```

늘지도 줄지도 않으면서 **수동 조절만 막는다.** metrics-server 가 들어오면 prd 부터 켠다 —
거기는 이미 replicas 2 고정이라 "최소 2 · 최대 4"로 바꾸는 것이 순이득이다.

**임베딩에는 어느 쪽이든 붙이지 않는다.** 파드가 5Gi 를 요청하고 모델 로드가 수십 초라
(startupProbe 최대 15분) 부하를 보고 늘리면 이미 늦고, 워커를 늘리는 길은 프로세스당
4.2 GiB 라 막혀 있다. values 에 키 자체를 두지 않고 **스키마가 추가를 거부한다.**

---

## 6. 이 측정들을 믿을 수 있게 만든 장치 넷

숫자보다 이쪽이 오래 남는다.

| 장치 | 막는 사고 |
|---|---|
| **동등성이 지연보다 먼저** (코사인 ≥ 0.9999 ∧ top-4 전부 일치, 미달 시 종료 코드 1) | 빠른데 벡터가 다른 엔진 채택. 차원이 같아 `dim` 검사도 호출측 대조도 못 잡는다 |
| **풀링 구현은 한 벌만** (측정 하네스도 서빙 코드를 그대로 탄다) | CLS→mean 오구현. 실측으로 코사인 **0.71** 까지 떨어지는데 차원은 1024 그대로다 |
| **누가 답했는지 먼저 단언** (`assert_served_model`) | A/B 한쪽이 외부 모델·베이스 가중치 성적. 병합 안 된 모델도 그럴듯한 답을 낸다 |
| **조건 고정**(4스레드·워밍업 2회·중앙값 3회·같은 아키텍처) | §4②처럼 환경이 결론을 뒤집는다 |

---

## 7. 재현

```bash
# amd64 에서 엔진 A/B — PR 에 라벨 `bench-embedding` 를 붙이면 그 브랜치로 돈다
gh pr edit <PR> --add-label bench-embedding

# 로컬(arm64) 전체 경로 — Triton 컨테이너까지
cd ai && uv pip install sentence-transformers onnx onnxruntime
uv run python embedding/export_onnx.py /tmp/kure_onnx
ORT_DIRECT=1 ONNX_DIR=/tmp/kure_onnx uv run python scripts/measure_triton_embedding.py
```

측정 규약의 정본은 `scripts/measure_embedding_precision.py`(TRIP-518)다 — 조건을 바꾸면
기존 수치와 나란히 놓을 수 없다.

---

## 8. 아직 못 잰 것

| | 왜 |
|---|---|
| HPA 실제 스케일 동작 | 로컬에 kind 없음 · 클러스터에 metrics-server 없음 |
| 컨테이너 안 ONNX 지연 | 이미지에 구운 그래프로 파드에서 재는 것이 남았다. **그 전까지 기본값을 안 바꾼다** |
| 무요청 24시간 청구액 0 | Bedrock·EKS 양쪽 미확인 (TRIP-968). 설계의 주장이지 확인된 사실이 아니다 |
| M-2 EKS GPU 기준선 | GPU 쿼터 미확보 (TRIP-961) |

---

## 9. 다음

- **TRIP-965** 인프로세스 ONNX — 컨테이너 실측 후 기본값 전환
- **TRIP-966** metrics-server → prd HPA
- **TRIP-969** 제약 디코딩으로 게이트 드롭률 낮추기 — GPU 서빙이 열린 뒤
- **TRIP-961** GPU NodePool·쿼터 (백엔드)

연결: 설계 정본은 `ai/aidlc-docs/construction/mlops-serving/functional-design/`,
정책 정본은 `ai/aidlc-docs/inception/application-design/mlops-llmops-design.md`.
