# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# Kotlin / Java
*.class
/bin/
/out/   # 루트 앵커 — 헥사고날 adapter/out/ 를 삼키지 않도록(#gitignore-out-trap)

# IDE
.idea/
*.iml
*.ipr
*.iws
.vscode/
*.code-workspace

# OS
.DS_Store
Thumbs.db

# Env / secrets
.env
.env.local
*.local.yml
application-local.yml
secrets/

# Logs
*.log
logs/

# 문서 인덱싱 도구 산출물(로컬 캐시) — 커밋 대상이 아니다.
# 머지 충돌을 해소하며 `git add backend` 로 뭉뚱그렸다가 한 번 딸려 들어왔다.
graphify-out/
