# 2026-08-06 20260806-trip288-seed-lifecycle

> 축약본 — 상세는 옵시디언 개발로그 `TripPilot/개발로그/2026-08-06 20260806-trip288-seed-lifecycle.md`
> (볼트가 없거나 MCP 실패 시 폴백. [메모리]는 이 파일이 아니라 옵시디언 쪽을 읽는다.)

## 대상

TRIP-288 [FE] g01 '꼭 갈 곳' 시드 수명 — 초기화·재시드 + 조회 실패 잠금 회귀 가드. TRIP-209 위에서
"시드를 언제 채우고 언제 지우나"를 확정하는 칸(위저드 진입마다 초기화 · 더 담기 복귀 재시드는
추가만). 편승 1건(조회 실패 시 `[다음]` 열림 회귀 가드, 구현 0줄) 동승.

## 게이트

**3개** — 게이트①-1(테스트 4파일 승인) → 구현 → 리뷰 1차(차단1: 초기화가 승인 AC-1 범위를 넘어
드래프트 9필드까지 덤으로 지움) → 사용자 확정으로 범위 재조정 → **게이트①-2**(FW-4 심판 1건
추가) → 구현 2차 + 5-c → 리뷰 2차(차단0) → **게이트②-1**(구현 4파일). 전부 승인.

## 검증

[검증] 6-a `04_qa-verifier_report_1_PASS.md` — 정적 4종 PASS, 게이트①-2 해시 4/4·게이트② 해시
4/4 일치, 변경 집합 미신고 0건, red 소급 12건 정확 재현, 구조 지도 불일치 1건(scribe 인계 → 이번
[기록]에서 반영 완료) → **PASS**.
6-b 실기 스모크 `04b_smoke_1_PASS.md` — 판정 4항목 PASS. ⚠️ **D4(셸 재마운트 여부)는 여전히
미검증** — 프리뷰가 셸·스토어를 안 태워 원리적으로 못 봄, 사용자 수동 확인 스크립트 A~E 인계.

## 핵심 결정

- 위저드 진입(셸 `_layout.tsx` 마운트)마다 시드·채움 플래그·제외 기억 3필드만 초기화.
- 재시드는 새 문 `addMustVisits`(기존 `initMustVisits`의 N2-2 동결은 안 건드림) — 새로 담긴 것만
  추가, x로 뺀 것은 안 돌아옴.
- 제외 기억은 `sourcePoiId` 집합 하나(`excludedMustVisitPoiIds`), 진입마다 함께 비움.
- 탐색 CTA(「담은 장소로 여행 만들기」) 복귀도 진입으로 확정.
- `createdTripId`는 이번 칸에서 안 건드림(정본 공백, "남긴다" — 판정은 g02 착수 시점).

## 예상과 달랐던 것

- 차단-1: code-critic이 뮤테이션으로 "셸 `reset()`이 승인 범위(3필드)를 넘어 9필드를 덤으로
  지운다"를 잡음 — 311케이스 중 그 초과를 보는 심판 0건. 새 문 `resetMustVisits()`(시드 3필드만)로
  좁힘.
- 2차 리뷰(측정 범위 1222케이스로 확장)가 문서 자체의 과장(경고-A: "새 필드도 자동으로 함께
  돌아간다"는 서술이 `resetMustVisits`에는 거짓 — 키 목록은 손으로 유지하는 사본)을 추가로 잡음.
- 6-b가 프리뷰 딥링크 재발 함정을 실제로 다시 밟음(문제로그 재발 실측 1건, 하네스 규칙 후보로
  승격).

## 정본 반영 (aidlc)

**제시 4 / 선택 0 / (A) 0 / (B) 0** — 3-a에서 사용자가 고른 항목 없음, `aidlc/` 미기록. 티켓 DoD
("확정 규칙을 §4에 구현 결정 라벨로 반영")는 **미충족**으로 명시.

## 후속 티켓 후보

1. g01 「가고 싶은 곳 담기」를 담은 목록 선택 화면으로(2026-08-06 사용자 확정) — 점선 박스가 d04
   장소 탐색으로 보내 시드 진입 문이 "새로 하트 누르기" 하나뿐이라, x로 뺀 곳은 이미 하트가
   채워져 있어 다시 넣을 수 없다. 담은 곳 목록에서 고르는 형태면 되돌리기가 구조적으로 해결된다.
   D7("뺀 것은 뺀 것")은 그대로 두고 화면으로 푼다. Figma 신규 프레임·신규 testID 필요.
2. 초기화 키 목록 누락 방지 장치 — `resetMustVisits`가 손으로 적은 키 목록이라 새 시드 필드 추가 시
   빠뜨려도 tsc·테스트 무반응.
3. 위저드 재진입 시 여행지·기간·`createdTripId`는 의도적으로 남음 — 사용자 기대와 맞는지 별도 판단.
4. `createdTripId` 수명 미정(정본 공백, g02 착수 시점 재판정).
5. 심판 공백 3건(경고-1 봉합·`addMustVisits` 플래그 조건·`removeMustVisit` 조기 반환).

## 하네스 규칙 후보

1. 프리뷰 다중 키 확인 시 키마다 콜드 런치 필요 — `verify-gates` 「실기 스모크」에 한 줄 추가
   제안(재발 실측 1건 확보).

## 미커밋

작업트리 8파일 변경(구현 4·테스트 4), 브랜치 `feature/TRIP-288-FE-seed-reset-reseed`.

## 상세

옵시디언 개발로그·게이트 노트 3개·이해부채 신규 1건+재발 2건·문제로그 재발 1건·개념 노트 신규
2건 참고.
