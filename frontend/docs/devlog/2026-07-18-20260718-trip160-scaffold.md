# 2026-07-18 · 20260718-trip160-scaffold — TRIP-160 frontend Expo 스캐폴딩

첫 코드 사이클. `frontend/` 코드 0줄 → 동작하는 Expo 스캐폴드(신규 46파일, 기존 tracked 수정 0). 부팅되는 빈 5탭 셸 + 실제 위반을 잡는 검증 파이프라인.

- 브랜치: `feature/TRIP-160-frontend-foundation`
- 정본: `frontend/README.md`(스택·구조·경계·테스트)
- 옵시디언 상세: `TripPilot/개발로그/2026-07-18 20260718-trip160-scaffold.md`

## 결과

- 게이트① (테스트) 승인 (03:47:32 KST) — PBT/fast-check 이해부채 등재. 테스트 2 해시 동결.
- 게이트② (구현) 승인 (12:58:24 KST) — NativeWind 정렬 플러그인 이연 동의. 46파일 해시 동결. 해시 동결 이해부채 등재.
- 검증 **n=1 PASS** (13:11:29 KST) — 해시①② 전수 일치(변조 0), red 소급으로 두 승인 테스트 생존 실증, verify-gates 5게이트 exit 0(6 tests passed), 전 AC(A1~E1) PASS.
- **AC-A3 `expo prebuild -p ios --clean` exit 0** — ios/ 생성 + pod install 성공(89 pods) + New Architecture 유지. `ios/`는 gitignored라 git 오염 0.
- [리팩토링] 건너뜀 — 스캐폴드 clean + 게이트② 해시 동결 유지.

## verify-gates (n=1)

| # | 명령 | exit | 요약 |
|---|---|---|---|
| 0 | `pnpm install --frozen-lockfile` | 0 | Already up to date |
| 1 | `pnpm format` (`prettier --write .`) | 0 | 46파일 (unchanged) — 멱등 |
| 2 | `pnpm lint` (`eslint .`) | 0 | 0 error / 0 warning |
| 3 | `pnpm tsc` (`tsc --noEmit`) | 0 | strict, 0 error |
| 4 | `pnpm test` | 0 | 2 suites · 6 passed (compareVersion 4 + importBoundary 2) |

## 주요 결정 / 델타

- Jest 실증은 AC-D2(import 경계)·AC-D3(compareVersion PBT) 2개만 — 나머지 스캐폴딩 AC는 명령/파일 점검.
- Q1 codegen: orval config만, 생성물 `shared/api/generated/` 미커밋. Q2: 네이티브 후보 X만, 소셜/지도 SDK 미포함(U0 이연). Q3: 빈 5탭 셸까지.
- 버전 핀은 `expo install` 해석값(Expo ~54.0.35 · RN 0.81.5 · TS ~5.9.2 · tailwindcss 3.4.17 정확 핀).
- NativeWind 클래스 정렬 ESLint 플러그인 이연(U0) — .prettierrc 해시 동결 + eslint-plugin-tailwindcss 로드 실패 위험 + YAGNI. 배선 자체는 완료.
- import 경계: `import/no-restricted-paths` 채택(boundaries는 global.css 오탐 위험이라 제거).
- 델타: jest 30→29(jest-expo 54 정합), zod v4 해석(미사용), pnpm-workspace.yaml·.npmrc 추가(정본 밖 필수 인프라), expo-env.d.ts 커밋.

## 예상과 달랐던 것

- Xcode 라이선스 미동의로 git·brew 차단 → `sudo xcodebuild -license accept`로 해소(비블로킹).
- 사이클 도중 하네스 규칙 2건 진화: 커밋 `d862d38`(이해부채 기본값은 등재) · `7907a00`(이해부채↔개념 노트 링크). 하네스 변경이력 기록됨.
- AC-A3 prebuild가 package.json android/ios 스크립트 재작성(결정적 부작용) → qa-verifier가 승인 해시로 원복, 게이트② 재제시 불요.

## 이해부채 (2건, 미상환 — 다음 [메모리]서 재노출)

- 속성 기반 테스트(PBT)/fast-check 문법 (게이트①)
- 해시 동결 / 게이트 무결성 (게이트②)

## 다음

- 스캐폴드 46파일 커밋 여부 사용자 확인. ios/=gitignored 로컬 산출물.
- U0 이연분: NativeWind 정렬 플러그인 · orval codegen 실행 · zod v4 RHF 호환 · Android prebuild · 소셜/지도 SDK.
