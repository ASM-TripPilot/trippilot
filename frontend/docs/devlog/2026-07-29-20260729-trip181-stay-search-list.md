# 2026-07-29 20260729-trip181-stay-search-list

## 대상

**TRIP-181 [FE] e02 숙소 검색 결과 — 목록·카드 default** (US-STAY-01, 상위 TRIP-70·유닛 U1). TRIP-179(`useStaySearch`)·TRIP-180(`formatPrice`) 두 배관에 **첫 소비 화면**을 붙였다 — FSD 3층(`app → pages → features/ui`)이 숙소 도메인에서 처음 완성됐다. 신규 프로덕션 6파일(340줄) + 테스트 4신규·1수정 + 인프라 1(`package.json` devDependency). 범위는 default 상태 하나뿐(로딩·빈결과 등 5상태는 TRIP-182). 직전 사이클: `2026-07-29-20260729-trip180-format-price.md`.

## 게이트·검증

- **게이트①-1**(2026-07-29) **승인**("ㄱㄱ") — 테스트 5파일, 해시는 옵시디언 게이트 노트 참조. red 로그 정상(예외 1건 사전 기록 — `fsdStructure.test.ts` 슬라이스개수 단언이 파일 배치만으로 조기 green).
- **게이트②-1**(2026-07-29) **승인**("검증 ㄱㄱ") — 구현 7파일. 게이트① 해시 전부 불변(심판 훼손 0건).
- **검증 n=1 PASS**(`04_qa-verifier_report_1_PASS.md`): 정적 검사 4/4 · 해시 5/5+7/7 일치 · 변경집합 자체도출=신고 일치(미신고 0, aidlc 0) · red 소급 확인 완료 · 경계면 QA(openapi↔타입·INV-3) 통과. 구조 지도 불일치 7건 → scribe 이관, 이번 [기록]에서 해소(`structure-index.cjs --check` → OK 115행/115파일).
- **실기 스모크 n=2**: 1차(22:04) 시뮬레이터 미부팅 실행 불가 → 2차(22:05~22:08, 사용자 요청으로 환경 구성) **PASS**. 판정 4항목 전부 PASS, `/stays/search` 실기 관측 12건(TRIP-179 이월 해소). **⚠️ 실기 미검증 아님** — 2차에서 실제로 실행·PASS했다. 단 P1(픽셀 대조) 미실행 + 카드 0건이라 카드 레이아웃은 실기 미확인.
- **🔴 2차가 발견한 결함**: `StaySearchScreen.tsx` 루트가 `SafeAreaView`가 아니라 `View`라 앱바가 상태바·노치와 겹침. jest 5개 전부 green인 채로 통과 — 6-b 존재 이유의 두 번째 실측 사례. 🧑 사용자 판정으로 **TRIP-182로 이관**(지라 AC 반영 완료).
- **적대적 리뷰(5-b)**: 차단 0 · 경고 3 · 참고 3. 경고1(하트가 회색 배경에 묻힘)·경고2(`region` 폴백이 빈문자열/배열 통과) **처리(5-c)**, 경고3(`/stays`가 `Stack.Protected` 밖 — 실기로 확정) **미룸+후속 티켓**(TRIP-183 선행 조건), 참고 3건 전부 동결 테스트 수정 필요해 **기록만**. 스타일 지적 0%.
- **Ponytail**: 제안 2(아이콘 재사용/배럴 생략) · 채택 0(설계 판정+관례로 기각, 테스트 강제 아님).
- **사고 1건**: 승인된 devDependency(`@types/react-test-renderer`)를 5-c가 미신고 의존성으로 오판해 되돌림(오케 프롬프트 결함, 즉시 복구). 하네스 규칙 후보로 승격.

## 예상과 달랐던 것 (요지)

1. 동결된 테스트 파일 안의 타입 갭(`react-test-renderer` `.d.ts` 부재)이 구현자 무관 실패로 나타남 — 사용자가 devDependency 추가안 선택.
2. 적대적 리뷰가 "구현 오류"가 아니라 "심판이 그 층을 못 본다" 유형 3건(하트 명도 대비·region 폴백 구멍·라우트 guard 공백) 지적.
3. jest 5개 전부 green인 채로 SafeArea 결함이 실기에서만 잡힘 — TRIP-182로 이관.
4. 실기 스모크가 TRIP-179의 2사이클 이월 항목(`QueryClientProvider` 체인 실기 미검증)을 해소.

상세(D1~D10 설계 근거·03b 6건 전문·정본 반영 3건·구조 지도 갱신 7행·모델 배치)는 옵시디언 개발로그 참조.

## 다음에 이어서 할 일

- TRIP-182(5상태 변형) 착수 시 SafeArea 수정 + `StaySearchScreen.tsx` 재사용.
- TRIP-183이 `/stays` 라우트 위치·보호를 결정해야 함(후속 티켓 선행 조건).
- P1(Figma 픽셀 대조) 여전히 미실행 — SafeArea 수정 후 목 부트스트랩 포트 전환으로 수행.
- 별도 티켓 후보 3종: 공유 토큰 모듈 신설 · AC-7(정직한 스텁) 심판 보강 · `onEndReached` 금지 단언 신설.
- 미상환 이해부채 18건(16+2)/상한 10건 — 정리 세션 판단 계속 유예.

## 관련

- 옵시디언 개발로그: `TripPilot/개발로그/2026-07-29 20260729-trip181-stay-search-list.md`
- 게이트①-1: `TripPilot/게이트/2026-07-29 20260729-trip181-stay-search-list 게이트①-1.md`
- 게이트②-1: `TripPilot/게이트/2026-07-29 20260729-trip181-stay-search-list 게이트②-1.md`
- 문제로그(신설): `TripPilot/문제로그/2026-07-29 승인된 devDependency를 5-c가 미신고 의존성으로 오판해 되돌렸다.md` · `TripPilot/문제로그/2026-07-29 실기 스모크에서만 SafeAreaView 누락이 잡혔다 (TRIP-181).md`
- 이해부채(신설): `TripPilot/이해부채/2026-07-29 JS 문법 기초 묶음.md` · `TripPilot/이해부채/2026-07-29 jest.mock 모듈 바꿔치기.md`
- 작업 공간: `_workspace/20260729-trip181-stay-search-list/`
