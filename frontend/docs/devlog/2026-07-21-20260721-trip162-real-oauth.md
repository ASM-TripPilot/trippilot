# 2026-07-21 · 실 OAuth 구조 배선 (expo-auth-session, Google 우선)

브랜치: `feature/TRIP-162-FE-register` · 사이클: `20260721-trip162-real-oauth` · **자율 실행**

## 요약
- `makeAuthorize`가 토글 off + clientId(env) 있으면 `await import('./realAuthorize')`로 지연 위임 → PKCE authorize → `{authorizationCode, codeVerifier, redirectUri}`. clientId 없으면 throw(INV-4).
- 모듈 로드 안전: makeAuthorize.ts에 top-level expo-auth-session import 없음(동적만). realAuthorize.ts가 유일 참조점 → fake 경로 미유입.
- config는 env(`oauthConfig.ts`). 패키지 3종 설치(auth-session ~7.0.11 등). 네이티브 리빌드·실 provider 검증은 등록 후(RO1).

## 검증
단위 159/159 · 통합 37/37 · prettier/lint/tsc clean · 게이트①3/3·②4/4 해시 일치 · lazy-import·settings.json 가드 독립 확인. qa **PASS**(n=1).

## 후속
RO1 등록후 실 E2E(라이브 백엔드 떠 있음) · RO2 옵션1 빈secret+PKCE 교환 실측 · RO3 kakao/naver/apple. 등록 가이드: 게이트②-1 노트 §등록 가이드.

## 변경 파일 (프로덕션 4 + 패키지)
`makeAuthorize.ts`(수정) · `oauthConfig.ts`(신규) · `realAuthorize.ts`(신규) · `package.json`·`pnpm-lock.yaml` · `.env.local`(gitignore)
