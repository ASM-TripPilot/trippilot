# 2026-08-10 20260810-trip339-map-surface

> 축약본 — 상세는 옵시디언 개발로그 `TripPilot/개발로그/2026-08-10 20260810-trip339-map-surface.md` (볼트 없는 사람·MCP 실패 시 폴백 전용).

## 대상

TRIP-339 [FE] h05·h11 지도 표면 정합 — 핀 마커 형태(물방울 SVG·`yAnchor:1`) · h11 순서 연결선(케이싱 2겹) · 지도 고정(`viewOnly` 옵트인) · h05만 선 끄기(`connectPins` 옵트아웃) · h11 프리뷰 카드 사진 3장 · 좌표 교정(41.2km→2.03km) · AC-V4 픽셀 대조 소급 상환.

## 결정

- 핀을 원 `<div>`에서 물방울 `<svg>`로 교체(흰 테두리가 이음매에서 안 끊기게 하나의 path로) + `yAnchor` `0.5`→`1`(꼬리 끝이 좌표를 가리키게)
- 연결선은 "겹당 폴리라인 1개(전체 경로)"로 구현 — 승인 테스트가 개수가 아니라 선분 목록으로 재므로 판정은 같고 객체 수가 적다
- `connectPins` 옵트아웃 스위치를 수정 루프에서 추가(기본 `true`) — h05만 끔. 1차에 "범위 확대"로 미룬 근거 둘 다 반증됨(§예상과 달랐던 것)
- h05 프리뷰 픽스처(13.6km)는 3-a에서 사용자 A(그대로 둔다) 선택 — AC 밖, 변경집합 불변

## 게이트 통과 현황 (`04_qa-verifier_report_1_PASS.md` 원문)

| # | 검사 | 결과 | 비고 |
|---|---|---|---|
| 1 | `pnpm format` | PASS | 전 파일 unchanged |
| 2 | `pnpm lint` | PASS | 0 errors / 31 warnings(사전 존재, 무관) |
| 3 | `pnpm tsc` | PASS | 출력 없음 |
| 4 | `pnpm test` | PASS | test:node 146/1413 · test:integration 26/190 |

게이트① 해시 4/4 일치 · 게이트② 해시 9/9 일치 · red 소급 통과(vacuous 아님) · 변경 집합 자체 도출 = 신고 목록 완전 일치 · `aidlc/` 무결 · 하네스 파일 무결.

**종합 판정: PASS.**

## 실기 스모크 (n=1)

**PASS (조건부)** — 눈으로 확인 11/11 PASS(`strokeLineCap` 1건은 원리적으로 판정 불가, 선 끝이 핀 뒤에 가려짐). **⚠️ 제스처 4항목(드래그·핀치줌·더블탭·무회귀) 항목별 결과 미수령** — 시뮬레이터에 터치 주입 API가 없어 오케가 대신 확인 불가, 사용자 응답은 구두 취지뿐. 이 축에는 자동 심판이 0개(핀치줌 잠금을 지워도, 세 이름을 오타로 바꿔도 전수 green — code-critic 2차 실측). **다음 사이클 착수 전 1회 재확인 필요.**

## 예상과 달랐던 것

1. 티켓 AC-4 핀 색 서술이 Figma 실측과 반대(흰 원+분홍 테두리 ↔ 실측 분홍 채움+흰 테두리) — 현재 구현은 이미 맞았음.
2. code-critic 1차 차단 B-1(h05 연결선) 대응 시 1차 근거 둘 다 반증됨.
3. 게이트①-2에서 "파일 하나만 열어라" 지시가 뮤테이션 M8로 뒤집힘(3번째 파일을 열어야 렌더 층 공백이 메워짐).
4. `strokeLineCap`이 실기에서 원리적으로 안 보이는 옵션으로 드러남.

## 다음에 이어서 할 일

1. **⚠️ 제스처 실기 재확인 1회** — 다음 사이클 착수 전 필수
2. `strokeLineCap` 실명 확인 또는 제거
3. h05 프리뷰 픽스처 13.6km → 2km대(사용자 A 선택으로 보류)
4. `MUST_VISIT_THUMBNAILS` 사진 채우기 · 일차 전환 pill(Figma 분홍 pill ↔ 구현 텍스트 탭)

## 정본 반영

제시 6 / 선택 3 / (A) 0 / (B) 2 — `aidlc/aidlc-docs/construction/u3-ai-itinerary/functional-design/{frontend-components.md §8, domain-entities.md §3}` 소급 기록 + 하네스 규칙 1건(밴드 맵 `[보관]` 세대 판정축, `frontend/.claude/skills/spec-perception/reference/figma-structure.md`).

## 구조 지도

`frontend/docs/structure.md` 282→284행, `--check` PASS. 「지금 작업하려면」 경고 절 15건(12건 상한 초과 — 상세는 옵시디언 개발로그).
