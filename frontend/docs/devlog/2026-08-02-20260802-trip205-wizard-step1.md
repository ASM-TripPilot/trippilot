# 2026-08-02 20260802-trip205-wizard-step1 (요약)

**TRIP-205 [FE] g01 여행 만들기 1/2 — 위저드 셸 + 입력 블록** (US-TRIP-01, 부모 TRIP-81) · 브랜치 `feature/TRIP-205-FE-trip-create-wizard` @ `341495d`. 이 브랜치 4사이클 중 3번째(앞: TRIP-203·204 / 뒤: TRIP-206).

> 옵시디언 개발로그의 축약본이다. 전체 서사·게이트 노트·문제로그·개념 노트는 볼트에 있다.

## 무엇을 했나

신규 8파일 — `features/trip/model/{tripWizardStep1,tripWizardStore}.ts`(순수 계산 + Zustand 스토어) · `features/trip/ui/{TripGlyphs,TripWizardStep1Screen}.tsx`(무상태 화면) · `pages/trip-new-step1/`(배선, `validateTripDraft`를 유일하게 부름) · `app/trips/new/{_layout,step1}.tsx`(라우트). 판정(TRIP-204 동결)·상태·렌더를 3층+배선 1층으로 분리해 `[다음]` 게이트와 오류 문구 표시 조건을 갈랐다(문구 표시는 TRIP-206 몫, 이 칸은 `touched`만 쌓음).

## 운영 모드

**자율 — 게이트①·②(각 1차수) 전부 오케스트레이터 대리 승인.** 한 줄 주해는 게이트①-1 노트 §4(**102덩어리**, 승인 테스트 5파일 전수)에 문서로 대체.

## 결과

- 검증 n=1 **PASS**(포맷·린트·타입·테스트 전부 1회 통과, 게이트①해시 5/5·게이트②해시 8/8 일치, 변경 집합 자체도출 완전 일치, red 소급 원장과 완전 일치, AC-10c·AC-13·AC-14·INV-3 위반 없음)
- 실기 스모크 n=1 **PASS**(4항목 전부 — 프로세스 부팅·레드박스 없음·예상 밖 네트워크 0·대상 화면 도달)
- code-critic: 뮤테이션 7개 + 날짜 전수 대조(1970~2100) + 중복 도시 프로브. 차단0·경고3(W-1·W-2 게이트①-2 이월, W-3 5-c 처리)·참고5(전부 판단 유보/변경 불필요)
- 구조 지도 불일치 9건 → 이번 [기록]에서 전부 해소(`structure-index.cjs --check` OK, 202행↔202파일)

## ⚠️ 정본 드리프트 (이번 사이클 최대 발견)

라이브 Figma가 티켓 작성(07-30) 이후 개정됨 — 예산 블록·'등록 숙소에서 날짜 가져오기'·「담은 곳 0」 변형이 전부 실재하게 됨. **TRIP-207·TRIP-208의 유일한 착수 불가 조건("Figma 개정 대기")이 해소됐다.** 화면 제목도 티켓의 "새 여행 만들기"에서 라이브 확정 문구 「어디로 갈까요?」로 교체. 밴드 맵(`figma-structure.md`)이 낡았으나(밴드 g 3→7프레임) 이번 사이클은 하네스 파일이라 미수정 — 아침 보고 항목.

## 문제로그 (3건, 상세는 옵시디언)

- **값 일치 제거 API가 중복에서 무너짐** — `removeDestination`이 `filter`(전체 일치 제거)라 중복 도시 하나를 지우면 둘 다 사라짐. `findIndex`+슬라이스로 5-c 수정 완료. 하네스 규칙 후보.
- **소스 스캔 가드는 우회 경로를 못 봄** — AC-10c 4겹 심판이 `validateTripDraft` "이름"만 잡아서, 화면이 그 함수 없이 자작 오류 문구를 그려도 47개 전부 green. 게이트①-2 이월. 하네스 규칙 후보.
- **prop 기본값 경로는 테스트가 안 밟음** — 모든 테스트가 `baseDate`를 명시 주입해 `todayIso()` 폴백(프로덕션 유일 경로)이 무검증. 게이트①-2 이월. 하네스 규칙 후보.

## 정본 반영

**사용자 선택 0건**(자율 모드) — `aidlc/` 미변경.

## 아침 보고 대기 (1순위 + 누적)

**1순위**: Figma 개정으로 TRIP-207·208 착수 가능 · 밴드 맵 낡음 · 화면 제목 정정 필요. **누적**: TRIP-203 6건·TRIP-204 6건+게이트 결정 4건이 아직 열려 있음(전체 목록은 옵시디언 개발로그).

## 다음에 이어서 할 일

1. TRIP-206(제출 배선 + 오류 문구 표시) 착수 — `touched` 소비, 게이트①-2 사안 2건(W-1·W-2) 흡수 여부 판단.
2. 후속 티켓 후보: testID 충돌 · 날짜 피커 캘린더 UI(Figma 부재) · `stripComments` 4파일 일괄 수정(이월).
3. 프로덕션 8파일 + 독립 테스트 1개 미커밋 상태 — 다음 단계에서 처리.

## 관련

- 상세: 옵시디언 `TripPilot/개발로그/2026-08-02 20260802-trip205-wizard-step1.md`
- 원장: `_workspace/20260802-trip205-wizard-step1/00_gates.md`
