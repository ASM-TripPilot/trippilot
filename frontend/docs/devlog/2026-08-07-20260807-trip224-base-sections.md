# 2026-08-07 20260807-trip224-base-sections

> 축약본 — 상세는 옵시디언 개발로그 `TripPilot/개발로그/2026-08-07 20260807-trip224-base-sections.md`
> (볼트가 없거나 MCP 실패 시 폴백. [메모리]는 이 파일이 아니라 옵시디언 쪽을 읽는다.)

## 대상

TRIP-224 [FE] 거점 구간 파생 순수 함수 `toBaseSections` + fast-check PBT (BR-U1-28 · US-TRIP-07 · 부모
TRIP-84). 신규 2파일(`frontend/src/features/trip/model/baseSections.{ts,test.ts}`) — 그 외 리포 변경 0.
규모 **기본**.

## 게이트

**2개** — 게이트①-1(테스트 1파일 승인, red 13건) → 구현 → code-critic(차단0·경고2·참고2) →
5-c(W-1만 주석 4줄) → **게이트②-1**(구현 1파일 승인). 전부 1차에서 승인(재제시 0회).

## 검증

[검증] 6-a `04_qa-verifier_report_1_PASS.md` — 정적 4종 PASS, 게이트①-1 해시 1/1·게이트②-1 해시 1/1
일치, 변경 집합 = 신고 2파일, red 소급(1차 필수, `git stash push -u`) vacuous 아님, 구조 지도 정합(scribe
작업 목록으로 전달) → **PASS**. 6-b 실기 스모크는 발동 조건(diff 5종) 전부 미해당 — 순수 함수 신규 1파일
+ 테스트뿐이라 앱 표면 무변경(건너뜀, 미검증 표기 대상 아님).

## 핵심 결정

박 번호는 여행 시작일 고정 기준(`firstNight = (dateFrom − trip.startDate) + 1`, 누적 카운터 아님 —
AC-6이 판별점) · 구분자는 en dash(U+2013) 상수 하나로 고정(D2, 티켓 AC의 ASCII 물결과 드리프트) · 판정
없음(D3·INV-2, 여행 기간 밖 배정도 산식 그대로) · `toEpochDay`는 private(D4, feature 간 import 회피).

## 예상과 달랐던 것

- 01 브리프와 01b Seed가 `nights` 음수/0 처리 방침에서 충돌 — 01b 쪽으로 해소(넓은 생성기), 그 결과
  `Math.max` 클램프 변이를 AC-P4가 실제로 잡음. 이미 알려진 교훈의 반대쪽 확인이라 신규 문제로그 안 만듦.
- code-critic이 필드 7개 명시 나열을 스프레드로 줄여 봤더니 승인 테스트 13건이 그대로 초록(W-1) — D1·
  INV-3의 유일한 방어선이 코드에 안 적혀 있던 것. 5-c에서 주석만 추가(로직 변경 0).
- 같은 숙소를 두 구간에 배정하는 케이스 심판 없음(W-2) — 승인 테스트 동결이라 이번 칸에서 못 메움.

## 정본 반영 (aidlc)

**제시 2 / 선택 2 / (A) 0 / (B) 0 — 미반영.** scribe가 3-a 선택 대화 원문을 못 받고 오케 사이클 요약만
받아 `audit.md`의 "원문 그대로" 요건을 채울 수 없었다. 내용(en dash 정정·박 번호 규칙 명문화)은 옵시디언
개발로그에 인계 관측으로 남김 — 하네스 규칙 후보로 제기.

## 이해부채

신규 2건([[2026-08-07 제자리 정렬과 입력 불변]]·[[2026-08-07 객체의 필드와 계약 밖 필드 유출]]) · 상환
0건. 미상환 총 35건(직전 33 → +2, 파산 상한 10 초과 지속 — 사용자 유예).

## 문제로그

신규 노트 생성 안 함 — 01/01b 충돌은 진입 조건(다음 사이클 조치를 바꾸는 정보) 미통과 판정.

## 후속 티켓 후보 / 다음에 이어서 할 일

1. W-2 — 같은 숙소 재배정 케이스 심판 공백 → TRIP-225 또는 게이트①-2급 후속.
2. N-1 — D6(2차 정렬 키 없음) 테스트 밖, 코드 주석만 지킴.
3. 호출자 0인 채로 머지 — g02 화면(TRIP-225)이 첫 소비자, 실서버 응답 미검증.
4. (B) aidlc 미반영 2건 — 원문 사용자 입력 확보 후 재시도.

## 하네스 규칙 후보

- scribe의 (B) 절차가 구조적으로 완주 불가능할 수 있다 — 3-a 선택을 오케 요약으로만 받는 구조와
  `audit.md`의 "완전한 원문 입력" 요건이 상충. 다음 몇 사이클 관찰 후 판단.

## 미커밋

작업트리 2파일 신규(구현 1·테스트 1), 브랜치 `feature/TRIP-224-FE-base-sections`(베이스
`origin/develop` @ `7acfbb4`).

## 상세

옵시디언 개발로그·게이트 노트 2개·이해부채 신규 2건·개념 노트 신규 2건·구조 지도 갱신(234→235행).
