# 2026-08-11 · 20260810-trip298-reason-surface

> 축약본. 상세(결정 이유·예상과 달랐던 것·개념 노트·문제로그)는 옵시디언 개발로그 `TripPilot/개발로그/2026-08-11 20260810-trip298-reason-surface.md`를 정본으로 본다.

## 대상

TRIP-298 [FE] h11·h35 추천 이유·후보 0건 표시 (부모 TRIP-91, US-SCHED-02·05). 브랜치 `feature/TRIP-298-recommendation-reason`. 티켓 3표면 중 **2표면만** 구현 — h11 슬롯 카드 추천 이유 줄(표면 1)은 사용자 결정 D1로 범위 밖(Figma 정본 부재·완전AI 계열 일관·서버 값 미보장).

## 요약

- 강등 안내(h11): `candidatesSummary.level`을 조용할 값 화이트리스트(`['OK','HIGH']`, 정규화 `trim().toUpperCase()`)로 판정. 모르는 값은 자동으로 안내가 켜짐(INV-4 방향).
- 후보 0건(h35, 신규 화면 `ZeroCandidateScreen.tsx`): 얼굴 판정축을 `days.length`에서 **슬롯 합계**로 변경. 완화 제안은 "필수 방문지 줄이기" 1행뿐(D7 — `GenerateItineraryRequest`에 완화 파라미터가 없어 나머지는 계약이 안 열림).
- **게이트①-2 재개방**: code-critic이 `failed`/`zero` 우선순위 축에 심판이 없음(뮤테이션 W-2)을 실측 → 사용자 결정으로 `failed > zero` 심판 추가 + 구현 반영.
- 승인 목록 밖 심판(`itineraryMapSurfaceStructure.test.ts`)을 구현자가 그 파일 자신의 "B 이행 체크포인트" 절차대로 1회차 처리 — code-critic 뮤테이션으로 심판 강도 유지 확인 후 게이트②에서 결재.
- code-critic 차단0·경고3·참고3. 사각지대 2종(W-1·W-2)이 사용자 결정 2건으로 이어짐. 5-c 리팩토링 실행 없음(처리 대상 없음).

## 검증

> `pnpm format` → `pnpm lint`(0 error / 31 warning, 기존) → `pnpm tsc`(0) → `pnpm test`.
> test:node 148 스위트 1432 통과 · test:integration 26 스위트 201 통과.
> 해시 대조: 게이트①-2 6/6 · 게이트② 7/7(전부 일치). red 소급 확인(구현 6파일 stash → node 12실패·integration 7실패 재현 → 정상 복원).

## 실기 스모크 (6-b, n=1 PASS)

부팅 PID 36041 · 레드박스 0 · 예상 밖 네트워크 0(기존 인증 부트스트랩 경로) · **AC-V3 Figma 픽셀 대조 종결**(`1906:1083`, 갭 0 — 어긋난 2건은 D7 귀결). 1차 딥링크 시도 실패(앱이 이미 떠 있어 `openurl` 무시) → `terminate` 후 `launch` 생략, `openurl`만으로 2차 성공. **하네스 규칙 후보**.

## 커버되지 않는 것

- 「빈 일자 + 요약 없음」 → `empty` 축에 심판 없음(사용자 결정 1로 현행 유지, 후속 티켓 후보).
- 완화 행 그물에 역할·문구 둘 다 피하는 형태 우회 가능(code-critic N-1) — 동결 테스트라 이번엔 못 고침.
- `LocationOffGlyph`가 `shared/location/LocationGlyphs.tsx`·`features/itinerary/ui/ItineraryGlyphs.tsx` 두 곳에 같은 이름·다른 색으로 존재.
- h35 완화 파라미터 — `GenerateItineraryRequest`에 완화 칸이 없어 CTA를 못 만든다(백엔드 계약 확장 필요).
