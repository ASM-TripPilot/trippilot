# 2026-07-21 — 20260721-trip162-onboarding

**결말: 완료** (게이트①-2·② 승인 + 검증 1차 PASS) · 브랜치 `feature/TRIP-162-FE-register` · **미커밋**

## 무엇을 했나

TRIP-162 온보딩 — auth 다음 **두 번째 실구현 feature**. 변경 29파일(게이트① 테스트/인프라 15 + 게이트② 프로덕션 14).

- **US-ONB-02 약관 동의 완결**: `TermsScreen`/`TermsContainer`/`useTermsConsent`. 약관 3종 로드 → 필수 2종(`TERMS_OF_SERVICE`·`PRIVACY_POLICY`)만 '다음' 활성, 마케팅 선택. 체크된 것만 `POST /me/consents` 1회(버전은 서버 값 그대로 되돌림, BR-U0-12). 실패 시 이동 안 함(INV-4).
- **US-ONB-03 닉네임 완결**: `NicknameScreen`/`NicknameContainer`/`useNickname` + 순수함수 `validateNicknameFormat`(코드포인트 2~20). 저장은 **정해진 순서** 형식→`/nickname/check`(서버 중복·금칙어 판정)→`PATCH`→`/onboarding/complete`. 각 단계 실패 시 다음 미호출.
- **US-ONB-04 축소분**: `src/shared/location/LocationPreprompt.tsx` — default/permission-denied 2상태 **프레젠테이션 프레임만**. `expo-location` 을 import조차 안 함(OS 다이얼로그 구조적 미호출), 라우트 미등록. `useLocationPermission` 훅은 **AC 부재로 범위 제외**.
- **온보딩 체인 최소 배선**: 순수함수 `resolveOnboardingStep`(진행상태→잔여단계). **가드 연결은 미배선(T2)**.
- **SafeAreaProvider 전역 도입** (`_layout.tsx`): `initialMetrics` 에 null 대비 기본값을 함께 줘야 jest에서 자식이 안 사라진다(실측 함정).
- **목 격리 강화**: `makeAuthorize.ts` 를 목→env(`EXPO_PUBLIC_AUTH_FAKE_OUTCOME`)로 전환 → 프로덕션의 `@/mocks/*` import 완전 제거. `src/__tests__/noMswInStaticGraph.test.ts` **부활**(직전 사이클 삭제분)로 기계 강제. 온보딩 목 거동은 `src/test-support/onboardingScenarios.ts`(앱 밖)로 분리.
- **프로젝트 설정 수정**: `eslint.config.js` 의 `import/no-restricted-paths` except 경로 버그. `['./src/features/${feature}']` → `['./${feature}']`(except 는 `from` 기준 상대경로). `onboarding` 이 코드를 갖는 첫 zoned feature 라 드러났다.

## 게이트·검증

- **게이트①-1 반려** — 사용자 "우리 mock 제거한거 아니야?". `makeAuthorize` 가 `getScenario().auth` 한 필드 때문에 목 모듈 전체를 앱 그래프로 끌고 있었고(직전 사이클이 정적 그래프 가드를 삭제해 기계 강제 부재), 이번 제시분이 그 목에 온보딩 시나리오를 얹었다.
- **게이트①-2 승인** — 시정: onboardingScenarios 분리 + noMswInStaticGraph 가드 부활 + scenarios.ts 원복.
- **게이트② 승인** — `makeAuthorize` 목→env 한 파일 변경으로 **맞물린 red 5건**(makeAuthorize 2 · noMsw G1·G3 · SocialLoginContainer cancel 1)이 동시 green.
- **검증 1차 PASS**: prettier/eslint/tsc clean · 단위 **140/140**(24 스위트) · 통합 **34/34**(4 스위트). 게이트① 해시 15/15 · 게이트② 해시 14/14 일치. red 소급 non-vacuous. 경계면(목 격리·서버 권한·INV-4·INV-3·aidlc) 전부 통과. openapi 관찰 1건(NicknameCheckReason 5→3 안전 축소) 비차단.

## 후속 (이관)

- **후속 티켓**: T1 온보딩 API 인증헤더 gap(인증 없는 `baseClient` 사용) · T2 진입 자동 리다이렉트 미배선(`resolveOnboardingStep` 가드 연결) · US-ONB-04 본체(발동·숙소폴백).
- **정본 수정 요청**(aidlc 쓰기 금지 — 요청만): R2 `BR-U0-10/11` ↔ D1(필수 2종+마케팅) 어긋남 · R4 `age.tsx` 드리프트 · A7 channel 클라 미전송 확정.
- **잔여 리스크**: R1 `LOCATION_TERMS` 미수집(LEGAL-01 잔여, c08에서 닫음) · R3 Figma c08 denied 문구 · R5 US-ONB-04 완료 표시 금지.
- **T3**: 필수약관 분류·라벨 클라 상수 — 서버가 required/label 주면 제거.

## 교훈

- **"목 제거"는 msw만이 아니라 목 데이터 모듈 전체의 앱 그래프 유입을 뜻한다.** 가드를 목과 함께 지우면 재발이 소리 없이 돌아온다 — 기계 강제(정적 그래프 가드) 없으면 게이트에서 걸린다.
- **한 필드 의존은 env DI로 끊는다.** 테스트가 결과를 조종하되 앱 번들엔 목이 안 실린다.
- **대용량 문서 산출은 API 중단에 취약** — test-designer 시정 라운드가 2회 끊겼다. 코드/문서 분리 산출이 복원력에 유리(코드는 프리즈로 무결 유지).
