# 2026-08-08 20260808-trip296-mustvisit-time

> 옵시디언 개발로그의 축약본. 상세(결정 이유·예상과 달랐던 것·문제로그 전문)는 옵시디언
> `TripPilot/개발로그/2026-08-08 20260808-trip296-mustvisit-time.md`를 정본으로 본다.
> [메모리]는 이 파일을 읽지 않는다 — 볼트 없는 사람과 MCP 실패 시 폴백 전용.

## 대상

TRIP-296 [FE] h05·h07 필수 방문지 선택 — 방문 시각 지정(고정 블록), US-SCHED-04.
`feature/TRIP-295-FE-timeband-slotkey` 브랜치, TRIP-295 위에 이어서 진행(기준 커밋 `ba67db3`).

## 특수 조건

**사용자 취침 중 무인 완주 지시.** 게이트 4개(①-1·①-2·①-3·②-1) 전부 선승인. 이해 체크
답변 0(무응답 — 오답·막힘 아님, 이해부채 미등재). 미상환 이해부채 41건, 파산 상한(10건) 초과 유예.

## 게이트① 재제시 2회 (둘 다 심판 결함)

1. **①-1→①-2**: 동결 테스트가 `pnpm tsc` 미통과(RN `TextInput` 타입 좁히기 TS2367). 구현자가
   발견해 고치지 않고 보고(옳은 판단). `String(node.type) === 'TextInput'`로 정정.
2. **①-2→①-3**: 409 케이스가 `router.back()`을 함께 요구해 안내가 실기에서 안 보임(목이 화면
   전환을 지워 테스트만 초록, code-critic 발견). `not.toHaveBeenCalled()` + 정착 창 확인으로 정정.

**하네스 규칙 후보 2건**: (1) red 확인 절차에 `pnpm tsc` 포함 (2) 화면 전환 단언 + 잔존 표시
단언이 같은 케이스에 있으면 의심.

## 5-b 리뷰 · 5-c 리팩토링

차단 0 · 경고 5 · 참고 4. 뮤테이션 8종 전부 red(비싼 심판 넷 생존) + 자체 반증 가설 4건 중
3건 green(심판 부재 발견). 5-c 실행(생략 아님) 5건: 409 되돌아가지 않기 · 다시 시도가 담은
장소도 재조회(실제 결함) · 진입 즉시 경고 제거 · `건너뛰기` 제거 · §0·§5 낡은 보고 정정.

## 게이트 통과 현황

04 리포트 n=1 PASS(정적 검사 4개 · 게이트① 8/8 해시 · 게이트② 13/13 해시 · red 소급 · 경계면
QA 전부 이상 없음). 04b 실기 스모크 n=1 PASS(h05·h07 콜드 딥링크 진입 확인).

## 구조 지도

`frontend/docs/structure.md` 246→258행 갱신(`--check` OK). 경고 절 13건 — 12건 상한 초과(+1,
eslint FEATURES 배열 사각지대 신규 항목).

## 개념·문제로그

신규 개념 4건 + 기존 5건 보강. 문제로그 신규 5건(게이트①-1→①-2·①-2→①-3·다시 시도 부분
재조회·eslint FEATURES 사각지대·반증가설 3/4 green). 상세는 옵시디언 개발로그 참고.

## 정본 반영

제시 7 / 선택 0(사용자 취침으로 3-a 진행 불가) — `aidlc/` 미기입.

## 다음에 이어서 할 일

BE PATCH 오퍼레이션 신설 · h07 배선 조인 심판 공백 · `건너뛰기` 복원 · 지도·검색·추가 타일 ·
TRIP-295 이월 2건 · 이해부채 정리 세션(파산 상한 초과 지속).

## 관련

- 옵시디언 정본: `TripPilot/개발로그/2026-08-08 20260808-trip296-mustvisit-time.md`
- 게이트: `TripPilot/게이트/2026-08-08 20260808-trip296-mustvisit-time 게이트{①-1,①-2,①-3,②-1}.md`
- 이전 사이클 devlog: `2026-08-08-20260808-trip295-timeband-slotkey.md`
