# 2026-07-22 — 20260722-trip163-trip-style

**결말: 완료** (게이트①-1·②-1 승인 + 검증 n=1 FAIL[린트 기준선, 라우팅상 통과 취급] + 실기 스모크 n=1 PASS) · 브랜치 `feature/TRIP-163-FE-trip-style`

> 상세는 옵시디언 개발로그(`TripPilot/개발로그/2026-07-22 20260722-trip163-trip-style.md`)가 정본이다. 이 파일은 요약본 — [메모리]는 이 파일을 읽지 않는다.

## 무엇을 했나

TRIP-163 취향 온보딩(US-ONB-05·06·07·09·10·11·14·15) — Figma `c09`(1/2)·`c09b`(2/2) 정합 구현. 화면 2개 + 리포 첫 Zustand 세션 스토어 + 내비게이션 + 일괄 탈출 + dev 프리뷰 2키.

- **신규 8**: `model/preferenceSelection.ts`(toggleMulti·toggleSingle 순수 함수) · `store/preferenceStore.ts`(Zustand, persist 금지) · `screens/PrefStep1Screen.tsx`·`PrefStep2Screen.tsx` · `containers/PrefStep1Container.tsx`·`PrefStep2Container.tsx` · `app/(onboarding)/pref1.tsx`·`pref2.tsx`
- **수정 3**: `NicknameContainer.tsx`(성공 목적지 `/`→`/(onboarding)/pref1`) · `app/_dev/preview.tsx`(pref1·pref2 2키) · `OnboardingGlyphs.tsx`(+19종 아이콘)
- **범위 밖**(브리프 §6): `PUT /me/preferences` 배선(Q1 — 라벨↔enum 매핑 미결), 활동 축 US-ONB-08(Q2 — Figma 화면 근거 없음), 예산 총액 직접입력(Figma 미설계)
- 구현 중 `ScrollView` 누락(콘텐츠가 화면 높이를 넘어 CTA·skip 링크가 밀려남)을 실기 스크린샷 대조로 자기 발견·수정 — jest는 스크롤 여부를 검증하지 않는 구조적 사각지대

## 게이트·검증

- **게이트①** 승인(1차, 반려 0, "이해했어 승인 ㄱㄱ") — 신규 테스트 10파일. 이해 체크 3라운드 재설명 후 이해부채 등재 0(개념 노트 `TS 타입 표기 읽는 법` 보존).
- **게이트②** 승인(1차, 반려 0, "이해했어 승인 ㄱㄱ 후속 티켓으로 기록해줘 마지막 기록까지 다 진행") — 구현 11파일. 독립 code-critic(03b) 차단 0·경고 1·참고 2, 전부 후속 티켓 이관(개념 노트 `구조 가드와 긍정 앵커`·`목 테스트의 이음매 사각지대` 보존).
- **검증 n=1 FAIL** (`04_qa-verifier_report_1_FAIL.md`): 기술 게이트 3/4(포맷·타입체크·테스트) PASS, 해시 21/21 일치, 변경 집합 자체도출 일치, red 소급 정상(vacuous 아님), 경계면 QA(openapi 참조 0·INV-3 실질 히트 0) 전부 PASS. **린트만 리포 전체 스코프 FAIL**(exit 1) — 원인 2건(하네스 파일 `structure-index.cjs` no-undef 1 + TRIP-162 기존 `makeAuthorize.realOauth.test.ts` require-import 경고 3) 전부 이번 사이클 변경 21파일 밖(git log 확인). 이번 21파일 한정 `eslint --max-warnings 0`은 exit 0. **라우팅: implementer 수정 대상 아님** — 기존 부채로 수용.
- **실기 스모크 n=1 PASS** (`04b_smoke_1_PASS.md`): 부팅·레드박스 없음·예상 밖 네트워크 0·pref1/pref2 도달 전부 충족. 특이사항(결함 아님): 같은 세션 연속 openurl 시 프리뷰 상태 미전환(딥링크 지연 초기화자 계약 한계 — 다음 스모크는 키마다 fresh 재기동).
- **구조 지도 불일치 11건**(qa 04 §6-2 이관) — [기록]에서 `docs/structure.md`에 반영 완료, `structure-index.cjs --check` 재통과(`OK — 문서 89행 ↔ 실제 89파일 일치 · 개념 경로 36개 전부 실존`).

## 후속 (이관)

1. 컨테이너 이음매 왕복 테스트 보강 — pace·budget·companion·transport·food-write 5축(03b 경고①, style·food-read만 있던 검증을 6축 전체로).
2. `onboardingPrefStructure.test.ts` 긍정 앵커 `/\bcreate\(/` → `from 'zustand'` 교체(게이트① 재승인 수반).
3. `NicknameContainer.tsx:3` 헤더 docstring stale 수정(03b 참고②).
4. lint 기준선 정비 — `structure-index.cjs` no-undef(하네스 파일, 사이클 밖 정비 세션) + `makeAuthorize.realOauth.test.ts` require-import 경고 3건.
5. 취향 wiring 사이클 — Q1 라벨↔enum 매핑 + `PUT /me/preferences` 배선.
6. (비차단) US-SHELL 홈 첫사용자 상태 사이클 — 범위 논의 완료, 다음 사이클 후보.

## 교훈

- **실기 스크린샷 대조가 jest의 원리적 사각지대(스크롤 여부)를 잡았다** — `ScrollView` 누락은 테스트 전부 green이어도 화면 하단이 실제로는 안 보이는 결함이었다.
- **목 테스트 두 개가 각각 통과해도 이음매는 비어 있을 수 있다** — code-critic이 컨테이너 prop↔스토어 액션 배선을 실제로 뒤바꿔 재실행해 승인 테스트 13건이 그대로 green임을 실증(경고①). 화면 목 테스트(가짜 콜백)와 스토어 고립 테스트(직접 호출)가 서로 다른 절반만 보고, 접착부(컨테이너 배선)는 어느 쪽도 안 본다.
- **저장 계약(openapi enum)과 화면 표시값(Figma)이 다를 때, 매핑을 짓지 않고 범위를 좁히는 것도 유효한 결정이다** — Q1에서 "이번엔 로컬 상태까지만, 매핑은 wiring 후속"을 택해 저장 불가능한 값(예: "해산물")을 억지로 매핑하지 않았다.
