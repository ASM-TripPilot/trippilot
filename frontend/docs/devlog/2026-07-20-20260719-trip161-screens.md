# 2026-07-20 · TRIP-161 로그인·스플래시 비주얼 재구현 (devlog)

- cycle-id: `20260719-trip161-screens` · 브랜치 `feature/TRIP-161-login`
- 대상: **US-SHELL-01**(스플래시)·**US-ONB-01**(소셜 로그인) 화면의 **프레젠테이션 재구현**(Figma 정합, figma-screen-impl 스킬). 유닛 U0 · 컴포넌트 C1 Auth. 검은 네모·텍스트 → 브랜드 그라디언트·실 SVG 로고/아이콘/일러스트. **직전 겹1 배선이 남긴 "Figma 비주얼 미구현" 문제 해소.**
- 옵시디언 개발로그: `TripPilot/개발로그/2026-07-20 20260719-trip161-screens.md` (게이트 노트·이해부채·개념·문제로그 링크 허브)

## 결과

- **동작 계약 불변**(props 8·testID·조건부 UI·onSignIn 무변경) 위에 표면만 교체 → 신규 시각 가드 **9 green** + 기존 행위 계약 **16 green**(auth 스크린 25/25). 전체 node **82/82** · 통합 **19/19** = **101 green, 회귀 0**. tsc strict 0 · eslint 0 · prettier clean.
- 신규(프로덕션): `src/features/auth/lib/gradients.ts`(그라디언트 stop 상수 1출처) · `components/AuthGlyphs.tsx`(AppIcon + 브랜드 4아이콘 인라인 SVG, path 11개 전부 Figma 에셋 복사) · `components/SplashIllustration.tsx`(비행경로 곡선+마커+핀2).
- 수정: `screens/SplashScreen.tsx`·`screens/SocialLoginScreen.tsx`(프레젠테이션 교체, testID·문구·시그니처 불변) · `src/app/_layout.tsx`(폰트 실로딩 배선) · `tailwind.config.js`(fontFamily 4종) · `src/__tests__/design-tokens.test.ts`(미러 동기화) · `package.json`/`pnpm-lock.yaml`.
- 설치 6종: `expo-linear-gradient` · `react-native-svg` · `expo-font` · `@expo-google-fonts/inter` · `/noto-sans-kr` · `expo-splash-screen`. (`react-native-svg-transformer`는 인라인 방식이라 미도입.)

## 결정 (D1~D6)

- **D1 그라디언트** = `auth/lib/gradients.ts` 상수 모듈(스플래시 3-stop·앱아이콘 2-stop). className이 될 수 없는 raw 배열 → 스플래시·로그인 공용 1출처, `as const` 튜플로 `LinearGradient.colors` 타입 충족.
- **D2 폰트** = `@expo-google-fonts`(Inter + Noto Sans KR). `useFonts` 로드 + `tailwind.config` `fontFamily` 매핑 → `design-tokens.test.ts` 미러 동기화(한 쌍).
- **D3 SVG** = 인라인 `<Svg><Path>`. Figma 에셋 좌표·색 1:1 복사, `var(--fill-0)` 폴백색 실색 고정. `viewBox` 고정 + `size`만 바꿔 앱아이콘 비례 축소(스플래시 56·로그인 34).
- **D4 도트** = 정적 3점(opacity 0.45). 애니메이션(reanimated 펄스) 후속.
- **D5 라벨** = 한글(구글/애플/네이버로 계속하기) — **단 kakao만 영문 유지**(트레이드오프 B, 아래).
- **D6 앱아이콘** = 비례 축소 단일 에셋.
- 핵심 함정: `shell-splash-gradient`(바깥 LinearGradient)와 `shell-splash-root`(안 View)를 **다른 노드로 분리**(한 요소당 testID 하나) · 아이콘 testID를 버튼 안에 중첩(`within` 요구) · 도트 3개 동일 testID.

## 게이트 / 검증

- 게이트①(테스트) 승인 2026-07-20 00:19:22 ("ㄱㄱ", 동작 신뢰 승인) · 게이트②(구현) 승인 2026-07-20 01:08:34 ("지금대로 View로 승인할게 ㄱㄱ", ②-2).
- **게이트②-1은 무효화** — implementer가 "`src/app/`이 비어 useFonts 자리 없음"이라 폰트 실로딩을 미뤘으나 **사실 오류**(RootLayout 실재). 오케스트레이터 정정 → 폰트 실로딩 배선 후 **②-2 재제시**(트레이드오프 A 해소).
- 검증: **n=1 PASS**(`04_qa-verifier_report_1_PASS.md`). prettier clean · eslint 0 · tsc 0 · node 82/82 · 통합 19/19(101 green). 게이트① 해시 **2/2** · 게이트② 해시 **10/10** 일치(변조 0). red 소급 9 red 재현(비-vacuous). INV-3 grep clean · aidlc 무변경 · 경계면 API 교차 해당 없음(순수 프레젠테이션).
- **B층(픽셀·폰트 실렌더)**: 미실행 — dev 빌드+시뮬레이터 부재. 비-FAIL 후속(스크린샷 ↔ Figma 육안 대조 AC-PX-1..8).
- advisory(비차단): `.claude/agents/{implementer,spec-analyst}.md` +1줄씩 **선존재 하네스 편집**을 검증서 발견(세션 시작 스냅샷·03 §4 교차 확인 → 코드 게이트 무관). 후속 권고: 하네스 변경이력 등재 + `chore(harness)` 커밋 분리(사용자 판단).

## 트레이드오프 (승인 시 사용자 수용)

- **(A) 폰트 실로딩** — 게이트②-2에서 **해소**(RootLayout에 `useFonts` + `expo-splash-screen`).
- **(B) kakao 라벨만 영문** — 동결 충돌 시트 테스트 `getByText(/카카오/)`가 화면에 "카카오" 하나만 가정 → 버튼 한글화 시 2매칭으로 깨짐. 테스트 수정 금지라 라벨 양보. 후속: `within(sheet)` 스코프화(별도 게이트①).
- **(C) SafeAreaView → View** — `SafeAreaProvider`/jest 목 미설정. 여백 `pt/mt` 고정값. 노치 인셋 실대응은 앱 셸 배선 후속.

## 이슈 (옵시디언 문제로그 2건)

- **implementer "앱 루트 비어있음" 사실 오류** → 게이트②-1 무효화·②-2 재제시. `find app` 오매칭이 원인. 교훈: 서브에이전트의 "없음/비어있음" 주장은 오케스트레이터가 실경로로 교차검증. (`TripPilot/문제로그/2026-07-20 implementer 앱루트 사실오류 (게이트②-1 무효화).md`)
- **미신고 하네스 편집(비차단)** — `.claude/agents/*.md` 선존재 편집. 직전 사이클 "하네스 파일 혼입" 에러의 반복 패턴이나 이번은 선존재라 비차단. (`TripPilot/문제로그/2026-07-20 미신고 하네스 편집 비차단 (.claude agents).md`)

## 이해부채 (건별 6 · 옵시디언 `TripPilot/이해부채/`)

- 게이트① IC-1 testID 신규·기존 비충돌 · 게이트① IC-2 미설치 패키지 비-import·clean red · 게이트② IC-1 RootLayout `return null` 82테스트 비영향 · 게이트② IC-2 네이티브 스플래시 vs 인앱 SplashScreen · 부분습득 useFonts · SafeAreaView vs View.
- 개념 노트 10종 신설/보강: expo-linear-gradient · react-native-svg · useFonts · 네이티브 스플래시 vs 인앱 스플래시 · SafeAreaView vs View · testID 가드 전략 · 깨끗한 red · RNTL 쿼리 · as const 튜플 · NativeWind 임의값.
- **잔고 20**(파산 상한 10 초과) — 다음 [메모리]서 이해부채 정리 세션 필수.

## 후속 / 이월

- B층 픽셀·폰트 실렌더 대조(dev 빌드+시뮬레이터) · kakao 한글화(`within(sheet)`) · SafeAreaProvider 배선(트레이드오프 C) · 로딩 도트 애니메이션(D4) · 하네스 정리(비차단) · 이해부채 정리 세션(우선).
- 카탈로그(`docs/와이어프레임-화면-IO정리.md`) 밴드 c(c01/c02): IO 계약 불변이라 Output 표 재작성 없음, 스냅샷 헤더에 2026-07-20 비주얼 재검증 주석만 갱신.
