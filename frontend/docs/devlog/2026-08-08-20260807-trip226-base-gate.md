# 2026-08-08 20260807-trip226-base-gate

> 축약본 — 상세는 옵시디언 개발로그 `TripPilot/개발로그/2026-08-08 20260807-trip226-base-gate.md`
> (볼트가 없거나 MCP 실패 시 폴백. [메모리]는 이 파일이 아니라 옵시디언 쪽을 읽는다.)

## 대상

TRIP-226 거점 지정 전제 게이트 — 좌표 미확정·날짜 없음·기간 밖(BR-U1-22·26·27). 브랜치
`feature/TRIP-224-FE-base-sections` 계속(선행 TRIP-225 위). 신규 3파일(`baseGate.ts`·`useBaseFix.ts`·
`TripBaseFixSheet.tsx`) + 기존 2파일 수정(`TripWizardStep2Screen.tsx`·`TripNewStep2Page.tsx`) + 225
동결 1파일 보강(`TripNewStep2Page.test.tsx`, `jest.mock` 4줄).

## 게이트

**4개**(①-1·①-2·①-3·②-1). ①-2·①-3은 전부 **적대적 리뷰/뮤테이션이 심판 사정거리 오판을 찾아** 재개방됐다
— "코드가 틀렸다"를 보인 뮤테이션은 23개 중 **0건**.

- **①-2**: 02a §1이 렌더 사정거리만 보고 모듈 목 사정거리를 빠뜨림 → 225 동결 T3 33케이스
  "No QueryClient set". `jest.mock` 4줄 보강.
- **①-3**: env 규율을 시트 파일(N2)에만 적고 배선 파일(N4)에 안 적음 → 좌표 축이 배선 층에서 무심판.
  뮤테이션 0/6→6/6 kill. 케이스 2개 추가(22→24).
- **게이트①-1 Q5 출제 사고**: 선택지 3개가 동시에 "안 바뀐다"로 성립해 문항 무효(오답 아님).

## 검증

[검증] `04_qa-verifier_report_1_PASS.md` — 정적 4종 PASS, 게이트①-2 해시 7/7·게이트② 해시 5/5 일치,
red 소급 통과(구현 5파일 스태시→red 확인→복원), 트립와이어(225 동결 82/82) 유지, 경계면 QA 새 결함 없음
→ **PASS**. `04b_smoke_1_PASS.md` 실기 스모크 n=1 **PASS**(기계 조건 0/5 미해당, 사용자 판단으로 실행 —
WebView×BottomSheet 리포 최초 조합) — WebView가 BottomSheet 안에서 실제로 지도를 그림, `info` 토큰
(리포 첫 소비자) 정상 렌더. ⚠️ **핀 탭 실동작·날짜 칩 탭 전이는 자동화 불가로 미검증.**

## 핵심 결정

BR-U1-27 기간 경계 등호 포함(`checkIn >= start && checkOut <= end`) · `baseBlockReason` 우선순위
`DATES_MISSING > DATES_REVERSED > COORD_UNCONFIRMED`(★3) · 시트는 `useState` 0(저장 판단은 배선 소유,
D3) · `mapUnavailable`은 env 키 부재만 판정(런타임 SDK 실패는 신호 없음, 자기신고) · 보완 진입 버튼 하나로
통합(★20).

## 예상과 달랐던 것

동결 82케이스 재제시 두 번 다 코드 문제가 아니라 심판 판정 문제였다(승인 6파일은 두 차수 다 한 글자도
안 바뀜). 테스트 파일 안의 정직한 근거 주석("이 파일은 env 키 세울 필요 없다")이 오히려 결함의 알리바이가
됐다. 적대적 리뷰가 찾은 것도 전부 "무심판"이지 "오동작"이 아니었다 — 6-b가 찾은 CTA 시각 결함(TRIP-225
선재)도 코드는 옳고 색상이라는 다른 축이 무심판.

## 정본 반영 (aidlc)

**제시 5 / 선택 4 / (A) 0 / (B) 4.** A(testID 드리프트 `trip-base-blocked-{id}`→`trip-base-assign-
blocked-{id}`) · B(BR-U1-27 경계 등호, 서버 정본 미확인 병기) · C(`error.code` enum 부재 관측, BE 티켓
후보) · D(`SavedStay` 신선도 필드 부재 관측, AC-19 영구 미충족, BE 티켓 후보). E(e05 편집 모드)는 3-a
미선택 — 지라 TRIP-192 소관. `audit.md`에 Post-Design Correction 9번째 후속으로 append.

## 이해부채

신규 5건 — [[2026-08-07 무엇을 목킹하고 무엇을 진짜로 태우나]] · [[2026-08-07 서버 갱신과 화면 기억은
따로 고쳐야 한다]] · [[2026-08-07 심판 사정거리 판정과 구현 정확성은 다른 것이다]] · [[2026-08-07 순차
await가 계약인 자리]] · [[2026-08-07 파생 데이터는 원본이 바뀌면 다시 받아야 한다]]. 상환 0건. 신규
개념 1건([[동기화의 차집합 계산]]) + 기존 3건 보강([[가드의 사정거리]]·[[jest.mock 모듈 바꿔치기]]·
[[순서 보장]]). ⚠️ 누적 미상환 **42건**(37+5), 파산 상한 10건 초과한 채 **4회 연속 유예**(TRIP-289·224·
225·226) — 사용자 명시 선택.

## 문제로그

신규 3건 — [[2026-08-07 동결 파일 개방 판정이 모듈 목 사정거리를 빠뜨렸다]](게이트①-2, 하네스 규칙
후보) · [[2026-08-07 env 규율을 한 파일에만 적용해 좌표 축이 통째로 무심판이었다]](게이트①-3, 하네스
규칙 후보 실측 2건째) · [[2026-08-08 차단 카드 CTA가 활성과 시각적으로 같은데 jest가 원리적으로 못
잡는다]](6-b 발견, TRIP-225 선재, 미해결).

## code-critic 실적

차단1·경고4·참고8·실제 조치2(차단은 게이트①-3으로, 경고 1건은 5-c로) · 스타일 지적 비율 0%(13건 전부
커버리지·구조 결함). 사다리(더 게으른 대안) 제안7·채택1.

## 후속 티켓 후보 / 다음에 이어서 할 일

1. ⭐ 차단 카드 CTA 색상이 활성과 구별 안 됨(TRIP-225 선재, 6-b 발견) — 최우선.
2. 스토어 통째 구독(리렌더 레버, 근본 해결은 게이트① 재진입 필요).
3. 확장 `PATCH /trips`의 404를 숙소 404로 오인.
4. ★9 정렬 금지가 배선 구조 심판 사정거리 밖.
5. e05 편집 모드 신설(TRIP-192).
6. BE: `error.code` enum 신설.
7. BE: `SavedStay` 신선도 필드 신설(AC-19 해소 선행조건).
8. 실기 미검증 2건(핀 탭 실동작·날짜 칩 탭 전이) 사용자 직접 확인.
9. US-TRIP-04 스토리 종료 판정 시 AC-19 영구 미충족 명시.

## 하네스 규칙 후보 (반영은 재발 관찰 후 유예)

1. 동결 파일 개방 판정에 모듈 목 사정거리 축 추가(실측 1건).
2. 테스트 환경 전제(env 키·목)는 그 축을 태우는 모든 파일에 적용 여부를 따진다(실측 2건째).
3. 6-b 스모크 발동 조건에 "기존 네이티브 모듈을 새 조합/컨테이너에 넣는 변경" 행 추가 여부(실측 1건).

## 미커밋

작업트리 12파일(구현 신규3·수정2·225 동결 보강1·dev preview1·테스트5) + `frontend/docs/structure.md`
+ `aidlc/` 3파일, 브랜치 `feature/TRIP-224-FE-base-sections`.

## 상세

옵시디언 개발로그·게이트 노트 4개(①-1·①-2·①-3·②-1)·이해부채 신규 5건·개념 노트 신규 1건+보강 3건·
문제로그 신규 3건·구조 지도 갱신(240→244행)·`aidlc/` 정본 반영 4건.
