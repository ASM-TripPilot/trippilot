# 2026-08-05 20260805-trip222-error-placement

> 축약본. 상세(결정 이유·예상과 달랐던 것·하네스 규칙 후보)는 옵시디언 개발로그를 정본으로 본다 — 이 파일은 볼트 없는 사람/MCP 실패 시 폴백이다.

## 대상

**경량 사이클.** d04 장소 탐색 화면(`PlaceExploreScreen.tsx`) `error` 얼굴의 에러 안내가 검색바·카테고리 칩·정렬(`ListHeaderComponent`)을 아래로 밀어내던 결함 수정. 커밋 `e2dae28`(TRIP-222). 브랜치 `feature/TRIP-221-place-explore-save`(origin 푸시됨, 이 커밋은 미푸시).

원장: `_workspace/20260805-trip222-error-placement/00_gates.md` 하나뿐(경량 사이클 규칙 — 01/02/03/03b/04 산출물 없음, 게이트①·② 1회 통합 제시, 적대적 리뷰 생략).

## 발견 · 원인

TRIP-223 완료 후 사용자 요청 프리뷰에서 발견 — jest는 testID 존재만 봐서 두 배치 모두 통과, 실기 스모크가 값을 낸 두 번째 사례. 원인은 TRIP-222 5-c의 W-1 수정(`ErrorNotice`를 `ListEmptyComponent` 밖으로 뺌) — 의도는 맞고, 뺀 자리가 `FlatList` 앞이라 헤더까지 밀어낸 게 문제.

## 변경

1. `ErrorNotice`를 `ListHeaderComponent` 안, `SortRow` 뒤로 이동.
2. 심판 1건 추가 — `within(grid).getByTestId('explore-places-error')`.
3. 주석에 "헤더 안인 이유" 남김.

## 검증

`pnpm lint` 0 error · `pnpm tsc` 0 · `pnpm test` node 967(신규 1건)/integration 137 전량 green · 선행 4칸 무회귀. 실기 재확인 PASS(컨트롤 순서 복구, 레드박스 없음 — 정식 `04b` 산출물은 없음, 인라인 수행).

## 하네스 규칙 후보 (상세는 옵시디언)

1. AC-V3 픽셀 대조 실행 슬롯 — 3회 관측 불변(이번 칸은 해당 없음), 별도 실측이 취지 보강.
2. `git stash`→`cp` — 해당 없음(기재만).
3. **신규**: 실기 스모크 발동 조건에 `src/features/*/ui/**` 레이아웃 변경이 빠져 있음 — 관측 1건.

## 구조 지도

파일 수 변화 없음 — `structure-index.cjs --check` OK(232행 일치). 갱신 불필요.

## 옵시디언 기록

- 개발로그: `TripPilot/개발로그/2026-08-05 20260805-trip222-error-placement.md`
- 문제로그: `TripPilot/문제로그/2026-08-05 리뷰 지적을 닫은 자리가 헤더를 밀어냈다 (TRIP-222 W-1 위치 오류).md`
- 개념 보강: `TripPilot/개념/testID 가드 전략.md`
- 개발일지: `TripPilot/개발일지/2026-08-05.md`
