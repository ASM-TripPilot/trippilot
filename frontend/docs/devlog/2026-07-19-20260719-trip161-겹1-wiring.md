# 2026-07-19 · TRIP-161 겹1 mock 배선 (devlog)

- cycle-id: `20260719-trip161-wiring` · 브랜치 `feature/TRIP-161-login`
- 대상: US-SHELL-01(앱 시작 분기) + US-ONB-01(소셜 로그인) 동작 완결 슬라이스. 유닛 U0 · 컴포넌트 C1 Auth·C2 User Profile. **직전 로그인 사이클이 defer한 프리뷰 배선 회수**.
- 옵시디언 개발로그: `TripPilot/개발로그/2026-07-19 20260719-trip161-wiring.md`

## 결과

- 신규 소스 **11** + 수정 **2** + 삭제 **1**(preview.tsx 흡수)로 게이트① 승인 테스트 4 + 인프라 6 **무수정** → 두 버킷 **91 green(node 72 + integration 19), 회귀 0**. tsc·eslint·prettier clean.
- 신규: `src/features/auth/lib/makeAuthorize.ts`(fake provider 팩토리) · `containers/SplashGate.tsx`(게이트 컨테이너) · `containers/SocialLoginContainer.tsx`(로그인 컨테이너, 이번 사이클의 심장) · `src/mocks/native.ts`(RN dev MSW) · `src/app/(auth)/{_layout,login}.tsx` · `src/app/(onboarding)/{_layout,index}.tsx` · `src/app/reconsent.tsx` · `src/app/force-update.tsx` · `src/app/_dev/scenarios.tsx`
- 수정: `src/app/_layout.tsx`(`<Stack>` → `<SplashGate/>` + 모듈 로드 시점 MSW init, GestureHandlerRootView 유지) · `src/app/(tabs)/index.tsx`(`/preview` 링크·`Link` import 제거)

## 결정 (D1~D6)

- **D1 라우트 = A안(선언형 가드, Stack.Protected)**. 루트 `index.tsx` 신설 안 함 → `(tabs)/index`와 URL `/` 충돌 회피(홈 유지). `destination` 값 변화로 잠정→교정 재분기 자동 반영(명령형 되감기 버그 원천 차단).
- **D2 mock = MSW**(테스트+런타임 공용). `msw/node`=jest 통합, `msw/native`=RN dev. 기존 57 유닛 `jest.mock` 유지.
- **D3 성공 후 = 부트스트랩 재평가**(게이트 복귀 → 서버 재판정). 컨테이너가 `isNewUser`로 직접 분기 안 함(분기 권위 서버 단일화).
- **D4 dev 시나리오** = preview.tsx → `_dev/scenarios` 라우트 재활용. `(tabs)/index` `/preview` 링크 제거.
- **D5 refresh/충돌** = mock 핸들러 계약 잠정 고정(실 single-flight는 겹2). env 키 `EXPO_PUBLIC_API_MOCK`·`EXPO_PUBLIC_AUTH_FAKE`. `conflictProvider`=provider 코드(`kakao`)로 통일.
- **D6 폴더 관례** = `containers/`·`lib/`를 README:54 관례에 추가(코드-정본 정합). containers/=훅↔화면 접착, lib/=순수함수 아닌 팩토리(makeAuthorize).
- R2 QueryClientProvider **미도입**(YAGNI — 현 훅 TanStack Query 미사용). [리팩토링] 생략(clean + 게이트② 해시 동결 유지).

## 게이트 / 검증

- 게이트①(테스트) 승인 2026-07-19 17:20:04 · 게이트②(구현) 승인 2026-07-19 19:14:56.
- 검증: **n=1 PASS**(`04_qa-verifier_report_1_PASS`). prettier clean · eslint 0 · tsc 0 · test:node 12 suites/72 · test:integration 2 suites/19 (**91 green, 회귀 0**). 게이트① **13/13** · 게이트② **13/13** 해시 일치(변조 0). red 소급 비-vacuous · 미신고 stray 0 · aidlc 무수정 · 경계 QA(W-04·12·13·14·15·INV-3 충족).
- advisory(비블로킹): 409 바디 `existingProvider`가 openapi `ErrorResponse`에 미열거(mock 잠정 계약) → 겹2 backend 연동 시 openapi 409 스키마 명시 권고.
- 커밋: 사용자 지시 시.

## 이슈 (옵시디언 문제로그 2건)

- **MSW를 jest에 태우기**: `msw/node` 미해석·ESM 트랜스폼·`--experimental-vm-modules` 충돌(`importBoundary.test`는 이 flag 필요) → 테스트를 node/integration 두 버킷으로 분리(`*.integration.test.*`), `pnpm test` 순차 실행. 동작하나 설정 복잡도 증가.
- **로그인·스플래시 Figma 비주얼 미구현**: 기능 와이어프레임(디자인 토큰 0·브랜드 버튼 없음·로고 검은 네모). 원인=TDD에 Figma 정합 검증 축 부재(구현자가 testID+동작만 만족) → 전용 화면 비주얼 사이클(Figma read→토큰·자산→스크린샷 대조) 후속 필요.

## 이해부채 (신규 11 · 잔고 14, 파산 상한 10 초과)

- 게이트① 7: MSW 3요소 · 컨테이너/프레젠테이션 분리 · Stack.Protected 선언형 가드 · DI(makeAuthorize) · env 토글 · 부트스트랩 재평가(D3) · RNTL 비동기 관찰.
- 게이트② 증분 4: 팩토리 패턴 · PKCE · router.replace · React effect 순서/모듈로드 타이밍.
- 사용자 명시 이월(개념 나중에 직접 확인·상환). **잔고 14로 상한 초과** → 다음 [메모리]서 정리 세션 우선 제안 대상.

## 후속 / 이월

- 이해부채 정리 세션(우선) · 겹2 실연동(실 OAuth·백엔드·MSW native RN 런타임·single-flight refresh 실배선) · 로그인·스플래시 전용 비주얼 사이클 · openapi 409 `existingProvider` 명시(겹2).
- R1 MSW native RN 런타임 **미검증**(best-effort, 폴백 env fake seam). 테스트 경로 `msw/node`는 견고(오라클 11 green이 인터셉트 증명).

## 하네스 (별도 트랙)

- 이 세션 중 **대화로그 자동 적립 훅** 신설(UserPromptSubmit → 사용자 입력 프롬프트를 `TripPilot/대화로그/YYYY-MM-DD.md`에 날짜별 append). 옵시디언 `하네스 변경이력` 1행 등재. TRIP-161 산출물과 별개(`settings.local.json`은 gitignore, 훅 스크립트는 `~/.claude/`).
