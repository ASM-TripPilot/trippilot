# 2026-08-02 20260802-trip210-social-sdk (요약)

**TRIP-210 [FE] 소셜 로그인 네이티브 SDK 경로 — 카카오·네이버 access token** · 브랜치 `feature/TRIP-210-social-native-sdk-login` · 부모 TRIP-75, AC 근거 US-ONB-01

> 옵시디언 개발로그의 축약본이다. 전체 서사·게이트 노트·이해부채는 볼트에 있다.

## 무엇을 했나

`POST /auth/social/{provider}/token`을 부르는 프론트 경로 신설(카카오·네이버 네이티브 SDK access token). 신규: `kakaoAuthorize.ts`·`naverAuthorize.ts`(SDK 정적 import 유일 파일) · `postSocialTokenLogin` · config plugin 2개(`app.config.ts`, 리포 최초). `AuthorizeResult` 판별자를 `'success-code'`/`'success-token'`으로 확장, google은 기존 code 경로 유지.

## 운영 모드

**자율 — 게이트①(3차수)·②(1차수) 전부 오케스트레이터 대리 승인.** 실시간 대화 없음, 한 줄 주해는 게이트①-1 노트 §4에 문서로 대체(옵시디언에만 있음).

## 결과

- 검증 n=1 **PASS**(포맷·린트·타입·테스트 전부 통과, 해시 26/26 일치, red 소급 genuine)
- 실기 스모크 n=1 **PASS(관측 범위 내)** — 부팅·렌더까지만, 실 로그인(AC-1R·AC-2R·AC-9)은 **여전히 미검증(L2)**, pod install 전
- code-critic: 차단 0 · 경고 4 · 참고 3 → 5건 처리(게이트①-3 심판 신설), 2건 미룸(Android 취소 포맷·iOS 전용 게이트, 후속 티켓 후보)

## 문제로그 (상세는 옵시디언)

- `socialSdkSecrets.test.ts` 대입 탐지기가 `\s`의 개행 포함을 놓쳐 오탐 → 게이트①-2에서 여러 줄 자가검사 추가로 수복
- 호스트 JDK 부재 → 컨테이너 실행, `GRADLE_USER_HOME`을 리포 밖으로 지정 필요
- `expo prebuild`를 `.env` 채우기 전에 돌리면 `Info.plist`에 빈 값이 굳음 — 순서 확정(`.env` → prebuild → pod install → 재빌드)

## 정본 반영

3건 선택(A·B·C) — A·B(aidlc construction 스펙 정정)는 **3사이클 연속 보류**(AI-DLC 실시간 승인 게이트를 자율 모드에서 못 밟음, `aidlc/` 미변경). C(Jira TRIP-210 본문의 "TRIP-197이 config plugin 구조를 세운다" 전제 오류)는 `jira.sh comment`로 처리 완료.

## 다음에 이어서 할 일

1. 정본 반영 A·B 처리(누적 4건 — TRIP-199분 2건 + 이번 A·B)
2. TRIP-211(백엔드, 하네스 없음) — `existingProvider` 계약 추가
3. push → PR → develop 머지
4. 게이트①-2·①-3·②-1 노트 옵시디언에 작성(이번 [기록] 범위 밖, 오케 후속)

## 관련

- 상세: 옵시디언 `TripPilot/개발로그/2026-08-02 20260802-trip210-social-sdk.md`
- 원장: `_workspace/20260802-trip210-social-sdk/00_gates.md`
