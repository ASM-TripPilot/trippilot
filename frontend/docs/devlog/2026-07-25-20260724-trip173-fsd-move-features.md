# 2026-07-25 — 20260724-trip173-fsd-move-features

**결말: 코드층 완료, 미커밋** (게이트①-1 무효 재제시 → ①-2 승인 · ②-1 승인 + 검증 n=1 PASS + 실기 스모크 n=1 PASS) · 브랜치 `chore/TRIP-173-FE-fsd-architecture`

> 상세는 옵시디언 개발로그(`TripPilot/개발로그/2026-07-25 20260724-trip173-fsd-move-features.md`)가 정본이다. 이 파일은 요약본 — [메모리]는 이 파일을 읽지 않는다.

## 무엇을 했나

TRIP-173 FSD 아키텍처 이주 **사이클 1/11**(전체 계획: 1~4 이주 → 5~11 코드 정독). **순수 파일 이동 + 개명, 동작 변경 0.**

- `features/auth`: `screens`+`components`→`ui`, `hooks`→`model`, `lib`의 `oauthConfig.ts`·`gradients.ts`→`config`(`makeAuthorize.ts`·`realAuthorize.ts`는 `lib/` 제자리).
- `features/onboarding`: `screens`+`components`→`ui`, `hooks`+`store`+기존 `model`→`model`(`store/` 디렉토리 폐지).
- 신설 `src/pages/`(5슬라이스: login·onboarding-{terms,nickname,pref1,pref2}) — 구 `containers/*`가 이주, 심볼도 `*Container`→`*Page` 개명.
- 신설 `src/app-shell/` — `SplashGate`가 `src/app` 밖으로 이주(Expo Router가 `app/`을 이미 점유).
- 신규 구조 가드 `src/__tests__/fsdStructure.test.ts` 1개(사이클 2~4가 여기 덧붙여 자람).

## 게이트·검증

- **게이트①** ①-1이 사용자 질문("fsd 확인되면 저 테스트 삭제해도 되지 않나")으로 **무효 재제시** — `fsdStructure.test.ts`에 졸업 조건(A 영구/B 한시) 주석 추가 후 ①-2 승인.
- **게이트②** ②-1 승인. 5-b code-critic(03b) 차단 0·경고 3·참고 2 — 실제 조치 2건(서술 정정·낡은 주석), 미처리 3건은 후속(git add -A 필요·배럴 강제 미흡·금칙어 가드 사정거리는 5-c에서 이미 해소).
- **검증 n=1 PASS**(`04_qa-verifier_report_1_PASS.md`): 포맷·린트·타입·테스트(350/350) 전부 PASS · 해시 대조 전부 일치(재제시 불필요) · 변경집합 자체도출 71=71 완전일치 · red 소급 확인 완료(대피 중 배열 사고 있었으나 즉시 복구, byte-identical 확인) · A~E 전부 PASS · F(구조지도 74건 불일치)는 FAIL 아니고 [기록] 이관 → 이번 [기록]에서 완전 해소(`structure-index.cjs --check` OK).
- **실기 스모크 n=1 PASS**(`04b_smoke_1_PASS.md`): Metro 캐시 비우고 재기동, 1855 modules 전부 해석(`Unable to resolve` 0), 로그인 화면+프리뷰 딥링크 도달.
- **구조 지도**: `frontend/docs/structure.md` 전면 갱신(신설 pages/app-shell 절 + auth/onboarding 계층 개명 반영 + `## 재사용 공개 API` 표 정정 — 갱신 안 했으면 다음 사이클이 `tsc TS2307`로 죽었을 것). `structure-index.cjs --check` → `OK — 105행/105파일 일치 · 개념 47개 실존`.

## 후속 (이관)

1. **사이클 2/11** — `features/home` + `shared` 세그먼트 개명 + `entities/session` 신설.
2. **미커밋 상태** — 커밋 직전 `git add -A` 후 `git status --short` 빈 출력 확인 필수(경고-3, 배럴 6개가 인덱스 밖이라 지금 커밋하면 깨진 커밋).
3. 사이클 3 대기: 기존 auth/onboarding 배럴 신설 + 딥 import 35건 정리 + `fsdStructure.test.ts`의 `toContain` 부분 문자열 완화(회귀 방지력 보강).
4. 이해부채 파산 차단 유예 지속 — 미상환 15건(상한 10 초과, 개선 추세).

## 교훈

- **서브에이전트 오경로 산출 + 정리 시 `rm -rf` 사고** — `03_implementer_notes.md` 소실 후 5-c에서 실제 diff로 재작성 복원. 절대경로를 줘도 오경로 산출이 2회 반복(하네스 규칙 후보).
- **macOS 기본 bash(3.2)에 `mapfile` 없음** — qa-verifier red 소급 절차에서 조용한 실패 → 빈 배열 → 71개 전체 스태시 사고, 즉시 감지·복구·해시 검증.
- **장치 판정 규칙(졸업 조건) 실측 위반 1건** — 신규 가드 제시 시 졸업 조건 누락, 사용자 질문으로 발각·즉시 교정.
- **트립와이어 형식상 해당·기제상 미해당 판정** — 게이트① 반려·재제시 1회가 발생했지만 원인이 분할 핸드오프 실패가 아니라 오케 규칙 누락이라 트립와이어는 미발동.

상세(전줄 결정 근거·문제로그 2건 신규·이해부채 2건 등재·개념 노트 2건 신설·모델 배치)는 옵시디언 개발로그 참조.
