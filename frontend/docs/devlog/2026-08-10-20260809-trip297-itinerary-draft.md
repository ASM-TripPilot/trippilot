# 2026-08-10 · 20260809-trip297-itinerary-draft

> 축약본. 상세(결정 이유·예상과 달랐던 것·개념 노트·문제로그)는 옵시디언 개발로그 `TripPilot/개발로그/2026-08-10 20260809-trip297-itinerary-draft.md`를 정본으로 본다.

## 대상

TRIP-297 [FE] h11 "AI 추천안 초안" 화면 본체 — 시간대 라벨·고정 블록 시각 예외·2단계 생성(PARTIAL→COMPLETE) 폴링 (US-SCHED-01·03·04, 부모 TRIP-90). 브랜치 `feature/TRIP-297-itinerary-draft`, 17파일(프로덕션 10·테스트 6·프리뷰 1) 한 커밋 `bba097a`.

## 요약

- 폴링은 자체 타이머 없이 TanStack Query `refetchInterval` 함수형 인자의 `query.state.dataUpdateCount`(반환값엔 없음) 하나로 센다.
- 고정 블록(`isFixed:true`)만 `startAt.slice(0,5)`로 시각을 예외 노출, 나머지는 시간대 라벨만.
- 적대적 리뷰 3회차 — 1차 차단3 → 2차 차단2(그중 1건은 **1차 수정이 새로 연 결함**: 폴링 카운터를 되감으려 쓴 `resetQueries`가 캐시 데이터까지 버려 1차에서 막은 사고가 다른 문으로 재발) → 3차 차단0.
- 승인 동결 테스트(I3)가 "더 곧은" 구현(조회 완료 전 선제 비활성화)을 막음 — 사용자가 게이트①-2를 열지 않기로 결정(데이터 손실은 이미 두 겹 방어).
- 게이트①·② 각 1차 승인. 이해 체크 오답 3+4, 이해부채 신규 8건.

## 검증

> `pnpm format` → `pnpm lint`(0 error / 31 warning, generated 선재) → `pnpm tsc`(0) → `pnpm test`.
> test:node 141 스위트 1359 통과 · test:integration 26 스위트 180 통과.
> 해시 대조: 게이트① 4/4 · 게이트② 12/12 · 게이트②-2 16/16(전부 일치).

| # | 심은 위반 | 결과 |
|---|---|---|
| 1 | 비고정 카드 시각 강제 표기(`isFixed?` 조건 제거) | red(AC-1) |
| 2 | 폴링 상한 판정 조건 제거 | red(M8) |
| 3 | 재시도 1차 방어선(`canRetry`) 제거 | red(I3) |
| 4(추가) | `handleRetry` 내부 확정 재확인 제거 | green — 이미 공개된 커버리지 공백의 재확인(후속 티켓 후보, FAIL 아님) |

## 실기 스모크 (6-b, n=1 PASS)

프리뷰 5상태 전부 확인 — 카카오 지도가 실제로 핀을 그리는 것을 최초 확인. `prebuild`(네이티브 재빌드)는 **불필요**했다 — 01b·02a가 선행조건으로 표시했으나 `RNCWebView` 심볼이 이미 링크돼 있었음(TRIP-197/199). 미실행 1건: **Figma 픽셀 대조(AC-V4)** — 17개 AC 중 유일한 미실행.

## 커버되지 않는 것

- `handleRetry` 내부 확정 재확인(2차 방어선)에 자동 회귀 테스트 없음.
- 지도 대본(`mapHtml.ts`) raw hex 2종이 색 가드 모집단 밖.
- `WEEKDAY_LABELS` 중복 정의(`draftView.ts`↔`mustVisitTimeForm.ts`).
- `features/*/ui` 조회·변이 훅 금지를 기계로 강제하는 검사 없음(관례만).
- 재생성 실패 배너가 다음 재생성까지 안 사라짐 · 재방문 시 폴링 예산 이미 소진.

## 구조 지도

`frontend/docs/structure.md` 누락 7행 추가(전부 이 사이클 신규 파일) + 프리뷰 항목에 딥링크 최초 마운트 전용 규약 한 줄. `structure-index.cjs --check` PASS.

## 통합 버킷 간헐 실패 (미해결 관측)

커밋 게이트 1회차 실행에서 `Test Suites: 1 failed, 25 passed`(`enqueueSetState` 계열 증상). 37회 재실행 전부 green — 재현 불가·스위트명 특정 실패. 재발 시 대응 절차는 옵시디언 문제로그 참고.

## 정본 반영

Jira TRIP-297 코멘트(티켓 드리프트 5건) + `aidlc/aidlc-docs/construction/u3-ai-itinerary/functional-design/frontend-components.md` §8(testID 13종 소급 기록) + 발명값 3건 명시(성격 축 라벨 미구현·고정 블록 시각 절삭·폴링 2초·30회). `audit.md` Post-Design Correction append.

## 다음에 이어서 할 일

- Figma 픽셀 대조(AC-V4) 실행 — 유일한 미실행 수용 기준
- `handleRetry` 회귀 테스트 공백 메우기 판단
- 후속 티켓 후보 7건 우선순위화

## 관련

- 옵시디언 개발로그(정본): `TripPilot/개발로그/2026-08-10 20260809-trip297-itinerary-draft.md`
- 작업공간: `_workspace/20260809-trip297-itinerary-draft/`
- 선행 사이클: `frontend/docs/devlog/2026-08-09-20260809-trip295-contract-refresh.md`
