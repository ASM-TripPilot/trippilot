# 2026-08-07 20260807-trip248-social-signup-unblock

> 축약본 — 상세는 옵시디언 개발로그 `TripPilot/개발로그/2026-08-07 20260807-trip248-social-signup-unblock.md`
> (볼트가 없거나 MCP 실패 시 폴백. [메모리]는 이 파일이 아니라 옵시디언 쪽을 읽는다.)

## 대상

TRIP-248 소셜 신규 가입 영구 차단 해소 — 400 `ageConfirmation` → `needs-age` 전이 (**카카오 token 갈래
한정**, D0). 신규 가입이 실서버에서 0건이었다 — 진입 조건이 `200 + isNewUser:true`(성공 응답)여서 도달
불가 분기였다. 미해결 문제로그 2건(목이 200을 줘서 결함을 가림 · 결함이 AC 목록에서 누락)의 종결 대상.

## 게이트

**2개** — 게이트①-1(테스트 6파일 승인, red 4건) → 구현 → code-critic(차단0·경고2·참고2) → **게이트②-1**
(구현 3파일 승인). 전부 승인.

## 검증

[검증] 6-a `04_qa-verifier_report_1_PASS.md` — 정적 4종 PASS, 게이트①-1 해시 6/6·게이트②-1 해시 3/3 일치,
변경 집합 = 신고 9파일, red 소급(1차 필수) vacuous 아님, 구조 지도 정합 → **PASS**.
6-b 실기 스모크 `04b_smoke_1_PASS.md` — 판정 4/4 PASS(부팅·레드박스 없음·네트워크 0건·연령확인 시트
도달 확인). ⚠️ **탭 자동화 불가라 "카카오→400→시트" 왕복은 미검증** — 티켓 DoD의 "실제 가입 완주"는
사용자 수동 확인(스크립트 A~E) 몫으로 남는다.

## 핵심 결정 (D0~D5, `01b_ouroboros_seed.md` §2)

카카오(token 갈래) 한정 · 죽은 분기(`200+isNewUser→needs-age`) 제거 · 판정은 body `code`+`fields`만(상태
코드 안 봄) · code 갈래는 지금처럼 error 유지(재인가 없이 시트로 보내면 더 나쁨) · 목 `age-restricted`
시나리오 미변경 · 400 경로에서 `isNewUser` 미설정.

## 예상과 달랐던 것

- **조합 함정**: 통합 버킷의 dev fake 인가가 provider 무관 항상 code 갈래를 줌 × D3(code는 시트 안 띄움)이
  곱해져 AC-3이 원리적으로 통과 불가능한 상태였다. `jest.mock`으로 OAuth 어댑터만 갈아끼워 해결(신규
  문제로그 분리 기록).
- D4(목 미변경)의 부작용이 서술보다 컸다 — `age-restricted` 시나리오가 이제 거절이 아니라 **성공 로그인**
  으로 끝난다(code-critic 경고①, 서술 정정 완료).
- 판정 함수 두 조건 중 하나(`code` 문자열)가 완전 무심판(code-critic 경고②) — 게이트②-1 이해 체크 오답
  2건이 같은 지점을 가리킴, 이해부채 신규 등재.

## 정본 반영 (aidlc)

**제시 3 / 선택 0 / (A) 0 / (B) 0** — 3-a에서 사용자가 고른 항목 없음, `aidlc/` 미기록. 관측만 남김
(openapi 400 참조 누락 · `frontend-components.md` 422 서술 공백·testID 목록 낡음).

## 이해부채

신규 1건([[2026-08-07 조건이 있다와 그 조건을 누가 본다는 별개다]]) · 상환 0건. 미상환 총 34건(전체
65건 중, 파산 상한 10 초과 지속 — 사용자 유예).

## 문제로그

- [[2026-08-02 목이 성공만 흉내내 도달 불가 분기가 초록으로 남았다]] — 미해결 → **해결**.
- [[2026-07-24 결함 B가 AC 목록에서 누락돼 게이트를 통과했다]] — 미해결 → **부분 해결**(프로세스 격차는
  절차로 반영, 결함 B 자체는 D0로 범위 밖 잔존).
- [[2026-07-24 red 상태의 pnpm test가 MSW 버킷을 건너뛴다]] — **재발 3회째**, 반영 대상으로 승화.
- 신규: [[2026-08-07 각자 옳은 두 결정이 곱하면 통과 불가능한 AC를 만든다]].

## 후속 티켓 후보 / 다음에 이어서 할 일

1. ⚠️ 실기 미검증 — 사용자 수동 스크립트 A~E(특히 E: 실서버 카카오 신규 가입).
2. 실기 성공 시 TRIP-249를 백로그로 이동(타입 변환 필요, 실패 시 보류).
3. 다음 칸 1순위 — `code==='VALIDATION_ERROR'` 절 무심판 보강(게이트①-2 필요, 별도 칸).
4. code 갈래 재인가 착수 시 AC-13(F4-7·F5-1) 심판 2건을 먼저 뒤집어야 함.

## 하네스 규칙 후보

1. `verify-gates` red 진단 예외(3회 재발 실측) — 다음 정비 사이클 반영 제안.
2. test-designer 절차 — dev-only 목 조합을 케이스 작성 전에 먼저 확인(관찰 1건).

## 미커밋

작업트리 9파일 변경(구현 2·테스트 인프라 1·테스트 6), 브랜치 `feature/TRIP-248-FE-social-signup-unblock`
(베이스 `origin/develop` — **main 아님**).

## 상세

옵시디언 개발로그·게이트 노트 2개·이해부채 신규 1건·문제로그 상태 갱신 2건+재발 1건+신규 1건 참고.
