# 2026-08-06 20260805-trip209-mustvisit-seed

> 축약본 — 상세는 옵시디언 개발로그 `TripPilot/개발로그/2026-08-06 20260805-trip209-mustvisit-seed.md`
> (볼트가 없거나 MCP 실패 시 폴백. [메모리]는 이 파일이 아니라 옵시디언 쪽을 읽는다.)

## 대상

TRIP-209 [FE] g01 '꼭 갈 곳' 시드 섹션 — 담은 장소 시드 + must-visits 등록. 탐색에서 담은
장소를 여행 만들기 1단계로 복사해 보여주고, 여행 생성 성공 뒤 시드를
`POST /trips/{id}/must-visits`로 등록.

## 게이트

**3개** — 게이트①-1(테스트 9파일, 신규5·수정4) → 구현 → 리뷰 1차 → 5-c → **게이트①-2**
(code-critic이 심판 구멍 3개를 찾아 동결 재개방, 해시 2파일 대체) → 후속 구현(`[다음]` 잠금 +
프리뷰 2키) → 리뷰 2차 → **게이트②-1**(구현 6파일). 전부 승인.

## 검증

[검증] 6-a `04_qa-verifier_report_1_PASS.md` — 정적 4종 PASS, 게이트① 해시 9/9·게이트② 해시
6/6 일치, 변경 집합 미신고 0건, red 소급 확인, 구조 지도 불일치 1건(scribe 인계) → PASS.
6-b 실기 스모크 `04b_smoke_1_PASS.md` — 판정 4항목 PASS. 1차 시도 레드박스는 Metro 미기동
(코드 결함 아님).

## 핵심 결정

- `try` 둘 분리(여행 생성 / must-visit 등록) — 성공 후 부작용이 성공을 실패로 둔갑시키는 것 방지.
- `Promise.allSettled` 병렬 등록, 409는 성공으로 수렴.
- 얼굴 판정은 시드를 가장 먼저 봄(잔존 시드가 실패·로딩 얼굴에 안 덮임).
- 제출 잠금은 `useRef`(즉시 읽힘), 성공 후 이동 시 잠금을 안 풂.
- 게스트는 잠금에서 제외(`isAuthed && isPending`) — 비회원과 승인 동결 테스트 3파일을 동시에 지킴.
- 썸네일 상한 3장 고정(특례 없음, 게이트①-1 사용자 결정).

## 정본 반영 (aidlc)

제시 1 / 선택 1 / (A) 0 / (B) 6. `aidlc/aidlc-docs/construction/u1-accommodation-trip/functional-design/frontend-components.md`
§4·§6 갱신 + `aidlc/aidlc-docs/audit.md` Post-Design Correction append. 6건 전부 "요구사항 근거
아님 — 구현 결정" 라벨.

## 후속 티켓 후보

1. 앱 재시작 전까지 옛 시드가 남음(`reset()` 미호출) — 이번 릴리스에 그대로 나감.
2. 「더 담기」로 담고 돌아와도 시드 미반영.
3. "이미 만들어진 뒤의 step1"을 화면이 설명 안 함(세 얼굴 한 질문).
4. 조회 실패 상태 "열려 있어야 함" 단언 없음.
5. 발명 문구 위치가 계약 지정 파일 밖.
6. 프리뷰 딥링크 연속 전환 안 됨.
7. 긴 장소명 라벨 한 줄 잘림(디자인 확인 필요).

## 하네스 규칙 후보

1. verify-gates §0에 Metro 기동 확인 추가.
2. 게이트 주해에 코드명·AC코드 금지, 일상어 재서술 기본화.
3. 코드 노출은 "앞으로 또 나올 모양" 1~2개만.
4. 게이트 퀴즈 정답 강조 표시 금지.

## 상세

옵시디언 개발로그·게이트 노트 3개·이해부채 4건·문제로그 2건·개념 노트 신규 3건 참고.
