# 2026-07-23 — 20260722-trip170-home

**결말: 완료** (게이트①-1→①-2 재제시 승인 + ②-1→②-2 재제시 승인 + 검증 n=2 PASS + 실기 스모크 n=2 PASS) · 브랜치 `feature/TRIP-170-FE-home`

> 상세는 옵시디언 개발로그(`TripPilot/개발로그/2026-07-23 20260722-trip170-home.md`)가 정본이다. 이 파일은 요약본 — [메모리]는 이 파일을 읽지 않는다.

## 무엇을 했나

TRIP-170 홈 대시보드 + 하단 5탭 셸(US-SHELL-02/03/04) — Figma `a01-home`(상태 5종) + `BottomTab`을 픽셀 정합. 서버 API 부재로 프레젠테이션 전용 슬라이스(props/상수 구동, 네트워크 0).

- **신규 5**: `features/home/model/homeTypes.ts`(prop 계약, 판별 유니온) · `homeFixtures.ts`(4상태 Figma 고정 목업) · `components/HomeGlyphs.tsx`(인라인 SVG 10종) · `screens/HomeScreen.tsx`(4상태 화면) · `shared/ui/BottomTabBar.tsx`(순수 뷰 탭바)
- **교체/수정 3**: `app/(tabs)/_layout.tsx`(tabBar 렌더프롭+어댑터) · `app/(tabs)/index.tsx`(no-trip 픽스처 래퍼) · `app/_dev/preview.tsx`(홈 4키 append)
- **테스트 신규 6**(테스트 5 + 목 인프라 1): `HomeScreen.test.tsx`·`BottomTabBar.test.tsx`·`tabsShell.test.tsx`·`homeStructure.test.ts`·`devPreviewHome.test.tsx`·`test-support/expoRouterTabsMock.tsx`
- **범위 밖**: 실 데이터 배선(서버 API 없음) · 홈 외 4탭 내용 · 일정 탭 허브 수렴 · 새 몰입 화면

## 게이트·검증

- **게이트①** ①-1 승인(1차) → **①-2로 `HomeScreen.test.tsx` 1파일 재제시**(RNTL `toHaveTextContent(문자열)`이 완전 일치라 3 it 구현 불가 + Figma 재확증 2건[더보기 3곳·empty/loading hero 오버레이 실존]) → 승인. 나머지 5파일은 ①-1 그대로 유효.
- **게이트②** ②-1 승인(1차, 구현 8파일) → 6-b 픽셀 재작업(소셜 하트 아웃라인→채움) + 7단계 리팩토링(`OVERLAY_FREE_TRIP` 이름-내용 모순 제거)으로 **②-2 재제시(2파일)** → 승인. 독립 code-critic(03b) 차단 0·경고 3·참고 1, 실제 조치 1건(경고3)·이관 3건(경고1·2·참고1).
- **검증 n=1 PASS**(`04_qa-verifier_report_1_PASS.md`): 정적 검사 4종·해시 6/6+8/8·red 소급·경계면 QA 전부 PASS.
- **검증 n=2 PASS(재제시 필요)**(`04_qa-verifier_report_2_PASS.md`): 게이트② 2파일 불일치(신고된 재작업과 정확히 일치) → 게이트②-2로 해소.
- **실기 스모크 n=1 PASS**(픽셀 재작업 1건 발동) → **n=2 PASS**(해소, 수치 불일치 잔고 0). 잔여 diff 4건(플레이스홀더·topBar 프리뷰 겹침·탭바 실기 이연·taste 뷰포트 밖)은 Q6 규칙대로 통과 처리.
- **구조 지도 불일치 9건**(qa 04 §6 이관) — [기록]에서 `docs/structure.md`에 반영 완료, `structure-index.cjs --check` 재통과(`OK — 문서 98행 ↔ 실제 98파일 일치 · 개념 경로 41개 전부 실존`).

## 후속 (이관)

1. 홈탭 press 재매핑 이음매 테스트 추가(03b 경고1 — 게이트① 영역).
2. 탭바 bottom inset + 실기 대조(03b 경고2·G-5 — 실연동 사이클, 백엔드 필요).
3. tabPress emit 프로토콜 정식 채택 검토(03b 참고1 — 탭 스택 도입 시점).
4. US-SHELL-02 스토리 문구(첫사용자 2진입) 수정 팀 전달 — Figma 단일 CTA로 확정, `aidlc/` 쓰기 금지라 코드로 못 고침(Q1).
5. SKILL `trippilot-dev-cycle` 4-a/4-b 절 개정 — 트립와이어 발동(통합 fable 단독 복귀), 사이클 종료 후 별도 세션.
6. 실 데이터 wiring(TanStack Query) — `homeFixtures.ts` 자리를 API 훅으로 교체.

## 교훈

- **`toHaveTextContent(문자열)`은 부분 포함이 아니라 완전 일치다** — 매처 사전 오기가 설계(4-a)에서 번역(4-b)으로 그대로 전파돼 구현 불가능한 테스트 3건을 만들었다. 재발 방지: 정정본을 02a에 append하고 [[RNTL 쿼리]] 개념 노트에 보강.
- **Figma는 구현 중에도 재확증이 필요하다** — 게이트①에서 승인된 전제(오버레이 없음·더보기 2곳)가 구현 중 재조회로 뒤집혔다. 임의로 덮지 않고 게이트를 다시 연 것이 맞는 판단이었다.
- **하네스 장치(트립와이어)가 실제로 작동했다** — 4-a/4-b 분할 도입 직후 첫 반려에서 곧바로 통합 복귀 카운터가 발동, 원인(매처 오기)과 장치 발동을 분리 기록했다.

> 2026-07-23 추기: 사이클 산출물 16파일이 이 문서를 포함한 단일 커밋으로 반영됨(feature/TRIP-170-FE-home). aidlc/·루트 CLAUDE.md는 사용자 별도 작업이라 제외.
