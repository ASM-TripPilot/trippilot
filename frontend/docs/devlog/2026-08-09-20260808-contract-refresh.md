# 2026-08-09 · 20260808-contract-refresh

> 축약본. 상세(결정 이유·예상과 달랐던 것·개념 노트·문제로그)는 옵시디언 개발로그 `TripPilot/개발로그/2026-08-09 20260808-contract-refresh.md`를 정본으로 본다.

## 대상

openapi 계약 확장 흡수 — `pnpm codegen` 재생성 + 동결 단언 갱신. 티켓 없음. `origin/develop` 머지(`b7baff0`)가 `backend/docs/design/openapi.yaml`을 125줄 확장(슬롯 10필드·`candidatesSummary`·change-log)해 동결 심판이 이미 red인 상태로 시작. 새 기능 0 · 손으로 쓴 코드 0줄.

## 요약

- 동결 단언 3건을 값 갱신이 아니라 모양 자체를 완화(D1 부분집합+하한 · D2 패턴 스캔 · D3 심볼 하한 92)해 재생성 전에도 설계 가능하게 함.
- 게이트①이 code-critic 뮤테이션 20건(13 잡힘/7 놓침)으로 심판 결함 3건(C-1 파생 상수 이중 역할 · C-2 `ItineraryCandidatesSummary` 무심판 · C-3 사전 신고 숫자 불일치) 발견 → ①-2로 재제시, 전부 닫음.
- 구현 = `pnpm codegen` 한 번(67→75파일, 이름까지 8/8 적중, 예측 밖 1건 — description만 바뀌어도 JSDoc 복사로 바이트 변경).
- 검증 n=1 FAIL(aidlc 미신고 변경 3건 — 다른 세션의 동시 작업으로 확인) → 오케스트레이터가 예외 범위 정정 → n=2 PASS. FAIL 카운터 비집계(범위 정정, 코드 변경 아님).
- 적대적 리뷰 차단0·경고4·참고3(조치3). 사다리 제안0·채택0(명령 하나뿐). 이해 체크 5문항 답변0(3사이클 묶음 검토 예정).

## 게이트

- 게이트①-1 → 무효(심판 결함 3건) → 게이트①-2(재동결)
- 게이트②-1 → 승인

## 검증

| 항목 | n=1 | n=2 |
|---|---|---|
| 정적 검사 4종 | PASS | 승계 |
| 게이트①/② 해시 대조 | 일치 | 승계 |
| red 소급 | 확인 | 승계 |
| frontend 미신고 변경 | 0건 | 0건(재확인) |
| aidlc 변경 | 3건 미신고 → FAIL | 범위 밖(정정 목록과 일치) → PASS |
| 구조 지도 | 불일치 8건([기록] 이관) | 동일 |
| 실기 스모크 | 미해당 | 미해당 |

## 구조 지도

`frontend/docs/structure.md` 258→266행 갱신(신규 8파일 + 기존 3행 보강 + codegen 파라그래프). `structure-index.cjs --check` 통과.

## 다음에 이어서 할 일

- 잔여 사각(하한 목록 밖 파일 개명) — D4 화면 티켓(h36)에서 앵커와 함께 닫기
- C-4(일정 표면 밖 생성 파일 INV-3 복합형 무심판) 미룸
- PBT-U3-1·U3-3 입력은 갖췄으나 `distanceRange` null 구간 합산 규칙이 정본에 없음
- 생성물 통합 해시 계산 스크립트 정본화 보류(다음 계약 동기화 사이클에서 재발하면 승격)
- PR #130에 이 커밋이 얹힘

## 관련

- 옵시디언 개발로그(정본): `TripPilot/개발로그/2026-08-09 20260808-contract-refresh.md`
- 작업공간: `_workspace/20260808-contract-refresh/`
