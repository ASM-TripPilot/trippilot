# 2026-08-05 20260805-trip221-preview-entries

> 축약본. 상세(결정 이유·예상과 달랐던 것·하네스 규칙 후보)는 옵시디언 개발로그를 정본으로 본다 — 이 파일은 볼트 없는 사람/MCP 실패 시 폴백이다.

## 대상

**경량 사이클.** dev 프리뷰(`_dev/preview`)에 d04 장소 탐색·d02 담은 장소 두 화면의 `results` 얼굴 2키 추가. TRIP-221·TRIP-223. 커밋 `de234c0`. 브랜치 `feature/TRIP-221-place-explore-save`(직전 커밋까지 origin 푸시됨, 이 커밋은 미푸시).

원장: `_workspace/20260805-trip221-preview-entries/00_gates.md` 하나뿐(경량 사이클 규칙 — 01/02/03/03b/04 산출물 없음, 게이트①·② 1회 통합 제시, 적대적 리뷰 생략).

## 발견 · 원인

사용자가 프리뷰를 열어보자고 해서 열었더니 레지스트리가 지도(TRIP-197)에서 멈춰 있었다 — 25키 등록, 그 뒤 만든 e02·g01·d04·d02가 전부 0키. d04·d02는 실화면 딥링크로 원리적으로 도달 불가한 `results` 얼굴이 있어(d04는 백엔드 401로 항상 error, d02는 세션 없어 항상 게스트 안내) 2열 카드·태그 칩·순번 목록·하트·`dataStatus` 배지를 아무도 못 봤다.

## 변경

1. `exploreFixtures.ts` 신설(`homeFixtures.ts` 선례) — 장소 6 + 담은 장소 4.
2. `preview.tsx`에 `places-results`·`saved-places-results` 2키 추가.
3. `devPreviewExplore.test.tsx` 신설 — `devPreviewMap.test.tsx` 구조 그대로, 루트뿐 아니라 카드·항목 개수까지 단언.

설계 결정: `imageUrl`은 전부 `null`(INV-1, 외부 URL 발명 금지·로컬 에셋 라이선스 미정). 동결 `devPreview*.test.tsx` 5개는 한 글자도 안 고침.

## 검증

`pnpm lint` 0 error · `pnpm tsc` 0 · `pnpm test` node 969(신규 2건)/integration 137 전량 green · 선행 5칸 무회귀. 실기 확인 PASS — 콜드 런치 딥링크로 두 키 모두 캡처해 카드·순번·배지·CTA 확인.

## 관측 (고치지 않음)

- 프리뷰 칩 행이 full-bleed 화면 앱바를 가림(`map-default`도 동일, 이번 것 아님).
- **딥링크는 초기 마운트에만 반영된다** — 세션 중 연속 `openurl`은 상태를 안 바꾼다. `terminate` 후 콜드 런치 필요.

## 하네스 규칙 후보 (상세는 옵시디언)

없음(승격 대상). 딥링크 계약은 약한 후보로만 문제로그에 기재(재발 실측 0건).

## 구조 지도

신규 파일 2개 반영(`exploreFixtures.ts`·`devPreviewExplore.test.tsx`) — `structure-index.cjs --check` OK(234행 일치). 228→230.

## 옵시디언 기록

- 개발로그: `TripPilot/개발로그/2026-08-05 20260805-trip221-preview-entries.md`
- 문제로그: `TripPilot/문제로그/2026-08-05 프리뷰 딥링크는 초기 마운트에만 반영된다.md`
- 개념(신규): `TripPilot/개념/useState 지연 초기화자.md`
- 이해부채(기존 노트 확장): `TripPilot/이해부채/2026-08-03 TRIP-220 코드 주해 이연.md`
- 개발일지: `TripPilot/개발일지/2026-08-05.md`
