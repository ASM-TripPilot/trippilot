# 2026-07-29 20260729-trip180-format-price

## 대상

**TRIP-180 [FE] 가격 표기 순수 함수 `formatPrice` + PBT.** 숙소 카드에 찍을 금액 문자열(`120,000원~` / `가격 미확인`)을 만드는 순수 함수 1개 + fast-check PBT 5건. 신규 2파일(`src/features/stay/model/formatPrice.ts` 7줄 + `formatPrice.test.ts`), 호출자 0개로 머지(TRIP-181 `StayCard`가 첫 소비자). 직전 사이클: `2026-07-29-20260728-trip179-stay-search-contract.md`.

**⚠️ 무인 자동승인 운행** — 사용자가 취침 중이라 사이클 시작 전 사전 승인 1회로 게이트①·②를 통과시켰다. 게이트별 사용자 판단은 없었다(승인은 절차적 통과). 상세 근거·아침 판정 대기 4건은 옵시디언 개발로그 참조.

## 게이트·검증

- **게이트①-1**(2026-07-29 03:32) **승인(자동)** — 테스트 5건(T1~T5), 해시 `845169104e…cbc74d`. 깨끗한 red 확인.
- **게이트②-1**(2026-07-29 03:54) **승인(자동)** — 구현 7줄, 해시 `bda97c1bad…923c1c89`. 게이트① 심판 무결성 대조(오케 직접 재계산) 완전 일치.
- **검증 n=1 PASS**(`04_qa-verifier_report_1_PASS.md`): 정적 검사 4/4 · 변경집합 자체도출=신고 목록 일치(aidlc 변경 0건) · 해시 일치 · red 소급 확인 완료(잔류 stash 발견 → 스크래치패드 대피 대체, 훼손 없음) · 구조 지도 불일치 1건(scribe 이관, 해소됨) · openapi↔타입 일치 · INV-3 grep 위반 0건 · 실기 스모크 미해당.
- **적대적 리뷰(5-b)**: 뮤테이션 15종 · 차단 0 · 경고 2 · 참고 2. **M10 생존** — `toLocaleString()`으로 바꿔도 5/5 green(로케일 무관 en-US 환경에서만), `LC_ALL=de_DE.UTF-8`에서만 red. 이 구현의 핵심 결정(D5, stdlib 미사용)에 자동 테스트 가드가 0개라는 뜻 — 소스 주석이 유일한 가드.
- **Ponytail**: 제안 1(`toLocaleString`/`Intl`) · 채택 0.
- **⚠️ 실기 미검증**: 이번 사이클은 스모크 미해당. **TRIP-179 이월 건**(`_layout.tsx`의 `QueryClientProvider` 배선) 여전히 미해소 — 다음 사이클 인수인계 필수.

## 예상과 달랐던 것 (요지)

1. **4-a 실측이 설계를 3번 반증**: `min:0` arbitrary만으로는 falsy 함정(`amount=0`)이 500런에도 20% 확률로 안 잡힘 → 예제 테스트(T3)로 보강. 오라클 독립 2속성에 구멍(`currency`가 `","`면 정보보존 속성이 같이 새는 걸 놓침) → 모양 앵커 추가. "결정성이 시계 읽기를 막는다"는 설계 주장은 틀림(연속 두 호출이 같은 밀리초라 통과) → 아침 판정 대기.
2. **code-critic이 "구현 결함"이 아니라 "심판 사각지대·주석 오류" 유형 지적** — M10(로케일 회귀 무방비) + `ponytail:` 주석의 정밀도 한계 서술 오류(`2^53-1`을 int64 상한으로 잘못 적음).
3. **구현자 자기 신고 오류 1건** — "`== null` 선례가 리포에 없다"는 서술이 틀림(`shared/storage/index.ts:45`에 이미 있음). 파일 2개만 보고 일반화한 것이 원인.
4. **[[속성 기반 테스트]] 개념이 네 번째로 재출현**(등재→상환→재출현→오늘) — 개념 노트가 예고한 재검토 지점 도달. 단 무인 운행이라 이해 여부 관측 불가, 판정을 사용자에게 이관.

상세(D1~D8 설계 근거·정본 반영 4건·구조 지도 갱신·모델 배치)는 옵시디언 개발로그 참조.

## 다음에 이어서 할 일

- 아침 사용자 판정 4건(toLocaleString 미채택 · amount=0 판정 · jest.spyOn(Date,now) 추가 여부 · 속성 기반 테스트 상환 기준 재검토) — 옵시디언 개발로그 상단 참조.
- 잔류 stash `stash@{0}`(TRIP-173) — **두 사이클 연속(179→180) 미해소**, 3일째. 세 번째도 반복되면 정리 여부를 명시적으로 물어야 함.
- 후속 티켓 후보: A(정본 서식 `₩`→`원` 정정) · B(`capturedAt` 계약 공백) · C(openapi `amount minimum:0`) · D(179 이월 실기 스모크).
- TRIP-181(`StayCard`)에서 `formatPrice` 첫 소비자 연결.

## 관련

- 옵시디언 개발로그: `TripPilot/개발로그/2026-07-29 20260729-trip180-format-price.md`
- 게이트①-1: `TripPilot/게이트/2026-07-29 20260729-trip180-format-price 게이트①-1.md`
- 게이트②-1: `TripPilot/게이트/2026-07-29 20260729-trip180-format-price 게이트②-1.md`
- 문제로그(신설): `TripPilot/문제로그/2026-07-29 잔류 stash가 두 사이클 연속 red 소급 표준 경로를 밀어냈다.md`
- 작업 공간: `_workspace/20260729-trip180-format-price/`
