# 2026-07-20 · 목 서버 import 지연 로딩 (부팅 크래시 해소) (devlog)

- cycle-id: `20260720-mock-lazy-import` · 브랜치 `feature/TRIP-161-login` · 성격 **경량**(단일 파일, 선존재 R1 버그픽스, 게이트①·② 통합 1회 제시)
- 대상: 티켓 없는 **선존재 런타임 버그픽스**. 앱을 처음 실제로 실행하자 부팅이 죽었다 — `ReferenceError: Property 'MessageEvent' doesn't exist` @ `src/mocks/native.ts` + 연쇄 `_layout.tsx missing default export`. 팀이 이미 R1로 표기해 둔 미검증 경로가 첫 실행에서 예정대로 발현.
- 옵시디언 개발로그: `TripPilot/개발로그/2026-07-20 20260720-mock-lazy-import.md` (게이트 노트·이해부채·개념·문제로그 링크 허브)

## 결과

- **수정 1건**: `src/app/_layout.tsx` — 정적 `import { startMockServer } from '@/mocks/native'` 제거 → `EXPO_PUBLIC_API_MOCK==='1'`일 때만 동적 `import('@/mocks/native')` + `.catch(console.warn)`. 인프라 신규/변경 없음, `native.ts`·`msw` 등 다른 파일 무변경.
- 검증 **차수 1 PASS**(2026-07-20 02:17:21 KST): prettier clean · eslint 0 · tsc 0 · jest node **82/82** · 통합 **19/19** = **101 green 유지, 회귀 0**.
- **AC-1 런타임 실증**: `npx expo run:ios` → iPhone 17 Pro 시뮬레이터 부팅(mock OFF) → **MessageEvent 크래시 소멸**, 로그인 화면 정상 렌더(브랜드 그라디언트 앱아이콘·실 브랜드 아이콘 4·한글 라벨·약관 푸터).
- 게이트 해시 `c5f03e4fb05335b566575399e7d80e730d88f322d24813a494e26a06b6f6f63c` ↔ 리포 실측 일치(변조 0).

## 결정

- **핵심 진단은 "목이 고장났다"가 아니라 "목을 쓰지도 않는데 목을 불러오다 죽었다".** `EXPO_PUBLIC_API_MOCK`은 app.config·.env 어디에도 설정돼 있지 않았다(grep 0). 목은 시작조차 안 되는데 `if` **밖의 정적 import 한 줄**이 무조건 `native.ts`를 로드해 부팅을 죽였다. 고칠 대상은 msw가 아니라 **로딩 시점**.
- **(기각) 웹 전역 폴리필** — msw가 RN에서 요구하는 전역이 하나가 아닐 가능성이 커 두더지잡기. 이번 목적(화면 보기)에 불필요. 겹2 유예. → **후속 실측에서 이 기각이 옳았음이 확인됨**(부재 전역 최소 셋).
- **(기각) `native.ts` 삭제/비활성** — 겹2에서 쓸 자산을 지우는 과잉 조치.
- **(채택) 조건부 동적 import + `.catch`** — 목 OFF(기본)면 `native.ts`가 아예 로드되지 않고, 목 ON이면 시도하되 실패 시 하드 크래시 대신 경고(조용한 실패 아님 → INV-4 정합).
- 격리성: `@/mocks/native` import처는 `src/app/_layout.tsx` **단 1곳**(grep). 테스트는 `msw/node`(`src/mocks/server.ts`)라 `native.ts`와 무관. `_layout.tsx`엔 테스트 없음.

## 테스트 성격 (jest red 의도적 생략)

- 이 크래시는 **RN(Hermes) 런타임 전용**이라 node jest에서 재현되지 않는다 — 직전 사이클 jest 101개가 이걸 못 잡은 이유가 바로 이것.
- 따라서 red = **관측된 런타임 크래시(사용자 실행 로그)**, green = **시뮬레이터 부팅 확인**. jest는 회귀 가드(101 green)로만 기여. 이 선택은 게이트에서 명시 제시하고 승인받았다.

## 게이트

- 게이트①② **통합 승인** 2026-07-20 02:13:28 KST — "① 승인, 시뮬레이터 부팅해서 화면 확인하자"(동작 신뢰 승인). 경량 사이클이라 ①(테스트 성격)·②(구현)을 1회 병합 제시.
- **`04_qa-verifier_report_*` 없음** — 검증을 오케스트레이터가 직접(런타임) 수행했고 판정은 원장 `00_gates.md` §검증 이력에 기록. 서브에이전트 미개입 사이클이라 리포트가 애초에 생성되지 않으며, 원장이 이를 명시하므로 **삭제 의심 없음**.
- 경계면 QA: 해당 없음(단일 파일 로딩 시점 변경, API·경계면 무변경).
- 드리프트 1건(경미): 게이트 노트 frontmatter `status: 대기`가 본문 §결과 "승인"과 어긋나 있었다 → [기록]에서 `승인`으로 정정.

## 예상과 달랐던 것

- **"missing default export"는 원인이 아니라 증상.** `_layout.tsx`의 export는 정상이었다 — 모듈 로드가 중단되면 export 등록 줄까지 도달하지 못한다. 엉뚱한 파일을 뜯기 직전까지 갈 수 있던 지점.
- **부수 발견(범위 밖·비-FAIL)**: 부트스트랩이 `EXPO_PUBLIC_AUTH_FAKE` 없이 인가 시도 → `makeAuthorize.ts`의 의도된 겹2 가드가 반복 throw(비치명, UI 정상). 당시 별건으로 판단했으나 **후속 조사에서 같은 뿌리로 밝혀짐**(아래).
- **부가 성과**: 직전 비주얼 사이클의 B층(픽셀·폰트 실렌더)이 이 실행으로 일부 **육안 확증**됨.

## 후속 판정 (사이클 종료 후 같은 날) — `msw/native` 작동 불가 확정

**원 사이클의 판정은 정확했다.** 이 사이클은 "목 OFF 부팅 크래시 소멸"만 검증했고 "목 실동작(폴리필)은 겹2"로 명시 유예했다. 아래는 그 **유예 항목을 이어받은 후속 판정**이지 정정이 아니다.

2026-07-20 21:3x KST 실기+CDP(Chrome DevTools Protocol — 실행 중인 앱 내부를 바깥에서 관찰하는 디버거 규약) 실측:

- 디바이스 로그에 **실 커넥션이 그대로 기록**(`.../api/v1/bootstrap start → cancelled`). 가로챘다면 커넥션 자체가 안 생긴다.
- 앱 런타임 `globalThis.XMLHttpRequest`가 **RN 네이티브 그대로** — MSW 인터셉터 프록시로 교체되지 않았다.
- 배선은 정상(env 인라인 확인, 번들에 `@mswjs` 27건). **번들에 있는 것과 실행되는 것은 다르다** — 죽는 지점은 인터셉터 설치 단계.
- 원인: `MessageEvent`·`BroadcastChannel` 부재(둘 다 undefined) + `Response.body` 미지원. `URL`은 spec 준수로 **문제없음**.
- **값비싼 반증**: 중간에 "RN의 URL 파서가 범인"이라 판단하고 jest에서 재현까지 했으나 **틀렸다** — Expo SDK 54 winter 런타임이 `install('URL', ...)`로 spec 구현(`whatwg-url-without-unicode`)을 덮어써 실기에서는 정상이다. **RN 소스 정적 분석 ≠ Expo 앱 런타임 실측.**
- `makeAuthorize.ts` 반복 throw는 별건이 아니라 **같은 뿌리의 두 번째 증상**(목이 안 떠서 실 네트워크 실패 → 폴백 경로에서 가드가 던짐).

**검증 방법론 교훈**: RN 런타임 사실은 jest 에뮬레이션으로 판정할 수 없다. jest 재현 시도가 3회 연속 서로 다른 환경 아티팩트에 막혔다(① jest에 `XMLHttpRequest` 자체가 없어 fetch 경로로 샘 → ② RN XHR을 주입하니 axios가 `document` 참조로 실패 → ③ 실기+Hermes CDP로 전환해 확정). jest는 **회귀 가드로는 최고, 런타임 사실 판정기로는 부적격**.

## 다음에 이어서 할 일

- **목 상환 티켓 개설(미개설)** — `shared/api` 레벨 fake transport로 교체(상환 방향 ②). 폴리필(①)은 실측상 비권장. `scenarios.ts`가 MSW와 분리돼 있어 시나리오 데이터 재사용 가능, `handlers.ts`는 jest 전용 존치.
- `makeAuthorize.ts` 반복 throw가 위 목 상환과 **함께 사라지는지** 확인(별도 수정 대상이 아닐 수 있다). `EXPO_PUBLIC_AUTH_FAKE` 배선은 겹2.
- `native.ts` 주석의 "미검증(best-effort)" 표현을 **"작동 불가 확정"**으로 갱신(사실과 어긋난 주석이 남아 있다).

## 이해부채 (건별 2 신규 · 옵시디언 `TripPilot/이해부채/`)

게이트 이해 체크 2문항에 명시 답변 없이 동작 신뢰 승인 → scribe 판정으로 **부채 등재**(정적/동적 import는 이번 수정의 작동 원리 그 자체).

- 게이트①② IC-1 정적 import가 조건문을 무시함 · IC-2 동적 import가 부팅을 살리는 이유 → 개념 노트 `정적 import vs 동적 import`·`모듈 로드 크래시 연쇄` 신설.
- 기존 `2026-07-19 MSW 3요소`는 상환 기준 (3)의 실측 근거가 확보됐으나 **미상환 유지**(사용자가 자기 말로 설명해야 상환) — 다음 [메모리] 재노출 1순위.

## 구조 스냅샷

새로 만들지 않음 — **델타 0**. 이 사이클의 `_layout.tsx` 수정은 커밋 `308e8b0`에 포함됐고 이는 최신 스냅샷 기준 커밋 `e4e197b`의 **조상**이라, 최신 스냅샷이 이미 이 상태를 반영한다. 파일 추가·삭제·이동 없음, 스텁 채움 없음.
