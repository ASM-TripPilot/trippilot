# 2026-08-13 · TRIP-301 h26 지도 뷰·h23 핀 상세·h31 폴백

> 축약본 — 상세는 옵시디언 개발로그 `TripPilot/개발로그/2026-08-13 20260812-trip301-map-view.md` (볼트 없는 환경·MCP 실패 시 폴백).

## 대상

`TimelineScreen`(h25) 지도 세그먼트 placeholder를 실지도(h26)·핀 상세 읽기전용 시트(h23)·폴백(h31) 세 표면으로 채움 + 통일 POI 카드(`PoiSlotCard`, peek/list 겸용) 신설. `shared/map`은 additive 확장만(`PIN_TAP`·`onLoadFailed`·`maxLevel`, 기존 호출부 5곳 무영향). 범위 좁힘: 시간표 카드 업그레이드·인라인 지도는 TRIP-354 이관, 길찾기·스크러버 드롭(Jira 스코프 갱신 완료). 커밋 `5a78984`(TRIP-301 파일 12개만 선택 스테이징).

## 게이트

- 게이트①(테스트): 승인 1차 — 2026-08-12 16:13 KST, 이해 체크 5/5(오답 0). 지난 미상환 [[가짜의 두께]] 상환.
- 게이트②(구현): 승인 1차 — 2026-08-13 00:36 KST, 이해 체크 5/5(오답 3, 전부 구조축) — 재설명 후 "이해됐어" 확인으로 즉시 해소(신규 이해부채 등재 0). ponytail 대안(경고 글리프 원 재사용) 미채택.

## 검증

- 04 QA 리포트 n=1 **PASS**. 해시 7/7·7/7 완전 일치·red소급 게이트①과 정확히 재현(20 failed/7 passed/27 total)·미신고 변경 0(aidlc 포함)·경계면 QA(testID·openapi·INV-3) 전부 정합. 전체 회귀 node 1501/1502(범위밖 1건 격리)·integration 218/218.
- 6-b 실기 스모크 **미해당**(발동 조건 5개 어디에도 안 걸림 — `shared/map/mapHtml.ts` WebView 대본 변경이 표에 없음). **⚠️ 실기 미검증**: viewOnly 제스처·핀 클릭→시트·`setMaxLevel(9)`·폴리라인 가시성. 프리뷰 상태 `itinerary-map` 추가(오케가 [검증] 중 보완), 폴백(h31)은 프리뷰로 못 띄움.

## code-critic (03b, 1차)

⚠️ 서브에이전트 세션 한도로 오케 인라인 대체(전수 감사 아님). 차단 0 · 경고 1(W1, 롱프레스 stray 마커 — `enablePin`↔`onMapMessage` 커플링, 후속 티켓 미룸) · 참고 1(N1, 경고 글리프 재사용 대안 — 게이트②에서 미채택). 5-c 생략(개선 대상 없음).

## 이해부채

신규 등재 0(게이트②에서 즉시 재설명·확인). 개념 노트 3건 신설: [[핀 결번 역참조]]·[[RNTL 완전일치 leaf]]·[[key remount 재시도]]. 지난 미상환 [[가짜의 두께]] 게이트①에서 **상환**.

## 구조 지도

**변경 있음 — 신규 3행 추가.** `PoiSlotCard.tsx`·`PinDetailSheet.tsx`·`MapFallback.tsx` + `TimelineScreen.tsx`·`ItineraryGlyphs.tsx`·`shared/map/{mapHtml.ts,KakaoMapView.tsx}` 행 갱신. `--check` 재확인 `OK — 문서 318행 ↔ 실제 318파일 일치 · 개념 경로 145개 전부 실존`. 「지금 작업하려면」 경고 절 16건(12건 상한 초과, 이번 사이클 신규 유입 0 — "화면 비주얼"·"엣지 케이스 화면" 2건이 배제 규칙 경계 유출 후보로 의심됨).

## 하네스 규칙 후보

1. 6-b 발동 조건표에 "WebView 주입 대본 변경" 항목 추가 제안 → 문제로그(옵시디언).
2. implementer 체크리스트에 "새 화면 표면 구현 시 프리뷰 상태 함께 추가" 후보 → 문제로그(옵시디언).
3. 서브에이전트 세션 한도 중단은 obsidian-second-brain 규칙상 문제로그 진입 조건(교훈 원리적 불가) 미달 — 문제로그 미생성, 이 각주로만 남김.

## 정본 반영(aidlc)

제시 0 / 선택 0 / (A) 0 / (B) 0 — `aidlc/` 무접촉(`git status aidlc/` 변경 0건).

## 다음에 이어서 할 일

1. 실기 확인(6-b, 다음 세션) — viewOnly 제스처·핀 클릭·줌 상한·폴리라인 눈으로 확인.
2. TRIP-354(시간표 카드+인라인 지도) 착수 시 `PoiSlotCard` 재사용.
3. W1(롱프레스 stray 마커) 후속 티켓화 여부 판단.
4. 경고 절 12건 초과 정비(다음 [기록] 때).

상세(결정 이유·예상과 달랐던 것·모델/effort 배치·사다리·§0 실적 등 전체 집계)는 옵시디언 개발로그 참고.
