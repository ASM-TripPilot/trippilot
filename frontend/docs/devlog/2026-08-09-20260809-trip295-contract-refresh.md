# 2026-08-09 · 20260809-trip295-contract-refresh

> 축약본. 상세(결정 이유·예상과 달랐던 것·개념 노트·문제로그)는 옵시디언 개발로그 `TripPilot/개발로그/2026-08-09 20260809-trip295-contract-refresh.md`를 정본으로 본다.

## 대상

openapi 계약 확장 2차 흡수 — `generationMode` 필수 승격·요청 enum `MANUAL`·슬롯 `violationReason`·`revisions`·`slot-candidates` 경로+스키마 4종. 티켓 없음, 직전 사이클 `20260808-contract-refresh`의 재판(`origin/develop`이 또 움직임). 발단은 PR #130 `frontend-ci`가 `pull_request` 트리거에서만 red(push는 green).

## 요약

- 규모는 경량(사용자 사전 위임)이지만 하네스 판정은 기본(동결 재개방 위반) — 생략은 5-b 리뷰·게이트 2회 제시뿐, 뮤테이션 실측(게이트①-2 규약)은 생략하지 않음.
- 심판 갱신 7건(J-1~J-7). J-7(응답 enum `itineraryGenerationMode.ts` 신규 앵커)이 유일한 신규 장치 — orval이 요청·응답 enum을 다른 파일 두 벌로 뽑아 한쪽만 늘어도 `tsc`가 안 깨지는 사각을 닫음.
- 뮤테이션 실측 5/5 전건 잡힘(M0~M5). 1차 실행은 미추적 codegen 산출물을 `git checkout --`로 되돌리려다 실패해 대조군이 오염 → `pnpm codegen` 재실행으로 복구 후 재측정.
- 게이트①·②는 대화 없이 통합·사전 승인(해시만 원장에 기록). 이해 체크 없음(무응답, 이해부채 신규 등재 없음).

## 검증

> `pnpm format` → `pnpm lint`(0 error / 31 warning, 기존 패턴) → `pnpm tsc`(0) → `pnpm test`.
> test:node 137 스위트 1321 통과 · test:integration 25 스위트 176 통과 · 실패 0.
> 실기 스모크: 미해당(화면·네비게이션 변경 0).

| # | 심은 위반 | 결과 |
|---|---|---|
| M0 | (무변경 대조군) | 35 passed |
| M1 | 계약 required에서 `generationMode` 제거 | 1 failed |
| M2 | 계약 요청 enum에서 `MANUAL` 제거 | 1 failed |
| M3 | 생성물 `generationMode`를 선택으로 뒤집기 | 1 failed |
| M4 | 생성물 `violationReason` 줄 삭제 | 1 failed |
| M5 | 생성물 응답 enum에서 `MANUAL` 제거 | 1 failed(J-7이 잡음) |

## 커버되지 않는 것

- 신규 경로 3종(`slot-candidates`·`revisions`·`revisions/{id}/restore`) 계약 앵커 없음(프론트 미사용, 착수 티켓에서 세움).
- 신규 스키마 4종(`Revision`·`RevisionList`·`SlotCandidates`·`SlotCandidatesRequest`) 내용 무심판 — 직전 사이클 C-2와 같은 유형 2회째 반복.
- 5-b 적대적 리뷰 생략(경량 지시).

## 구조 지도

`frontend/docs/structure.md` 생성물 75→84파일(신규 9) 반영, 기존 행 3개 보강. `structure-index.cjs --check` PASS. 별도 커밋 `9812cdc`(코드 커밋 `f6321c9`와 분리).

## 다음에 이어서 할 일

- 신규 스키마 4종 내용 무심판 — 규칙화 판단 대상(2회째 반복)
- `GET /trips/{tripId}/change-log`는 "생산자 없음" — 화면에 붙이지 말 것
- `Revision.actor/kind`가 h-대역 되돌리기 UI 배지 원천
- 되돌리기 복원 시 고정 블록은 현행이 이긴다(BR-U3-33·INV-U3-03)
- PR #130 머지 대기

## 관련

- 옵시디언 개발로그(정본): `TripPilot/개발로그/2026-08-09 20260809-trip295-contract-refresh.md`
- 작업공간: `_workspace/20260809-trip295-contract-refresh/`
- 선행 사이클: `frontend/docs/devlog/2026-08-09-20260808-contract-refresh.md`
