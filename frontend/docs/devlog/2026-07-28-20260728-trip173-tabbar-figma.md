# 2026-07-28 20260728-trip173-tabbar-figma

## 대상

**TRIP-173 FSD 완결 3/4** — `src/shared/ui/BottomTabBar.tsx` 1개 파일을 Figma 마스터 `1236:1177`에 정합(풀폭 직각 74px → 390×84 알약, 좌우 28px 여백). 신설 심판 `src/__tests__/tabbarVisual.test.ts`. **자율 실행 사이클**(게이트①②·해시·red 로그·5-b·6-a·7 전부 정상 수행, 생략된 것은 한 줄 대화뿐).

**이 사이클의 실질은 탭바 코드가 아니라 두 가지다** — ① 심판 자신이 만든 거짓 GREEN(헤더 주석이 `rounded-pill` 긍정 단언을 만족 — 이 리포에서 처음 관측된 방향, 반대 방향은 2026-07-26에 기록됨) ② 실기 스크린샷 대조(오케 직영, 4회차)가 jest+tsc+lint 전부 green인 상태에서 잡은 시각 결함 3건.

## 게이트·검증

- **게이트①-2**(2026-07-28, ①-1 무효화) **자율 승인** — 5-b가 ①-1 승인본에서 차단 1건(B-1, 거짓 GREEN)을 내 재차수. `stripComments` 근본수정(스캔 전 주석 제거) + W-1(렌더 크기 미잠금)·W-2(스크린샷 수정값 6종 미잠금) 보강.
- **게이트②-1**(2026-07-28) **자율 승인** — `BottomTabBar.tsx` 1파일 변경, 해시 일치, 스크린샷 대조 4회차 수렴(픽셀 표 원장 참조).
- **검증 n=1 PASS**(`04_qa-verifier_report_1_PASS.md`): 포맷·린트·타입체크·테스트(327 node + 43 integration)·해시 대조 2건·red 소급·변경집합 자체도출·경계면 QA·심판 보강 표본 2건 재확인 전부 PASS. 구조 지도 대조만 예고된 FAIL(신규 테스트 파일 미기재, [기록]에서 해소).
- **6-b 실기 스모크**: 발동 조건 5종 전부 "아니오" → 미해당. ⚠️ 실기 미검증 표식 **해당 없음** — 오케가 `figma-screen-impl` 스크린샷 대조를 실기로 4회차까지 이미 수행해 시각 층은 별도로 커버됨.
- **적대적 리뷰(5-b)**: 차단 1(B-1, ①-2에서 근본수정) · 경고 2(W-1·W-2, ①-2에서 닫음) · 참고 2(N-1 메커니즘 미확정 미처리, N-2 01b Q3 뒤집힘 [기록]에서 표기). 실제 조치 3건, 스타일 지적 비율 0%.
- **[리팩토링] 5-c**: 건너뜀 — 03b 지적이 전부 심판 보강(①-2)이라 5-c 범위 해당 0건.
- **Ponytail**: 제안 1 · 채택 0(아이콘 `<Svg>` 공통 래퍼 제안, AC-V2 위반으로 미채택). 4-a가 이 제안을 T-3으로 선제 예측 — 적중.

## 예상과 달랐던 것 (요지)

1. **이미 승인된 게이트①이 자기 심판의 결함으로 재차수를 탔다** — B-1(헤더 주석이 긍정 단언을 만족시킨 거짓 GREEN)은 이 리포에서 처음 관측된 방향이고, 2026-07-26 기록(거짓 RED 방향)의 정반대다.
2. **[인지] 단계의 raw CSS 판정이 스크린샷 실측으로 다시 뒤집혔다**(M-3) — Figma raw 속성(`rgba(255,255,255,0.68)`)을 읽은 것과 프로덕션 렌더 실측(`#F7F7F7`)은 다른 질문이었다. 사용자의 원래 전제(`surface-soft`)가 맞았다.
3. 정적 심판(jest+tsc+lint) 전부 green인 상태에서 스크린샷이 시각 결함 3건(M-1 알약 높이 내용물 구동·M-2 이 사이클이 만든 시각 회귀·M-3 색 되정정)을 잡았다 — 전부 게이트② 전에 발견돼 재제시 없이 고쳤다.

상세(뮤테이션 실측표·스크린샷 픽셀 수렴표·개념 노트 신설 3건+보강 1건·구조 지도 갱신·모델 배치·Ponytail 누적)는 옵시디언 개발로그 참조.

## 다음에 이어서 할 일

- TRIP-173 FSD 완결 4/4.
- 🔴 최우선 후속 티켓: `homeStructure.test.ts`의 거짓 GREEN 2건(INV-3 `hasDistance`·D-5 배선 가드) — 리포 전역 4개 스캔 가드에 `stripComments` 일괄 적용, 조각내지 말 것.
- N-1(M-3 메커니즘 미확정) — 프로덕션 탭바가 마스터의 인스턴스라는 데서 실마리 조사.
- 하네스 규칙 후보 (a)~(d) — `harness-rule-edit` 판정 대상. (d)는 1/4에서 이월된 3건 재게시(미판정으로 보임).

## 관련

- 옵시디언 개발로그: `TripPilot/개발로그/2026-07-28 20260728-trip173-tabbar-figma.md`
- 게이트①-2: `TripPilot/게이트/2026-07-28 20260728-trip173-tabbar-figma 게이트①-2.md`
- 게이트②-1: `TripPilot/게이트/2026-07-28 20260728-trip173-tabbar-figma 게이트②-1.md`
- 작업 공간: `_workspace/20260728-trip173-tabbar-figma/`
