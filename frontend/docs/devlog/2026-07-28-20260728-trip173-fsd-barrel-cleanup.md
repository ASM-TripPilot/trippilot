# 2026-07-28 20260728-trip173-fsd-barrel-cleanup

## 대상

**TRIP-173 FSD 완결 2/4** — `src/` 아래 참조 0인 빈 배럴(`export {};` 한 줄) 14개 삭제(프롬프트 열거 12개 + `home`·`onboarding` 2개, 기준 재적용으로 오케가 확장) + `fsdStructure.test.ts`에 재발 방지 AC-4 신설. **자율 실행 사이클**(게이트①② 1회 통합 자동 승인, 생략된 것은 한 줄 대화뿐).

**이 사이클의 실질은 삭제가 아니라 심판 보강 2건이다.** 삭제는 `git rm` 14회로 끝났다.

## 게이트·검증

- **게이트①②** 1회 통합 제시(2026-07-28) **자율 승인** — 해시 3/3 일치, red 로그(1 failed/8 passed/9 total)가 대상 14개를 정확히 목록으로 찍음.
- **검증 n=1 PASS**(`04_qa-verifier_report_1_PASS.md`): 포맷·린트·타입체크·테스트(322 node + 43 integration)·해시 대조·red 소급·변경집합 자체도출·경계면 QA·심판 보강 2건 실효 재확인 전부 PASS. 구조 지도 대조만 예고된 FAIL(비차단, [기록]에서 해소).
- **6-b 실기 스모크**: 발동 조건 5종 전부 "아니오" → 미해당(실행 불가 아님).
- **적대적 리뷰(5-b)**: 차단 0 · 경고 2(둘 다 5-c에서 조치) · 참고 3(1건은 [기록]에서 처리, 2건은 미처리 — 사유는 옵시디언 개발로그 참조). 실제 조치 2건, 스타일 지적 비율 0%.
- **[리팩토링] 5-c**: 실행됨(2건) — AC-4 스캔범위 앵커 + `eslint.config.js` 고아 zone 정리.
- **Ponytail**: 제안 3 · 채택 0 — 전부 뮤테이션 실측(요구 4종 중 일부만 잡음)으로 기각. 자리 문제(FSD1/4의 "제안 0")와는 성질이 다른, 대안의 실측 기각 사례.

## 예상과 달랐던 것 (요지)

1. **삭제가 이 사이클이 만든 심판 구멍을 두 개 드러냈다** — `importBoundary.test.ts`가 배럴 삭제로 경계 규칙 대신 모듈 해석 실패를 잡는 공허한 테스트가 됐고(구멍①), 신설한 AC-4의 긍정 짝이 스캔 범위(`BARREL_LAYERS`)를 앵커하지 않아 그 배열을 통째로 비워도 green이었다(구멍②, 내가 만든 가드).
2. **오케의 최초 진단이 틀렸다** — "grep이 부작용 import를 못 봤다"가 아니라 "grep은 잡았는데 문자열 리터럴이라 무해하다고 잘못 해석했다"였다(탐지 실패 vs 해석 실패). 재발 방지책이 갈리므로 문제로그로 분리.
3. `pnpm exec jest <파일>` 단독 실행이 `importBoundary.test.ts`에서 `NODE_OPTIONS=--experimental-vm-modules` 부재로 실패 — 테스트 결함으로 오인하기 쉬운 반복 패턴(최소 3개 사이클째).

상세(전줄 결정 근거·개념 노트 신설 1건+보강 3건·구조 지도 갱신·모델 배치·Ponytail 누적)는 옵시디언 개발로그 참조.

## 다음에 이어서 할 일

- TRIP-173 FSD 완결 3/4·4/4 — eslint 층 방향 규칙 집행 등 남은 항목.
- 후속 티켓 후보 3건(`eslint.config.js` FEATURES에 auth 추가 · README 옛 세그먼트 정정·HomeGlyphs 토큰화 · 주석/0바이트 빈 배럴 확장) 판단.
- 하네스 규칙 후보 — (a) 테스트에 박힌 문자열도 참조로 센다 (b) `verify-gates`에 NODE_OPTIONS 안내 추가 여부 (c) 1/4에서 이월된 3건. `harness-rule-edit` 판정 대상.

## 관련

- 옵시디언 개발로그: `TripPilot/개발로그/2026-07-28 20260728-trip173-fsd-barrel-cleanup.md`
- 게이트: `TripPilot/게이트/2026-07-28 20260728-trip173-fsd-barrel-cleanup 게이트①②-1.md`
- 작업 공간: `_workspace/20260728-trip173-fsd-barrel-cleanup/`
