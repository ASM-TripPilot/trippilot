# 2026-08-02 20260802-trip207-budget-block (요약)

**TRIP-207 [FE] g01 예산 블록 — 총액 입력 + 취향 러프값 프리필** (US-TRIP-01, BR-U1-38) · 브랜치 `feature/TRIP-207-FE-trip-budget-block` @ 기준 `8928876`(TRIP-203~206 머지 PR #67). **이 브랜치의 1번째 사이클, 아직 미커밋.**

> 옵시디언 개발로그의 축약본이다. 전체 서사·게이트 노트·문제로그·개념 노트는 볼트에 있다.

## 무엇을 했나

신규 1파일(`features/trip/model/budgetAmount.ts` — 판별 유니온 파싱/포맷 순수 함수) + 기존 4파일 수정(`tripWizardStore.ts`·`createTripRequest.ts`·`TripWizardStep1Screen.tsx`·`TripNewStep1Page.tsx`). **성격은 "추가"가 아니라 "기존 자동 취향 부착의 반전 + 동결 심판 3건 갱신"** — `buildCreateTripRequest`가 이미 취향 러프값을 사용자 입력 없이 서버로 보내고 있었고(TRIP-203 승인 PBT로 동결), 그 함수를 2인자→1인자로 바꿔 취향이 예산에 영향을 못 주게 타입 단계에서 막았다. 프리필은 스토어에 안 쓰는 **파생값**으로 둬 "프리필 쓰기≠사용자 입력" 불변식을 구조로 만족시켰다.

## 운영 모드

**자율 — 게이트①·② `00_gates.md`에서 대리 승인.** 한 줄 주해는 [기록] 종료 후 채팅으로 일괄 제시 — 이번엔 게이트 노트가 [기록] **이후**에 만들어지는 순서다(이전 사이클들과 다름).

## 결과

- 검증 n=1 **PASS**(포맷·린트·타입·테스트 전부 1회 통과, 게이트①해시 8/8·게이트②해시 5/5 일치, 변경 집합 자체도출 완전 일치, red 소급 재현, 경계면 QA 전부 실코드 확인)
- 실기 스모크 n=1 **PASS**(1차 시도 레드박스는 Metro 미기동이 원인 — 코드 결함 아님, 재시도로 4항목 전부 PASS, Figma `2225:2375` 8요소 정합)
- code-critic: 뮤턴트 11종(죽음 8·생존 3). 차단0·경고2(둘 다 5-c 처리)·참고3(1건은 03에 사후 추가, 1건은 이번 [기록]에서 구조 지도 반영, 1건은 다음 티켓 인수인계)
- 구조 지도 불일치 2건(qa-verifier·code-critic 동시 지목) → 이번 [기록]에서 전부 해소(`structure-index.cjs --check` OK, 206행↔206파일)

## 문제로그 (2건 신규, 1건 교차 링크만 — 상세는 옵시디언)

- **Metro 미기동을 실기 스모크 §0이 못 잡는다** — dev build는 JS 번들을 내장 안 해 Metro가 안 떠 있으면 어느 화면이든 레드박스가 뜨는데, §0 사전 점검은 시뮬레이터·앱 설치만 본다. 해결(재시도로 확인). 하네스 규칙 후보(강한 후보) — `curl -s http://localhost:8081/status`를 §0에 추가 제안.
- **(untouched × invalid) 조합에 테스트가 0건이라 조용한 실패가 살아남았다** — 두 독립 게이팅 축의 조합 사분면 중 한 칸이 비어 있어 "이유 없이 [다음]이 막힌 상태"가 심판 없이 존재했다(code-critic W-2). 해결(5-c, 신뢰 경계에서 원천 차단).
- **PBT 생성기 공백은 기존 문제로그에 교차 링크만 추가** — `fc.string()` 기본값이 현실적 숫자 입력을 거의 못 만드는 것을 test-designer가 사이클 안에서 혼합 생성기+예제 테스트로 즉시 메웠다. TRIP-204/206의 "생성기가 원리적으로 못 만든다" 사례와 원인이 달라(확률적 희소) 새 노트를 안 팠다.

## 정본 반영 (aidlc)

**제시 8 / 선택 4 / (A) 0 / (B) 5.** 3-a에서 사용자가 4건 선택 + 티켓이 직접 요구한 `[구현 결정]` 행 1건 = 5건, 전부 `u1-accommodation-trip/functional-design/{business-rules,frontend-components}.md` 정정(이미 승인된 문서의 사후 정정 — TRIP-181·TRIP-197 [기록] 선례 그대로 `audit.md`에 append, 스테이지 진행 아님).

## 다음에 이어서 할 일

1. **주해 일괄 제시(채팅)** — 게이트 노트(§0~§4) 이 시점에 신설. 사용자 판단 보류 3항목(`BUDGET_ERROR_MESSAGE` 발명 문구·`preference` 인자 삭제 확정·자릿수 상한 가드) + Metro 하네스 규칙 후보 함께 제시.
2. **미커밋 상태** — 코드 5파일 + `structure.md` + aidlc 3파일, 전부 작업트리에만 있음(브랜치 1번째 사이클).
3. `onPressBudgetEdit` prop 소비자 없음(N-3) — 다음 티켓이 채울지 결정.

## 관련

- 상세: 옵시디언 `TripPilot/개발로그/2026-08-02 20260802-trip207-budget-block.md`
- 원장: `_workspace/20260802-trip207-budget-block/00_gates.md`
