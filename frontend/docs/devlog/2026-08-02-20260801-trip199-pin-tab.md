# 2026-08-02 20260801-trip199-pin-tab

> 이 파일은 옵시디언 개발로그의 축약본이다(볼트 없는 사람·MCP 실패 시 폴백). 상세는 `TripPilot/개발로그/2026-08-02 20260801-trip199-pin-tab.md` 참조.

## 대상

**TRIP-199 [FE] e05 핀 지정 탭 + 지도 실패 폴백 완결**(부모 TRIP-75 · US-STAY-06 · U1). 브랜치 `feature/TRIP-199-FE-stay-link-pin-tabs`.

착수 전 사용자 결정으로 **역지오코딩을 프론트(카카오 지도 JS SDK `services`, `coord2Address`)로 처리** — 서버 `/stays/reverse-geocode` 계약 대기를 해소했다. LinkPasteTab은 `/stays/parse-link` 부재로 범위 밖 유지(BR-U1-24가 파싱 판정을 서버 정본으로 못박음).

## 만든 것 (신규 파일 0개, 수정만 6개 + 신규 테스트 5개)

| 파일 | 내용 |
|---|---|
| `shared/map/mapHtml.ts` | `KakaoMapMessage` 타입 · `buildMapHtml(center, jsKey, enablePin)` — 롱프레스·마커·`coord2Address` 대본 (수정, +104) |
| `shared/map/KakaoMapView.tsx` | `onMapMessage` prop · html을 마운트 시점에 고정(B-1 수정) (수정, +78) |
| `shared/map/index.ts` | `KakaoMapMessage` 타입 재수출 (수정, +6) |
| `features/stay/model/stayRegisterForm.ts` | `resolveName` export · `registerRoute`가 `coordSource` 반영 (수정, +56) |
| `features/stay/ui/StayRegisterScreen.tsx` | `PinPanel` 신설, 3탭 활성화, 배경막(W-10) (수정, +336) |
| `pages/stay-register/ui/StayRegisterPage.tsx` | `coordSource`·`pinAddressStatus`·`handleSelectTab` (수정, +76) |

## 게이트·검증

- **게이트①-1 승인**(9파일, "구현 진행 ㄱㄱ") → **①-2 재제시·승인**(`stayRegisterForm.test.ts` PBT 리터럴 누락, `tsc`가 [검증]에서 발견 — 뮤테이션 실측으로 red 로그 대체).
- **게이트②-2 승인**("ㄱㄱ 기록하고 커밋 푸쉬 머지 pr로 develop 브랜치에 합치자") — ②-1 제시 중 미룬 안건 2건(W-5·W-8)을 사용자가 "지금 처리"로 골라 5-c 복귀 후 새 차수.
- **적대적 리뷰(code-critic) 3차**: 차단 2(B-1·B-2, 둘 다 처리) · 경고 10(전부 처리) · 참고 10(처리 2·관찰 3·후속 이관 5). 스타일 지적 비율 0%.
- **⚠️ N-9(가장 중요)**: 3차 리뷰가 스크래치 사본에서 게이트②의 수정 4건을 하나씩 되돌려 승인 테스트를 재실행 — **4건 전부 되돌려도 671개 전부 통과**. `jest.mock('@/shared/map')`이 지도 컴포넌트의 마운트/언마운트/리렌더를 통째로 가려서다. 되돌린 4건 중 3건은 순수 RN 층이라 심판을 세울 수 있었는데 승인 계약에 그 케이스가 없었다 — **다음 사이클 테스트 설계 입력**.
- **검증 n=1 PASS** — 671/671 green, 해시 9+6 전부 일치, red 소급 정확 재현. N-9 한계를 그대로 승계("PASS ≠ 구현 정확성 보장").
- **실기 스모크 n=1 PASS(부분)** — 프로세스 부팅·레드박스 없음·화면 도달 전부 ✅. **⚠️ 실기 미검증**: 롱프레스·역지오코딩 성공·시트 열림 중 배경 지도 생존 등 7항목은 접근성 자동화 한계로 손 검증 필요.
- **사다리(ponytail)**: 제안 14 · 채택 0 — 매번 승인 테스트 파괴 또는 드리프트 재발 위험이 이유.

## 예상과 달랐던 것

1. **게이트①의 사정거리 공백** — test-designer가 red 시점 `tsc` 오류를 "전부 신규 계약 이름 부재"로 판정한 것은 그 시점엔 맞았지만, "타입이 생긴 뒤에도 남을 오류"는 구현 후 `tsc`로만 발견 가능했다.
2. **N-9** — 위 참고. 671 green이 "구현이 옳다"가 아니라 "심판이 이 층을 못 본다"는 뜻.
3. **주해가 결함 발견 경로로 작동함(N-12)** — 게이트② 주해 중 사용자가 "맵을 자유롭게 움직일 수 있나? 핀 자리는 고정인가?"를 물었고, 그 답을 확인하는 과정에서 code-critic 3차까지 못 본 결함(핀이 화면 밖으로 나가면 복귀 수단 없음)이 드러났다.

## 정본 반영 (제시 4 / 선택 4 / 완료 2 / 미반영 2)

- **완료**: TRIP-199 지라 티켓 본문 드리프트 3건 정정(`jira.sh describe`) · `figma-structure.md` 48행 "x좌표 세대 판정 규칙은 보편적이지 않다" 경고 추가(harness-rule-edit 절차, 하네스 변경이력 기록).
- **미반영**: `aidlc/aidlc-docs/construction/.../frontend-components.md:68`(PinTab 행) · `nfr-requirements.md:64`(SEC-U1-05 공백) — CONSTRUCTION 설계 단계 승인 게이트(대화형)·`audit.md` 원문 append 요건을 이 자동 기록 호출 안에서 채울 수 없어 보류. 다음 상호작용에서 사용자와 함께 처리 필요.

## 미룬 것 / 후속 티켓 후보

N-3(핀 좌표가 검색 탭 미리보기로도 뜸)·N-4(G-6 부분문자열 검사)·N-5(G-5 모집단 좁음)·N-7(짧은 탭이 핀이 됨)·N-10(SDK 2벌 로드)·N-11(달력 시트 배경막 부재)·**N-12(핀이 화면 밖으로 나가면 복귀 수단 없음)**. LinkPasteTab 분리 티켓 미생성.

## 다음에 이어서 할 일

1. 실기 손 검증(특히 좌표→주소 조회 실동작)
2. (B) 정본 반영 2건 — 사용자와 CONSTRUCTION 승인 게이트 직접 진행
3. N-9 대응 — 지도 컴포넌트 마운트/언마운트/리렌더를 실제로 보는 승인 테스트 추가
4. N-12 수정 방향 결정(「내 핀으로」 버튼 또는 시트 열 때 재중심)
5. 커밋·푸시·PR·develop 머지
