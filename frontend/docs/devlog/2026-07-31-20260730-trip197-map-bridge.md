# 2026-07-31 20260730-trip197-map-bridge

> 이 파일은 옵시디언 개발로그의 축약본이다(볼트 없는 사람·MCP 실패 시 폴백). 상세는 `TripPilot/개발로그/2026-07-31 20260730-trip197-map-bridge.md` 참조.

## 대상

**TRIP-197 [FE] 지도 계층 선행 — 카카오 SDK 브리지 + `src/shared/map`**(부모 TRIP-75 · US-STAY-06 · 유닛 U1). 브랜치 `feature/TRIP-197-FE-kakao-map-bridge` — **미커밋 상태**(사용자 결정 대기).

카카오가 RN 네이티브 지도 래퍼를 안 만든다(공식 패키지 저자가 2025-03-19 커밋으로 map 패키지 삭제) — **WebView + 카카오 지도 JS SDK**로 우회, 재빌드한 iOS dev build에서 타일 렌더 실기 확인 완료.

## 게이트·검증

- **게이트①** 2차 재제시·승인 — 1차(승인 후 무효화): 승인된 심판(`stripComments`)이 URL의 `//`를 주석으로 오인해 구조적으로 통과 불가능한 케이스 + 조용한 결함(A-4가 위반을 못 봄) 2건 발견. `(?<!:)` 룩비하인드로 수복 후 2차 재제시·승인.
- **게이트②** 1차 승인 — 신규 3파일(`mapHtml.ts`·`KakaoMapView.tsx`·`index.ts`) + `.env.example` + `_dev/preview.tsx` 편집 + `react-native-webview` 의존성 추가.
- **적대적 리뷰(code-critic)**: 차단 0 · 경고 6 · 참고 8, 그중 3건(W4·W5·W6) 5-c에서 처리 — W4(SDK 스크립트 401 감지 배선 신설)는 실기 스모크에서 실제로 작동 확인.
- **검증(qa-verifier) n=1 PASS** — 해시 대조 전부 일치, red 소급 재현, 변경집합 자체도출 15/15 일치.
- **실기 스모크 n=1 PASS** — 층 B(Podfile.lock 링크 확인)·층 C(타일 렌더·실패 표면·크래시 없음) 전부 통과. R1(iOS WKWebView baseUrl origin) 리스크가 실기로 해소됨(폴백 불필요 확정).

## 예상과 달랐던 것

1. 승인된 심판 자체의 구조적 결함 2건(위) — 게이트①-2 재제시.
2. 티켓의 "`.env` 파일 0건" 주장이 틀림(실측 `.env` 2줄·`.env.local` 34줄, 둘 다 미추적) — Jira 본문 정정.
3. R1(최대 리스크로 예비)이 실기에서 깔끔히 해소됨.
4. 백그라운드 빌드 로그를 `tail -N`으로 가려 진행 상황을 못 봄 → `strings` 바이너리 스캔 시도도 위음성 → `Podfile.lock`으로 회귀.
5. 카카오 콘솔 등록 위치 착오(`웹 도메인` ≠ `JavaScript SDK 도메인`) — 401 원인.

## 하네스 규칙 후보 (3건)

1. 매처 실검증 의무가 단일 매처만 재고 전처리×탐지기 조합은 안 잰다(이번 결함의 실제 원인).
2. 백그라운드 명령에 `| tail -N`을 걸면 진행 로그가 통째로 사라진다.
3. 네이티브 모듈 링크 판정의 정본은 `Podfile.lock`이다 — 바이너리 `strings`는 위음성을 낸다.

트립와이어 발동(사용자 결정 "규칙대로 통합 복귀") — 게이트① 재제시 재발로 4-a/4-b 분할을 단일 test-designer로 복귀 예정(사이클 종료 후 오케스트레이터가 SKILL.md 편집). 하네스 변경이력에 원인 분석과 함께 기록 완료.

## 정본 반영 (3-a 선택 3건)

1. Jira TRIP-197 본문 `.env` 정정 — **성공**(description 직접 수정)
2. `frontend/docs/structure.md` 갱신(신규 6 + 기존 부채 17) — **성공**(`structure-index.cjs --check` OK, 143행↔143파일)
3. aidlc `SEC-U1-05` 예외 명시 — **(B) 미반영**(U1 NFR Requirements 스테이지가 이미 승인·종료돼 재개 승인 게이트에 필요한 사용자 원문 발화가 없어 절차를 끝까지 못 밟음 — `aidlc/` 무변경, 상세는 옵시디언 개발로그)

## 다음에 이어서 할 일

- 커밋 여부 사용자 결정 대기
- 트립와이어 실행(사이클 종료 후 하네스 편집)
- e05 티켓에서 `KakaoMapView` 첫 소비 — `Coords` 타입 승격 재론
- 이해부채 4건(옵시디언) — IC-3(INV-3 duration)이 최우선 재노출 대상
