/**
 * Generated by orval v8.22.0 🍺
 * Do not edit manually.
 * TripPilot U1 API (소셜 로그인 전용 MVP)
 * U1 기반·계정·온보딩 (M1 Auth · M2 Profile · C3 Moderation). 소셜 로그인 전용 — 이메일/비밀번호 로그인은 후속 이연. 정본 대조: docs/design/U1-API-설계.md, U1-DB스키마-설계.md, U1-내부아키텍처-설계.md
 *
 * OpenAPI spec version: 0.1.0-draft
 */
import { useMutation } from '@tanstack/react-query';
import type {
  MutationFunction,
  QueryClient,
  UseMutationOptions,
  UseMutationResult,
} from '@tanstack/react-query';

import type { PushTokenResponse, RegisterPushTokenRequest } from '../schemas';

import { customInstance } from '../../mutator';

/**
 * **멱등이라 POST 다** — 같은 토큰을 다시 보내면 새 행이 아니라 갱신이고, 클라이언트는 자기 토큰의 서버 id 를 모른다(토큰 자체가 키다). 앱은 포그라운드 복귀마다 다시 보내도 된다.
 *
 * `osPermission` 을 함께 받는 이유는 **서버가 OS 권한을 알 방법이 없기** 때문이다 (`location_consent_state.os_permission_mirror` 와 같은 꼴의 미러). 이 값이 채널 판정에 들어간다 — `GRANTED` 가 아니면 쏘지 않는다. 쏴도 닿지 않고 레이트리밋만 먹는다.
 *
 * 같은 토큰이 **다른 계정**에 있으면 그 기기를 새 계정으로 옮긴다. 기기 교체·계정 전환의 실제 모습이고, 옮기지 않으면 남의 알림이 그 기기로 간다.
 *
 * 한 계정에 토큰이 **여러 개일 수 있다**(INV-U6-06 다기기) — 발송은 유효 토큰 전부에 한다.
 * @summary 푸시 토큰 등록(l02)
 */
export const postMePushTokens = (
  registerPushTokenRequest: RegisterPushTokenRequest,
  signal?: AbortSignal
) => {
  return customInstance<PushTokenResponse>({
    url: `/me/push-tokens`,
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    data: registerPushTokenRequest,
    signal,
  });
};

export const getPostMePushTokensMutationOptions = <
  TError = void,
  TContext = unknown,
>(options?: {
  mutation?: UseMutationOptions<
    Awaited<ReturnType<typeof postMePushTokens>>,
    TError,
    { data: RegisterPushTokenRequest },
    TContext
  >;
}): UseMutationOptions<
  Awaited<ReturnType<typeof postMePushTokens>>,
  TError,
  { data: RegisterPushTokenRequest },
  TContext
> => {
  const mutationKey = ['postMePushTokens'];
  const { mutation: mutationOptions } = options
    ? options.mutation &&
      'mutationKey' in options.mutation &&
      options.mutation.mutationKey
      ? options
      : { ...options, mutation: { ...options.mutation, mutationKey } }
    : { mutation: { mutationKey } };

  const mutationFn: MutationFunction<
    Awaited<ReturnType<typeof postMePushTokens>>,
    { data: RegisterPushTokenRequest }
  > = (props) => {
    const { data } = props ?? {};

    return postMePushTokens(data);
  };

  return { mutationFn, ...mutationOptions };
};

export type PostMePushTokensMutationResult = NonNullable<
  Awaited<ReturnType<typeof postMePushTokens>>
>;
export type PostMePushTokensMutationBody = RegisterPushTokenRequest;
export type PostMePushTokensMutationError = void;

/**
 * @summary 푸시 토큰 등록(l02)
 */
export const usePostMePushTokens = <TError = void, TContext = unknown>(
  options?: {
    mutation?: UseMutationOptions<
      Awaited<ReturnType<typeof postMePushTokens>>,
      TError,
      { data: RegisterPushTokenRequest },
      TContext
    >;
  },
  queryClient?: QueryClient
): UseMutationResult<
  Awaited<ReturnType<typeof postMePushTokens>>,
  TError,
  { data: RegisterPushTokenRequest },
  TContext
> => {
  return useMutation(getPostMePushTokensMutationOptions(options), queryClient);
};
/**
 * 없거나 남의 것이면 404 — 존재를 알려 주지 않는다.
 * @summary 푸시 토큰 해제(로그아웃·기기 해제)
 */
export const deleteMePushTokensToken = (
  token: string,
  signal?: AbortSignal
) => {
  return customInstance<void>({
    url: `/me/push-tokens/${token}`,
    method: 'DELETE',
    signal,
  });
};

export const getDeleteMePushTokensTokenMutationOptions = <
  TError = void,
  TContext = unknown,
>(options?: {
  mutation?: UseMutationOptions<
    Awaited<ReturnType<typeof deleteMePushTokensToken>>,
    TError,
    { token: string },
    TContext
  >;
}): UseMutationOptions<
  Awaited<ReturnType<typeof deleteMePushTokensToken>>,
  TError,
  { token: string },
  TContext
> => {
  const mutationKey = ['deleteMePushTokensToken'];
  const { mutation: mutationOptions } = options
    ? options.mutation &&
      'mutationKey' in options.mutation &&
      options.mutation.mutationKey
      ? options
      : { ...options, mutation: { ...options.mutation, mutationKey } }
    : { mutation: { mutationKey } };

  const mutationFn: MutationFunction<
    Awaited<ReturnType<typeof deleteMePushTokensToken>>,
    { token: string }
  > = (props) => {
    const { token } = props ?? {};

    return deleteMePushTokensToken(token);
  };

  return { mutationFn, ...mutationOptions };
};

export type DeleteMePushTokensTokenMutationResult = NonNullable<
  Awaited<ReturnType<typeof deleteMePushTokensToken>>
>;

export type DeleteMePushTokensTokenMutationError = void;

/**
 * @summary 푸시 토큰 해제(로그아웃·기기 해제)
 */
export const useDeleteMePushTokensToken = <TError = void, TContext = unknown>(
  options?: {
    mutation?: UseMutationOptions<
      Awaited<ReturnType<typeof deleteMePushTokensToken>>,
      TError,
      { token: string },
      TContext
    >;
  },
  queryClient?: QueryClient
): UseMutationResult<
  Awaited<ReturnType<typeof deleteMePushTokensToken>>,
  TError,
  { token: string },
  TContext
> => {
  return useMutation(
    getDeleteMePushTokensTokenMutationOptions(options),
    queryClient
  );
};
