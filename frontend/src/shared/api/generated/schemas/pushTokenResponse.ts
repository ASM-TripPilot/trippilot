/**
 * Generated by orval v8.22.0 🍺
 * Do not edit manually.
 * TripPilot U1 API (소셜 로그인 전용 MVP)
 * U1 기반·계정·온보딩 (M1 Auth · M2 Profile · C3 Moderation). 소셜 로그인 전용 — 이메일/비밀번호 로그인은 후속 이연. 정본 대조: docs/design/U1-API-설계.md, U1-DB스키마-설계.md, U1-내부아키텍처-설계.md
 *
 * OpenAPI spec version: 0.1.0-draft
 */
import type { PushTokenResponseOsPermission } from './pushTokenResponseOsPermission';
import type { PushTokenResponsePlatform } from './pushTokenResponsePlatform';

/**
 * 토큰 자체는 돌려주지 않는다 — 클라이언트가 이미 갖고 있고 응답·로그에 흘릴 이유가 없다.
 */
export interface PushTokenResponse {
  deviceId?: string | null;
  platform: PushTokenResponsePlatform;
  osPermission: PushTokenResponseOsPermission;
  /** 이 기기에 **실제로 쏠 수 있는가**. 화면이 `권한 필요` 안내를 그릴 근거다 — 등록에 성공했다는 것과 알림이 간다는 것은 다른 말이다. */
  deliverable: boolean;
}
