/**
 * Generated by orval v8.22.0 🍺
 * Do not edit manually.
 * TripPilot U1 API (소셜 로그인 전용 MVP)
 * U1 기반·계정·온보딩 (M1 Auth · M2 Profile · C3 Moderation). 소셜 로그인 전용 — 이메일/비밀번호 로그인은 후속 이연. 정본 대조: docs/design/U1-API-설계.md, U1-DB스키마-설계.md, U1-내부아키텍처-설계.md
 *
 * OpenAPI spec version: 0.1.0-draft
 */
import type { ReplanSessionOriginKind } from './replanSessionOriginKind';
import type { ReplanSessionScope } from './replanSessionScope';
import type { ReplanSessionStatus } from './replanSessionStatus';

export interface ReplanSession {
  sessionId: string;
  tripId: string;
  /** 어느 일정을 다시 짜는가 */
  itineraryId: string;
  triggerId?: string | null;
  scope: ReplanSessionScope;
  /** '지금 이후'의 기준점 — 이 시각 이전 슬롯은 대상이 아니다 */
  fromInstant: string;
  originKind: ReplanSessionOriginKind;
  originLat?: number | null;
  originLng?: number | null;
  /** GPS 가 아니면 추정 출발지다 — 화면이 그 사실을 밝혀야 한다(US-PLANB-10) */
  originEstimated: boolean;
  reasons?: string[];
  directives?: string[];
  freeText?: string | null;
  excludedPoiIds?: string[];
  /** COLLECTING→SOLVING→DRAFT→APPLIED|CANCELED. 실패는 FAILED(오류)·NO_SOLUTION(해 없음)로 갈린다 — 사용자가 할 일이 다르다(재시도 vs 조건 완화) */
  status: ReplanSessionStatus;
  createdAt: string;
  closedAt?: string | null;
}
