/**
 * Generated by orval v8.22.0 🍺
 * Do not edit manually.
 * TripPilot U1 API (소셜 로그인 전용 MVP)
 * U1 기반·계정·온보딩 (M1 Auth · M2 Profile · C3 Moderation). 소셜 로그인 전용 — 이메일/비밀번호 로그인은 후속 이연. 정본 대조: docs/design/U1-API-설계.md, U1-DB스키마-설계.md, U1-내부아키텍처-설계.md
 *
 * OpenAPI spec version: 0.1.0-draft
 */
import type { TriggerKind } from './triggerKind';
import type { TriggerScope } from './triggerScope';

export interface Trigger {
  triggerId: string;
  /** 4종뿐이다(BR-U4-01). ai TriggerKind 가 정본이며 백엔드가 종류를 늘리지 않는다 — '체류 초과'는 DELAY 의 payload 변형이고 '교통'은 존재하지 않는다 */
  kind: TriggerKind;
  affectedDate: string;
  /** 영향 슬롯의 경계 키 "{date}#{poiId}"(BR-U2-04). null = 날짜 전체 영향 */
  slotKey?: string | null;
  /** 사용자 노출 문구의 근거(예: "비 예보 70%") */
  reason: string;
  /** 발화 시 재계획 범위 — [대안 보기] 가 이 값으로 세션을 연다 */
  scope?: TriggerScope;
  detectedAt: string;
}
