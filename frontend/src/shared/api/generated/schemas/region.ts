/**
 * Generated by orval v8.22.0 🍺
 * Do not edit manually.
 * TripPilot U1 API (소셜 로그인 전용 MVP)
 * U1 기반·계정·온보딩 (M1 Auth · M2 Profile · C3 Moderation). 소셜 로그인 전용 — 이메일/비밀번호 로그인은 후속 이연. 정본 대조: docs/design/U1-API-설계.md, U1-DB스키마-설계.md, U1-내부아키텍처-설계.md
 *
 * OpenAPI spec version: 0.1.0-draft
 */
import type { RegionLevel } from './regionLevel';

export interface Region {
  /** 법정동코드 앞자리 — 시도 2자리·시군구 5자리 */
  regionCode: string;
  name: string;
  /** 묶음 표시용 상위 시도명 */
  sidoName: string;
  level: RegionLevel;
  /** 목적지로 고를 수 있는가. 도(道)는 범위가 넓어 POI 후보가 흩어지고, 일반시의 행정구(수원시 장안구)는 여행지로 범위가 어긋나 false 다 */
  selectable: boolean;
  /** 이 지역의 ACTIVE POI 수. 0이면 후보풀이 비어 일정이 조용히 빈다. 시도 행은 그 안 시군구를 모두 합한 값이다 — 시도를 고르면 그 안이 전부 후보풀이 되기 때문이다. 조회 시점에 세므로 저장된 값과 어긋나지 않는다. */
  poiCount: number;
}
