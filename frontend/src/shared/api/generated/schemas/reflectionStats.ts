/**
 * Generated by orval v8.22.0 🍺
 * Do not edit manually.
 * TripPilot U1 API (소셜 로그인 전용 MVP)
 * U1 기반·계정·온보딩 (M1 Auth · M2 Profile · C3 Moderation). 소셜 로그인 전용 — 이메일/비밀번호 로그인은 후속 이연. 정본 대조: docs/design/U1-API-설계.md, U1-DB스키마-설계.md, U1-내부아키텍처-설계.md
 *
 * OpenAPI spec version: 0.1.0-draft
 */
import type { ReflectionStatsDistanceSource } from './reflectionStatsDistanceSource';

/**
 * 근거 수치 — **비어 있을 수 없다**(INV-U5-07). 방문 0곳이어도 0으로 채운다. **이동 소요시간 필드가 없다**(INV-3 · PBT-U5-5) — 거리만 낸다. 개별·일별 체류 시간도 없다(BR-U5-08); 누적 평균만 예외이고 그건 스타일 분석 소관이다.
 */
export interface ReflectionStats {
  visitCount: number;
  distanceKm: number;
  /** `VISIT_LINE` 은 **방문점을 이은 직선 합**이지 도로 거리가 아니다(BR-U5-43). `actual_route_point` 가 미실장이라 서버가 실제 이동 경로를 모른다 — 밝히지 않으면 사용자가 근사값을 실측으로 읽는다. */
  distanceSource: ReflectionStatsDistanceSource;
  photoCount: number;
}
