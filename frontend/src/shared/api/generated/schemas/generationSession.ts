/**
 * Generated by orval v8.22.0 🍺
 * Do not edit manually.
 * TripPilot U1 API (소셜 로그인 전용 MVP)
 * U1 기반·계정·온보딩 (M1 Auth · M2 Profile · C3 Moderation). 소셜 로그인 전용 — 이메일/비밀번호 로그인은 후속 이연. 정본 대조: docs/design/U1-API-설계.md, U1-DB스키마-설계.md, U1-내부아키텍처-설계.md
 *
 * OpenAPI spec version: 0.1.0-draft
 */
import type { GenerationSessionMode } from './generationSessionMode';
import type { GenerationSessionStatus } from './generationSessionStatus';

export interface GenerationSession {
  sessionId: string;
  /** day1 확정 전에는 null — 일정 행이 생기기 전에도 세션은 존재한다. */
  itineraryId?: string | null;
  /** RUNNING=1차 진행 중 · DAY1_READY=day1 노출됨(2차 진행 중) · COMPLETED=전 일자 완료 · FAILED=2차 실패(day1 분은 일정에 유효하게 남는다) · CANCELED=사용자가 취소. */
  status: GenerationSessionStatus;
  mode: GenerationSessionMode;
  /** 폴백 배너의 근거(INV-4 침묵 금지). day1 시점에 실려 배너가 첫 노출부터 사실을 말한다. */
  isFallback: boolean;
  candidatesLevel?: string | null;
  startedAt: string;
  day1ReadyAt?: string | null;
  /** 끝난 세션에만 있다 — 진행 중이면 null(DB CHECK 로도 강제). */
  finishedAt?: string | null;
}
