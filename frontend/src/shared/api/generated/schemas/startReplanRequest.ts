/**
 * Generated by orval v8.22.0 🍺
 * Do not edit manually.
 * TripPilot U1 API (소셜 로그인 전용 MVP)
 * U1 기반·계정·온보딩 (M1 Auth · M2 Profile · C3 Moderation). 소셜 로그인 전용 — 이메일/비밀번호 로그인은 후속 이연. 정본 대조: docs/design/U1-API-설계.md, U1-DB스키마-설계.md, U1-내부아키텍처-설계.md
 *
 * OpenAPI spec version: 0.1.0-draft
 */
import type { StartReplanRequestOriginKind } from './startReplanRequestOriginKind';
import type { StartReplanRequestScope } from './startReplanRequestScope';

export interface StartReplanRequest {
  /** 재계획 범위(DEC-U4-3). ai ReplanScope 어휘를 그대로 쓴다 */
  scope: StartReplanRequestScope;
  /** 출발 기준점. **생략하면 서버가 사다리로 정한다**(BR-U4-19) — 실측 → 수동 핀 → 마지막 완료 방문지 → 등록 숙소 앵커. 위치를 못 잡았다고 재계획을 막지 않으며, 어떤 가정을 썼는지는 응답의 originKind·originEstimated 로 밝힌다 */
  originKind: StartReplanRequestOriginKind;
  /** GPS·MANUAL 이면 필수 */
  originLat?: number | null;
  /** GPS·MANUAL 이면 필수 */
  originLng?: number | null;
  /**
   * i10 의 '왜'(다중). 서버가 어휘를 강제하지 않는다 — 해석은 재계획 경계가 한다
   * @maxItems 10
   */
  reasons?: string[];
  /**
   * i10 의 '어떻게'(다중)
   * @maxItems 10
   */
  directives?: string[];
  /** @maxLength 500 */
  freeText?: string | null;
  /** '건너뛰기'가 채운다 — 재계획 시 후보에서 뺀다 */
  excludedPoiIds?: string[];
  /** 자동 진입이면 근거 트리거. 수동이면 null */
  triggerId?: string | null;
}
