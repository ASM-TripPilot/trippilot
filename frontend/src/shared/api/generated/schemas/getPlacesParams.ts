/**
 * Generated by orval v8.22.0 🍺
 * Do not edit manually.
 * TripPilot U1 API (소셜 로그인 전용 MVP)
 * U1 기반·계정·온보딩 (M1 Auth · M2 Profile · C3 Moderation). 소셜 로그인 전용 — 이메일/비밀번호 로그인은 후속 이연. 정본 대조: docs/design/U1-API-설계.md, U1-DB스키마-설계.md, U1-내부아키텍처-설계.md
 *
 * OpenAPI spec version: 0.1.0-draft
 */
import type { PoiCategory } from './poiCategory';

export type GetPlacesParams = {
  /**
   * 지역 **이름**(표준명·별칭 모두 — `부산`·`부산광역시` 둘 다 통한다). 서버가 행정구역 코드로 해석해 **접두사**로 조회하므로 시도를 고르면 그 안 시군구가 전부 잡힌다. 동명이지역(`동구` 는 대전·대구·광주·부산에 있다)은 **그 이름을 가진 지역 전부**가 대상이다 — 하나를 고르면 거짓이 된다. 카탈로그에 없는 이름은 **빈 목록**이다(전국으로 넓히지 않는다).
   */
  region?: string;
  category?: PoiCategory;
  /**
   * 이름 부분일치(대소문자 무시). **서버가 검색한다** — 클라가 받은 장 안에서만 거르면 뒷장에 있는 것이 "없다"로 보인다. LIKE 메타문자(`%`·`_`)는 글자 그대로 취급한다.
   */
  q?: string;
  /**
   * 한 장의 최대 건수. **미지정·초과도 200 으로 맞춘다** — 지역을 안 골라도 전량이 나가지 않는다.
   * @minimum 1
   * @maximum 200
   */
  limit?: number;
  /**
   * 이전 응답의 `nextCursor`. 불투명 문자열이라 값의 모양에 기대지 않는다. offset 이 아닌 **지점 기준**이라, 이어 받는 사이에 장소가 추가돼도 행이 중복·누락되지 않는다. 형식이 깨진 값은 400 이다(조용히 처음으로 되돌리면 목록이 리셋된 이유를 알 수 없다).
   */
  cursor?: string;
};
