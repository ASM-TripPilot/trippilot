import { Text, View } from 'react-native';

export default function RecordsScreen() {
  return (
    <View className="flex-1 items-center justify-center">
      <Text className="text-base">기록</Text>
    </View>
  );
}
