import { TripNewStep1Page } from '@/pages/trip-new-step1';

/** g01 여행 만들기 1/2 — 얇은 라우트, 배선은 `pages/trip-new-step1`가 진다(`stays/index.tsx` 선례). */
export default function TripWizardStep1Route() {
  return <TripNewStep1Page />;
}
