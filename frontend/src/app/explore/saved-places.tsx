import { SavedPlacesPage } from '@/pages/saved-places';

/**
 * d02 담은 장소 — 정본 `frontend-components.md` §1의 `explore/saved-places.tsx` 자리.
 * `region.tsx`·`places.tsx`와 같은 `(tabs)` 밖 파일시스템 라우트(몰입 화면, 탭바 없음).
 */
export default function SavedPlacesRoute() {
  return <SavedPlacesPage />;
}
