/**
 * OSM 데이터 사용 고지(ODbL) 페이지 — 설정 데이터 출처 블록의 OSM 줄이 연다(TRIP-886).
 * pages 층은 URL 리터럴을 두지 않으므로(`pagesLayerStructure.test.ts`) 여기서 쥔다.
 */
export const OSM_COPYRIGHT_URL = 'https://www.openstreetmap.org/copyright';
