---
name: trippilot-dev-cycle
description: "TripPilot frontend(React Native/Expo) 코드를 변경하는 모든 작업 — 기능 구현, 화면/컴포넌트 개발, 버그 수정, 리팩토링, 스캐폴딩 — 요청 시 반드시 이 스킬을 사용하라. '사이클 시작해/돌려/이어서 해', '다시 실행', '재실행', '사이클 ~만 다시', '이전 사이클 결과 기반으로' 같은 사이클 후속 요청도 포함한다. 사용하지 않는 경우: 코드 변경이 없는 단순 질문·문서 열람, 문서 편집(README·CLAUDE.md·docs/*.md — 단 의미 있는 문서 변경은 obsidian-second-brain으로 기록), backend/ai 코드 작업(전용 하네스 미구축 — 하네스 부재를 알리고 일반 워크플로우로 진행)."
---

# TripPilot Dev Cycle — 오케스트레이터

루프 엔지니어링 개발 사이클: 인지 → 메모리 → 설계 → 테스트(게이트①) → 구현(게이트②) → 검증 → 리팩토링 → 재검증 → 기록.

**실행 모드: 하이브리드** — 오케스트레이터(메인 세션)가 사용자 게이트와 MCP 호출(Ouroboros/Obsidian/Figma)을 직접 관리하고, 단계별 전문 작업은 서브 에이전트(`Agent` 도구)에 위임한다. 사용자 검토 게이트가 2개 있어 실시간 팀 통신보다 순차 파이프라인이 적합하다.

## 자리별 모델 배치 (2026-07-21)

| 자리 | 모델 | 왜 |
|---|---|---|
| **오케스트레이터** | `fable` (`/model`) | **상류다** — 서브 프롬프트를 오케가 쓴다. 최고 판단(맹점 훑기·이해 체크 출제)이 여기 있다 |
| **test-designer 4-a 설계** | `fable` | 인터페이스·단언·장치를 정하고 게이트①로 **동결**될 판단 전부 — 되돌리기 비용 최고, 검사는 학습자 게이트뿐 |
| test-designer 4-b 작성 | `sonnet` | 4-a 명세를 코드·주해로 표현 — 명세가 단언 수준이라 판단 잔량이 적고, red 재확인(오케)+red 소급(qa)이 삼중 검사다 |
| spec-analyst · code-critic | `opus` | 요구 누락은 조용히 전파된다 / 적대적 리뷰는 순수 추론이다 |
| implementer · qa-verifier · scribe | `sonnet` | 실패가 시끄럽거나(삼중 검사 위) 절차 준수가 대부분이다 |

원칙: **올리는 건 되돌리기 비싼 자리부터, 내리는 건 실패가 시끄러운 자리부터.**

**호출 시 `model` 파라미터를 반드시 명시한다** — 명시 파라미터가 에이전트 frontmatter보다 우선이므로, 둘이 어긋나면 파라미터가 이긴다.

> ⚠️ **메타 스킬 `harness:harness`의 "모든 에이전트는 `model: "opus"`" 지침보다 이 표가 우선한다.** 그 스킬이 로드돼도 일괄 opus로 되돌리지 마라 — 이 배치는 12사이클 실측(자리별 실패 가시성·되돌리기 비용·호출 빈도) 위에 있다.
>
> **3사이클 관찰 대상이다.** scribe가 개발로그에 이 사이클의 모델 배치를 한 줄 남기고, 지표(맹점 훑기 적중 / 04 로그 원문 유무·미신고 변경 탐지 / 전줄 주해 품질·수정 루프 횟수)를 보고 되돌릴지 판정한다.
>
> **test-designer 자리는 관찰을 조기 종결하고 4-a/4-b로 분할했다(2026-07-22, 사용자 결정 — 비용).** 1사이클 실측(163k, 반려 0)에 기반하며, 트립와이어(게이트① 반려 1회 → 통합 복귀)가 품질 안전망이다. 나머지 자리의 관찰은 계속된다.

## 참조 파일 — 해당 시점에 반드시 읽는다

| 파일 | 읽는 시점 |
|---|---|
| [reference/resume.md](reference/resume.md) | **Phase 0 진입 시** — 사이클 ID 탐지·재개 판별·RESUME.md·데이터 전달 프로토콜 |
| [reference/gate-protocol.md](reference/gate-protocol.md) | **게이트①·② 제시 직전** — 원장 기록 형식·해시 규약·제시 형식·이해부채·학습자 모드 |
| [reference/troubleshooting.md](reference/troubleshooting.md) | **검증 FAIL·에이전트 실패·MCP 불가 시** — 에스컬레이션 상한·분기 처리 |

요약으로 대신하지 마라. 게이트 제시와 재개 판별은 형식이 곧 규칙이라, 원문을 읽지 않으면 원장이 오염된다.

## 서브 에이전트 호출 공통 (1단계부터 적용)

**모든 서브 에이전트(spec-analyst·test-designer·implementer·code-critic·scribe) 프롬프트에 "학습자 톤" 지시를 명시 주입한다.** 이 사용자는 코드 초심자다:

1. 새 문법·API·패턴은 이름만 쓰지 말고 한 줄 개념 설명 병기
2. 테스트는 "준비(Arrange)→실행(Act)→단언(Assert)" 3동작 뼈대로 서술
3. 코드를 읽을 줄 안다고 가정 금지 — diff보다 "무엇을 보장하나" 동작 서술 우선

주입을 빠뜨리면 산출물 톤이 조용히 어긋난다. 게이트 시점의 학습자 규칙(핵심 개념 선정 등)은 reference/gate-protocol.md "학습자 모드"에 있다.

## 경로 규약 (모든 단계 공통)

- **하네스 파일을 가리킬 때는 `<리포 루트>/frontend/.claude/...`로 쓴다.** `<리포 루트>`는 읽는 쪽이 `git rev-parse --show-toplevel`로 푼다.
  - **서브 에이전트의 cwd는 호출마다 다르다**(`frontend/`일 때도, `frontend/.claude/`일 때도 있다 — 실측 확인). 따라서 `.claude/...`도 `frontend/.claude/...`도 안전하지 않다. 기준점을 문자열에 박아야 어디서 읽어도 열린다.
  - 예외: SKILL.md가 **자기 스킬의** `reference/x.md`를 가리킬 때는 스킬 루트 기준 상대경로가 맞다(Claude Code가 스킬 디렉토리 기준으로 해석한다).
- **리포 루트**는 `git rev-parse --show-toplevel`로 확정한다 (예: `~/Desktop/dev/trippilot`).
- 사이클 작업 공간: `<리포 루트>/_workspace/{cycle-id}/` — **항상 리포 루트 기준**. cwd 기준 상대경로 금지.
- 서브 에이전트 프롬프트에는 작업 공간과 산출물 경로를 **절대 경로로** 전달한다 — 서브 에이전트의 cwd(frontend)에서 상대경로를 해석하면 생산자와 소비자가 서로 다른 디렉토리를 보게 된다.

## 사이클 체크리스트

응답에 복사해 진행하면서 체크한다:

```
사이클 {cycle-id} 진행:
- [ ] Phase 0: 컨텍스트 확인 (reference/resume.md 읽기)
- [ ] 1. [인지] spec-analyst → 01_brief
- [ ] 2. [메모리] 옵시디언·devlog 조회 + 미상환 이해부채 재노출
- [ ] 3. [설계] 🧑 맹점 훑기 제시 → Ouroboros 인터뷰 → 01b_seed (폴백 포함 항상 생성)
- [ ] 4. [테스트] 4-a 설계(fable)→02a · 4-b 작성(sonnet)→02+red → 🧑 게이트① 승인·원장 기록
- [ ] 5. [구현] implementer → 03 → code-critic → 03b(차단 0건) → 🧑 게이트② 승인·원장 기록
- [ ] 6. [검증] 6-a qa-verifier → 04_report_{n} · 6-b 실기 스모크(조건부) → 04b_smoke_{n}
- [ ] 7. [리팩토링] (선택) → re-green + 게이트② 재제시 판정
- [ ] 8. [기록] scribe → 옵시디언·devlog
- [ ] 9. 종료 — RESUME.md "완료" 마킹
```

**각 단계(1~8) 종료 시 공통**: 다음 단계로 넘어가기 전 RESUME.md를 현재 상태로 덮어쓴다 — 이 한 장이 `/clear` 후 새 세션의 유일한 무맥락 진입점이다. 갱신을 빠뜨리면 재개가 직전 단계를 다시 하거나 건너뛴다.

## 규모 분기 (경량 사이클)

단일 파일 수준 + 기존 테스트 유지 범위의 소규모 작업(오타·한 줄 버그픽스·문구 변경)은 [인지]·[설계]를 오케스트레이터가 직접 축약하고(서브 에이전트·인터뷰 생략, 브리프·01b 즉석 작성) **게이트①·②를 1회 통합 제시**로 병합한다.

**깎는 것은 절차뿐이다** — `00_gates.md` 기록 형식(해시·red 로그)과 **이해 체크의 두 축**은 그대로 유지한다. 통합 제시 시의 역질문 배분 규칙은 reference/gate-protocol.md "경량 사이클의 이해 체크"에 있다.

**[기록]은 생략 대상이 아니다 (2026-07-21 명문화).** 경량 사이클도 8단계를 돈다 — 개발로그와 리포 devlog를 남긴다. `20260719-trip161-preview`가 `종료: 완료`로 마킹됐는데 **개발로그·devlog 둘 다 없다.** 워크스페이스에 `00_gates.md`와 `RESUME.md`뿐이라 02/03 원문이 없고, **백필이 구조적으로 불가능하다** — 그 사이클은 영구 소실이다. 규모 분기가 축약하는 것은 [인지]·[설계]와 게이트 제시 횟수뿐이다.

## Phase 0: 컨텍스트 확인

**reference/resume.md를 읽고 그 절차대로 수행한다.** 요약하면: 사이클 ID 확정(브랜치에서 티켓키 도출) → `_workspace/` 상태로 초기실행·재개·부분재실행 분기 → RESUME.md와 00_gates.md 교차 확인 → **사용자에게 상태 요약 제시 후 진행**.

## 사이클 단계

### 1. [인지] — spec-analyst (서브)
`Agent(spec-analyst, model: "opus")` 호출, 대상 작업 설명 + 작업 공간 **절대 경로** 전달.
산출: `01_spec-analyst_brief.md`. 열린 질문이 있으면 다음 단계 전에 사용자에게 제시한다.

**화면·컴포넌트 비주얼이 이번 범위에 있으면 프롬프트에 `figma-screen-impl` 스킬 사용을 명시 지시한다** — 1~2단계(추출·토큰 스냅)로 화면 요구·토큰·에셋 목록이 브리프에 들어온다. 이 분기 판단은 오케 몫이다(에이전트 정의에도 규칙이 있지만, **태울지 말지를 정하는 건 여기다**). Figma 접근 실패는 폴백이 없다 — 리포에 화면 사본이 없으므로 열린 질문으로 올라온다.

### 2. [메모리] — 오케스트레이터 직접
`obsidian-second-brain` 조회 규칙대로. **최근 N건을 훑는 게 아니라 그래프를 탄다**:
1. **`<리포 루트>/frontend/docs/structure.md`** — 파일별 역할·무엇이 빈 스텁인지·"지금 작업하려면" 경고. 코드를 뒤지기 전에 이 지도를 먼저 본다. **리포 추적 파일이라 옵시디언 조회가 아니라 Read다.** (볼트 `구조/`는 2026-07-21 폐지 — 과거 4건은 이력으로만 남는다.)
2. **건드릴 파일 행의 "먼저 읽을 개념" → 개념 노트 본문 → 역링크 검색**(대괄호 두 개로 감싼 개념 이름). 그 개념을 다룬 과거 사이클·게이트·**미상환 부채**가 나온다 — 걸려 나온 부채가 이번 작업의 위험 지점이다.
3. 보완: 최근 개발로그·문제로그·**에러로그** 2~3개 + 리포 `frontend/docs/devlog/` 최근 파일.

요약을 이후 단계 프롬프트에 반영한다(읽고 버리면 메모리가 아니다). **스냅샷과 실제 리포가 어긋나면 리포가 옳다** — 어긋남을 기억해 뒀다가 [기록]에서 새 스냅샷에 반영한다.
**미상환 이해부채 1~2건을 사용자에게 재노출한다**(간격 반복) — 이번 작업과 관련된 항목 우선. 사용자가 "이제 이해했다"고 하면 상환 처리(status 갱신)를 [기록] 단계에 포함한다.

**파산 차단**: 미상환 부채가 **10건을 넘으면** 새 사이클 대신 정리 세션(폐기/통합/상환)을 먼저 제안한다 — 적립이 상환을 초과하는 장부는 학습 도구가 아니라 빚 독촉이 된다. 이 판정은 여기서 한다(게이트까지 미루면 이미 늦다).

### 3. [설계] — 오케스트레이터 직접 (맹점 훑기 → Ouroboros MCP)

#### 3-a. 맹점 훑기 🧑 — **채팅에 직접 제시한다**

구현 전에 사용자가 **못 볼 것**을 짚어 준다. 사용자는 자기가 모르는 것을 질문할 수 없으므로, 질문 목록 자체를 하네스가 만들어야 한다.

**문서에만 적으면 도달하지 않는다** — `20260721-trip162-real-oauth` 브리프에는 lazy-import 크래시 경고가 있었지만 사용자에게 전달되지 않았고, 그 개념은 결국 게이트에서 처음 만나 이해부채가 됐다. 이 단계의 산출물은 파일이 아니라 **채팅 메시지**다.

재료: 01 브리프 + [메모리]에서 걸린 개념·부채 + `docs/structure.md`의 "지금 작업하려면" 경고. 형식은 **3~5줄, 각 줄 "무엇 + 왜 물리나"**:

- 이 리포에서만 걸리는 함정 (일반론 금지 — "OAuth는 보안이 중요합니다" 류는 쓰지 마라)
- 시작 전에 알아둘 개념 이름 — **처음 등장하는 것 위주**
- 이번 범위에서 **못 하는 것** (자격증명·외부 등록·네이티브 리빌드 등 선행조건)

제시 후 사용자 응답을 받는다. "이건 안다/모른다"가 곧 3-b의 입력이고, **모른다고 한 개념은 게이트 노트 §1 "지금 배울 핵심 개념"의 1순위**가 된다.

#### 3-b. 인터뷰

0. **실행 조건 (2026-07-21 반전)**: 3-a에서 **사용자가 처음 만나는 개념이 하나라도 나오면 인터뷰를 실행한다.** 판정 근거는 `TripPilot/개념/`의 노트 존재 여부 + 미상환 부채 목록이다.
   - **구 조건("열린 질문 0건이면 생략")은 12사이클 내리 인터뷰를 0회로 만들었다.** 정본이 답하는 것은 *무엇을 만드나*고, 인터뷰가 캐야 하는 것은 *어떤 경우에 어떻게 동작하나*다 — 문서가 답했다는 사실은 생략 근거가 되지 못한다.
   - 생략은 **신규 개념이 0건일 때만**. 생략 시 01b에 "인터뷰 생략 — 신규 개념 0"을 명시한다.
1. `mcp__ouroboros__ouroboros_interview`로 브리프의 모호점을 인터뷰한다(세션 기반 — 위임 영수증이 반환되면 지시대로 처리). **한 번에 1~2개씩** 묻는다 — 목록으로 쏟으면 사용자가 대충 넘긴다.
2. 모호성 기준 도달 시 `ouroboros_generate_seed(session_id)`로 Seed를 생성한다. Seed는 인터뷰 대화에서 추출되므로 추적 코드가 빠질 수 있다 — 오케스트레이터가 브리프의 추적 코드(US/ADR/INV)를 병기해 보강한다.
3. 결과를 **`01b_ouroboros_seed.md`로 저장한다 — 폴백 포함 항상 생성**: Ouroboros 불가·모호성 게이트 미달 시 브리프의 AC 초안을 01b로 저장하고 "폴백"을 명시한다. (01b가 항상 존재해야 test-designer 입력 경로가 보장된다.)
4. 01b 요약을 사용자에게 제시한다(설계 방향 확인).

### 4. [테스트 작성] — 4-a 설계 → 4-b 작성 (2026-07-22 분할) → 🧑 게이트 ①

**분할 근거**: 사이클 실측(trip162-screens)에서 test-designer 집필의 절반이 전줄 주해였고, 코드 수준 판단은 명세로 옮길 수 있었다. 분할선은 에이전트가 아니라 **산출물 경계**를 지난다(2026-07-21 "모델 분할 기각"의 기각 사유가 가리킨 방향).

#### 4-a. 테스트 설계
`Agent(test-designer, model: "fable")`, 입력: 01 + 01b (절대 경로). **코드 작성 금지 — 명세만.**
산출: `02a_test-design_spec.md` — 파일 목록 · 케이스별 AAA 뼈대 · **단언 목록**(무엇을 어떤 매처로) · 신규 testID/prop 계약 · red/선제 green 판정+사유 · **★장치·함정 목록(강제 항목)** — 확장 타입 재대입·목 호이스팅 규칙·press 버블링·queryBy+루트존재 짝처럼 코드 수준에서 갈리는 판단 전부를 여기 박는다. ★가 비면 4-b가 판단을 떠안게 되므로 반려 사유다.

#### 4-b. 테스트 작성
`Agent(test-designer, model: "sonnet")`, 입력: 01 + 01b + **02a** (절대 경로).
산출: 실패 테스트(02a 명세의 번역 — 임의 판단 금지, 명세 공백 발견 시 임의로 메우지 말고 반환에 명시) + red 실행 확인 + `02_test-designer_map.md`(매핑 표 + 전줄 주해 — 02a의 "왜"가 주해 입력이다).

#### 게이트① 제시
**제시 전 reference/gate-protocol.md를 읽는다.** 전문(02의 "테스트 설계 설명" + 매핑 표 + 주해 달린 테스트 코드)은 게이트 노트에 수록하고, 채팅은 요약 형식(생략 불가 항목 포함) + 이해 체크 2개 → 승인 → 00_gates.md에 게이트① 기록(승인 발화 인용 + 포맷 후 파일 해시 + red 로그). **승인 전 구현 금지.**

- **반려 라우팅(오케 판단)**: 설계 탓(단언·계약·장치가 틀림) → 4-a 재호출 / 표현 탓(명세는 맞는데 코드가 어긋남) → 4-b만 재호출.
- **트립와이어**: 게이트① 반려·재제시가 1회라도 발생하면 **통합(구 방식: fable 단독이 설계+작성)** 으로 복귀하고 변경이력에 기록한다.

### 5. [구현] — implementer → code-critic → 🧑 게이트 ②

#### 5-a. 구현
`Agent(implementer, model: "sonnet")`. 승인된 테스트를 green으로. 산출: `03_implementer_notes.md`.

**화면 비주얼 구현이면 프롬프트에 `figma-screen-impl` 스킬을 명시 지시한다** — 3~5단계(에셋·RN 번역·스크린샷 대조). **testID 보존이 게이트① 테스트를 green으로 유지하는 계약**이라, 이 지시를 빠뜨리면 프레젠테이션만 바꾸려다 승인된 테스트를 깨뜨린다(= 게이트① 재제시).

#### 5-b. 적대적 리뷰 (2026-07-21 신설)
`Agent(code-critic, model: "opus")`. 산출: `03b_code-critic_findings.md`.

**게이트② 제시 전인 것이 요점이다** — 해시 동결 전이라 리뷰 지적으로 코드를 고쳐도 재제시를 유발하지 않는다. [검증]에 두면 리뷰가 붙는 순간 `PASS(재제시 필요)`가 상시화된다.

- 프롬프트에 **입력 순서를 명시 주입한다**: ① diff + 승인 테스트만 보고 지적 확정 → ② 그 다음 03을 열어 대조. 03을 먼저 주면 구현자 프레임에 갇힌다.
- **차단 0건이어야 5-c로 간다.** 차단이 있으면 5-a 수정 루프(implementer 재호출 → code-critic 재호출).
- 경고·참고는 고치지 않고 게이트② 요약의 **생략 불가 항목 ⑤**로 올려 사용자가 판단한다.
- **경량 사이클(규모 분기)에서는 생략한다** — 단, "리뷰 생략"을 게이트 요약에 명시한다. 조용히 빠지면 안 한 건지 0건인지 구분이 안 된다.

#### 5-c. 게이트② 제시
**제시 전 reference/gate-protocol.md를 읽는다.** 전문(03의 "변경 설명" + 전줄 주해 + §0 자리 지도)은 게이트 노트에 수록하고, 채팅은 요약 형식(생략 불가 ①~⑤) + 이해 체크 2개 → 승인 → 00_gates.md에 게이트② 기록(승인 발화 인용 + 포맷 후 파일 해시 — 테스트 인프라 파일 포함 변경 전체).

### 6. [검증] — qa-verifier (서브) → 실기 스모크 (오케, 조건부)

#### 6-a. jest 층
`Agent(qa-verifier, model: "sonnet")`, 입력: 변경 파일 목록 + 00_gates.md **절대 경로**.
산출: `04_qa-verifier_report_{n}_{PASS|FAIL}.md` — n은 사이클 내 검증 차수(채번 = 기존 최대 n+1), **덮어쓰기·삭제 금지**. 판정·차수·시각을 00_gates.md에도 append한다(카운터의 정본은 원장).
FAIL·실행 불가·PASS(해시 불일치)의 분기 처리는 reference/troubleshooting.md "검증 단계의 분기"를 따른다.

#### 6-b. 실기 스모크 🎯 (2026-07-21 신설)
**절차 정본은 `<리포 루트>/frontend/.claude/skills/verify-gates/SKILL.md`의 `## 실기 스모크`** — 발동 조건(diff 판정 5종)·명령·판정 4항목을 그대로 따른다. 산출: `04b_smoke_{n}_{PASS|FAIL|SKIP|실행불가}.md`.

**오케 직영인 이유**: 시뮬레이터·dev build·Metro 기동 여부에 따라 실패 원인이 "코드가 틀림"인지 "환경이 안 됨"인지 갈리는데, 그 구분에는 사용자와의 대화가 필요하다("시뮬레이터 띄워줄래?"). `20260720-trip161-mock-seam`에서 사용자가 **"qa-verifier는 jest만 돌린다"**로 확정한 바 있고, 이 배치는 그 결정을 따른다.

**이 단계가 없던 12사이클 동안, 발견된 유일한 실제 동작 결함이 여기서 나왔다** — jest 전부 통과 후 시뮬레이터 레드박스. 발동 조건에 걸리면 건너뛰지 마라.

### 7. [리팩토링] — implementer (서브, 선택)
동작 변경 없는 구조 개선. **완료 후 qa-verifier 재실행 필수(re-green) + 게이트② 재제시 규칙 적용.** 개선 여지가 없으면 건너뛴다.

### 8. [기록] — scribe (서브)
`Agent(scribe, model: "sonnet")`, 입력: `_workspace/{cycle-id}/` 절대 경로 + 문제/에러 발생 내역 + **게이트에서 수집한 이해부채 목록(상환된 항목 포함)** + **게이트 노트 경로·차수 목록**(개발로그가 링크 허브가 되기 위한 입력). 최신 04 리포트는 경로로 전달하지 않는다 — scribe가 `04_qa-verifier_report_*`를 직접 나열해 최대 n을 스스로 확정한다(선별 전달 오염 방지).
게이트 통과 현황은 scribe가 **04 리포트의 표를 원문 복사**한다 — 오케스트레이터의 요약 재서술로 대체 금지(심판받는 루프의 자기서술이 기록을 오염시킨다). 이해부채는 `TripPilot/이해부채/`에 건별 기록하고, 상환된 항목은 status를 갱신한다.

### 9. 사이클 종료
결과 요약(완료 내용, 게이트 현황 — 00_gates.md·최신 04 리포트 기준, 기록 위치) + 다음 사이클 후보 제시. **RESUME.md의 종료 필드를 "완료"로 마킹**한다(더는 자동 재개 대상이 아님). 하네스 개선 피드백 기회를 제공한다.

## 금지 사항

- 어떤 단계에서도 `aidlc/` 이하 쓰기 금지 — Edit/Write 도구는 settings deny로 기계 강제, **Bash 경유 쓰기는 규칙 준수 + qa-verifier의 `git status <리포 루트>/aidlc` 검사로 탐지**. atlassian MCP 사용 금지.
- 원장(`00_gates.md`) 기록 없는 게이트를 통과로 취급하지 않는다 — 산출물 파일 존재는 "작성됨"이지 "승인됨"이 아니다.
- 에이전트 실패 시 침묵 진행 금지(reference/troubleshooting.md).
- **미완료 사이클을 두고 하네스 파일(`frontend/.claude/**`)을 고치지 않는다 (2026-07-21 신설).** 고치면 작업트리에 섞여 qa-verifier의 **미신고 변경 검사에 FAIL로 걸린다** — `20260718-trip161-login` #1 FAIL이 정확히 이것이었고, 리포트가 *"이 FAIL은 implementer 코드 수정 대상이 아니다 — 하네스 파일의 미신고 변경"*이라고 라우팅을 따로 적어야 했다.
  - 원칙은 **미완료 사이클이 없을 때 고치는 것**이다.
  - 불가피하면 **즉시 별도 커밋으로 분리**해 작업트리에서 뺀다(qa는 `git diff --name-only`로 HEAD와 대조하므로 커밋되면 변경 집합에서 사라진다).
  - 사이클 도중 [인지]가 밴드 맵 드리프트를 발견해도 **고치지 않고 보고만 하는 규칙**(spec-perception)이 같은 이유에서 나왔다.
