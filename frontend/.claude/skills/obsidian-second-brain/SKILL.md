---
name: obsidian-second-brain
description: "TripPilot 제2의 뇌(옵시디언) 기록·조회 규칙. 대화 로그, 개발 일지, 개발 로그, 문제 로그(에러·게이트 실패 포함)의 기록/조회, '기록해', '일지 써', '어제 뭐 했지', '지난 사이클 확인', '옵시디언에 남겨' 요청 시 반드시 사용하라. 개발 사이클의 [메모리]·[기록] 단계 전담 규칙."
---

# Obsidian Second Brain

볼트 워크스페이스: `TripPilot/` (`🗂️ 전체 Index.md`의 ✈️ TripPilot 섹션에서 링크됨)

## 구조

**볼트는 독자로 갈린다.** 사람은 `개념/`과 `게이트/`만 읽으면 되고, 나머지는 AI가 읽어 필요분만 꺼내 보인다. 이 구분을 지키는 것이 사용자의 읽기 부담을 일정하게 유지하는 장치다 — AI용 문서를 사람이 읽어야 하는 상태가 되면 구분이 무너진 것이다.

| 위치 | 독자 | 용도 | 파일명 규칙 |
|---|---|---|---|
| `TripPilot/개념/` | 🧑 **사람** | 영구 개념 설명(evergreen, 학습 자료 본문) | `개념명.md` (날짜 접두 없음) |
| `TripPilot/게이트/` | 🧑 **사람** | 게이트 4계층(§0 자리 지도·§1 핵심 개념·§2 파일별 요약·§3 **막힌 줄만**) — 열람·복습용. 전줄 주해는 여기 담지 않는다(대화 소관) | `YYYY-MM-DD {cycle-id} 게이트{①\|②}-{n}.md` |
| `TripPilot/개발로그/` | 🤖 AI | 사이클 단위 상세 기록 | `YYYY-MM-DD cycle-id.md` |
| `TripPilot/개발일지/` | 🤖 AI | 하루 단위 요약 — **하루 3~5줄**(사이클 링크 + 사이클 밖 작업). 자동 조회 경로 없음 | `YYYY-MM-DD.md` |
| `TripPilot/문제로그/` | 🤖 AI | 막힌 문제 + 해결 과정 — **에러 메시지·검증 게이트 실패도 여기다**(아래 "문제로그 진입 조건") | `YYYY-MM-DD 문제요약.md` |
| `TripPilot/이해부채/` | 🤖 AI | 승인했지만 이해 못 한 것 + 상환 추적 (장부) — [메모리]에서 AI가 사람에게 꺼내 보인다 | `YYYY-MM-DD 주제.md` |
| `TripPilot/대화로그/` | 🤖 AI | 세션별 대화 요약 — **사이클 산출물 아님, 사용자 요청 시 수동 기록 전용** | `YYYY-MM-DD 주제.md` |
| `TripPilot/하네스 변경이력.md` | 🤖 AI | 하네스 변경 원장 (append-only 표) | 고정 |
| `TripPilot/✈️ TripPilot Index.md` | 공용 | MOC — 입구. 상단에 사람이 읽을 것(개념·게이트)을 먼저 둔다 | 고정 |
| `TripPilot/온톨로지.md` | 공용 | **클래스·관계 속성 규약 정본** — 새 속성을 만들기 전에 여기부터 고친다 | 고정 |
| `TripPilot/_templates/` | 🤖 AI | 각 로그 템플릿 | `{로그종류} 템플릿.md` |
| `TripPilot/_bases/` | 🧑 사람 | Bases 뷰(Obsidian UI 전용) — **MCP로는 실행 안 된다**, YAML 원문만 돌아온다 | `{주제}.base` |

**폐지된 폴더 — 다시 만들지 마라**(폴더·템플릿 모두 삭제됨, 상세는 하네스 변경이력): `구조/`(정본은 리포 `frontend/docs/structure.md`, 아래 "구조 지도") · `에러로그/`(→ `문제로그/`로 통합). 이 가드는 장식이 아니다 — 폐지 후 재생성이 **실제로 한 번 샜다**(그래서 표에서 뺐어도 이 한 줄은 남긴다). *유지 판정: 각 폴더가 6사이클 재생성 시도 0건이면 그 항목을 이 줄에서 뗀다.*

## 기록 규칙

- `_templates/`의 해당 템플릿 섹션 구조를 그대로 따른다 — 섹션 임의 변경 금지(구조가 흔들리면 다음 사이클의 조회가 깨진다). 예외 하나: 문제로그의 `## 에러 메시지 (원문)`은 **선택 섹션**이라, 메시지가 없으면 절을 통째로 지운다(빈 코드블록을 남기지 마라).
- 관련 노트를 [[링크]]로 연결한다: 개발로그 ↔ 문제로그 ↔ 개발일지. 링크가 제2의 뇌를 그래프로 만든다.
- **문제로그 진입 조건**: 노트를 만드는 기준은 하나다 —

  > **다음 사이클의 나에게 조치를 바꿀 정보가 있는가?**

  없으면 개발로그 각주 한 줄로 끝낸다. **`## 교훈`이 원리적으로 채워질 수 없는 사건은 로그가 아니라 사건 기록이다** — API 세션 한도 중단, 일시적 권한 부재 같은 것. 이런 건은 다음 사이클의 입력이 되지 못한다(실측, 상세는 하네스 변경이력).
- **문제로그의 `## 관련`에 `개념: [[...]]`은 필수다.** 조회 규칙이 문제로그를 **역링크로만** 잡는데, 개념 링크가 없는 노트는 **원리적으로 도달 불가**다(실측 다수, 상세는 하네스 변경이력). 코드에 안 매이는 문제(하네스 규약·검증 방법론 등)면 그 성격의 개념 노트를 걸거나 만든다. *유지 판정: 6사이클 관찰 — 개념 링크 없는 문제로그가 계속 생기면 규칙이 아니라 템플릿 강제(생성 시 차단)로 올린다.*
- **문제로그의 `## 교훈`에는 "하네스 규칙 후보인가" 판정을 명시한다 — 이 판정 형식의 정본이 여기다.** 문제로그가 실제로 값을 낸 경로는 [메모리] 조회가 아니라 **하네스 규칙으로 승화되는 것**이었는데(실측 3건, 상세는 하네스 변경이력), 그 승화는 누가 알아채야만 일어나는 우연이었다. 후보면 개발로그에도 올려 사이클 종료 보고에 실린다. *유지 판정: 6사이클 관찰 — 후보 판정이 실제 규칙 개정으로 이어진 건수 0이면 이 줄을 떼고 문제로그를 순수 검색 자산으로 격하한다.*
- **온톨로지 속성**([[온톨로지]] 정본). 프론트매터에 딱 둘만 쓴다 — 나머지 관계는 백링크가 이미 하므로 중복 저장 금지:
  - **개념 노트를 만들거나 보강하면 `설명하는코드`를 채운다** — 그 개념이 설명하는 리포 파일들(`frontend/` 기준 상대경로 문자열 리스트). 이것이 `[메모리]`의 코드→개념 진입점 **정본**이다. 실재하지 않는 경로를 쓰면 역인덱스가 거짓이 된다 — 경로는 반드시 리포에서 확인하고 쓴다. 코드에 매이지 않는 개념(테스트 규율·하네스 규약 등)은 `[]`로 비운다.
  - **이해부채를 등재하면 `학습자료`를 채운다** — 그 부채를 갚을 개념 노트 이름 리스트. 부채→개념→코드 2단 조인이 여기서 성립한다.
  - 새 속성이 필요해 보이면 노트에서 즉흥적으로 만들지 말고 [[온톨로지]]를 먼저 고친다. 규약에 없는 속성은 조회가 못 본다.
- 학습 자료 성격의 내용은 기존 스터디 노트(`개발 공부/TripPilot/`)와 교차 링크한다.
- **이해부채 ↔ 개념 노트**: 이해부채를 등재하면 그 개념의 실제 설명을 담은 **개념 노트**(`개념/{개념명}.md`, 날짜 없는 영구 노트)를 생성하거나 기존 노트에 보강하고, 부채 노트 `## 관련`에서 `[[개념명]]`으로 링크한다. **부채 노트 = 장부(상환 기준·status), 개념 노트 = 학습 자료(설명 본문).** 부채 노트만 있으면 재방문해도 "못 이해함"만 재확인하고 정작 배울 설명이 없다 — 개념 노트가 그 공백을 메운다. 같은 개념의 다음 부채도 **같은 개념 노트를 공유**한다(중복 생성 금지, 설명만 보강). 개념 노트는 상환돼도 남는다(영구).
- **상환 기준에는 코드 축을 반드시 포함한다**: 개념 설명만으로 상환 조건을 쓰면 **위키백과로 갚을 수 있는 장부**가 된다(실측 사례는 하네스 변경이력). 마지막 항으로 이걸 넣는다:

  > **이 개념이 우리 코드 어느 파일에 어떻게 나타나는지 짚을 수 있으면**

  게이트 역질문이 코드에 앵커돼 있어도 부채를 개념으로 일반화해 등재하면 코드 이해는 장부에서 사라진다 — 이 한 줄이 그 누수를 막는다.
- 도구: `mcp__obsidian__obsidian_append_content`(새 노트 생성/추가), `obsidian_patch_content`(기존 노트의 특정 섹션 수정).
- 새 노트 생성 후 `✈️ TripPilot Index.md`의 해당 로그 섹션에 링크를 추가한다(섹션당 최근 5개 유지, 오래된 것은 제거해도 노트는 남는다). **예외: `게이트/` 노트는 Index에 개별 링크하지 않는다** — 사이클당 여러 개라 도배가 된다. 해당 사이클 개발로그가 게이트 노트들의 [[링크]] 허브다.
- **개발일지는 하루 3~5줄로 끝낸다.** 섹션은 둘뿐 — `진행한 사이클`(개발로그 [[링크]] + 각 한 줄)과 `사이클 밖 작업`(코드 사이클 외 작업: 문서 편집·환경 구축·하네스 변경 등, 한 줄 + 원장 [[링크]]). 사이클 외 작업은 **반드시 남긴다** — 기록 없는 작업일을 만들지 않으며, 개발로그는 사이클 키라 이걸 받을 수 없다(개발일지만 담을 수 있는 유일한 것).
  - **사이클 상세를 옮겨 적지 마라** — 결정 이유·예상과 달랐던 것·다음에 할 일은 전부 개발로그 소관이다. 개발일지에 `오늘 목표`·`배운 것`·`내일 할 일` 절을 만들지 않는다(개발로그와 중복).
  - 이유: **개발일지는 자동 조회 경로가 없다.** [메모리] 조회 규칙 어디에도 개발일지를 읽으라는 항목이 없고 사용자도 평소 읽지 않는다 — `"어제 뭐 했지"` 류의 사용자 질의에만 쓰인다. 그 용도에 3~5줄이면 충분하고, 더 쓰면 아무도 안 읽는 글만 쌓인다.
- **하네스 변경 시** `하네스 변경이력.md`의 표에 행을 추가한다(append-only, 날짜·변경 내용·대상·사유) — frontend/CLAUDE.md에는 이 원장을 가리키는 포인터만 있다.
- **구조 지도**([기록] 단계) — 정본은 **리포의 `frontend/docs/structure.md`**다.

  **왜 옮겼나**: 볼트 스냅샷은 매 사이클 전문을 복사해 오는 비용 때문에 델타 메모로 퇴화했다(실측 추이는 하네스 변경이력). 리포에 두면 복사 왕복이 사라지고, **갱신 누락이 사이클 diff에 드러나** qa-verifier의 변경 집합 검사에 자동으로 걸린다.

  - **기계 몫과 사람 몫을 가른다.** 파일 목록·export 심볼은 스크립트가 만든다:
    ```bash
    node <리포 루트>/frontend/.claude/skills/trippilot-dev-cycle/scripts/structure-index.cjs
    ```
    scribe가 쓰는 것은 기계가 못 주는 것뿐 — **용도 한 줄 · 스텁 여부**(경고는 `.claude/rules/repo-traps.md` 소관).
  - **이번 사이클이 만진 행만** 고친다(전면 재작성 금지). 파일 추가·삭제·이동이나 스텁을 채운 경우가 대상이다.
  - **경로는 리포 상대 전체 경로를 백틱으로** 적는다(`src/features/auth/lib/makeAuthorize.ts`) — 대조 검사가 이 형태만 인식한다.
  - **반환 전 대조를 돌린다**: `structure-index.cjs --check` → *파일은 있는데 행이 없다*(새 파일 누락) · *행은 있는데 파일이 없다*(삭제·이동 미반영 — 이게 가장 흔한 실패였다) · ***개념 유령***(개념 노트의 `설명하는코드`가 없는 파일을 가리킴). 불일치면 고치고 다시 돌린다.
    - **`개념 유령`의 수정 대상은 `structure.md`가 아니라 볼트 개념 노트다.** 죽은 경로를 살아 있는 후속 파일로 바꾸거나 목록에서 뺀다(전부 빠지면 `[]` — 코드에 안 매이는 개념은 그게 정상이다). 손으로 채우는 지도는 반드시 낡는다는 것이 구조 지도에서 이미 확인됐고, 온톨로지 속성도 같은 병에 걸린다 — 다만 이제 같은 기계가 잡는다.
  - **가장 중요한 항목은 여전히 "무엇이 빈 스텁인가"**다. 디렉토리 존재를 구현으로 오인하는 것이 이 문서가 막아야 할 1순위 오류이고, 기계가 못 갈라 주는 유일한 지도 정보다.
  - **개념 링크는 넣지 않는다** — 리포 마크다운에서 `[[ ]]`는 죽은 링크이고, 코드→개념 진입점의 정본은 개념 노트의 `설명하는코드` 속성이다(경로 문자열을 `obsidian_simple_search`에 넣으면 잡힌다). 사본을 두 곳에 두면 또 갈라진다.
  - 구조 변화가 없는 사이클이면 건드리지 않는다.

## 조회 규칙 ([메모리] 단계)

- **`frontend/docs/structure.md`를 먼저 읽는다 — 단 통독하지 않는다.** 매번 읽는 것은 **「한눈에」**. 층별 파일 표는 `.claude/rules/layer-*.md`가 폴더 만질 때 자동 로드한다(정본은 사이클 SKILL [메모리] 1번; 경고는 `.claude/rules/repo-traps.md`가 무조건 로드). **옵시디언이 아니라 리포 파일이므로 Read로 연다.** 실제와 어긋나면 리포가 옳다 — 다만 `--check`(DOC + `.claude/rules/layer-*.md`) 대조가 붙어 있어 조용히 어긋나지는 않는다(아래 "구조 지도").
- **미해결 부채·후속 티켓은 `structure.md`가 아니라 여기서 찾는다**(2026-07-31 정비). 경고 절에 쌓이던 부채 6건을 문제로그로 옮겼고, 그 절은 이제 "부정 사실·의도·기계 강제 없는 계약"만 담는다. **프론트엔드의 안티패턴 장부도 문제로그다** — 리포 `docs/conventions/anti-patterns.md`는 백엔드 소관이고 프론트 하네스는 그 문서를 읽지 않는다(참조 0건 실측). 규칙 후보 판정은 아래 문제로그 `## 교훈` 규칙 그대로.
- **그다음은 시간순이 아니라 그래프로 판다.** 볼트는 이미 링크 그래프다(개념 28노트·110링크) — "최근 N건"은 이번 작업과 무관한 것을 읽고 정작 관련된 과거를 놓친다.
  1. **건드릴 파일 경로를 그대로 `obsidian_simple_search`에 넣는다** — 예 `src/features/auth/hooks/useBootstrapGate.ts`. `설명하는코드`에 그 경로를 선언한 개념 노트가 잡힌다. **이것이 코드→개념 진입점의 유일한 정본이다** — 구조 지도(`docs/structure.md`)는 개념 링크를 두지 않으므로 사본 분기가 없다.
  2. 그 **개념 노트 본문**을 읽는다 — 사용자가 이 개념을 어느 수준까지 설명받았는지가 거기 있다(존재 여부만 보면 판단이 얕다).
  3. **역링크로 이력을 판다**: `obsidian_simple_search`에 대괄호 두 개로 감싼 개념 이름을 넣으면 그 개념을 참조하는 노트가 전부 나온다 — 개발로그·게이트 노트·이해부채. MCP에 백링크 전용 도구는 없지만 이 검색이 같은 일을 한다.
  4. 걸려 나온 **미상환 이해부채가 이번 작업의 위험 지점**이다. 우선 재노출 대상으로 삼는다. (부채의 `학습자료` 속성이 곧장 읽을 개념 노트를 가리킨다.)
  - **`.base` 파일을 조회 수단으로 쓰지 마라** — MCP는 Bases를 실행하지 못하고 YAML 원문만 돌려준다. 결과 표를 봤다고 착각하기 쉬운 지점이다.
- **그래프로 잡히지 않는 축은 하나뿐이다 — 직전 사이클 개발로그 1건(인수인계).** `다음에 이어서 할 일`·`⚠️ 실기 미검증`·미커밋 상태는 코드가 아니라 **시간**에 매달려 있어 역링크로는 원리적으로 안 걸린다.
- **문제로그는 두 경로로 잡는다 — 어느 하나로는 새는 것이 실측됐다.**
  1. **역링크** — 개념 노트를 경유해 위 3번에서 나온다. 단 `개념:` 링크가 있는 노트만이다(그래서 위 기록 규칙에서 필수화했다).
  2. **작업 키워드 검색** — 이번 작업이 쓰는 도구·API·파일 이름(`Metro`·`jest.mock`·`expo-auth-session`·`eslint` 등)으로 검색한다. 확인 항목의 "반복되는 에러 패턴"은 **최근성이 아니라 반복**이 관건이라, 최근 N건 읽기로는 원리적으로 안 잡힌다. 시점과 무관하게 같은 패턴이 모인다.
  - 구 규칙은 "문제로그는 역링크로 걸리므로 따로 훑지 않는다"였는데, **걸리지 않았다** — 01브리프가 문제로그를 인용한 실적이 0이었다(실측, 상세는 하네스 변경이력). 안 훑는 쪽만 지켜졌다.
- 확인할 것: 미해결 문제, 이번 작업과 충돌하는 과거 결정, 반복되는 에러 패턴.
- **이해부채 재노출(간격 반복)**: `이해부채/`에서 status가 "미상환"인 노트 중 이번 작업과 관련된 것 1~2건을 사용자에게 다시 보여준다. 사용자가 이해를 확인하면 status를 "상환"으로 갱신하고 상환 기록 섹션에 한 줄 남긴다 — 재노출 없는 부채 장부는 장부가 아니다.
- 조회 결과 요약을 이후 단계(설계·테스트·구현) 프롬프트에 포함시킨다 — 읽고 버리면 메모리가 아니다.

## 기록 내용 우선순위

1. 왜 그렇게 결정했는가(검토한 대안과 기각 이유)
2. 예상과 달랐던 것
3. 다음에 이어서 할 일
4. 무엇을 했는가 — 마지막(코드·git이 이미 말해주는 것은 짧게)
