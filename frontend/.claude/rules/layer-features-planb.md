---
paths:
  - "src/features/planb/**"
---
# `src/features/planb/` — 재계획(Plan-B) 도메인 (TRIP-439 재신설, TRIP-440으로 i12·i14 추가, TRIP-441로 확정/취소 래퍼·i19 추가, TRIP-442로 origin 조립·판정 추가, TRIP-443으로 i15·i22 수동 편집 화면 추가, TRIP-561로 i08 트리거 조회·억제 추가, TRIP-562로 i09 감시 항목 열람 화면 추가, TRIP-563으로 i13 재계획안·i16 대안없음 골격 추가, TRIP-748로 트리거 표면을 배너 알약+영향 배지로 재배치, TRIP-749로 i09 감시 목록 화면을 i03 위험 상세 시트로 흡수·파일째 삭제)

TRIP-173에서 빈 배럴째 디렉토리가 삭제됐다가, 선행 TRIP-438(planb-triggers, 감지·열람)이 미완료로 끝나 재신설을 못 한 채로 남아 있었다 — **TRIP-439가 이 사이클에서 처음 다시 만든다.** 선행 미완의 여파로 triggers 소비가 없어, 이번 슬라이스는 **수동 진입 경로(`triggerId=null`)** 만 짓는다(감지 배너·`[끄기]`는 `trigger?` prop이 있을 때만 조건부 렌더, 자동 진입 배선은 후속 티켓). **TRIP-561로 이 공백이 닫혔다** — `useActiveTriggers`(조회)·`useSuppressTrigger`(끄기)가 신설돼 `pages/live-itinerary`가 실제 발화 중인 트리거를 i08 칩·i01 배너로 표시하고 억제한다(아래 신규 3행). **TRIP-748(i02 재배치)로 `useSuppressTrigger.ts`는 소비처 0이 되어 파일째 삭제됐다** — × 끄기 표면이 로컬 숨김(D3, 서버 미호출)으로 바뀌어 서버 억제(BR-U4-15)를 만드는 경로가 앱에 없다(관측, 새 티켓 후보 — 개발로그 참고).

바텀시트(i10)의 `@gorhom/bottom-sheet` 목이 열림/닫힘·딤을 원리적으로 못 보므로(repo-traps 바텀시트 절), 이 도메인은 **관측 가능한 계약**(제출 시 이 body로 POST가 나간다 / 이 라우트로 push한다)으로 심판을 세운다 — 그래서 순수 조립·순수 폼 상태·순수 시트를 배선(pages 층)과 엄격히 분리했다.

| 파일 | 역할 |
|---|---|
| `src/features/planb/model/replanScope.ts` | **TRIP-750으로 사유·방향·`ReplanChoice`를 `config/replanChoices.ts`로 이동** — 이제 범위 카탈로그만 남는다: `REPLAN_SCOPES`(`PARTIAL_SLOTS`·`FULL_DAY` 정확히 2종)·`DEFAULT_REPLAN_SCOPE`, 타입 `ReplanScopeOption`. 와이어값은 안정 코드(ASCII key), 한글은 표시용 라벨. `@jest-environment node` 구조가드(`planbScopeStructure.test.ts`)가 소스를 직접 스캔하므로 **RN을 런타임 import하지 않는다**(`StartReplanRequestScope`는 `import type`만) |
| `src/features/planb/config/replanChoices.ts` | **신규(TRIP-750)** — `replanScope.ts`에서 이동한 사유·방향 카탈로그(순수 데이터, node-safe). `REPLAN_REASONS`(6종)·`REPLAN_DIRECTIVES`(11종 — `EARLIER`·`END_NEAR_STAY`·`KEEP_BUDGET`·`KEEP_DINNER`·`AVOID_OUTDOOR` 신규, `NIGHT_VIEW` 삭제, `LESS_MOVE` 라벨 "이동 짧게"로 개정)·타입 `ReplanChoice`·`TRIGGER_REASON_KEY: Record<WatchKind, string>`(트리거 kind → 사유 key 매핑, kind가 늘면 tsc가 누락을 잡는다). **AVOID_OUTDOOR는 ai 지시 사전에 없어 `unknown_directives`로 조용히 무시된다**(D5, 새 티켓 후보 — 옵시디언 문제로그 참고). EARLIER·END_NEAR_STAY·KEEP_BUDGET·KEEP_DINNER 4종도 ai 사전엔 있으나 SOLVER 미배선이라 현재 무효과(같은 문제로그) |
| `src/features/planb/model/replanRequest.ts` | 순수 조립 함수 `buildStartReplanRequest`(+ 타입 `ReplanFormValues`) — 폼 값 4개를 서버 봉투(`StartReplanRequest`, 정확히 7키)로 바꾼다. 위치 미입력이어도 **`originKind: null`을 반드시 명시**한다(생략=undefined가 아니라 값 `null` — codegen `required:[scope,originKind]`, nullable). **TRIP-442로 두 번째 인자 `origin?: ReplanOrigin`을 additive 확장** — `if (!origin) return base`로 미제공 시 기존 7키와 바이트 동일(originLat/originLng **키 자체가 없음**, optional은 값 null이 아니라 키 부재), 제공 시 `{...base, originLat, originLng}`로 9키(개념 [[후방호환 옵셔널 파라미터 (additive prop)]]) |
| `src/features/planb/model/replanOrigin.ts` | **신규(TRIP-442), TRIP-979 슬라이스 A로 `buildGpsOrigin` 추가** — 순수 조립·판정 3함수. `buildManualOrigin(coords:{lat,lng})`가 좌표를 `{originKind:'MANUAL', originLat, originLng}` 정확히 3키로 옮긴다(지도 롱프레스 이후 소비, TRIP-979 슬라이스 B 몫 — 화면이 아직 호출 안 함). `buildGpsOrigin(coords)`는 같은 모양으로 `{originKind:'GPS', originLat, originLng}`를 만든다(`useReplanGpsOrigin`이 소비). `isEstimatedOrigin(originKind)`는 `originKind !== 'GPS'` 한 줄 — GPS만 실측, 나머지(MANUAL·LAST_VISIT·STAY_ANCHOR·`null`)는 전부 "추정"(세션 되읽기 없이 로컬 즉시 도출). 파일 헤더의 "test-designer 스텁 — red 유도용" 주석은 TRIP-442 때부터 낡은 문구(두 함수는 이미 구현됨, 03 자기신고 6) |
| `src/features/planb/model/useReplanGpsOrigin.ts` | **신규(TRIP-979 슬라이스 A)** — `useReplanGpsOrigin()`: `useGetMeLocationConsent()`로 앱 위치 동의(`legalConsent===true`)를 확인하고, ON이면 `readDevicePosition()`(shared/location)을 불러 좌표를 `buildGpsOrigin`으로 조립한다. 동의 OFF·미도착·오류는 전부 `undefined`(위치 API 호출 0회). **`shared/location/useLocationConsent`(`consentOn`)는 의도적으로 안 쓴다** — 그 훅은 마운트 시 권한 조회 + PATCH 미러 보고 부수효과가 있어 이 자리엔 안 맞는다. 훅이 렌더마다 새 함수를 돌려주므로(useCallback 미사용) 페이지의 최신 렌더를 항상 부른다(낡은 클로저 방지, 03 H5) |
| `src/features/planb/model/replanFormStore.ts` | Zustand 폼 스토어 `useReplanFormStore`(+ `ReplanFormState`) — `scope`(단일)·`reasons`(Set)·`directives`(Set)·`freeText`·`sheetOpen` 소유. RHF는 리포 프로덕션 사용 0(`stayRegisterStructure.test.ts` 선례)이라 미도입, 페이지 로컬 `useState`(더 게으른 대안)는 시트↔페이지 상태 공유 때문에 기각 |
| `src/features/planb/model/useStartReplan.ts` | `usePostTripsTripIdReplanSessions`(codegen)를 감싸는 얇은 래퍼 `useStartReplan` — 통합 테스트가 이 심볼을 목 seam으로 잠그므로 페이지가 codegen 훅을 직접 부르는 대안은 기각. 무효화 로직 없음(세션 열기는 무효화할 로컬 목록이 없다) |
| `src/features/planb/ui/ReplanRequestSheet.tsx` | **TRIP-750으로 재작성** — 순수 시트(`ReplanRequestSheet`·`ReplanRequestSheetProps`, `ReplanDetectionBanner`는 삭제). Figma 섹션 순서(왜 바꾸나요→범위→방법→직접 말하기→[AI가 다시 짜기])로 재배치. 감지 트리거가 있으면 선두에 `ReplanDetectedChip`(⚠ 라벨·대상, 선택 시드)을 얹고 매핑되는 정적 칩(날씨/날씨·지연·휴무)은 숨긴다(숨긴 선택값 자체는 스토어에 남음 — 03b 경고-2, 제출 필터는 배선판 몫). 사유·방향 칩 카탈로그는 `config/replanChoices.ts`에서 가져온다(`replanScope.ts`에서 이동). 스크림 `backdrop`+`enablePanDownToClose`, 자유텍스트는 플레인 RN `TextInput`(바텀시트 목이 `BottomSheetTextInput` 미export). ~~`[끄기]`~~·~~부제~~·~~각주~~·~~[직접 고르기]~~ 삭제(D3). **20260923 경량 사이클(TRIP-751 계열, 티켓 없음)**: `errorText?: string | null` prop 추가 — CTA(`[AI가 다시 짜기]`) 바로 위 조건부 `Text` 한 줄(완전일치, 접두 아이콘 없음). 여행 기간 밖에서 서버 409를 받으면 배선판(`PlanbRequestPage`)이 이 prop으로 실패 안내를 넘긴다(아래 `planb-request` 행 참고) — 그 전엔 이 경로 자체가 도달 불가(허브 막힘 화면이 가려 줬다) |
| ~~features/planb/ui/OutOfScopeNotice.tsx~~ + 형제 테스트 | **TRIP-750으로 파일째 삭제** — `outOfScope` 표면 자체를 걷어냄(D3, 서버 `OUT_OF_SCOPE` 상태 없음이 그대로 원인이었다). 소비처였던 시트 분기도 함께 제거 |
| `src/features/planb/model/replanState.ts` | **신규(TRIP-440)** — 순수 판별 유니온 `resolveReplanState`. `ReplanSessionStatus` 7값(`COLLECTING`·`SOLVING`·`DRAFT`·`APPLIED`·`CANCELED`·`FAILED`·`NO_SOLUTION`)을 5종 화면 `kind`(`solving`·`draft`·`noSolution`·`failed`·`closed`)로 exhaustive switch 폴드(`COLLECTING`·`SOLVING`→solving, `APPLIED`·`CANCELED`→closed) — default 없이 tsc가 누락 case를 강제. 개념: [[상태를 좁혀 접는 판정 — resolveReplanState의 폴드]] |
| `src/features/planb/model/slackTime.ts` | **신규(TRIP-440)** — 순수함수 `slackTime`(PBT-U4-F2 차단 게이트 대상). 두 확정 시각(`HH:mm[:ss]`)의 차를 `여유 N시간 M분`/`여유 없음`으로 포맷 — wall-clock(`new Date`/`Date.now`) 미사용, `toMin`이 `split(':')`로 쪼개 분 환산. **model이라 `executionDurationStructure`·`liveTimeStructure` 두 구조가드 밖**이라 PBT-U4-F2(되파싱 오라클)가 유일 심판(repo-traps 참고). 개념: [[두 확정 시각의 차 — 여유(slack) 계산]] |
| `src/features/planb/model/replanFromInstant.ts` | **신규(TRIP-752)** — 순수함수 `readFromInstant(fromInstant: string)`. 서버 ISO 순간에 고정 `KST_OFFSET_MS`(+9h)를 더한 뒤 **UTC 필드**(`toISOString`·`getUTCHours`)로 읽어 `{date, hour}`(여행지 KST)를 만든다 — 기기 로컬 시간대(`getHours`류)를 쓰면 이 저장소 기계(KST)에선 green, UTC로 도는 CI에서만 red가 된다(G7 정규식이 흔한 getter 5종만 조기경보, 진짜 그물은 CI 자체). 잘못된 문자열은 `toISOString()`이 `RangeError`로 throw(계약 위반 시 크래시, 침묵 실패 아님). `pages/planb-draft/ui/PlanbSolvingPage.tsx`가 유일 소비처. 개념: [[순간(instant)을 여행지 시각으로 — readFromInstant의 UTC 트릭]] |
| `src/features/planb/model/useReplanSession.ts` | **신규(TRIP-440)** — `useGetTripsTripIdReplanSessionsSessionId`(codegen) 얇은 래퍼. `status`가 COLLECTING·SOLVING일 때만 2000ms `refetchInterval`(함수형, DRAFT 등 도착 후 자동 정지) |
| `src/features/planb/model/useSlotCandidates.ts` | **신규(TRIP-440)** — `usePostTripsTripIdItinerarySlotCandidates`(codegen) passthrough 래퍼. **이번 사이클 소비처 0**(SlotCandidateSheet를 배선할 컨테이너는 draft 계약 갭으로 아직 없음) — 시드 D6가 명시해 미리 만든 스텁, YAGNI 판단은 게이트② 검토 몫(ponytail lite 제안, 미채택) |
| `src/features/planb/ui/PlanbGlyphs.tsx` | **신규(TRIP-440)** — 체크리스트 done/active/waiting 3종 인라인 SVG 글리프(로컬 복제 관례, 공용 글리프 아님). **TRIP-752로 이 3종 + `MUTED_SOFT` 상수 삭제** — 유일 소비처였던 `ReplanSolvingScreen.tsx`가 같은 사이클에 파일째 삭제돼 고아가 됐다(아래 행 참고) |
| ~~features/planb/ui/ReplanSolvingScreen.tsx~~ + 형제 테스트 | **TRIP-752로 파일째 삭제(`git rm`)** — i05 진행 화면이 `pages/planb-draft/ui/ReplanSolvingView.tsx`(순수 뷰, `layer-pages.md` 참고)로 흡수됨. 이유는 751 `ReplanDraftView`와 같은 셸 재사용: 셸(`MapSheetShell`)이 widgets 층이라 features는 못 부른다. 옛 "진행바 66%+체크리스트 4행" 모양은 폐기 — 새 화면은 `widgets/map-sheet-shell/ui/GenerationProgressCard.tsx`(h07 선재 위젯)에 `title?`·`onCancel?` 옵트인 prop을 얹어 재사용한다(아래 `layer-widgets.md` 참고) |
| `src/features/planb/ui/SlotCandidateSheet.tsx` | **신규(TRIP-440)** — i14 순수 인라인 패널(바텀시트 라이브러리 import 0 — TRIP-483 `SlotCandidatePanel` 선례 따라 통과형 목 사각 회피, repo-traps 바텀시트 절 참고). 후보 카드마다 rationale·distanceRange·`{slackLabel}`(model이 만든 값을 변수로만 렌더, 리터럴 0) leaf. `degraded===true`만 강등 고지, `candidates=[]`면 empty. **배지 없음**(현재재계획안 없음 — ReplanSession.draft 계약 갭, i14 AC-9/10 후속) |
| `src/features/planb/model/useApplyReplan.ts` | **신규(TRIP-441)** — codegen `usePostTripsTripIdReplanSessionsSessionIdApply`를 감싸는 유일 seam(`PlanbDiffPage.tsx` 1곳만 호출 — T1 G2/G4 구조가드로 봉인). 성공 시 `onSuccess: (_data, variables) => invalidateQueries(getGetTripsTripIdItineraryQueryKey(variables.tripId))`로 itinerary 캐시 무효화(확정=원 일정을 실제로 바꾸는 유일 지점, INV-U4-05). 무효화 correctness는 jest 사각 — T1 G3는 심볼+`invalidateQueries` 문자열 실재만 확인(맹점①, repo-traps 참고). 개념 [[계약이 못 받치면 안 그린다]] |
| `src/features/planb/model/useCancelReplan.ts` | **신규(TRIP-441)** — codegen `usePostTripsTripIdReplanSessionsSessionIdCancel` 순수 passthrough(무효화 없음 — cancel은 세션만 CANCELED로 닫고 원 일정은 안 바꿈, INV-U4-05). ⚠️ apply와 달리 **codegen 훅 직접 호출 봉인 가드가 없다** — 형제 `pages/planb-draft/ui/PlanbSolvingPage.tsx`가 이 codegen 훅을 직접 부른다(구조 강제 비대칭, repo-traps 참고) |
| ~~`ReplanAppliedScreen.tsx`~~ | **TRIP-441 신규 → TRIP-754로 `git rm`(대상 소멸).** i19 전면화면 반영완료 얼굴은 폐지됐다 — 확정(i06 적용하기·diff 확정) 성공 시 허브(`live/index.tsx`)로 `replace(... applied: sessionId)`하고, 허브 위 바텀시트 `ReplanAppliedSheet.tsx`(아래 행)가 그 신호를 그린다. 옛 `{onBack, onContinue}` 콜백 계약·`[여행 계속하기]` 문구도 함께 소멸했다 |
| `src/features/planb/ui/ReplanAppliedSheet.tsx` | **신규(TRIP-754)** — i08 변경 반영 시트. 허브 위 조건부 마운트(`?applied` 쿼리가 있을 때만), `RiskDetailSheet` 골격 선례. 스크림은 `backdropComponent`(본문 밖 — `within(sheet)`로 안 잡힘), `enablePanDownToClose`+`onClose=onConfirm`으로 끌어 닫기도 확인과 동치. 요약 배지 3(`appliedSummaryBadges`, 아래 `appliedSummary.ts` 행)·내역 행(`AppliedDiffRow[]`, 받은 순서 그대로 `.map`)은 빈 값이면 **컨테이너째 안 그림**(자리표시 없음). `[되돌리기]`는 서버 호출 없이 `showRevertNotice` prop으로 안내만 켠다(00_decisions E2, 되돌리기 API 없음). 개념 [[계약이 못 받치면 안 그린다]] |
| `src/features/planb/model/appliedSummary.ts` | **신규(TRIP-754)** — `appliedSummaryBadges({changedCount,visitsBefore,visitsAfter,distanceDeltaM})` 순수 함수. 거리 배지 부호는 U+2212(음수)·`+`(양수), `\|Δ\|<10`이면 부호 없이 `이동 0m`(오케 결정), `distanceDeltaM===null`이면 배지 자체를 뺀다(2개). `formatDistance`(entities) 재사용, INV-3(소요시간 없음)은 타입에 `duration` 필드 자체가 없어 원리적으로 못 나옴 |
| ~~`ManualEditScreen.tsx`~~ + 형제 테스트 2종 | **TRIP-805로 `pages/planb-manual/ui/`로 이관 → TRIP-753으로 그 슬라이스째 `git rm`(대상 소멸, `layer-pages.md` 참고).** i07은 이제 `pages/itinerary-edit`의 `ItineraryEditPage`(h12)를 `inTrip`으로 재사용한다 — 옛 `variant` 진입 신호·`planbManualStructure` 가드도 함께 소멸(대체 가드는 `src/__tests__/planbEditUnifyStructure.test.ts`, `layer-test.md` 참고) |
| `src/features/planb/model/reorderKeepingLocked.ts` | **신규(TRIP-753)** — 순수 재정렬 `reorderKeepingLocked(original, reordered, lockedPoiIds)`. 잠긴 칸(`isFixed \|\| lockedPoiIds.includes(poiId)`)은 `original`의 절대 index에 그대로 두고, 빈 칸은 `reordered`에서 잠긴 것을 뺀 목록을 순서대로 채운다(칸 채우기, 입력 배열 불변 — `.filter`/`.map`이 새 배열 생성). `features/itinerary/model/itineraryEditStore.ts`의 `reorderKeepingFixed`와 같은 알고리즘의 **이름 다른 사촌**(형제 feature 직접 import 금지라 넓히지 못하고 새로 둠). `pages/itinerary-edit/ui/ItineraryEditPage.tsx`의 `onReorder`가 유일 소비 — 단 그 콜백을 부르는 프로덕션 드래그 표면이 0(`EditorView`가 `onReorder`를 구조분해조차 안 함), 그래서 이 규칙은 **테스트(P3)에서만 실행되고 라이브 앱에서는 아직 닿지 않는다**(새 티켓 후보: 시트 안 드래그 실배선) |
| `src/features/planb/model/triggerLabel.ts` | **신규(TRIP-561) → TRIP-748로 CLOSURE 라벨 `휴무 확인`→`휴무` 개정(i02 배지·알약 카피 축약).** 순수 번역표. `TRIGGER_LABELS`가 `TriggerKind` 4종(WEATHER·CLOSURE·DELAY·MANUAL)을 `{label, iconKey}`로 매핑, `triggerLabel(kind)`는 표 조회 한 줄. **정적 요지만**(상세 사유는 서버 `reason`이 동적으로 실어옴 — 라벨에 안 섞는다). node-safe(`import type`만, RN 런타임 미import) — `planbTriggerKindStructure.test.ts`가 소스를 직접 스캔해 4종 매핑·금칙어(교통·체류 초과 미발명) 확인, `RiskDetailSheet.labelSource.test.ts`(TRIP-749로 `TriggerWatchlistScreen.labelSource.test.ts`에서 rename)가 표에서 라벨을 도출하도록 강제(공허화 방지) — 단 이 가드는 리터럴만 잡아 import 우회는 못 본다(03b 경고-1, 아래 §5-b 대상). ⚠️ **`liveTimeStructure`·`executionDurationStructure`(둘 다 execution 전용 스캔) 밖이라 이 파일엔 INV-3 소요시간 내용 가드가 없다**(repo-traps `traps-execution.md` 참고, `slackTime.ts`와 동형 계열 — model이라 가드 사각) |
| `src/features/planb/model/triggerPillCopy.ts` | **신규(TRIP-748)** — 순수 함수(+ 타입 `TriggerPillSlot`). i02 지도 위 알약 카피를 kind별 템플릿으로 조립: WEATHER `{슬롯명} {H}시`(H는 `slot.startAt.slice(0,2)`→`Number`, 분 버림) · DELAY `{슬롯명} 방면` · CLOSURE `{슬롯명} 주변 시설`. 매칭 슬롯이 없으면(`slotKey` null·다른 날·`nameKo` null) `triggerLabel(kind).label`만 반환. `startAt` 옆에 산술기호 없음(`liveTimeStructure` 준수) |
| `src/features/planb/model/useActiveTriggers.ts` | **신규(TRIP-561)** — `useActiveTriggers(tripId, {enabled})`. `useGetTripsTripIdTriggers`(codegen) 얇은 래퍼(로직 0줄, `useLiveItinerary` 동형) — `enabled`만 받아 active 얼굴에서만 조회하게 게이팅(`useVisitCheck` 선례). 서버는 "발화 중인 것만" 반환(빈 배열=표면 없음, INV-U4-01) — 클라 필터링 없음(MANUAL 제외는 페이지 몫, 아래 `layer-pages.md` 참고) |
| `src/features/planb/model/triggerWatchlist.ts` | **신규(TRIP-562) → TRIP-749로 라벨 표를 `config/watchLabels.ts`로 이관(동작 무변경).** 순수 사영 함수 `triggerWatchlist(triggers)`. `useActiveTriggers`가 주는 발화 목록(칩·배너용, 그대로)을 i03 시트 배지 3행 상태(`{activeBanner, rows[3]}`)로 **다르게 접는다** — 같은 GET /triggers를 화면마다 다른 모양으로 쓰는 사례(개념 [[사영 (projection) — 같은 데이터 다른 접기]]). `filter(kind!=='MANUAL')` 후 `displayTriggers[0]`이 `activeBanner`, 행은 `['WEATHER','DELAY','CLOSURE']` 고정 순서로 각 kind가 발화 중이면 `active`+그 `reason`, 아니면 `normal`. 행 이름은 `triggerLabel(kind).label`(활성 트리거 제목)이 아니라 `config/watchLabels.ts`의 `WATCH_CATEGORY_LABEL` Figma 카테고리명(날씨/이동/영업) — i08 칩과 다른 표면(상시 존재 카테고리 vs 활성 트리거 제목). node-safe(`import type`만) |
| `src/features/planb/config/watchLabels.ts` | **신규(TRIP-749)** — `triggerWatchlist.ts` 안에 있던 비공개 상수 `WATCH_CATEGORY_LABEL`(날씨·이동·영업)·`WATCH_STATUS_LABEL`(활성·정상)·타입 `WatchKind`를 config로 꺼냄(상태 카피는 옛 `TriggerWatchlistScreen.tsx` 안 리터럴뿐이었다). `RiskDetailSheet.tsx` 배지 3개의 카피 소스. **kind 타입이 model의 `TriggerWatchlistRow['kind']`와 같은 3리터럴을 두 벌로 갖는다** — config→model 타입 import는 순환(config→model 타입, model→config 값)이라 따로 적음, 어긋나면 tsc가 `Record` 키 불일치로 잡는다 |
| `src/features/planb/model/riskAffectedRow.ts` | **신규(TRIP-749)** — 순수 조인 함수 `riskAffectedRow(days, slotKey)`. slotKey(`날짜#poiId`)를 `parseSlotKey`(entities 재사용)로 쪼개 `days.find(date)`→`findIndex(poiId)`로 그 날짜의 슬롯을 찾고, `{time, name, meta}` 셋만 반환(INV-3). 트리거 자신의 날짜로 찾지, 화면이 지금 보는 날짜가 아니다. `time: startAt.slice(0,5)`(산술 없음, `liveTimeStructure` 준수), `meta`는 `[N번째, category, formatOpeningHoursLabel(hours)].filter(part=>part).join(' · ')`. 없는 slotKey·날짜·poiId·nameKo는 전부 `null`(상자 생략) |
| `src/features/planb/model/foldScope.ts` | **승격(TRIP-562, 원 위치 `pages/live-itinerary/ui/LiveItineraryPage.tsx` 로컬 함수)** — `foldScope(scope)`: `FULL_DAY`만 통과, `PARTIAL_SLOTS`·`NONE`·`null`·`undefined`는 전부 `PARTIAL_SLOTS`로 접는다. i09 활성 CTA와 라이브 화면 `openReplan`이 공용하게 되어 페이지 로컬 정의를 지우고 이 파일로 승격(복제 아님 — 로컬 정의 삭제 후 import 교체, 로직 바이트 동일) |
~~`useTriggerWatchlist.ts`~~ · ~~`ui/TriggerWatchlistScreen.tsx`(+test 2)~~ | — | **TRIP-749로 파일째 삭제** — i09 감시 목록 전체 화면(위 아이콘 매핑 함정 포함)을 `RiskDetailSheet.tsx`(아래)가 흡수. 소비처였던 `pages/planb-triggers/` 전체와 라우트 `app/trips/[tripId]/planb/triggers.tsx`도 함께 삭제(`layer-pages.md` 참고). 옛 아이콘 매핑 사각 항목은 `traps-execution.md`에서도 제거 대상(scribe [기록] 처리) |
| `src/features/planb/ui/RiskDetailSheet.tsx` | **신규(TRIP-749)** — i03 위험 상세 시트(순수, props+콜백만). eyebrow "위험 요소 · {config 카테고리명}" + 빨강 `RiskWarningGlyph`(execution `WarningFilledGlyph` 복제 — features 간 import 금지라 불가피, `LocationOffGlyph` 3벌과 동형) · 제목 = 서버 `reason` 그대로 · `riskAffectedRow` 결과가 있을 때만 회색 영향 상자 · `triggerWatchlist(...).rows` 배지 3개(상자·글자 색을 같은 `active` 조건으로) · `[대안 보기]`(`onPressAlternative`). 조건부 마운트(`riskTrigger ? <Sheet/> : null`)로 열림/닫힘을 표현(개념 [[조건부 마운트로 시트 열기]]) — 페이지가 `<><LiveHubView/>{riskSheet}</>` **형제**로 그려 딤이 허브 전체를 덮음(개념 [[z-order = 렌더 순서 (RN absolute 형제는 나중 렌더가 위)]]). `backgroundComponent={null}`로 gorhom 기본 배경을 끄고 본문 `BottomSheetView`가 직접 배경을 그림 — **03b 경고-2: 위로 끌면(over-drag) 시트 아래 여분 패딩(~92pt)이 투명해지는 gap이 생긴다**(gorhom이 그 패딩을 기본 배경이 칠해준다는 전제로 설계됨), 6-b 실기 확인 대기. 개념 [[gorhom 배경 끄기와 over-drag 여분 패딩]] |
~~`ui/ReplanDraftScreen.tsx`~~ · ~~`ui/NoAlternativeScreen.tsx`(+test 2)~~ | — | **TRIP-751로 파일째 삭제(`git rm`)** — i06 재계획안(펼침·대안없음·failed 3얼굴)이 `pages/planb-draft/ui/ReplanDraftView.tsx`(순수 뷰, `layer-pages.md` 참고)로 흡수. 이유는 셸 재사용: `MapSheetShell`이 widgets 층이라 features는 못 부른다. `ReplanSlotRow`(entities 소유)는 무변경 유지, 소비만 뷰로 이동 |

⚠️ `features/planb/ui/**`는 기존 구조가드 `executionDurationStructure.test.ts`(INV-3, `features/{execution,planb}/ui/**` 재귀 스캔)에 **자동 편입**된다 — 소요시간 표기(`N분`·`N시간`·`소요`) 0건 강제, raw-hex 가드는 아직 없음(참고, 후속 티켓 후보). `slackTime.ts`는 `model/`이라 이 자동 편입 밖 — PBT-U4-F2가 유일 심판. **TRIP-563 추가(TRIP-751로 대상 파일 삭제됨)**: 옛 `ReplanDraftScreen.tsx`·`NoAlternativeScreen.tsx`가 `<MapView>`(구 `<KakaoMapView>`, TRIP-861로 대체)를 렌더해 **별개의** 전역 소스 스캔 가드 `itineraryMapSurfaceStructure.test.ts`(`src/` 전체 재귀, 이름은 itinerary지만 사정거리는 전역 — `layer-test.md` 참고)에 자동 편입돼 있었다. 이 가드는 `executionDurationStructure`와 달리 **소진 단언**(`.toEqual(LOCKED_CALLERS 명부)`)이라, 등재하지 않으면 자동 통과가 아니라 **자동 FAIL**이다(6-a n=1에서 실제로 걸림 — TRIP-442에 이은 두 번째 반복, 개념 [[소스 스캔 가드의 폴더 전수와 자동 편입]] 참고). TRIP-751로 두 파일이 `git rm`됐고, 같은 지도(`planb-draft-map`/`planb-noalt-map`)는 이제 `pages/planb-draft/ui/ReplanDraftView.tsx`가 그린다 — `LOCKED_CALLERS` 등재는 test-designer가 그 파일로 재조준했다(개념 [[선재 가드 재조준 (코드 이동 추적)]]). **TRIP-809**: `src/__tests__/planbReplanDraftStructure.test.ts`의 G3(INV-3 긍정 짝)는 `readOne`(단일 파일 지정)으로 `ReplanSlotRow.tsx`를 읽는데, 그 파일이 `entities/itinerary-slot/ui/`로 이사하며 test-designer가 대상 경로 1줄만 재조준했다(앵커 리터럴 무변경) — 위 두 가드(재귀 스캔)와 달리 이 가드는 파일이 폴더를 떠나면 재조준 없이는 즉시 red다(개념 [[선재 가드 재조준 (코드 이동 추적)]] 사례 5).

`src/features/planb/ui/PlanbGlyphs.tsx`(TRIP-440 신규 행, 위 참고)는 **TRIP-441로 `AppliedBackGlyph`·`AppliedCheckGlyph`·`AppliedAlertGlyph` 3종 + `INK` 상수 추가**(체크리스트 done/active/waiting 3종 무변경) — 로컬 복제 글리프 관례(리포 `*Glyphs.tsx`) 그대로, 공용 글리프 아님. **TRIP-562로 `WeatherCloudGlyph`·`ClockGlyph`·`ShopGlyph` 3종(i09 행 아이콘) + `MUTED` 상수 + `TintGlyphProps` 타입 추가** — execution 동명 글리프(`ExecutionGlyphs.WeatherCloudGlyph` 계열 없음, FAB 아이콘은 별도)는 cross-feature import 금지라 벡터만 로컬 미러(상점 아이콘은 리포에 선례가 없어 새 근사 벡터). **TRIP-563으로 `ChevronRightGlyph`·`LockGlyph` 2종 + `PRIMARY_TEXT` 상수 추가**(i13 슬롯 행 어포던스용) — `ItineraryGlyphs`에 동명 글리프가 있으나 features 간 import 금지라 로컬 복제(확립된 관례). **TRIP-749로 `RiskWarningGlyph`(execution `WarningFilledGlyph` 복제, i03 시트 삼각) 추가 + i09 전용이던 `WeatherCloudGlyph`·`ClockGlyph`·`ShopGlyph`·`MUTED`·`TintGlyphProps`는 소비처(TriggerWatchlistScreen) 삭제로 함께 정리**(고아 export 남기지 않음). **TRIP-754로 `AppliedBackGlyph`·`AppliedCheckGlyph`와 그 둘만 쓰던 `INK` 상수를 삭제**(i19 화면 소멸, i08 시트는 이 글리프를 안 씀 — `AppliedAlertGlyph`는 다른 소비처가 있어 잔존).
