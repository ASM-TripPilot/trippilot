---
paths:
  - "src/__tests__/**"
  - "**/*.test.ts"
  - "**/*.test.tsx"
---
# 테스트 인프라


| 파일 | 역할 |
|---|---|
| `src/mocks/handlers.ts` | msw 핸들러 — **테스트 오라클 전용** |
| `src/mocks/server.ts` | msw/**node** 서버 (통합 버킷 전용) |
| `src/mocks/scenarios.ts` | 시나리오 정의·상태. **앱 런타임은 안 씀**(makeAuthorize가 env로 전환됨) |
| `src/test-support/onboardingScenarios.ts` | 온보딩 목 거동. **앱이 참조하지 않는 테스트 전용 모듈** |
| `src/test-support/expoAuthSessionMock.ts` | `expo-auth-session`·`web-browser`·`crypto` 가상 목 + 스파이 |
| `src/test-support/expoRouterStackMock.tsx` | expo-router `Stack` 목(관찰 마커) |
| `src/test-support/expoRouterRedirectMock.tsx` | expo-router `Redirect`·`Stack` 목 — 진입 가드 테스트용 |
| `src/test-support/expoRouterTabsMock.tsx` | expo-router `Tabs`/`Tabs.Screen` 관찰 목(TRIP-170) — `capturedTabsProps` 홀더 + `tabs-route-*` 마커. **TRIP-370 추가**: `export function useRouter()`(no-op `push`) — `(tabs)/index.tsx`가 `useRouter()`를 물게 되면서 이 목을 쓰는 `tabsShell.test.tsx`(루트 렌더만 단언, 라우팅 무관)가 크래시하는 것을 막는 무해 스텁. 목적지 왕복 심판은 `tabsHomeRoute.test.tsx`가 파일-로컬 `jest.mock('expo-router')`로 별도 수행(두 목 비간섭). 유일 소비자 `tabsShell.test.tsx`(grep 확인) |
| `src/test-support/splashGateMock.tsx` | `SplashGate` 목 |
| `src/test-support/queryClientProbe.tsx` | **신규(TRIP-179)** — `SplashGate` 자리를 대신할 관찰용 가짜 컴포넌트. 렌더될 때 `useQueryClient()`를 불러 모듈 변수에 담고(`getObservedQueryClient`) `<View testID="query-client-probe" />`를 마커로 그린다. `resetObservedQueryClient`로 파일 간 상태를 비운다 |
| `src/test-support/nativeSocialSdkMock.ts` | **신규(TRIP-210)** — `@react-native-seoul/kakao-login`·`naver-login` 두 SDK의 `{virtual:true}` 가짜 모듈 + 관찰 스파이(`expoAuthSessionMock.ts`와 동형 패턴). default·named 네임스페이스·named 함수 세 import 형태 모두 같은 스파이에 닿는다(V2 실측). `naverInitializeSpy`는 **일부러 리셋하지 않는다**(★D8 — 모듈 스코프 메모이즈 구현도 관측되게 하려는 설계) |
| `__mocks__/@gorhom/bottom-sheet.tsx` | 네이티브 모듈 자동 목 |
| `__mocks__/react-native-draggable-flatlist.tsx` | **신규(TRIP-302, 슬라이스1)** — `react-native-draggable-flatlist`를 모듈 스코프 파일로 완전 대체(인라인 `jest.mock(팩토리)` 금지 — NativeWind babel이 주입하는 `_ReactNativeCSSInterop` 참조가 스코프 밖으로 걸림, `@gorhom/bottom-sheet` 선례와 동형). `renderItem`을 실제로 돌려 카드를 렌더하고(통과만 하는 목이면 카드 심판이 공허 통과하거나 red), `onDragEnd`는 호스트 View의 prop으로 노출해 테스트가 `props.onDragEnd({data,from,to})`로 직접 발화(webview 목의 `onError` 직접 호출 선례와 동형) |
| `src/__tests__/noMswInStaticGraph.test.ts` | 정적 import 그래프를 fs로 훑어 프로덕션의 `@/mocks/*`·`msw` import 0을 기계 강제 |
| `src/__tests__/importBoundary.test.ts` | import 경계 가드 — 계층·feature 격리 위반 차단. **FSD 완결 2/4에서 보강**(34→54줄, code-critic 발견 구멍 수리) — 프로브를 배럴 의존(구 `@/features/stay`, 삭제 예정 대상이었다)에서 실파일 딥 경로로 떼고, 단언을 에러 개수(`errorCount>0`)에서 **룰 ID**(`import/no-restricted-paths` 포함 · `import/no-unresolved` 불포함)로 바꿔 "경계 위반"과 "모듈 해석 실패"를 구분한다. **단독 실행 시 `NODE_OPTIONS=--experimental-vm-modules` 필요**(`pnpm exec jest` 단독 금지 — 없으면 2/2 실패, 테스트 결함처럼 보이지만 실행 방법 문제) |
| `src/__tests__/importBoundaryLayers.test.ts` | **신규(TRIP-804, `@jest-environment node`)** — `importBoundary.test.ts`(home 1탐침)의 자매 파일. `ESLint.lintText`+가짜 filePath로 FSD 6층 방향(app→pages→widgets→features→entities→shared)을 실제로 막는지 검사한다: 13개 feature 전부(`src/features` 디렉토리에서 읽어 `it.each`)에서 형제 feature·역방향(features→pages) import가 `import/no-restricted-paths`로 잡히고 `import/no-unresolved`는 아님 + shared→features·entities→features·widgets→pages·pages→app-shell 각 1탐침 + 허용 방향(features→shared·widgets→features는 error 0, features→entities·pages→widgets는 경계 룰 미발화). **★ 빈 층(widgets·entities) 대상은 실파일이 없어 `no-unresolved`가 떠 error 0을 단언 못 함** — 허용 프로브는 "경계 룰 미발화"까지만. `importBoundary.test.ts`와 같은 단독 실행 함정(`NODE_OPTIONS` 필요) |
| `src/__tests__/fsdLayerStructure.test.ts` | **신규(TRIP-804, `@jest-environment node`)** — FSD 층 구조를 fs로 훑어 굳힌다. G0(탐지기 자가검사, `stripComments`×상위층 정규식이 주석 걷힘·코드 URL·자기참조 생존을 실측) · AC-4a(src 직계 디렉토리 ⊆ 허용목록 11: `__tests__·app·app-shell·assets·entities·features·mocks·pages·shared·test-support·widgets`, 실재 9개 대표 긍정 짝) · AC-4b(features·pages 각 슬라이스 세그먼트 ⊆ {ui,model,lib,config}, 옛 칸 부활 금지 + 슬라이스 수 앵커) · AC-5(shared 층 재귀 스캔에 상위층 import 0 + `@/shared/` 자기참조 앵커). `fsdStructure.test.ts`의 A/B 졸업 원장(완전일치)을 흔들지 않도록 별 파일로 둔다(부분집합 계약은 여기서 전 슬라이스로 일반화). **TRIP-805 갱신**: AC-4b 세그먼트 스캔에 `widgets` 편입 + 슬라이스 수 앵커(`widgetSlices≥3` + `['fab-stack','itinerary-edit','time-sheet']` 대표 이름) |
| `src/__tests__/widgetsStructure.test.ts` | **신규(TRIP-805, `@jest-environment node`) → TRIP-753으로 A·C 앵커 갱신.** widgets 층 첫 입주 구조 심판(fs 소스 스캔). G0(탐지기 자가검사) · A(shared/itinerary-edit 소멸 + 전 소스 옛 경로 0 — **옛 목적지 `widgets/itinerary-edit`도 TRIP-753으로 슬라이스째 사라져 앵커를 남은 위젯 `time-sheet`로 옮김**) · B(shared 직계 도메인명 디렉토리 0) · C(features/planb 에 `@/widgets` 0 + `ManualEditScreen`이 `features/planb/ui/`·`pages/planb-manual/ui/` 어디에도 없음 — TRIP-753으로 후자도 소멸) · D(3화면 소스에 `saved-menu-toggle` 리터럴 0 + `@/widgets/fab-stack` 소비) · E(`SlotTimeSheet.tsx` 소멸 + 소비처가 `@/widgets/time-sheet` 소비 — TRIP-753으로 `PlanbManualPage` 소비처 소멸, 현재는 `ItineraryEditPage`(h12·i07 공용)·`PlaceAddPage` 2곳) · F(위젯 UI 에 expo-router·`@/features`·`@/pages`·raw hex 0 + useState 0(단 `TimeSheet.tsx` 예외 — D8 선택 셀 상태)). 모든 부정은 같은 it 안 긍정 짝과 쌍 |
| `src/widgets/time-sheet/ui/TimeSheet.test.tsx` | **신규(TRIP-805, co-located RNTL)** — 공용 시각 시트 CS1~CS8 ×2접두(`describe.each([itinerary-edit-time, planb-manual-time])`, 16 it). 접두별 testID 트리·셀 선택(`toBeSelected`)·`labels` 흐름(`getByText` 완전일치)·`endsNextDay` 시·분 두 축(CS3 시·CS7 분)·INV-3 분 셀 bare 숫자(CS6 `DURATION_TEXT` 매치 0)를 잠근다. 실개폐·2스냅은 `@gorhom/bottom-sheet` 통과형 목이 못 봄(6-b 실기 전용) |
| `src/__tests__/fsdStructure.test.ts` | **TRIP-173 신설(사이클 1 유일한 신규 파일)** — auth `{config,lib,model,ui}` 4칸·onboarding `{model,ui}` 2칸·**home `{model,ui}` 2칸(FSD 완결 1/4 신설, it 1-3)** 대표 파일 존재, `pages` **6슬라이스**(TRIP-181로 `stay-search` 추가, B 카운터 0→1 — `PAGE_SLICES` forEach 상수는 미포함이라 개별 슬라이스 검사는 여전히 5개 대상)의 배럴·라우트 참조, `app-shell`이 `src/app` 밖에 있는지를 검사. **폴더 배치만 본다 — import 방향·소스 내용은 안 본다**(code-critic E3·E5·E6 실측). **FSD 완결 2/4(AC-4, it 4-1) 신설** — `features`·`shared` 두 층 전수 스캔으로 빈 배럴(`export {};`) 0개 단언(A·영구) + 진짜 배럴(`shared/api`·`shared/storage`) 대표 심볼 생존 긍정 짝(B) + **스캔이 실제로 두 층에 닿았는지**(`scannedLayers` 앵커, code-critic W-1 보강 — 안 넣으면 `BARREL_LAYERS`를 비워도 green이었다)까지 확인한다. 사이클 3~4가 이 파일에 덧붙여 자란다. 주석 상단에 A(영구)/B(한시) 졸업 조건 명시(게이트①-1→①-2 재제시로 추가됨). ⚠️ "pages 슬라이스가 정확히 N개" 배열 단언은 **디렉토리 이름만** 보고 내용은 안 봐서, 통합테스트 파일을 최종 위치에 물리적으로 쓰기만 해도 구현 전에 조기 green이 될 수 있다(TRIP-181 실측 — red 소급 확인에서 오판 금지 사전 기록 필요). **TRIP-695 추가**: home 세그먼트 완전일치가 `['model','ui']`→`['lib','model','ui']`로 확장(신규 `features/home/lib/formatCountBadge.ts` co-located 테스트가 강제) — test-designer가 이 갱신을 02a 대상 목록에서 놓쳐 구현 후 red, 오케가 5단계에서 정정 |
| `src/__tests__/staySearchStructure.test.ts` | **TRIP-181 신규 → TRIP-182로 강화**(기존 4개 describe `it` 본문 무변경 + 신규 describe 3개 추가). duration 식별자 0(INV-3, `STAY_SURFACE_DIRS` 3층 전체) · raw hex 0(V1, `TOKENIZED_HEX` 7색→**11색** 확장) · 프레젠테이션 순수성(`useState`·`useReducer`·`zustand`·`expo-router`·query·axios·타 feature import 11종 0건) · 3파일 상호 배제. **TRIP-182 신규**: ① `features/stay/ui` 디렉토리 **동적 스캔**(`*Glyphs.tsx` 제외)으로 V1 사정거리를 화면 1파일에서 부품 파일 전체로 확장(`SCREEN_FILES` 배열 자체는 안 건드림 — 배열 순서 의존 회귀 방지) ② AC-13 SafeArea 구조 가드(`SafeAreaView`+`edges` `top`+`stay-search-root` 유지, 실제 겹침은 6-b 소관) ③ AC-8 배선 단일 출처(`resolveStaySearchState`는 `StaySearchPage`만 호출, 화면은 재호출 금지) |
| `src/__tests__/stayRegisterStructure.test.ts` | **신규(TRIP-198)** — e05 등록 표면 구조 가드 9묶음(G-0~G-8). FSD 상태 소유(화면에 `useState` 0 · 페이지가 진다) · INV-3 `duration` 0건 · raw hex 0 · 토큰 경유 · 지도 재사용(신규 지도 컴포넌트 0) · 배럴·라우트 배선 · e02→e05 진입(`router.push`·`/stays/register`·`onPressRegister`, 화면 쪽은 `expo-router` 0의 부정 짝) · zod 규약과 새 의존성 0. ⚠️ **이 파일은 게이트①-2에서 3건이 수복됐다** — 모집단 비대칭(`pages/stay-register` 전체 → `/ui`), `/\bduration\b/i` → `/duration/i`(단어 경계가 `stayDuration`을 못 잡아 **자가검사가 항상 false**였고, 그게 곧 INV-3 가드의 실제 커버리지 구멍이었다), `toContain('useRouter')` → `toContain('router.push')`(동결 목이 `useRouter` 미제공이라 훅 구현이 동결 7건을 즉사시켰다). 검증 n=1이 이 3건을 잡았고 구현 결함은 0건이었다 |
| `src/__tests__/onboardingStructure.test.ts` | 온보딩 계층·경계 구조 가드(서버 권한 경계 등). `PAGES_DIR`+`ONBOARDING_PAGE_SLICES`(TRIP-173 신설 — 온보딩 컨테이너 4개가 `pages/`로 나가며 금칙어 가드 사정거리에 다시 편입) |
| `src/__tests__/onboardingPrefStructure.test.ts` | 취향 스토어·모델 구조 가드(TRIP-163) — persist 금지·`@/shared/api` 미참조·`create(` 표기(구조 가드 6-2, 개념 [[구조 가드와 긍정 앵커]]) |
| `src/__tests__/homeStructure.test.ts` | 홈 소스 스캔 가드(TRIP-170, `@jest-environment node`) — 픽스처 상수화(D-1)·INV-3 `duration` 식별자 0(D-2)·토큰 raw-hex 0(D-3)·SafeArea 규약(D-4)·탭바 격리(D-5). D-3·D-4 모집단은 TRIP-173 FSD 완결 1/4에서 `screens/` 폴더 전체 → `ui/*Screen.tsx` 파일명 필터 + `HOME_SCREEN_SOURCE_FILES` 동결목록(현재 1건, `onboardingStructure` 선례와 동형)으로 교체됐다. **TRIP-317 추가**: "단계 얼굴 단일 파일 강제" describe 1개 — 신 testID 5종이 `HomeScreen.tsx` 한 파일에 실재(긍정 짝) + `ui/*Face.tsx` 분리 0(부정 짝), `HOME_SCREEN_SOURCE_FILES` 동결목록 자기검증 |
| `src/__tests__/onboardingPrefRoutes.test.tsx` | 취향 1/2·2/2 라우트 존재·내비게이션 계약 가드(TRIP-163) — push/replace/back 분기 |
| `src/__tests__/tabsShell.test.tsx` | `(tabs)/_layout.tsx` 배선 가드(TRIP-170) — 5탭 등록 순서·`tabBar` 렌더프롭·어댑터 활성 매핑/press→navigate·홈 라우트 래퍼·**껍데기 유지 단언 대상 축소**(explore·records·my → **TRIP-299로 일정 탭 제외** → **TRIP-201로 탐색 탭도 제외, records·my 2탭만 남음**: 두 탭 모두 실 화면으로 승격돼 껍데기 전제가 깨짐, `ExploreRoute` 미사용 `require` 제거 필수 — TRIP-299 동형·★S-1). 탐색 탭 실동작은 `tabsExploreRoute.test.tsx`가, 일정 탭은 `tabsItineraryRoute.test.tsx`가 별도로 잠근다. 검증-수정 비동결. **TRIP-371 추가**: `jest.mock('@/shared/api/generated/trips/trips', () => ({useGetTrips: jest.fn(...빈 목록)}))` + `jest.mock('@/features/explore/model/savedPlaces', () => ({useSavedPlaces: () => ({savedPoiIds:[]})}))` — 홈 라우트가 두 훅을 물게 되면서 SC-1(`render(<IndexRoute/>)`)이 QueryClient 부재로 크래시하는 것을 막는 무해 스텁(AC 아님, 배선 회귀 방지 인프라). 기존 6 describe·단언 무변경. **TRIP-575로 records describe 교체** — placeholder(`shell-tab-placeholder-records`) describe가 "기록 탭 허브(AC-1·셸 교체)" describe로 갈렸다(my 탭 TRIP-604 승격 선례 — placeholder 계약 정당 종료, `queryByTestId('shell-tab-placeholder-records')` null 단언). `record-calendar-empty` present로 허브 렌더를 확인(기존 전역 `useGetTrips` 목(TRIP-371, 빈 목록)이 그대로 빈 얼굴 안전 렌더+크래시 방지 스텁을 겸해 신규 목 불필요). **5탭 전부 실화면으로 승격 완료**(explore→TRIP-201·itinerary→TRIP-299/468·my→TRIP-604·records→TRIP-575) — `shell-tab-placeholder-*` 잔존 참조는 `tabsMyRoute.test.tsx`(TRIP-604 선례)·`tabsRecordsRoute.test.tsx`(이번)의 사후 부재 확인용뿐, 실 placeholder describe는 이 파일에 0개. **TRIP-695 추가**: `jest.mock('@/features/stay/model/savedStays', () => ({useSavedStays: () => ({savedCount: 0})}))` 1줄 — 홈 라우트가 새로 무는 `useSavedStays`(딥 경로, `features/trip/model`의 동명 훅과 별개) QueryClient 크래시 방지 스텁(단언 무변경). |
| `src/__tests__/tabbarVisual.test.ts` | **TRIP-173 FSD 완결 3/4 신설** — `BottomTabBar.tsx` 비주얼 소스 스캔 가드(AC-V1 밴드 84+알약 반경 · AC-V2 아이콘 좌표계 27 전량 · AC-V3 옛 풀폭 직각 바 흔적 0 · AC-V4 색 토큰 경유). **모든 `it`이 `stripComments()`로 주석을 제거한 뒤 스캔** — 게이트①-1에서 헤더 주석의 `rounded-pill` 문구가 부정 단언을 실제 코드 없이 만족시키는 거짓 GREEN이 나온 것을 게이트①-2에서 근본 수정. 렌더 크기(`size=27`, 호출부+기본값 12곳)·스크린샷이 잡은 수정값 6종(`h-[64px]`·`leading-[13px]`·`bg-surface-soft`·`w-[62px]`·`px-[28px]`·`style={PILL_SURFACE_STYLE}`)도 함께 잠근다. `active=1~4` 변형은 탭을 누를 수 없어(접근성 권한 부재) 코드 대조로만 담보 — 스크린샷은 홈 활성 1상태만 검증. **TRIP-201 개정** — AC-T1/T2/O1(신 치수 6종 존재+구값 0)·AC-V2(아이콘 렌더 크기 `27→22`, `viewBox 27` 좌표계는 `count`가 정확 문자열이라 오염 안 됨 ★T-1)·AC-O1(`bg-canvas` 0건, M-2 역전 — 밴드 투명은 이 소스 스캔만으로 담보, 다른 불투명 토큰을 넣으면 거짓 green 가능성 ★O-1) 추가. 렌더 위치·실제 오버렙은 이 파일 사정거리 밖(★O-2, `tabbarOverlay.test.ts`도 위치는 못 봄) |
| `src/__tests__/tabbarOverlay.test.ts` | **신규(TRIP-201)** — 탭바 오버레이 소스 가드(`@jest-environment node`). AC-O2: `(tabs)/_layout.tsx`의 `tabBarStyle`+`position:'absolute'` 문자열 존재(커스텀 탭바엔 무효 — AC-O3 아래 참고) · AC-O3: 화면별 하단 패딩 ≥84(홈·일정 선제 green, 탐색 신규). **게이트①-2로 진짜 메커니즘 심판 추가** — `BottomTabBar.tsx` 루트 className에 `absolute`+`bottom-0`+`h-[84px]` 동거 요구(뮤테이션 실측: `absolute` 제거·`bottom-0` 제거·`relative` 전환 전부 red). 이전엔 `_layout.tsx`의 죽은 `tabBarStyle` 문자열만 잠가 진짜 오버레이 회귀(BottomTabBar 루트 position)를 지워도 승인 4개가 전부 green이었다(03b 차단-1) — 개념 [[가드의 사정거리]] 실측 12. 위치 스캔이라 실제 겹침·투명 밴드 비침·터치 통과(`pointerEvents`)는 여전히 사정거리 밖(6-b 실기 전용). **게이트①-2로 TRIP-468 재조준(해시 `257e76f7...`)** — AC-O3 셋째 `it`(일정 빈 상태 중앙정렬 선제 green)의 파일-타깃을 `app/(tabs)/itinerary.tsx` → `features/itinerary/ui/MyTripsListScreen.tsx`로 옮김. TRIP-468이 `itinerary.tsx`를 로직 0 얇은 배선으로 재작성하며 empty 상태(`itinerary-tab-empty`+`justify-center`)가 `MyTripsListScreen.tsx`로 이주해 가드의 파일-타깃이 낡았던 것을 정정(TRIP-222 승격 때 `sharedUiStructure`가 파일 이주를 따라간 것과 동형, 개념 [[선재 가드 재조준 (코드 이동 추적)]]). 가드 의도(빈 상태 중앙정렬)는 보존, 앵커 문자열은 그대로 |
| `src/__tests__/tabsExploreRoute.test.tsx` | **신규(TRIP-201)** — `(tabs)/explore.tsx` 라우트 배선 가드. AC-E1~E7: 6구획 렌더+`explore-nearby` 부재 · 검색 제출→`/stays?region=`(★E-1 `decodeURIComponent` 필수 — 리포 관례 `encodeURIComponent`와 raw 두 형태 모두 통과) · 카드·금액(`getByText` exact, "· 1박" 접미 없음 BR-U1-12)·모두보기 · lane_itin "준비 중" 자리 · bridge CTA 분기(savedCount 0/≥1, ★E-2 `toHaveTextContent`는 exact라 부분포함엔 regex 필수) · 부분 실패(숙소 레인만 에러 대체, 나머지 구획 생존 — INV-4) · axisSeg 비활성 3탭 무동작. `useStaySearch`·`useSavedPlaces`는 배럴이 아니라 딥 경로로 목 seam 고정(★E-4, 다른 경로면 실 훅이 돌아 QueryClient 부재로 죽는다). **TRIP-447로 동결 개봉** — AC-E1/E6 렌더·생존 목록에서 `explore-axis-all` 제거, AC-E7(축 탭 describe) 통째 삭제, 나머지 E2~E5 무변경 |
| `src/__tests__/exploreLandingAxisRemoval.test.ts` | **신규(TRIP-447, `@jest-environment node`)** — AC-1 소스 스캔 가드. `stripComments` 조합 자가검사(주석 속 금칙어는 걷힘·코드 속은 생존·`:` 뒤 URL 보존) 후 `ExploreLandingScreen.tsx`에서 금칙 4토큰(`AXES`·`AxisSegment`·`explore-axis-`·`accessibilityRole="tab"`) 0건 + 앵커 3종(`explore-landing-heading`·`explore-landing-search`·`explore-lane-stay`) 생존 + 파일 존재 앵커(`fs.existsSync`, 빈 파일 가짜통과 차단) |
| `src/__tests__/tabsExploreRouteSave.integration.test.tsx` | **신규(TRIP-447)** — d01 랜딩 숙소 카드 저장 하트 라우트 통합 승인 테스트(AC-4~8, msw+`QueryClientProvider`). `useStaySearch`·`useSavedPlaces`는 딥 경로 목, `useSavedStays`(features/stay)는 **실물**(목 안 함 — 낙관/롤백을 실제로 관찰). 담김/미담김은 `explore-stay-heart-filled-{key}`/`-outline-{key}` **서로 다른 글리프 testID**(fill 색 토글 아님, ★1 — repo-traps 글리프 함정 회피) + `accessibilityState.selected`. AC-6(실패 배너+안 굳음 동시)·AC-7(서버 미호출+로그인 push 동시) 이중 단언. 라우트가 조건부 자식 `SavableStayLane`(게스트는 훅 미마운트)으로 `useSavedStays`를 격리 — `tabsExploreRoute.test.tsx`(F2, 토큰 미설정 렌더)가 `QueryClientProvider` 없이 라우트를 통째 렌더해 크래시하는 것을 막는 배선(`(tabs)/index.tsx`의 `PlanningHome` 패턴 계승) |
| `src/__tests__/loginVisual.test.ts` | **TRIP-173 FSD 완결 4/4 신설** — `AuthGlyphs.tsx`의 `WarningTriangleGlyph` 비주얼 **소스 스캔** 가드(AC-V4: `viewBox="0 0 18 18"` · `strokeWidth={1.575}` 배열 비교(개수·값·순서) · 렌더 크기 3곳 잠금(기본값·`size`→`width`/`height` 배선·호출부) · 색 상수 경유 · 스캔 대상 경로 존재 앵커). `stripComments` + 함수 블록/JSX 태그 슬라이스로 이 모듈의 다른 글리프 5개가 개수를 채워주는 우회를 차단(게이트①-2 보강, W-1·W-3 — `tabbarVisual.test.ts`와 같은 구멍이 두 사이클 연속 재현). AC-V1~V3(라벨 웨이트·에러 배너)은 이 파일이 아니라 `SocialLoginScreen.visual.test.tsx`의 **렌더 층**이 잠근다 — `className`이 jest 렌더 트리에 평문 prop으로 남는다는 4-a 실측으로 Seed를 뒤집은 결과(개념 [[가드의 사정거리]] 실측 6, "렌더 층과 소스 층은 사정거리가 다르다") |
| `src/__tests__/authSheetHandleStructure.test.ts` | **신규(TRIP-713, 2026-09-18, `@jest-environment node`)** — c02 소셜 로그인 공용 `Sheet`의 이중 그랩버 제거 소스가드. `SocialLoginScreen.tsx`의 `<BottomSheet\b[^>]*handleComponent=\{null\}` **엘리먼트 결합** 정규식(`stripComments` 경유)으로 gorhom 기본 핸들이 꺼졌는지 잠근다 — 1차는 리터럴 *존재*만 봐서 `handleComponent={null}`을 `<BottomSheetView>`로 옮겨도 green이던 구멍을 5-c에서 엘리먼트 바인딩으로 조였다(TRIP-735 `HANDLE_WIRING` 선례 동형, 개념 [[가드의 사정거리]]). **소스가드가 증명하는 것은 "prop이 그 자리에 있다"까지** — 기본 핸들이 실렌더에서 실제로 0개인지는 gorhom 목이 통과형이라 jest 사정거리 밖(실기·사용자 육안 대기, 개념 [[바텀시트 핸들 이중화 — gorhom 기본 인디케이터 끄기]]) |
| `src/__tests__/devPreviewPref.test.tsx` | 프리뷰 `pref1`·`pref2` 상태 렌더 가드(TRIP-163) — 빈 선택 상태로 직접 렌더, 가드 우회 아님 |
| `src/__tests__/devPreviewHome.test.tsx` | 프리뷰 홈 4키 가드(TRIP-170) — 딥링크 4키·토글 진입·미존재 키 splash 폴백 결정론 |
| `src/__tests__/onboardingEntryGuard.test.tsx` | 온보딩 진입 리다이렉트·완료자 방어 가드 |
| `src/__tests__/rootLayout.test.tsx` | 루트 부팅 골격 |
| `src/__tests__/rootLayoutSafeArea.test.tsx` | `SafeAreaProvider` 도입 후에도 자식이 렌더되는지 |
| `src/__tests__/devPreview.test.tsx` | 프리뷰 상태 렌더. 런타임 지뢰 목으로 네트워크 격리 |
| `src/__tests__/devPreviewDeepLink.test.tsx` | 프리뷰 딥링크 `?state=` 파라미터 → 초기 화면 결정론 가드(부재·오타·대소문자·빈 문자열·배열 값 → 전부 splash 폴백) |
| `src/__tests__/design-tokens.test.ts` | 디자인 토큰 가드 |
| `src/__tests__/openapiContract.test.ts` | **TRIP-179 신설** — `backend/docs/design/openapi.yaml`을 글자로 읽어 검사(리포에 YAML 파서 의존 0). `/stays/search` 경로 존재·`servers`가 `/api/v1`로 끝남·정의 없는 `$ref` 0건(계약 회귀 앵커) + `/stays/search` 파라미터 이름을 `['region','amenity','stayType']`로 순서 포함 완전일치(게이트①-2 보강 — 헤더가 약속한 "스펙 드리프트 잠금" 사정거리를 뮤테이션 테스트로 실측해 좁힌 결과, `sort`·`page` 같은 파라미터 추가가 그 전엔 무심판으로 통과했다) |
| `src/__tests__/rootLayoutQueryProvider.test.tsx` | **TRIP-179 신설** — 앱 루트를 실제 렌더해 `SplashGate` 자리(`queryClientProbe` 목) 안쪽에서 `QueryClient`가 잡히는지 확인(단언 3개: 던지지 않음·프로브 도달·진짜 인스턴스) |
| `src/__tests__/staySearchGenerated.test.ts` | **TRIP-179 신설** — 코드젠의 출력을 검사(`pnpm codegen`을 직접 돌리지 않고 커밋된 생성물을 fs로 읽음, 모든 스캔은 `stripComments` 경유). B-0 전처리 자기검증·B-1 생성 파일 목록 동결 8경로·B-2 심볼 존재·B-3 파라미터·B-4 `duration` 식별자 0(INV-3)·B-5 응답 표현력 6종. ⚠️ B-1은 완화형(하한목록 부분집합+개수 하한 75+태그 폴더 집합)이라 TRIP-619가 codegen 재유입한 U5 records 15파일은 이 가드 밖(content-무심판, 03b 참고-1) |
| `src/__tests__/visitCheckGenerated.test.ts` | **신규(TRIP-619, `@jest-environment node`)** — `staySearchGenerated.test.ts`와 같은 방식(커밋된 `generated/` fs 스캔)으로 `VisitCheck.updatedAt`(G2)·`adjustTimesRequest.expectedUpdatedAt`·`errorResponseError.{visitCheckId,serverUpdatedAt}`(G3) 3파일만 검사. **경고-1(03b) 해소**: G2가 non-nullable을 주석에 적었으나 정규식(`/updatedAt:\s*string/`)이 `string \| null`도 매치해 실제로는 안 잠갔던 구멍을, `NULLABLE_UPDATED_AT` 부정 짝을 추가해 뮤테이션 실측(nullable 변조→red)으로 봉합. 개념: [[낙관적 락 기준버전 (updatedAt)]] |
| `src/__tests__/recordsDurationStructure.test.ts` | **신규(TRIP-613)** — G1·G2. `features/record/ui/**` 재귀 스캔(폴더 자동편입이라 신설 `VisitTimeSheet.tsx` 포함)으로 소요시간 표기(`/(\d+\s*분\|\d+\s*시간\|소요)/`) 0건 단언(INV-3). ⚠️ **선재 `recordsStructure.test.ts`(G6, `layer-features-record.md` 관례)와 스캔 디렉토리·정규식이 사실상 동일한 기능적 중복**(03b 참고-2) — 신설 자체는 공허하지 않으나(둘 다 실`15분`에 red), 동적 `${m}분` 렌더 사각을 실제로 메우는 것은 `VisitTimeSheet.test.tsx` S5(렌더 층)다. 공식 위치는 `src/__tests__/`(전역 소스 스캔 관례)이지만 실질은 record 전용 가드. |
| `src/__tests__/recordAttributionStructure.test.ts` | **신규(TRIP-569, `@jest-environment node`)** — G2(★타임존 AC-6)·G3(★조회배선 AC-8). `stayAttribution.ts`에서 `new Date(`/`Date.now(` 0건(`Date.UTC(`·문자열 슬라이스는 허용, 개념 [[UTC 사전식 날짜 경계 (타임존 안전)]]) + `features/record/**`가 생성 훅 `useGetTripsTripIdBases`·`useGetSavedStays`를 직접 감싸는 긍정 앵커(cross-feature 벽 우회의 반대쪽 증거, 개념 [[생성훅 직접 사용 (cross-feature 우회)]]). AC-8 나머지(G2 import 0·G5 새 HTTP 0·G6 INV-3)는 `recordsStructure.test.ts`·`recordsDurationStructure.test.ts`의 재귀 스캔이 신규 파일을 자동 편입해 잠근다(이 파일에서 복제 안 함). dateTo 배제 경계 3케이스는 `stayAttribution.test.ts`(AC-6 행동 테스트) 소관 — 이 파일은 소스 텍스트만 본다. |
| `src/__tests__/recordPhotoBinaryGuard.test.ts` | **신규(TRIP-566)** — G0(탐지기 자가검사)·G1(신규 6파일 편입 앵커)·G2(바이너리/네이티브 유출 심볼 0 + `AddPhotoRequest` 실참조, INV-U5-03). `features/record`+`shared/photo` 그래프를 재귀 스캔 — 8금칙어(사진 바이너리·`storage_key` 등)와 `expo-image-picker`/`expo-media-library` import를 함께 잠가 네이티브 스텁 경계를 한 가드로 겸한다. 전처리는 **`stripComments` 후 스캔**이라 주석 속 리터럴은 안전(예: `shared/photo/index.ts`의 `ponytail:` 후속 메모), `://`(URL 슬래시)를 금칙어로 오인하지 않는다(문제로그 [[stripComments가 URL 슬래시 오인]] 계열 재발 방지). 스캔 범위를 `features/record`+`shared/photo`로 **한정** — `shared/api/generated/trips/trips.ts` 주석에 `storage_key`가 실재해 `shared` 전체로 넓히면 거짓 red. |
| `src/__tests__/reflectionFallbackStructure.test.ts` | **신규(TRIP-571, `@jest-environment node`)** — AC-8 표시본 단일출처 가드. `features/reflection/model/reflectionFallback.ts`에 `resolveDisplayNarrative`+폴백 토큰 존재(긍정) ∧ 그 파일 **제외**한 `features/reflection/**`에 `resolveDisplayNarrative` 참조 0건 ∧ `features/reflection/ui/**`에 `draftNarrative`·`editedNarrative` 참조 0건(화면이 자체 폴백을 못 만들게 — 페이지만 유일 호출자). `recordsStructure.test.ts`의 G2(cross-feature import 금지) 계열과 달리 이건 **같은 feature 안에서 특정 함수의 호출처를 한 곳으로 좁히는** 첫 사례 — 부재 확인형(소스 스캔)이라 새 화면·모델이 이 규칙을 어겨야만 red, 몰라도 안전(개념 [[가드의 사정거리]] 계열과 반대로 "넓혀서 안전"이 아니라 "좁혀서 안전"). |
| `src/__tests__/reflectionStructure.test.ts` | **신규(TRIP-571, `recordsStructure.test.ts` G1~G6 미러)** — 신규 파일 정본 경로 실재(라우트·페이지·`features/reflection/{model,ui}`) · **G2**: `features/reflection/**`가 다른 `features/*`(특히 `record`) 직접 import 0건 — `eslint.config.js`의 `FEATURES` 배열이 `['onboarding','home']`뿐이라(repo-traps "features 경계") record↔reflection 상호 import 금지의 **유일한 그물**. 라우트→페이지→화면 3층 · testID 4종 실재 · 새 HTTP 함수 0(생성 훅 재사용, G5) · `features/reflection/ui` 소요시간 문자열 0(INV-3, G6). **j04 요약 화면·모델은 G2·G5·G6에 자동 편입**(재귀 스캔이라 신규 8파일도 같은 경계로 이중 방어, `reflectionSummaryStructure.test.ts`가 위임해 중복 신설 안 함). **j06 공유 카드(TRIP-574)도 동형 자동 편입**(`features/reflection/**` 신규 6파일이 같은 G2·G5·G6 재귀 스캔에 자동으로 들어감, `shareCardStructure.test.ts`가 위임해 중복 신설 안 함). |
| `src/__tests__/reflectionSummaryStructure.test.ts` | **신규(TRIP-572, `@jest-environment node`)** — j04 요약 슬라이스 전용 소스가드. G0(탐지기 자가검사: `stripComments`×`DURATION_TEXT` 조합, `://` 보존·`14:30`·`840m` 미검출) · G1(신규 7파일 편입 앵커) · G4(testID 2종 소유 앵커: `reflection-summary-stats`→`TripSummaryScreen`, `reflection-summary-day-card`→`DayHighlightCard`) · AC-4(INV-3, `features/reflection/ui/**` 재귀 소요시간 0 + 신규 화면·카드 편입, 예외 화이트리스트 없음 — j04 DTO에 avgDwellMinutes 부재) · G3(3층: 라우트가 페이지만 위임·페이지가 `useTripSummary`·`TripSummaryScreen` 참조) · G5(`useTripSummary`에 customInstance·axios 0). 경계(G2)·재귀 INV-3(G6)·재귀 무HTTP는 선재 `reflectionStructure.test.ts`가 자동 편입해 이 파일은 요약 표면 전용 명시적 홈만 둔다(`recordsDurationStructure` 선례, 중복 신설 안 함). |
| `src/__tests__/shareCardStructure.test.ts` | **신규(TRIP-574)** — j06 공유 카드 슬라이스 전용 소스가드. 신규 파일 정본 경로 실재(라우트·페이지·`features/reflection/{model,ui}` 6파일) · testID 3종(`reflection-share-format-seg`·`reflection-share-save`·`reflection-share-export`) 실재 · **AC-5(BR-U5-46)**: 5파일 그래프에 금칙 11종(`react-native-view-shot`·`expo-media-library`·`expo-sharing`·`expo-file-system`·`/ai/v1`·업로드 계열) 0건 + `buildShareCard`·`captureShareImage` 실참조(긍정 앵커, `recordPhotoBinaryGuard.test.ts` 패턴 동형). 경계(G2)·재귀 INV-3(G6)·재귀 무HTTP(G5)는 선재 `reflectionStructure.test.ts`가 자동 편입해 이 파일은 공유 카드 표면 전용 명시적 홈만 둔다(`reflectionSummaryStructure` 선례, 중복 신설 안 함). |
| `src/__tests__/travelStyleStructure.test.ts` | **신규(TRIP-573, `@jest-environment node`)** — j05 여행 스타일 분석 슬라이스 전용 소스가드(`reflectionSummaryStructure`·`shareCardStructure`·`recordsCompareStructure` 선례 동형). G0(탐지기 자가검사) · G1(신규 9파일 편입 앵커) · G3(3층: 라우트→`@/pages/travel-style`만 위임·페이지→`useStyleAnalysis`+`TravelStyleScreen` 참조) · G4(testID 2종 소유 앵커: `reflection-style-bar`→`CategoryBarList`, `reflection-style-evidence`→`EvidenceLink`) · **G5(AC-4 저장 mutation 0, INV-U5-09)**: 신규 feature 6파일에 mutation 훅(`usePost\|usePut\|usePatch\|useDelete\|useMutation`)·`customInstance`·`from 'axios'` 0 + `useGetMeStyle` 참조(재사용 앵커) — 선재 `reflectionStructure.test.ts` G5는 `customInstance`/`axios`만 훑어 **mutation 훅 import는 사정거리 밖**이라(`recordsCompareStructure` G4 선례와 동형 gap-filler) 이 가드가 유일한 그물. **AC-6(라우트 경계)**: `app/records/style.tsx` 존재 + `app/records/index.tsx` 부재(부정 짝, `/records` 탭 충돌 회피). **feature 경계(맹점②)**: 신규 파일에 `@/features/settings`·`buildStyleCardModel`·`styleCardModel` 0건 — 선재 `reflectionStructure.test.ts` G2(재귀)와 이중 방어, `styleThreshold`가 `buildStyleCardModel`과 같은 `official` 계약을 병렬 판정하는 드리프트(Follow-up E 후속)를 이 경계가 원천 봉쇄하진 않는다. **지도 degrade(맹점③ N/A)**: 신규 파일에 `MapView`·`@/shared/map` import 0건 → `itineraryMapSurfaceStructure.test.ts` `LOCKED_CALLERS` 등재 불필요(계약 좌표 공백, `recordsCompareStructure` 선례 동형). 경계(reflection→record/execution import 0)·재귀 INV-3(`features/reflection/ui`, avgDwell 값 인터폴레이션이라 화이트리스트 없이 통과)는 선재 `reflectionStructure.test.ts`가 신규 파일을 자동 편입해 이중 방어(위임, 중복 신설 안 함). |
| `src/__tests__/recordsCalendarStructure.test.ts` | **신규(TRIP-575, `@jest-environment node`)** — j07 여행 캘린더(기록 탭 허브) 슬라이스 전용 소스가드(`recordsCompareStructure`·`travelStyleStructure` 선례 동형). G0(탐지기 자가검사) · G1(신규 8파일 편입 앵커) · G2(3층+셸 교체: 라우트가 `shell-tab-placeholder-records` 버리고 `@/pages/records-calendar`만 위임) · G3(testID 2종 소유 앵커: `record-calendar-month`→`TripCalendarMonth`, `record-calendar-past-trip`→`PastTripList`) · **G4(맹점② 경계 ★)**: 신규 record 파일에 `@/features/stay`·`@/features/trip` 0 + `recordsCalendar.ts`가 `@/shared/date` 참조(양방향 짝 — 부정+긍정, 세 벌째 재구현 원천 차단) + `monthGrid.ts` 순수(react/expo-router/타feature 0) · G5(읽기전용 — `useRecordsCalendar` mutation 0 + `useGetTrips` 앵커) · G6(INV-3 gap-filler — `shared/date/monthGrid`는 `recordsStructure`·`recordsDurationStructure` 재귀 스캔 밖이라 이 가드가 유일 그물, `recordsCompareStructure` G5 선례) · G7(`recordsCalendar.ts` 순수 — useGet/useQuery/react/expo-router/타feature import 0). 경계(record→reflection/execution import 0)·재귀 INV-3(`features/record/ui`)·재귀 무HTTP는 선재 `recordsStructure.test.ts`가 신규 record 파일을 자동 편입해 이중 방어(위임, 중복 신설 안 함). |
| `src/__tests__/tabsRecordsRoute.test.tsx` | **신규(TRIP-575)** — `(tabs)/records.tsx` 라우트 렌더 가드(`tabsHomeRoute.test.tsx`·`tabsItineraryRoute.test.tsx` 선례 동형). 카드→`push('/trips/{id}/records/compare')`(AC-4) · 빈상태→`push('/trips/new/step1')`+placeholder testID 부재(AC-5) · 월 이동 상태 전이(라벨 상대 변화·원복, AC-6). 파일-로컬 인라인 목(`expo-router`·`@/shared/api/generated/trips/trips`)만 쓴다 — 신규 mock/setup 0건. |
| `src/__tests__/mapBridgeStructure.test.ts` | **TRIP-197 신설** — 소스 스캔 7케이스(`@jest-environment node`). A-1 webview 버전 정합·A-2 키는 env 참조로만(+ 자기검증)·A-3 git 추적 전수에 키 리터럴 0(`.env.example` 동반 단언)·A-4 로컬 검색 지문 0·A-5 INV-3 `duration` 0·A-9 도메인 리터럴 정확히 1파일·1회·1-7 `@/features/` 0건. `stripComments`에 `(?<!:)` 룩비하인드 필수(게이트①-2 수정 — 없으면 `https://`의 `//`를 주석으로 오인해 URL이 스캔 전 사라진다, 결함 상세는 `[[반대 방향 앵커]]`·`02c_gate1-2_fix.md`). **A-3의 "git 추적 전수"는 `git ls-files -z`로 인덱스를 열거해 각 경로를 `readFileSync` 한다 — 화면 재작성 등으로 테스트 파일을 삭제할 때 `rm`만 하면(언스테이지) 인덱스엔 그대로 남아 있어 이 스캔이 디스크에 없는 경로를 열어 **ENOENT**로 FAIL한다(TRIP-665 실측, 구현 결함 아님). `git add`(또는 `git rm`)로 삭제를 인덱스에 스테이지해야 열거에서 빠진다 — 커밋 여부와 무관, 스테이지 여부가 관건 |
| `src/__tests__/tripDraftBoundary.test.ts` | **신규(TRIP-204)** — AC-8 소스 스캔, `tripDraft.ts`의 import를 **전이 의존까지** 순회(평면 금칙 목록은 `@/shared/api` 배럴 한 줄로 뚫린다 — 그 배럴이 axios를 끈다, 02a §6-⑥ 실측). `stripComments`는 `mapBridgeStructure.test.ts`의 `(?<!:)` 룩비하인드 대신 `` `:` 뒤 `//`는 주석으로 안 본다``는 동치 규칙을 `(^|[^:])`로 구현(2026-07-31 실사고를 회귀 케이스로 박음). **기존 4파일(`loginVisual.test.ts`·`staySearchStructure.test.ts`·`staySearchGenerated.test.ts`·`tabbarVisual.test.ts`)은 미수정 원본 정규식(`/\/\/.*/g`)을 아직 쓴다** — 문제로그 참고 |
| `src/__tests__/tripWizardEntryReset.test.tsx` | **신규(TRIP-288)** — 라우트/레이아웃 컴포넌트를 직접 import해 렌더하는 진입 앵커 테스트(선례 `onboardingPrefRoutes.test.tsx`가 `@/app/(onboarding)/_layout`을 직접 부르는 것과 같은 자리·형태). `TripWizardLayout`(`app/trips/new/_layout.tsx`)을 렌더해 마운트 시 `resetMustVisits()`가 도는지(FW-1) · 형제로 배치한 화면 효과가 부모 초기화보다 먼저 돈 뒤 다시 채워지는지(FW-2, React 자식→부모 효과 순서를 형제 배치로 재현) · 리렌더는 진입이 아니라 편집이 살아남는지(FW-3) · 초기화가 시드 3필드만 비우고 사용자 입력 8필드는 남기는지(FW-4, 게이트①-2 추가 — 차단-1 대응)를 잠근다. `jest.mock` 팩토리는 `@/test-support/expoRouterStackMock`을 `require`로 끌어온다(NativeWind babel 모듈 스코프 변수 충돌 회피). **jest가 증명 못 하는 것**: 실제 라우터가 이 컴포넌트를 정말 재마운트시키는지(D4, [검증] 6-b 실기 스모크 몫 — 이번 사이클은 이 축을 프리뷰로 확인 못 함, 문제로그 참고) |
| `src/__tests__/tripWizardStep1Boundary.test.ts` | **신규(TRIP-205)** — AC-14·AC-5·AC-1·AC-10c 소스 스캔(`@jest-environment node`, `tripDraftBoundary.test.ts`와 같은 `(^|[^:])` 룩비하인드 계승). 화면에서 전이 순회한 그래프에 쿼리 훅·라우터·`expo-location` 0건 · 화면·순수 함수 어디에도 `new Date(`·`Date.now(` 0건(기준일은 주입, 01b D5) · `validateTripDraft`·위반 코드 리터럴 0건 + `canProceed` 실재(긍정 짝) · 위저드 라우트 존재 + `(tabs)` 아래 부재(부정 짝). **TRIP-675로 시계 금지 스캔 대상에서 `DATE_SHEET`(`TripDateSheet.tsx`) 상수 제거** — S3(TRIP-667)가 이미 `PeriodEditSheet.tsx`로 대체해 소비자를 잃은 채 orphan 존치되던 파일이 이번에 삭제돼 스캔 모집단이 한 파일 줄었다(`DATE_PICKER`=`tripDatePicker.ts`는 생존이라 유지, describe/it 제목도 "두 파일"→"한 파일"로 정정) |
| `src/__tests__/tripWizardStep2Structure.test.ts` | **TRIP-206이 만든 `tripWizardStep2Placeholder.test.ts`(자리만 라우트 가드)를 TRIP-225가 대체.** `@jest-environment node` 소스 스캔(`@/pages`·`@/features` 넷째 조회 실측). AC-G1(라우트 `step2.tsx` 5줄, `View`/`Text`/훅 0건) · ★1 소스면(`baseScreen.ts`·`useTripBases.ts`·화면·배선 4파일 전수에 문자열 리터럴 `'GAP'`·`'OVERLAP'` 0건 — `blocked` 재판정 금지) · ★7 소스면(화면·배선에 `.sort(`·`` `${n}박` `` 0건 — 정렬·박 번호 재계산은 `toBaseSections` 하나만 소유). `stripComments`는 `tripDraftBoundary.test.ts`의 `(^|[^:])` 룩비하인드를 계승(머리말 Figma URL·설계 근거 주석 속 `GAP`/`.sort(` 오탐 방지, 실측 확인 완료). ⚠️ **TRIP-672(S8)로 ★1(GAP/OVERLAP 스캔)이 소멸(삭제)** — g02 default 재작성이 coverage 개념 자체를 제거해 주제가 없어짐. **★7은 완화 유지** — `` `${nightNumber}박` `` 라벨이 화면에 정당하게 실재하므로(신 디자인 요구), 박 라벨 포맷은 순수 헬퍼(`nightlyBaseCards`)로 빼고 화면·배선 소스에 `.sort(`·인라인 박수 재계산 0건만 계속 잠근다(AC-3 구조 스캔과 이중). ⚠️ **TRIP-673(S9)로 AC-3 needle 목록에서 `useAssignBase` 한 줄만 제거**(정당한 계약 플립 — S9가 배선에서 그 훅을 다시 잇는 것 자체가 정의) — 나머지 6 needle(`useTripCoverage`·`useUnassignBase`·`baseGate`·`useBaseFix`·`useFixSavedStay`·`useExtendTripPeriod`)은 그대로 유지(페이지 실측 0건, 뮤테이션으로 teeth 확인). describe 제목의 `·지정`을 `(D2 · 지정은 TRIP-673/S9로 재연결)`로 정정. test-designer가 이 needle 갱신을 02a 최초 작성에서 놓쳐(§6이 "무수정"으로 잘못 명시) 구현 후 재호출로 처리(문제로그 [[2026-09-08 test-designer가 대규모 화면 재작성에서 형제 테스트 스킵을 놓쳤다]] 참고). ⚠️ **TRIP-674(S10)는 무변경·green 무회귀** — test-designer가 02a 작성 착수 전 이 파일을 grep sweep으로 먼저 확인해 신 계약(empty 재도입)과 충돌 없음을 선판단(needle 목록 무변경 유지가 정당함을 사전 확인), 구현 후 재호출 0건(위 문제로그의 grep sweep 강화 지시가 처음 실효를 본 사례). ⚠️ **TRIP-675로 5-c 봉합** — `tripBaseGateStructure.test.ts` 통째 삭제(Option A)로 그 REMOVED_WIRING이 잠그던 `setPeriod` needle(살아있는 `tripWizardStore` 액션이라 tsc 사각, 이 파일 AC-3 6-needle 목록에도 없었다)이 대체 없이 증발할 뻔한 것을, AC-3 REMOVED_WIRING 배열에 **`setPeriod` 추가**로 그물을 이관해 봉합(뮤테이션 실측: page에 setPeriod 참조 주입→red, 원복→green — 상세는 03c_judge-seal_note). 나머지 6 needle(`useTripCoverage`·`baseGate`·`useBaseFix`·`useFixSavedStay`·`useExtendTripPeriod`)은 삭제 심볼이라 tsc가 이미 대체(그 이름들 자체가 더는 존재하지 않음), `useAssignBase`는 S9(TRIP-673)로 이미 needle 목록에서 빠짐 |
| `src/__tests__/tripBudgetStructure.test.ts` | **신규(TRIP-207)** — AC-5 로케일 안전 소스 스캔(`@jest-environment node`). `stripComments` 자가검사(주석 속 `toLocaleString`은 사라지고 코드·URL 줄은 살아남는지 먼저 확인) 후 `budgetAmount.ts`·`TripWizardStep1Screen.tsx`·`TripNewStep1Page.tsx` 3파일에 `toLocaleString`·`Intl.` 0건 — 동작 테스트는 node에서 로케일 API를 써도 통과하므로(node·Hermes 서식 갈림을 원리적으로 못 봄) 이 파일이 유일한 그물이다. 뮤턴트로 실사격 확인(code-critic 뮤턴트 #5, red) |
| `src/__tests__/savedPlacesStructure.test.ts` | **신규(TRIP-220)** — AC-9 상태 소유권 소스 스캔(`@jest-environment node`). "서버 DTO(`SavedPlace`/`savedPlaceId`/`savedAt`/`savedCount`/`dataStatus` 식별자)를 Zustand 스토어에 복사하지 않는다"(`frontend/README.md` §66) 부정 단언 — 두 정본이 반대로 적혀 있던 자리라(`frontend-components.md` §2 PlaceExplorer 문서 드리프트, README가 이김) 문서가 아니라 코드 쪽에 가드를 둔 것. 긍정 짝: `savedPlaces.ts`·`savedPlaceIndex.ts` 실재 + `savedPlaces.ts`가 `@tanstack/react-query`를 문다. **한계(설계상)**: DTO 이름을 안 쓰고 파생값만 복사한 스토어(예 `savedByPoiId: Record<string, boolean>`)는 못 잡는다 — `poiId`를 금칙어에 넣으면 정당한 UI 상태에 거짓 red가 나서 멈춘 것 |
| `src/__tests__/placeDetailStubRoute.test.tsx` | **신규(TRIP-446) → TRIP-456으로 재작성(스텁 동결 → d06 라우트 위임 테스트, 동일 경로 유지·삭제 아님).** 원래는 `explore/places/[poiId].tsx` 스텁 라우트 심판 4케이스(준비중+poiId 에코·canGoBack 폴백 2종)였으나, d06 실화면 배선이 스텁 testID(`place-detail-stub-*`)를 무효화해 **라우트 위임(R1)만** 검증하도록 재작성 — `@/pages/place-detail` 목으로 실훅을 차단해 `QueryClientProvider` 없이 렌더(스텁 테스트 INV-1 무조회 선례 계승). 게이트① 프리즈 해시가 이 파일 변경을 잡으므로 의도적 교체임을 명시(승인 발화·새 해시로 재기록) |
| `src/__tests__/placeExploreStructure.test.ts` | **신규(TRIP-221)** — d04 소스 스캔 9케이스(자가검사 1 포함, `@jest-environment node`). AC-G2(INV-3 `duration` 0건) · AC-G3(zustand import 자체 0건 — 이 칸 모집단의 파생값 스토어 구멍까지 메움) · AC-G4(이미지 URL 발명 금지, `imageUrl` 실사용 긍정 짝) · AC-G5(화면 순수성 — `useState`·쿼리 훅·라우터 0건) · AC-G6(`@/features/stay`·`@/features/trip` import 0건, lint가 못 잡는 자리를 소스 스캔이 대신 잠금 — `eslint.config.js`의 `FEATURES` 배열에 `explore`·`stay` 둘 다 없음) · AC-G7(토큰화된 9색 raw hex 0건) · AC-V2(SafeArea 구조). **모집단이 `features/explore`·`pages/place-explore`·`app/explore` 디렉토리 재귀**라 TRIP-222 신규 파일이 자동 편입됐다 |
| `src/__tests__/regionCatalogStructure.test.ts` | **신규(TRIP-445)** — 지역 카탈로그 서버 연동(`GET /regions`) + '내 주변' 완전 제거 소스 스캔(자가검사 1건 포함 6케이스, `@jest-environment node`, `stripComments`는 `(^|[^:])//` 룩비하인드 계승 — URL 리터럴이 주석으로 오탐되지 않는지 자가검사로 확인). AC-1(프로덕션 소비처 3파일에 `REGIONS`·`RegionCode`·`REGION_TINT` 0건 + `regionTint`/`useRegions` 참조 긍정 짝) · AC-1(`regions.ts` 자체에 `useRegions`·`regionTint`·`filterRegions` 실재 + `REGIONS`·`RegionCode` 0건) · AC-4(`RegionPickerPage.tsx`·`TripNewStep1Page.tsx` 둘 다 `useRegions` 참조 + `REGIONS` 미참조) · AC-7('내 주변' 배선 소스 완전 부재 — `resolveNearby`·`onSelectNearby`·`NearbyState`·`firstConfirmedSavedStayCoords`·`lat=&lng=`·`explore-region-nearby` 0건 + `resolveNearby.ts` 파일 부재 + `ExploreGlyphs.tsx`에 `NearbyPinGlyph` 0건, 고아 제거 확인) · 구조가드(`.sort(` 0건 — 서버가 정렬 순서를 보장, 화면 재정렬 금지). ⚠️ `regionTint` 팔레트 hex는 `placeExploreStructure.test.ts`의 raw-hex 스캔(`RegionPickerScreen.tsx` 제외)에도, 이 파일에도 안 걸린다(hex 자체는 미검사 대상 — repo-trap 참조) |
| `src/__tests__/sharedUiStructure.test.ts` | **신규(TRIP-222)** — `shared/ui` 디렉토리 재귀 소스 스캔(`@jest-environment node`, AC-G2). `StateNotice` 승격으로 리포에 없던 "`shared/ui`를 훑는 스캔"을 신설 — 경계(`@/features/`·`@/pages/`·`zustand` 0건) · INV-3(`duration` 등) · URL 리터럴 0건 · raw hex(면제 `BottomTabBar.tsx` 자기검사 포함) · 승격이 **이동이지 복제가 아님**(구 경로 파일 부재 + 신 경로 파일 존재 + `<StateNotice` 렌더 확인). ⚠️ **`useState`·`useReducer`·`expo-router`·`@tanstack/react-query`·`axios` 5항목은 이 파일 모집단 밖**(`FORBIDDEN`이 3항목뿐)이라 승격 전 `staySearchStructure.test.ts`가 잡던 그 5항목은 `eslint.config.js`의 파일 단위 블록이 대신 잡는다(code-critic W-3, 위 `shared/ui/StateNotice.tsx` 행 참고) |
| `src/__tests__/placeExploreStateStructure.test.ts` | **신규(TRIP-222)** — d04 상태·예외 소스 스캔 7케이스(`@jest-environment node`). AC-G3(판정 단일 출처 — `resolvePlaceListState`를 페이지만 호출) · AC-G7(새 prop 전부 옵셔널) · 타이머 금지(`setTimeout`·`setInterval` 0건, Seed Q5 — `setImmediate`·`requestAnimationFrame`·`InteractionManager` 우회로는 스캔 밖, 미룸) · 좌표 배선 존재 · 토큰 사정거리 확장(신규 스켈레톤·배너가 `PlaceExploreScreen.tsx` 지역 함수라 파일이 안 늘어 기존 스캔만으로 충족) |
| `src/__tests__/pagesLayerStructure.test.ts` | **신규(TRIP-223)** — `src/pages` **층 전수 재귀** 소스 스캔(`@jest-environment node`, 자가검사 1건 포함 7케이스). `pages` 슬라이스는 지금까지 `place-explore`·`stay-search` 등 **완전일치 모집단**만 각 스캔이 봤고, 새 슬라이스(`saved-places`)는 그 밖이었다 — TRIP-222의 `shared/ui` 승격 때(스캔 5항목을 잃었던 것)와 **같은 계열의 구멍**이라 이 칸이 층 전체를 훑는 스캔을 신설했다. 부정 전수: `duration`(INV-3) · `zustand`(README §66) · `https?://`(INV-1) · `setTimeout\|setInterval`(Seed Q9) · 토큰화 9색 raw hex — 전부 0건 + 긍정 짝(새 페이지가 실제로 훅을 부른다, 공집합 공허 통과 방지). d02 3층 책임(라우트·배럴·페이지·화면 5개 파일 실재 + 각 파일의 import 경계)도 이 파일이 잠근다 |
| `src/__tests__/devPreviewMap.test.tsx` | **TRIP-197 신설** — 프리뷰 `map-default` 상태 키 1개가 지도 컴포넌트(`map-root`)를 렌더하는지만 확인(층 C 실기 확인의 진입점이 실제로 열리는지). env를 세팅하지 않아 **항상 키 없음 분기(`map-failure`)만 밟는다** — 해피패스는 이 파일에서 0회 실행(설계 의도, code-critic N4) |
| `src/__tests__/socialSdkSecrets.test.ts` | **신규(TRIP-210)** — 소셜 SDK 키·시크릿 소스 스캔 4묶음(AC-7·AC-8′). `.env`·`.env.local` 미추적 + `.env.example` 4변수 이름만(값 없음) · git 추적 **전 파일**(확장자 무관) 전수에 `VAR=<값>` 대입 0건 · `app.config.ts`의 env 참조(주석 밖) + 32-hex 리터럴 0 · 네이버 시크릿이 코드·주석 양쪽에 등장(raw>stripped로 판정, ★D5) + README 명시. ⚠️ **게이트①-2에서 대입 탐지기를 수정했다** — `=` 뒤 `\s*`가 개행을 건너 값 없는 변수가 연달을 때 다음 줄 첫 글자를 값으로 오판했다(자가검사가 전부 한 줄짜리라 못 잡음). 공백 클래스를 `[^\S\n]`로 통일 + 여러 줄 자가검사 4건 추가 |
| `src/__tests__/socialSdkConfigPlugin.test.ts` | **신규(TRIP-210, 게이트①-3 — code-critic 경고3 대응)** — `app.config.ts`의 kakao·naver config plugin 등록 소스 스캔. 튜플 등록(패키지명 2개) · 옵션 키 이름 허용목록(`kakaoAppKey`/`urlScheme`) · 값이 실제로 그 provider의 env에서 오는지 · **카카오·네이버 값이 서로 뒤바뀌지 않는지**(교차 배선) 4중 단언. `plugins` 배열 타입이 `[string, any]`라 tsc가 옵션 오타·값 교차를 못 잡는 자리를 메운다 |
| `src/__tests__/devPreviewExplore.test.tsx` | **신규(경량 사이클 20260805, TRIP-221·223)** — 프리뷰 탐색 2키(`places-results`·`saved-places-results`) 진입점 가드. `devPreviewMap.test.tsx`와 같은 구조. **루트 렌더만 보지 않는다** — `explore-places-card-*`·`explore-saved-item-*` 항목이 실제로 1개 이상 그려지는지까지 잠근다(빈 목록도 통과하는 함정을 막으려는 설계) |
| `src/features/trip/ui/StaySelectSheet.test.tsx` | **신규(TRIP-673, S9, 11케이스)** — g02 숙소 선택 시트 유닛(props-only). 단일밤 구간 값(★1)·단일 선택 accessibilityState(★2)·지정 disabled 3단(★3, `toBeDisabled()`+실제 press+핸들러 0회 3단)·실패 인라인(★4)·가격/사진 미렌더(★5, `원|₩`·`\d+m|km` 렌더 0)·empty(★6, `-cand-` 0)를 잠근다. `@gorhom/bottom-sheet` 통과형 목이라 실개폐·딤은 사각(6-b 전용) |
| `src/pages/trip-new-step2/ui/TripNewStep2Page.staysheet.integration.test.tsx` | **신규(TRIP-673, S9, 6케이스 I1~I6)** — 카드 탭→시트 오픈→선택→지정 배선 통합(실화면+실시트+실 파생 `nightlyBaseCards`/`deriveEndDate`, 목은 조회 3종+라우터뿐). I2는 `useAssignBase` mutate 인자 `{tripId,data:{savedStayId,dateFrom,dateTo}}` **완전일치**(`toEqual`)라 dateTo=date·+2일·dateFrom 오프셋 뮤턴트를 전부 red로 잡는다. I3 성공→닫힘, I4 실패→유지+인라인 오류(스왑 뮤턴트 상호 검출), I5 단일 선택 교차 전환 |
| `src/__tests__/staySelectSheetStructure.test.ts` | **신규(TRIP-673, S9, 6케이스, `@jest-environment node`)** — g0 자가검사(탐지기 실측)+g1(시트 props-only — `useState`·router·query·store·타 feature import 0)+g2~g4(가격/사진 미렌더 소스 짝 — `require(`·URL 0). 대상 파일 미존재 시 `existsSync` false/read throw로 정당 red(module-not-found 계열) |
| `src/__tests__/itineraryMustVisitStructure.test.ts` | **신규(TRIP-296)** — h05·h07 소스 층 가드(`@jest-environment node`). `pagesLayerStructure.test.ts`의 재귀 스캔이 `pages/itinerary-mustvisit/`를 자동 편입해 잡는 항목(`duration`·zustand·URL·타이머·raw hex)에 더해, `TripNewStep1Page.tsx`의 409 판정 공용 승격(D9)을 직접 요구하는 케이스(C36)를 포함 |
| `src/__tests__/itineraryDraftStructure.test.ts` | **신규(TRIP-297)** — h11 소스 층 가드(`@jest-environment node`). G2(라우트 두께, 영구)·G4(타이머 금지, 영구 — `pages`+`features/itinerary` 전체에 `setTimeout`/`setInterval` 0건, **수단**만 봄. 폴링이 실제로 옳게 도는가는 `draftView.test.ts` M8이 **결과**로 별도 확인, 개념 [[가드의 사정거리]] 실측 10)·G3·G5(심볼·경로 계약 스냅숏, 한시적). `pagesLayerStructure.test.ts`의 재귀 스캔이 `pages/itinerary-draft/`를 자동 편입한다(편입 앵커만 두고 복제하지 않음). `startAt`/`endAt` 소스 스캔은 **만들지 않았다** — 고정 블록 예외 때문에 소스에 `startAt`이 등장하는 것 자체가 정당해 소스 스캔으로는 이 계약을 표현할 수 없다(`frontend-components.md` §6이 예고한 이름과 다른 이유). **TRIP-791로 G3 앵커 뒤집힘** — `ZeroCandidateScreen.tsx` **존재** 앵커를 **부재 확정**으로 플립(앵커 선-갱신 → implementer `git rm` 순서, `PlaceAddCard` REMOVED_CARD 선례) |
| `src/__tests__/itineraryMapSurfaceStructure.test.ts` | **신규(TRIP-339, 11케이스)** — h05·h11 공용 지도 대본 소스 스캔(`@jest-environment node`). 조합 자가검사(주석 지우기 × URL 스캔이 서로를 안 지우는지) · 옵트인 경계(`src/` 전체를 훑어 지도 태그 있는 파일이 알려진 5개와 정확히 같은지 + 잠글 곳 밖 프롭이 없는지) · 사진 유출(지도 핀 계약 3칸·h11 화면 계약 9칸에 `require(`·`@/assets` 0건) · 외부 URL(픽스처에 `http(s)` 리터럴 0건) · 사진 실재(`require('@/assets/…')` 경로가 디스크에 있고 같은 폴더에 출처 표기가 있는지) · 좌표 간격(최장 4km 이하·최단 300m 이상 + 서울↔부산 자가검사) · ~~색 팔레트 부분집합~~(`shared/map`의 hex가 `tailwind.config.js` 팔레트 안에 있는지 — TRIP-297 신설, **TRIP-864로 S7 삭제**: 당시 네이티브 전환 직후 raw hex 0건이라 심판 대상이 소멸했었다. **TRIP-745로 raw hex가 재등장**(핀 3상태·현재위치·경로선 색, SVG는 className을 못 받는 shared/map 관례 — 01b Q3에서 재도입 안 하기로 확정 재확인: AC 자체 색 단언이 대신 잠근다). `stripComments`는 `:` 뒤 `//`를 주석으로 오인하지 않는 룩비하인드를 계승(`mapBridgeStructure.test.ts` 계열). **TRIP-442로 옵트인 경계 명부(`OPEN_CALLERS`) 갱신** — 신설 `pages/live-location/ui/LiveLocationPage.tsx`(기존 롱프레스로 좌표 확정하는 것이 기능 자체라 `viewOnly` 미전달, `LiveMapScreen` 계열)를 등록, `openTags` 6→7·`defaultTags`(`connectPins` 무언급=기본값) 9→10. 신규 지도 호출부가 이 명부에 없으면 red(fail-closed) — 6-a 1차 FAIL 원인이 이 미등록이었다(repo-traps 하네스 규칙 후보 참고). **TRIP-563으로 두 번째 반복** — `LOCKED_CALLERS`에 `features/planb/ui/{ReplanDraftScreen,NoAlternativeScreen}.tsx` 등재(둘 다 `viewOnly` 잠긴 글랜스, h05·h11 계열) + 파생 `defaultTags` 10→12(S8, `LOCKED_CALLERS` 모집단 공유 카운트). 6-a n=1 FAIL 재현 후 test-fix(02b)로 정정 — TRIP-442 때도 같은 미등록으로 한 번 걸렸는데 후보 판정만 남고 규칙화되지 않아 재발(개념 [[소스 스캔 가드의 폴더 전수와 자동 편입]], 문제로그 [[2026-08-30 지도 옵트인 경계 명부 미등재가 두 번째로 반복됐다]]). **TRIP-571로 세 번째 반복** — 신설 `features/reflection/ui/DailyReflectionScreen.tsx`(j03 회고 히어로 지도, viewOnly 글랜스, `TripRecordsScreen` 선례 동형)가 `LOCKED_CALLERS`에 미등재라 6-a n=1이 또 FAIL(`defaultTags` 13→14로 정정). 문제로그에 세 번째 사례로 추가, 하네스 규칙 후보 상태는 아직 미채택(다음 사이클도 test-designer 체크리스트 항목 부재 시 재발 가능성 있음). **TRIP-572로 처음 선반영 성공** — `features/reflection/ui/TripSummaryScreen.tsx`(j04, viewOnly 글랜스)를 test-designer가 01b 설계 단계부터 `LOCKED_CALLERS`에 등재(`defaultTags` 14→15)해 6-a가 이 축으로는 FAIL하지 않았다(3번째 재발을 이 사이클이 끊음 — 실제 6-a n=1 FAIL 원인은 별개인 `structure.generated.md` 드리프트였다). **TRIP-675로 `OPEN_CALLERS`에서 `TripBaseFixSheet.tsx` 제거**(g02 D2 배선 제거 후 파일째 삭제 — 인터랙티브 지도를 바텀시트에 넣은 유일 사례였다) + 파생 카운트 `openTags` 7→6·`defaultTags` 15→14. **TRIP-789로 두 번째 선반영 성공 + `NO_LINE_CALLER`가 배열로 바뀜** — `features/itinerary/ui/GeneratingScreen.tsx`(h07 loading, 신설 지도 카드)를 test-designer가 02a 단계에서 `LOCKED_CALLERS`에 선등재(10→11, TRIP-572 이후 두 번째 선반영 성공). `MustVisitPickerScreen.tsx`(h05)에 이은 **두 번째 no-line caller**라 단일 상수였던 `NO_LINE_CALLER`가 `NO_LINE_CALLERS`(배열)로, S8의 `lineOffTags`도 `flatMap` 산식으로 바뀌었다 — `defaultTags`는 13으로 우연히 불변(개념 [[소스 스캔 가드의 폴더 전수와 자동 편입]] TRIP-789 절). **TRIP-791로 세 번째 선반영 성공** — `features/itinerary/ui/GenerationFallbackScreen.tsx`(h07 폴백 인터스티셜, 핀 route 실선 지도 카드)를 test-designer가 02a 단계에서 `LOCKED_CALLERS`에 **line caller**로 선등재(`defaultTags` 13→14). **S3(`DraftScreenProps` 동결)도 이 사이클에서 15→14필드로 플립** — `fallbackNotice` 제거(뮤테이션 실측: 14필드 기준선에서 프로퍼티형 필드 추가 시 red 확인). **TRIP-746으로 `OPEN_CALLERS`에서 `features/execution/ui/LiveMapScreen.tsx` 제거**(파일째 삭제, `openTags` 4→3) + **신규 S2b**(옵트아웃 경계) 추가 — 옛 `LiveMapScreen`이 지켰던 "여행 중은 자유 탐색"을 이제 i01 허브가 `MapSheetShell`에 `mapViewOnly={false}`로 이어받는다. 셸은 `<MapView>`를 감싸므로 `<MapView\b` census(태그 단위 S2) 밖이라, `mapViewOnly={false}` 리터럴을 쓰는 파일 전수 = `LiveHubView.tsx` 하나뿐임을 별도 census(`SHELL_UNLOCK` 정규식)로 잠근다 — 새 소비처가 셸 잠금을 풀면 이 가드가 먼저 걸려 사람 판단을 요구한다(fail-closed) |
| `src/__tests__/planbReplanDraftStructure.test.ts` | **신규(TRIP-563, `@jest-environment node`)** — i13·i16 소스 층 가드(G1~G8, `planbManualStructure.test.ts` 패턴 계승). G1(탐지기 자가검사) · G3(INV-3 — 신규 5소스 소요시간 0건, 주석의 "소요시간" 어휘는 `stripComments` 후라 미검출 확인) · G4(무쓰기 — `useApplyReplan`·itinerary PUT·apply codegen 훅 import 0, `useReplanSession`·`resolveReplanState` 긍정 짝) · G5(화면 순수성 — 3화면 소스에 판정/조회/라우팅 심볼 0, dispatch는 페이지 1곳). `executionDurationStructure`·`itineraryMapSurfaceStructure`(광역) 자동 편입과 조합해 이중 그물 |
| `src/__tests__/liveLocationRoute.test.tsx` | **신규(TRIP-442)** — i20·i21 라우트 얇은 위임 가드(R1). `jest.mock('@/pages/live-location')`로 `LiveLocationPage`를 props 캡처 스파이(null 렌더)로 치환 + `jest.mock('expo-router')`의 `useLocalSearchParams`가 `{tripId,state}` 목값 반환 → 라우트가 그 값을 그대로 넘기는지만 확인(`placeDetailStubRoute` 관례, 구현 전 모듈 미해석 red). **참고**: `state` 목이 한 쌍만 반환해 "params가 흘러간다"가 아니라 "그 값이 도달한다"까지만 증명(라우트가 state를 하드코딩해도 통과, 03b 참고-1) |
| `src/features/itinerary/ui/GenerationFallbackScreen.test.tsx` | **신규(TRIP-791)** — h07 인터스티셜 화면 심판(RNTL 3동작 뼈대). S0 자가검사(`toHaveTextContent`/`getByText` 완전일치 + INV-3 탐지기 `DURATION_TEXT`가 "3곳"·"4 / 4"는 안 잡고 "30분"은 잡음을 실 문자열로 확인 — node_modules `matches.js` 실측 근거)·AC-1~6(성공 변형: 앱바·메시지카드 카피 완전일치·체크리스트 4행(N=`it.each([0,3])`)·안내바·CTA 2종)·AC-7~8(하드실패 변형·상호배타)·AC-9(INV-3 렌더 텍스트 전수 스캔). `jest.mock('@/shared/map', () => require('@/test-support/mapViewMock'))`로 지도 실물 회피(리포 관례) |
| `src/__tests__/itineraryTimeStructure.test.ts` | **신규(TRIP-299)** — h25 INV-3 소스 층 가드(`@jest-environment node`). 탐지기 자가검사(`stripComments` 걷힘·시각 문자열은 미검출·URL 슬래시 보존) + `features/itinerary/ui` **폴더 재귀 전수**에 소요시간 문자열 0건(새 파일만 X — 기존 5화면이 이미 0건이라 성립, 자동 편입 — 개념 [[소스 스캔 가드의 폴더 전수와 자동 편입]]) + 라우트 두께(`app/trips/[tripId]/itinerary/index.tsx`) 앵커 |
| `src/__tests__/itineraryEditStructure.test.ts` | **신규(TRIP-302, 슬라이스1)** — h24 편집 화면 소스 층 가드(`@jest-environment node`). detector를 복제하지 않고 (a) 신규 파일 5종이 정본 경로에 실재 (b) `features/itinerary/ui`·`src/pages` **재귀 모집단**에 새 화면·페이지가 자동 편입(개념 [[소스 스캔 가드의 폴더 전수와 자동 편입]]) (c) 새 화면·스토어·페이지에 소요시간 문자열·raw hex 0건을 앵커. 라우트·배럴 두께도 확인 |
| `src/__tests__/itineraryEditSheetStructure.test.ts` | **신규(TRIP-302, 슬라이스3)** — h24 시각조정 시트 소스 층 가드(`@jest-environment node`). `itineraryEditStructure.test.ts`와 같은 패턴 — `features/itinerary/ui` 재귀 스캔이 `SlotTimeSheet.tsx`를 자동 편입해 소요시간 문자열·raw hex 0건을 잰다(개념 [[소스 스캔 가드의 폴더 전수와 자동 편입]]) + `ItineraryEditScreen.tsx`가 `@gorhom/bottom-sheet`·`useItineraryEditStore`를 직참조하지 않는지(화면 순수성) 소스 스캔으로 확인 |
| `src/__tests__/tabsItineraryRoute.test.tsx` | **신규(TRIP-299) — 구 위치 `src/app/(tabs)/`의 `itinerary.test.tsx`(게이트①동결, 내용 해시 `0186479a...`) → TRIP-401로 개봉·재작성(해시 `6b8acc82...`) → TRIP-468로 재작성 2차: 리다이렉트 계약을 "내 여행" 목록 뷰 계약으로 전면 뒤집음(해시 `17881c19...`, TRIP-401·371 선례로 새 사이클 개봉 정당).** 6-b 실기 스모크 n=1이 부팅 레드박스를 잡아 이 자리로 이동했던 내력은 유지. **TRIP-468 재작성**: 여행 2건+ → 모두 카드 렌더+무리다이렉트(AC-1) · 빈 배열 → empty+create-trip(AC-2, empty/create-trip testID는 구 계약에서 유지·개명) · `isPending` → 스켈레톤 2장(AC-3) · 최신순 정렬(AC-7, `getAllByTestId(/^my-trip-card-/)` 트리 순서 배열 비교) · 카드 탭 = **눌린 카드의 tripId** 목적지(AC-6, `[0]`/`sorted[0]` 고정 뮤테이션 검출 — 정렬상 첫째가 아닌 둘째 카드를 눌러 검증) · todayInTrip→live(AC-6b) · 배지·부가정보 카드별 itinerary GET 파생 + 로딩 degrade(AC-5). **구 리다이렉트 판정(404→method 등)은 폐기가 아니라 카드 탭(AC-6)으로 이관** — `resolveItineraryDestination` 재사용은 유지, 트리거만 Redirect→카드 press. `expo-router`의 `Redirect`는 **일부러 목으로 남겨** `redirect-href` 마커 부재를 무리다이렉트 트립와이어로 쓴다(★4, 목록 뷰가 실수로 리다이렉트 회귀하면 잡음). `useGetTripsTripIdItinerary`를 `.mockImplementation((tripId)=>script[tripId])`로 카드별 다른 응답을 준다(N+1 재현, QueryClientProvider 불필요). **TRIP-788 갱신**: AC-5 계약 갱신 — CONFIRMED 카드 extra가 `'확정 장소 3곳'`(슬롯합계)에서 `'추천안이 준비됐어요'`(Figma 문구, `deriveTripCardFace` 소비)로 **파괴적 변경**(옛 계약이라 test-designer가 갱신 안 하면 옳은 구현이 red). AC-8 신규 describe(empty illustration·2줄) — `itinerary-tab-empty-illustration` present + `className` `bg-surface-soft` 포함·`bg-primary-pale` 부재(회색 원 잠금) + `itinerary-tab-empty-calendar` present + 부제 `.props.children` `toBe('...\n...')` 직단언(`toHaveTextContent`는 `\n`을 공백 정규화해 못 봄). |
| `src/__tests__/tabsHomeRoute.test.tsx` | **신규(TRIP-370) → TRIP-371로 개봉·재작성**(370 게이트① 동결분, 새 사이클이라 개봉 정당) — `(tabs)/index.tsx` 홈 라우트 배선 가드. `tabsExploreRoute.test.tsx`·`tabsItineraryRoute.test.tsx`와 같은 자리(라우트를 직접 렌더해 실제 내비게이션을 관찰) — 370-AC-1 3케이스(FAB→`/trips/new/step1`·담은 곳→`/explore/saved-places`·지금 뜨는 장소 더보기→`/explore/places`를 `mockPush.mock.calls` deep 동치로 확정, 무회귀 유지) + **371 AC-1/2/3 추가**: `useGetTrips`/`useSavedPlaces` 목(파일-로컬 `jest.mock`) — 비-ENDED 있음→`home-trip-hero` present+`home-magazine-hero` absent(AC-1a) / 빈·전ENDED→discovery(AC-1b/c, 선제 green) / 카드 텍스트가 실 `Trip` 값(AC-2, fixture 문자열 아님) / `isPending`→스켈레톤(AC-3a, red) / `isError`→discovery(AC-3b, 선제 green — 형제 `itinerary.tsx` W1 반대 계약). `jest.mock('expo-router', ...)` 팩토리는 `useRouter: () => ({ push: mockPush })`로 지연 참조(hoist 함정 회피, place-explore 선례 차용). **TRIP-401 인프라 개봉(단언 무변경)**: `useGetTripsTripIdItinerary: jest.fn()` 목 1개를 `jest.mock('@/shared/api/generated/trips/trips')`에 추가 — 홈 라우트가 planning일 때 지배 여행 itinerary GET을 무조건 호출(React 훅 규칙)하게 되면서, 이 파일의 planning-트립 케이스가 미목킹 훅으로 크래시하는 것을 막는 무해 스텁(`tabsShell`의 TRIP-371 `useGetTrips` 스텁과 동일 계열, layer-test.md 관례). **TRIP-695 추가**: `useSavedStays` 딥 경로 목(`tabsShell` 동형) + `🔴 695-AC-4` describe — `savedPoiIds.length`(장소)·`savedCount`(숙소)에 **다른 값**(2·5)을 줘 라우트가 각 배지로 올바로 흘리는지(교차배선 뮤턴트 red) 확인 |
| `src/features/itinerary/model/itineraryDestination.test.ts` | **신규(TRIP-401, co-located) → 20260923 경량 사이클(TRIP-751 계열, 티켓 없음)으로 CONFIRMED 행 갱신** — `resolveItineraryDestination` 표 전수 `it.each` 5행(404→method·PARTIAL→generating·FAILED→draft·COMPLETE+PLANNED→draft·**CONFIRMED→live**). 입력 타입은 `Parameters<typeof resolveItineraryDestination>[0]`로 함수 시그니처에 고정, enum 값은 `ItineraryGenerationState`/`ItineraryStatus` 상수 사용(문자열 오타 방지) |
| `src/__tests__/tabsHomeItineraryCta.test.tsx` | **신규(TRIP-401)** — 홈 여행 카드 CTA(`home-trip-hero-cta`) 목적지 push 가드. 라우트 통째 렌더 + `useRouter().push` 목 관찰. 게이트①: 지배 planning 여행 1개 + itinerary 상태(404·PARTIAL)를 seam으로 주입 → press → `mockPush`가 탭과 **같은 목적지 문자열**로 정확 호출(AC-5 공유 증명, 트립 일치는 강요 안 함). **게이트①-2 추가(INV-4 잠금, code-critic 경고-1 봉합)**: 로딩 중 press→`mockPush` 미호출 · 비-404(500) 오류 press→미호출(둘 다 미정착 오이동 금지, 뮤테이션 실측 — 가드 조기반환 주석처리 시 이 2케이스만 red). **TRIP-695 추가 필요분(형제 목 누락, 02a가 놓침 → 구현 후 7 it 전멸 FAIL → 오케 5단계 정정)**: `jest.mock('@/features/stay/model/savedStays', () => ({useSavedStays: () => ({savedCount: 0})}))` — 이 파일도 `<HomeRoute/>`를 통째 렌더해 같은 QueryClient 크래시를 겪는다. traps-shell "새 훅이면 새 목"의 사정거리가 **같은 라우트를 렌더하는 형제 파일 전수**(tabsHomeRoute·tabsShell·tabsHomeItineraryCta 3곳)임을 실측 — 새 훅을 추가할 땐 `git grep 'render(<.*Route'`로 형제를 전수 확인할 것 |
| `src/features/itinerary/ui/MethodPickerScreen.test.tsx` | **신규(TRIP-404, co-located)** — h04 동시 생성 낙관적 스텁 화면 테스트 AC-1~4. AC-1(미차단 완전AI→콜백, 사유 없음)·AC-4(뒤로/copick 무회귀)는 구현 전 선제 GREEN(회귀 앵커) · AC-2(차단+사유, BR-U3-01)·AC-3(안내 액션)은 RED→구현 후 GREEN. 게이트②에서 code-critic 경고 대응으로 심판 보강(`not.toBeDisabled()`·사유 문구·안내 문구 `getByText`) — 뮤테이션 실측(disabled 제거·BLOCKED_REASON=''→ 강화 후 AC-2 RED). |
| `src/pages/itinerary-plan/ui/ItineraryPlanPage.escape.integration.test.tsx` | **신규(TRIP-402, co-located)** — h25 4얼굴(loading·notFound·failed·listed) 탈출구 승인 테스트. msw 핸들러로 얼굴 강제(훅 목킹 금지, 판정 회귀 가시성 보존) + `useRouter` 목 `{back,push,replace,canGoBack}` 4종(리포 선례 0인 신규 API — 목이 없으면 구현이 옳아도 거짓 red). AC-1~7 8 it(6 red+2 선제green) — notFound·failed·loading 뒤로가기(`itinerary-view-back`)·notFound "일정 만들기"(`itinerary-plan-create-cta`→h04 method)·딥링크(`canGoBack()===false`→`replace('/(tabs)')` 리터럴 완전일치, `/(tabs)/itinerary`는 trips[0] 리다이렉트 함정이라 금지)·listed 무회귀+사각봉합. 기존 동결 `ItineraryPlanPage.integration.test.tsx`는 무변조(별 파일, canGoBack 없는 구목이지만 handleBack을 press 안 해 무사, 개념 [[router.canGoBack() — 딥링크 히스토리 없음 폴백]]) |
| `src/__tests__/itineraryManualStructure.test.ts` | **신규(TRIP-338)** — h19·h20 소스 층 가드(`@jest-environment node`). G1(자가검사 — `TRAVEL_TIME=/이동\s*시간\|이동시각/`가 "이동시간을 AI가…"=true·"이 시간에 장소 추가"=false를 실측), G2~G4(신규 3파일 직접 스캔 + `features/itinerary/ui`·`pages` 재귀 스캔 자동 편입 — `itineraryTimeStructure.test.ts`의 폴더 전수 계약과 이중 방어, 개념 [[소스 스캔 가드의 폴더 전수와 자동 편입]]). §8③ 카피 각색(hero·guide 두 문구를 INV-3 안전하게 바꾼 것)이 회귀하면 이 파일이 red로 잡는다 |
| `src/features/itinerary/model/itineraryEditStore.addSlot.test.ts` | **신규(TRIP-338, co-located)** — `addSlot` 순수함수 테스트. M1(비파괴 append — 원본 배열 불변 + 반환은 새 배열) · M2(읽기전용 5필드(nameKo·distanceRange·hasViolation·violationReason·tags)를 심은 slot에서 `Object.keys(...).sort() toEqual [5키]` 완전일치 — 여분 키 누출 차단, AC-4) |
| ~~src/features/itinerary/ui/ManualPlanScreen.test.tsx~~ | **삭제됨(TRIP-797 묶음 C, 2026-09-21)** — 화면 본체와 함께 `git rm`. 이하는 삭제 전 이력: **신규(TRIP-338, co-located)** — h19 화면 테스트. S1(빈 슬롯 4밴드+하단 CTA, AC-3) · S2/S3(위반 배지 지속/부재, AC-6 — 배지 유무만 잰다. 시각 텍스트[밴드 라벨 vs 실시각] 표기 자체는 무심판, N-2 참고 — INV-2 자체는 로컬 슬롯 prop 부재라는 구조가 담보) |
| `src/pages/itinerary-manual/ui/ManualPlanPage.integration.test.tsx` | **신규(TRIP-338, co-located)** — h19 배선 승인 테스트. I1(마운트 POST `{generationMode:'MANUAL'}` 여분 키 0·firedRef 1회 가드, AC-1) · I2(폴백 배너 testID `queryByTestId===null`, AC-2 — **단 이 표면엔 폴백 코드 경로 자체가 없어 공허 통과**다. 지금 무엇을 바꿔도 green, `resolveFallbackNotice`를 이 화면에 배선한 **뒤라야** red가 되는 미래 회귀 트립와이어, N-1 참고) |
| `src/pages/itinerary-manual/ui/PlaceAddPage.integration.test.tsx` | **신규(TRIP-338, co-located)** — h20 배선 승인 테스트. P1~P3(검색 URL에 검색어 키 부재·이름 부분일치 클라 필터·카테고리 파라미터 재조회, AC-5). **add→PUT(`TimeSheet` 경유, 구 `SlotTimeSheet`) 전체 플로우는 이 파일 범위 밖** — add 버튼을 누르는 테스트가 0이라 W-1(캐시 무효화 누락)·W-2(콜드캐시 조용한 소실) 두 실결함이 이 사각에 있었다(5-c로 봉합, 회귀 심판은 여전히 없음 — 별도 사이클 몫, repo-traps 참고) |
| `src/entities/itinerary-slot/lib/categoryPlaceholder.test.ts` | **신규(TRIP-465, co-located) → TRIP-809로 `features/itinerary/model/`에서 git mv(단언 diff 0, SUT와 함께 entities/itinerary-slot/lib 이사)** — `resolveCategoryPlaceholder` 순수 매핑 단언. AC-3(7종 `it.each`, `.tintClass` **`toBe`** 정본 토큰 완전일치) · AC-4(액티비티·`SIGHT`·미지값·null·undefined·`''` 6종 → `bg-surface-soft` 폴백 `toBe`) · iconKey distinct(`new Set(...).size===7` + 폴백은 7종 밖). 리터럴 iconKey 값은 정본에 없어 발명하지 않고 "비어있지 않음+서로 다름" 구조만 잰다(★6). 프로토타입 키(`constructor` 등)는 이 6입력 폴백군에 **없어** 이 스위트가 경고-1(위 `categoryPlaceholder.ts` 행)을 못 잡는다 |
| `src/features/itinerary/ui/MyTripCard.test.tsx` | **신규(TRIP-468, co-located, 해시 `2a77e12c...`)** — h37 순수 카드 렌더 테스트. AC-4(제목·메타줄·부가정보 leaf `toHaveTextContent` **완전일치** — `~` 구분자·박수 형식을 이번엔 의도적으로 잠그는 데 씀, 개념 [[RNTL toHaveTextContent 완전/부분일치]]) · AC-5 표현 3케이스(done 배지+resume 부재 / draft 배지+resume present / `badge:null` degrade — 배지·resume 부재 부정과 카드·제목 present 긍정을 짝으로 잠가 "카드 자체가 안 떠서 공짜 통과"를 차단). `metaLine`은 컨테이너가 조립해 완성해 넘긴 값을 하드코딩 VM으로 받아, `formatCardDateRange`(`TripCardContainer.tsx`) 자체의 조립 로직은 이 파일이 실행하지 않는다(repo-traps 참고) |
| `src/__tests__/liveTimeStructure.test.ts` | **신규(TRIP-395, `@jest-environment node`) — 이번 사이클 문서 소급 반영(20260820) → TRIP-746으로 스캔 대상 확장.** BR-U4-34·PBT-U4-F1 — `features/execution/**` 소스 전체에 슬롯 시각 재추정 산술 0건 강제: `new Date`·`Date.now`·`.getTime/.getHours/.getMinutes`·날짜라이브러리 import·`startAt`/`endAt` 인접 산술(`+-*/`) 전부 금지. `stripComments`(콜론 예외 URL 보존) 전제 + "없어야 한다"/"있어야 한다" 짝 앵커 관례. **이 가드가 `formatKoreanDate`를 `shared/date`로 뺀 근거**(execution 안에서 날짜 포맷하면 이 가드가 red). **TRIP-746**: i01 허브가 execution 밖(pages·entities)으로 이사해 `HUB_FILES`(`pages/live-itinerary/ui/LiveHubView.tsx`·`entities/itinerary-slot/ui/SlotProgressCard.tsx`·`entities/itinerary-slot/lib/openingHoursLabel.ts`) 3파일을 명시 편입(안 하면 그물이 조용히 줄어든다) — 컨테이너(`LiveItineraryPage.tsx`)는 "오늘" 판정용 `new Date()`가 정당해 제외. `MODEL_FILES`에서 삭제된 `liveViewStore.ts` 앵커도 뺐다 |
| `src/__tests__/executionDurationStructure.test.ts` | **신규(TRIP-395, `@jest-environment node`) — 이번 사이클 문서 소급 반영(20260820).** INV-3·PBT-U4-F2 — `features/{execution,planb}/ui/**` 소스에 소요시간 표기(`N분`·`N시간`·`소요`) 0건. `DURATION_TEXT=/(\d+\s*분\|\d+\s*시간\|소요)/`(`HH:mm`은 숫자 뒤가 `:`라 안 걸림) — 렌더 스캔이 못 보는 `accessibilityLabel` 문자열도 여기서 잡는다 |
| `src/__tests__/noStepCountStructure.test.ts` | **신규(TRIP-397, `@jest-environment node`) — 이번 사이클 문서 소급 반영(20260820).** BR-U4-41 — `features`·`shared`·`pages`·`app` 전역에 걸음 수 계열 심볼(`pedometer`·`stepCount`·`걸음 수`·`만보`·`보수계`) 0건(합성어만 탐지, `stepper`·`step1`은 오탐 제외). sub-cycle B(20260820)에서 정규식 리터럴 2줄→1줄 prettier 재포맷(동작 무변, code-critic 참고-2 확인) |
| `src/__tests__/liveHubStructure.test.ts` | **신규(TRIP-746, `@jest-environment node`)** — AC-9(+G7 에셋) i01 허브 재작성의 삭제·이관 소스 층 가드(전 src 스캔, `liveMapStructure.test.ts`를 대체하며 삭제). H1(사라진 표면 7파일 소스 부재 + 새 자리 3파일 실재, `nextNav.ts`는 남음 — Seed Q5) · H2(방패 FAB 글리프 `ShieldGlyph` 소멸) · H3(삭제 모듈을 import·jest.mock하는 곳 0, census는 `from`절·`jest.mock(`줄을 먼저 뽑고 그 줄 안에서 경로를 찾는 순서 — 심볼명 선탐은 주석·동명이심볼 오탐) · H4(프리뷰 사진 에셋 4장 + CREDITS.md, G7) · H5(TRIP-747 추가 — 허브 뷰는 SVG를 직접 안 그림, `*Glyphs.tsx` 경유만). `liveMapStructure`가 지키던 "지도 위 `absolute` 오버레이 금지"는 그 화면(`LiveMapScreen.tsx`) 자체가 삭제돼 사정거리가 소멸 — 대체가 아니라 폐지 |
| `src/__tests__/devPreviewLiveHub.test.tsx` | **신규(TRIP-746)** — `_dev/preview.tsx`의 `live-hub-{closed,half,expanded}` 3키(옛 `live-itinerary` 키 삭제)가 실제로 `LiveHubView`를 마운트하고 각 키의 초기 스냅 인덱스(0/1/2)·픽스처 5슬롯이 넘어가는지 렌더 확인. `devPreviewBandNav.test.tsx`(밴드 i 전수, 170키)와 이중 방어 — 이 파일은 허브 3키만 깊게, 저건 전 키를 얕게 |
| `src/__tests__/planbManualRoute.test.tsx` | **신규(TRIP-443) → TRIP-753으로 대상 교체(파일명 유지, 내용 재작성).** 옛 i15·i22 `planb-manual` 라우트 위임 가드였으나, 그 라우트·페이지가 삭제되면서 지금은 **i07 라우트**(`app/trips/[tripId]/planb/manual.tsx`)가 `@/pages/itinerary-edit`의 `ItineraryEditPage`를 `tripId`+리터럴 `inTrip=true`로 부르는지를 잠근다(`liveLocationRoute.test.tsx` 선례 동형). `jest.mock('@/pages/itinerary-edit')`로 스파이 치환 + `useLocalSearchParams`가 `{tripId,variant:'error'}`를 줘도 `variant`가 props에 새지 않음을 확인(옛 진입 신호 소거, 02a ★14) |
| ~~`planbManualStructure.test.ts`~~(옛 `src/__tests__/`) | **TRIP-753으로 파일째 삭제(`git rm`)** — 대상(`features/planb/ui/ManualEditScreen.tsx`·`pages/planb-manual/*`)이 사라져 가드도 함께 소멸. 대체 가드는 `src/__tests__/planbEditUnifyStructure.test.ts`(아래 행) |
| `src/__tests__/planbEditUnifyStructure.test.ts` | **신규(TRIP-753, `@jest-environment node`)** — i07·h12 통합 후 소스 층 가드(AC-1·10·12·13). U1(라우트가 `ItineraryEditPage`를 `inTrip`으로 부르고 옛 `variant`·`@/pages/planb-manual` 모름) · U2(`pages/planb-manual`·`widgets/itinerary-edit` 디렉토리째 부재 — 삭제도 계약) · U3(비테스트 소스 어디에도 옛 셸·화면·경로·`mergeValidationFlags` 참조 0) · U4(옛 폴백·잠금 testID 7종 전역 0 + i07 소스 3개에 옛 문구 4종 0) · U5(프리뷰가 `EditorView`를 파일 경로로 직접 import) · U6(`reorderKeepingLocked.ts`·`EditorView.tsx`에 소요시간 표기 0, INV-3). 전제: 모든 스캔이 주석을 걷은 소스를 보고(U0 자가검사), "없어야 한다"는 같은 it 안 긍정 짝과 짝짓는다(공허 통과 방지) — `planbManualStructure.test.ts` 패턴 계승 |
| `src/features/planb/model/reorderKeepingLocked.test.ts` | **신규(TRIP-753, co-located)** — K1(잠긴 칸 그대로+빈 칸 채우기 예시 단언) · K2(입력 배열 불변, `.filter`/`.map` 새 배열 확인) · K3(fast-check PBT 3성질: ①고정 index 불변 ②결과가 원본의 순열 ③비잠금 요소의 상대 순서가 `reordered` 순서 보존). ⚠️ 이 규칙을 실제로 부르는 프로덕션 드래그 표면이 0이라(`EditorView`가 `onReorder` 미소비) **테스트에서만 실행되는 규칙**(P3가 유일 호출자, 새 티켓 후보와 연결) |
| `src/pages/itinerary-edit/ui/ItineraryEditPage.inTrip.integration.test.tsx` | **신규(TRIP-753, co-located RNTL)** — h12·i07 통합 후 페이지 레벨 계약. P1(완료 알약 press → 시각 시트 안 열림, 03b 경고-1 보강 대상 — V3 대조군과 짝) · P3(드래그 결과가 `reorderKeepingLocked` 경유 확인, `UNSAFE_getByType(EditorView).props.onReorder` 직접 호출) · P4(CONFIRMED 저장 409 → 정직 안내, 라우터 4방법 0회 — 751·752 "이동 0은 navigate 포함 네 방법" 계승) |
| `src/__tests__/planbSafeAreaStructure.test.ts` | **신규(TRIP-580, `@jest-environment node`, 자율/야간 사이클) → TRIP-752·TRIP-754로 대상 축소.** 원래 i19(`ReplanAppliedScreen`)·i12(`ReplanSolvingScreen`)·`PlanbDiffPage` 3파일 세이프에어리어 소스 스캔이었다. **TRIP-754**: i19 전면화면 `ReplanAppliedScreen`이 파일째 사라지고 허브 위 시트(`ReplanAppliedSheet`)로 바뀌었다 — 전면화면 전용 상·하 인셋 속성도 함께 없어져, G2는 "시트가 전면화면용 래퍼를 들고 오지 않는다"로 재조준했다(시트는 i03·i04처럼 gorhom이 위치를 맡음, 홈 인디케이터 겹침은 6-b 사각). **TRIP-752**: i12 `ReplanSolvingScreen`도 파일째 사라졌다(i05는 `MapSheetShell` 위 뷰로 재작성) — G6(TRIP-652 세로 중앙 정렬)은 폐지, G3(상단 인셋)은 대상을 셸로 옮겼다(i05 진행 카드가 셸 오버레이의 `SafeAreaView edges={['top']}` 안에 있다). 남은 대상은 `PlanbDiffPage` G4(top-edge 래퍼 **개수 ≥2** — pre-apply·error 두 얼굴만 감쌈, 성공 시엔 즉시 허브로 `replace`하므로 얼굴 자체가 안 뜬다)와 G5(★1 — `useSafeAreaInsets` 훅 미사용, Provider 부재 렌더크래시 TRIP-456 회피를 구조로 강제)뿐이다. **G4는 `≥2` 임계값이라 이중 래핑 회귀가 나도(`3>=2`) green을 유지한다**(code-critic 참고-1, 미조치 — `=== 2` 또는 별도 미래핑 단언은 5-c 후보). i01(`SlotProgressCard.tsx`) 방문 체크 z-order는 정적 코드에 재현되지 않아 이 파일이 심판하지 않음(no-op, 6-b 실기 전용) |
| `src/__tests__/savedStaysStructure.test.ts` | **신규(TRIP-461, `@jest-environment node`)** — e04 저장한 숙소 소스 스캔(자가검사 1건 포함). AC-1(라우트·배럴·페이지·화면 3층 책임 — 라우트는 `@/pages/stay-saved`만·`useSavedStays`/`FlatList` 미참조, 배럴은 `SavedStayPage` 재수출, 페이지는 `useSavedStays`·`resolvePlaceListState`·`getAccessToken` 보유+`FlatList` 미참조) · AC-8(화면 순수성 — `SavedStayListScreen.tsx`에 `useState`·router·query 훅·`@/features/explore`·`@/features/trip`·`resolvePlaceListState` 0건, `staySearchStructure`의 동결 FORBIDDEN엔 없어 여기서만 잠기는 축) · AC-V3(화면 raw-hex 0, `*Glyphs.tsx` 제외). `pagesLayerStructure.test.ts`(층 전수 재귀)가 `pages/stay-saved/`를 자동 편입해 duration·zustand·URL·타이머·raw-hex 0건을 이중 방어 |
| `src/__tests__/myStaysStructure.test.ts` | **신규(TRIP-605, `@jest-environment node`)** — l04 등록 숙소·예약 기록 소스 층 가드(자가검사 포함 4케이스). G1(탐지기 자가검사 — `stripComments`×`DURATION_TEXT` 조합 실검증: 주석 속 "소요시간"은 걷히고 코드 속 "15분"은 살아남고 `https://` 의 `://`는 슬래시 보존) · G2(INV-3 — `features/settings/ui` 재귀에 소요시간 문자열 0 + 앵커로 `MyStaysScreen.tsx` 실재 확인) · G3(raw-hex 0, `*Glyphs.tsx` 제외) · G4(SafeArea — `MyStaysScreen.tsx` 소스에 `SafeAreaView`+`edges` `top` 존재, 구현 전 RED 앵커). `pagesLayerStructure.test.ts`(층 전수 재귀)가 `pages/my-stays/`를 자동 편입해 duration·zustand·URL·타이머·raw-hex 0건을 이중 방어(이 파일은 `features/settings/ui` 쪽만 새로 잠근다, pages 중복 스캔 안 함). **TRIP-606에서 G3 필터 확장**(`/(MyStays\|BaseToggle)/`→`/(MyStays\|BaseToggle\|StyleSummary)/`) — code-critic 경고-1이 `StyleSummaryCard.tsx`가 파일명 필터에서 빠져 raw-hex 회귀 무심판임을 지적, 필터 확장 + 긍정 짝(`newFiles.some(f=>f.endsWith('StyleSummaryCard.tsx'))`) 추가로 봉합. |
| `src/features/settings/model/stayTripLink.test.ts` | **신규(TRIP-605, co-located)** — `buildStayTripLink` 6 it. 연결/미연결/혼합/first-wins(한 숙소 두 여행 거점 → trips 순서상 첫 여행 승)/유령 base 배제/빈 입력. Map 계약이 표로 명확해 PBT 미채택(ponytail lite). |
| `src/features/settings/ui/MyStaysScreen.test.tsx` | **신규(TRIP-605, co-located)** — l04 화면 VM 주입 렌더 테스트(8케이스). AC-1(행 표시, `연결된 여행 없음` leaf 완전일치) · AC-2(다이얼로그 게이트 — 토글 press→다이얼로그+콜백 0회→확정 press에서만 1회, 취소 짝) · AC-3(`toBeDisabled()` 급소+press 무반응+콜백 0) · AC-4(empty+탐색 콜백) · AC-5(렌더 소요시간 문자열 0, 선제green 회귀 앵커). |
| `src/pages/my-stays/ui/MyStaysPage.integration.test.tsx` | **신규(TRIP-605, co-located) → 5-c로 2 it 추가(경고-1 실 SavedStay 라벨 파생 짝·경고-3a invalidateQueries).** 화면을 props-캡처 목으로 치환해 페이지 배선(콜백→router/mutate)만 검증. AC-4(empty→`/stays` push) · AC-2(확정(assigned)→DELETE `{tripId,baseAssignmentId}`, POST 0회) · AC-1 생산(trips 0건→미연결 VM). 화면 목은 **null 반환**(NativeWind `_ReactNativeCSSInterop` 함정 회피). 경고-1 짝은 `otaStay()` 픽스처로 `sourceLabel`·`dateRangeLabel`·`monthDay` 실행을 실측(뮤테이션 테스트로 teeth 확인). 경고-3a는 인스턴스 스코프 `QueryClient` + `invalidateQueries` spy로 DELETE 성공 후 무효화를 잠근다. |
| `src/features/settings/model/styleCardModel.test.ts` | **신규(TRIP-606, co-located)** — `buildStyleCardModel` 유닛(AC-M1·M2·M3). AC-M1(official 매핑, 값 무변형 + `not.toHaveProperty('avgDwellMinutes')`) · AC-M2(insufficient, `not.toHaveProperty('descriptors'|'gauges')`로 preview 미유출) · AC-M3(traitGauges 0~5 전수 `it.each`+혼합 2행, PBT 미채택 — 유한 도메인 전수가 랜덤보다 완전, ponytail lite). |
| `src/features/settings/ui/StyleSummaryCard.test.tsx` | **신규(TRIP-606, co-located)** — VM 주입 렌더(AC-S1·S2·S3·S4·S6). AC-S2가 이 티켓의 헤드라인 — 값 `[5,0,3]`으로 `my-style-dot-filled`/`-empty`(서로 다른 exact testID) 개수를 축별로 세어 SVG 한 장 fill 함정을 구조로 차단. AC-S6은 `toBeDisabled()` + press 무크래시로 상세 진입 정직 degrade(INV-4) 확인. AC-S5(소요시간 렌더 스캔)는 선제 green 앵커일 뿐 — 진짜 그물은 `myStaysStructure.test.ts` G2. |
| `src/pages/my-page/ui/MyPage.integration.test.tsx` | **신규(TRIP-606, co-located)** — 위 layer-pages.md `my-page` 슬라이스 행 참고(AC-I1, 배선+배치 동시 검증). |
| `src/__tests__/notificationKindGuard.test.tsx` | **신규(TRIP-607, `@jest-environment` 기본 — RNTL 렌더 필요)** — 금지-2 구조 가드. `NotificationSettingsScreen`에 COMMUNITY·SYSTEM(계약상 안 오는 값을 `as ToggleValueMap` 캐스트로 강제 주입) 값이 섞여도 렌더 트리에 안 나오는지(긍정 짝: 6종은 존재) 잠근다. 화면이 6종 목록을 자체 소유(`VISIBLE_ROWS`)하는 설계를 검증 — `layer-features-notification.md` `ui/NotificationSettingsScreen.tsx` 행 참고. |
| `src/__tests__/notificationStructure.test.ts` | **신규(TRIP-576)** — `features/notification/**` 소스 재귀 스캔(경계 가드). `@/features/<타feature>` import 0건(긍정짝: 화면이 `@/shared`는 문다) + 라우트→pages→feature 3층 import-content 앵커. **이 폴더 features 경계의 유일한 그물**(eslint `FEATURES` 배열엔 여전히 없음) — `layer-features-notification.md` 경계 절 참고. |
| `src/__tests__/notificationDurationStructure.test.ts` | **신규(TRIP-576)** — INV-3 소스 가드(AC-7). `features/notification/**`·`pages/notification-inbox/**`·`app/notifications.tsx`에서 소요시간 심볼(`분\|시간\|소요`) 0건을 부정 단언(G2) + stripComments×탐지기 조합을 실제 문자열로 태우는 자가검사(G1 — 주석 속 "30분"은 걷고 `://`·`14:30`·`840m`은 안 걸림). `executionDurationStructure.test.ts`(execution 층) 자매 가드. |
| `src/features/settings/model/preferenceDraft.test.ts` | **신규(TRIP-610, co-located, node 버킷)** — 역변환 순수함수 단위(A1~A4·B1~B5). `initialSelection`(isNeutralDefault 축 미반영 시드) · `buildPreferenceInput`(안 만진 축 omit — `not.toHaveProperty`, `toStrictEqual({})`로 여분 `undefined` 키까지 검사). 개념 [[역변환 함수 (View→Input)]]·[[키 부재 vs null vs undefined]] §TRIP-610. |
| `src/features/settings/ui/PreferencesEditScreen.integration.test.tsx` | **신규(TRIP-610, co-located, integration 버킷, MSW)** — I1~I5. GET→타일 시드(AC-1)·한 축만 변경 시 PUT 바디가 그 축만 담음(AC-2, 와이어 바디 직접 관찰)·미설정 축 신규 설정(AC-3)·400 인라인(AC-4, INV-4)·우선 안내 문구(AC-우선안내). |
| `src/features/settings/ui/PreferencesEditScreen.baseline.test.tsx` | **신규(TRIP-610, co-located, 5-c 심판강화, 02b)** — lost update 방지 judge. `usePreferences` 모듈 목으로 view를 A→B로 갈아끼워도(재요청 시뮬레이션) 저장 diff 기준선이 시드 시점 A로 얼려 있어(baseline freeze) 안 만진 축은 omit·편집 중인 축(pace)은 생존함을 `toStrictEqual({pace:'알차게'})` 한 단언으로 동시에 잠근다. 개념 [[lost update — 저장 diff 기준선은 시드 스냅숏이어야 한다]]. |
| `src/features/settings/model/personalizationCopy.test.ts` | **신규(TRIP-612, co-located, node 버킷)** — `personalizationCopy(reason)` 문구표 전수(`it.each` 3행, `toBe` 원시값 완전일치) + enum 전수 앵커(`Object.values(PersonalizationInfoReason)` 3개 모두 표에 있음) + **함정 단위잠금**(C3): `personalizationCopy('NOT_ENOUGH_RECORDS')`가 `.not.toContain('동의하면')` — 이미 동의한 사용자에게 동의유도 문구가 순수함수 층에서도 못 나가게 잠근다(BR-U5-44). |
| `src/features/settings/ui/PersonalizationScreen.test.tsx` | **신규(TRIP-612, co-located, node/RNTL)** — reason 3얼굴 렌더(A1~A3) + 목록 개수=`sharedItems.length` 전수(A4, `it.each` [APPLIED×0·APPLIED×3·CONSENT_MISSING×0] — 하드코딩/placeholder 목록 차단) + 토글·뒤로 press 배선(A5). **★급소(A3)**: `reason='NOT_ENOUGH_RECORDS'`에서 토글 `toBeChecked()`(ON 유지) + `queryByText(/동의하면/)` `toBeNull()`(정규식 부분포함, 부재 단언은 `queryBy*`—`getBy*`는 못 찾으면 throw). |
| `src/pages/settings-personalization/ui/PersonalizationPage.integration.test.tsx` | **신규(TRIP-612, co-located, integration 버킷) → 오케 봉합(급소 케이스 1건 추가, 03b 차단-1).** I1(토글 OFF, 현재 APPLIED→REVOKE)·I2(토글 ON, 현재 CONSENT_MISSING→GRANT) + **급소 I3**(`{applied:false, reason:'NOT_ENOUGH_RECORDS'}`→토글 ON 유지→press→`patchConsent(...,'REVOKE')`). `fetchTerms` mock에 PERSONALIZATION 아닌 항목을 섞어 termsVersion 필터 정확성을 잠그고, 인스턴스 QueryClient+`spyOn(client,'invalidateQueries')`로 재조회 트리거를 관측(목 GET이라 실제 재조회는 안 뜸, MyStaysPage 경고-3a 선례). **적대적 리뷰 차단-1(개념 [[가드의 사정거리]]·[[뮤테이션 실측 (승인 후 신규 심판 검증)]] 참고)**: I3 추가 전엔 `reason` 기반 정답 도출과 `applied` 기반 오답 도출이 이 파일에서 구분되지 않아, 축 혼동 뮤턴트도 전 스위트 green이었다 — I3만 뮤턴트에 red로 반응함을 실측 후 봉합. |
| `src/shared/api/patchConsent.test.ts` | **신규(TRIP-612, co-located, node 버킷)** — `patchConsent` 와이어 계약(D5 드리프트 잠금). `jest.spyOn(authedClient,'patch')`로 GRANT·REVOKE 각각 `toHaveBeenCalledWith('/me/consents/PERSONALIZATION', {action, termsVersion})`(URL에 termsType, body 2필드) + `not.toHaveProperty('termsType')`(body 잉여 없음 — 티켓 서술 `{termsType,...}` body는 openapi와 불일치, `ConsentInput`(POST bulk용) 그대로 재사용 시 이 잉여가 실린다). |
| `src/__tests__/personalizationStructure.test.ts` | **신규(TRIP-612, `@jest-environment node`)** — l05 개인화 3층 배선·라우트 무로직 소스 스캔. S1(6파일 실재 `existsSync`) · S2(라우트가 `@/pages/settings-personalization`만 import, `useState`·`View`·`useGetMePersonalization` 0건 — `location.tsx` 5줄 래퍼 동형) · S3(배럴 재수출 앵커) · S4(페이지가 `usePersonalization`+`PersonalizationScreen` 둘 다 참조, 빈 파일 공허통과 차단). **경계(다른 feature import 0)는 이 파일이 아니라 `settingsBoundary.test.ts`가 재귀 스캔으로 자동 편입해 잠근다**(중복 신설 안 함, ponytail lite) — INV-3·raw-hex도 `myStaysStructure`·`pagesLayerStructure` 자동 편입 이중 방어라 재스캔 없음. |
| `src/__tests__/editSheetHandleStructure.test.ts` | **신규(TRIP-735, `@jest-environment node`)** — g01/g02 편집 시트 6종 바텀시트 핸들 단일화 소스 층 가드. jest 자연 심판 0(gorhom 통과형 목·그래버 testID 없음)이라 유일한 jest 그물. g0(탐지기 자가검사 — stripComments가 주석 idiom을 걷는지, `GRABBER_BAR`가 클래스 순서 무관인지) · 상수 shape(`SHEET_HANDLE_INDICATOR_STYLE`=폭40·높이4·r2·`#DDDDDD`) · 6시트 파일 실재+`HANDLE_WIRING`(`handleIndicatorStyle={SHEET_HANDLE_INDICATOR_STYLE}` 한 덩어리 바인딩)+그래버 바 부재(순서 무관 정규식). **5-c 보강**: 초회 버전은 그래버 검출이 정확 문자열이라 클래스 순서만 바꾼 재삽입에 뚫렸다(경고-1, code-critic 실측) → 순서무관 정규식(`GRABBER_BAR`)으로 교체 + stripComments 추가(참고-1) + prop-상수 바인딩을 한 정규식으로 결합(참고-2). **잔여 사각(참고-3, 미조치)**: `#DDDDDD`는 `tailwind.config.js`의 `hairline-strong` 토큰 값을 손으로 복제한 리터럴이라, 토큰이 바뀌어도 이 가드는 계속 green — 개념 [[가드의 사정거리]] "확장자가 사정거리 경계다" 절 참고. |
| `src/__tests__/homeGlyphColorStructure.test.ts` | **신규(TRIP-694, `@jest-environment node`)** — 5-c(오케) 신설, `HomeGlyphs.tsx`의 `LocationPinGlyph` stroke 색(AC-4, Figma 정합)을 렌더 트리가 아니라 **소스 텍스트 스캔**으로 잠근다. RNTL이 SVG `stroke`를 못 보므로(개념 [[글리프 fill 색 사각 (SVG 단일 노드는 값 변화를 못 잰다)]]) 유일한 jest 그물 — 뮤테이션 실측(`PRIMARY`→`WHITE` 되돌림 → red, 원복 → green)으로 심판에 이빨이 있음을 증명했다(code-critic 경고-1 봉합). |
