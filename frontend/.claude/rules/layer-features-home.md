---
paths:
  - "src/features/home/**"
---
# `src/features/home/` — 실구현 ③ (TRIP-170, TRIP-316에서 계약·화면 통째 재작성)


계층: `model`(순수 타입·상수) → `ui`(화면+전용 글리프, TRIP-173 FSD 완결 1/4에서 `screens`·`components` 2칸이 합류). **컨테이너·훅 없음** — 서버 API 부재로 프레젠테이션 전용 슬라이스(props/상수 구동, 네트워크·라우팅 0).

**TRIP-316(2026-08-14)**: 구 세대 "여행 상태 대시보드"(`trip`·`nextPlan`·`resume`·`taste` 계약)를 신 세대 "발견·영감 피드"(`hero`+`sections` 계약, 라이브 Figma 2091:1357)로 폐기·재작성. no-trip 픽셀 정본이 신 프레임에 없어 `HOME_NO_TRIP_PROPS`는 `HOME_DEFAULT_PROPS`와 바이트 동일(가정 B, 상태 5종 별도 티켓 몫).

| 파일 | 역할 |
|---|---|
| `src/assets/home/` | **신규(TRIP-694) — 생성 플레이스홀더 이미지 13장**(단색/그라디언트 `.jpg` 12장 + `CREDITS.md`), **실사진 아님**. 히어로 5·컬렉션 3·스팟 4 카드가 `require`해 `homeFixtures.ts`의 `toUri` 헬퍼로 uri 문자열로 변환 후 배선. 실사진 교체는 후속 티켓(`CREDITS.md`에 명시) — 이 폴더가 존재한다고 실제 장소 사진이 있다고 오인하지 말 것 |
| `src/features/home/model/homeTypes.ts` | prop 계약 타입(TRIP-316 재작성) — `HomeScreenProps{hero: HomeMagazineHero; sections: HomeSections}`. `HomeSections`는 판별 유니온(`ready`/`loading`, 3섹션을 한 덩어리로 묶어 부분 실패 시 전 섹션 동시 전환 — `empty` 갈래는 TRIP-701로 삭제). 카드 타입 `HomeCollectionCard`·`HomeSpotCard`·`HomeItineraryCard`·`HomeMagazineHero` 신규. **TRIP-317 추가, TRIP-701(2026-09-17)로 축소**: `HomeScreenProps.phase?: HomePhase`(옵셔널 additive) — `HomePhase`는 `discovery`(폴백, payload 없음)·`planning`·`postTrip` **3종**(collecting·upcoming 갈래는 TRIP-701로 삭제) 판별 유니온. 페이로드 타입 `TripHeroData`·`PastTrip`·`HomeSoftNote` **3종**(`HomeStatTile`·`NextStop`·`NearbyCard`는 TRIP-701로 삭제, upcoming 전용이라 함께 소멸. **`RecapCard`는 TRIP-698로 완전 삭제** — postTrip 재작성이 가로 사진 카드로 대체, 이 행이 그때 갱신되지 않아 "4종" 서술이 스테일이었다가 TRIP-700 `--stale` 감사로 정정). `HomeCollectionCard.savedAtLabel?`도 TRIP-701로 삭제(collecting 저장일 메타였음, `CollectionCard` 삼항이 항상 지역+핀 분기로 축약됨). **TRIP-370 추가**: `onPressCreateTrip?`·`onPressSavedPlaces?`·`onPressSpotsMore?` 콜백 3종(전부 옵셔널 — `HomeScreen.test.tsx`·`_dev/preview.tsx`가 콜백 없이 호출하므로 필수화 금지) — 컴파일용 타입 선언, 런타임은 `HomeScreen.tsx`가 소비. **TRIP-401 추가**: `HomePhase`의 `planning` 갈래에 `dominantTripId?: string`(옵셔널 — 동결 `HomeScreen.test.tsx`의 `PLANNING_PHASE` 리터럴과 비동결 `homeFixtures.PLANNING_PHASE`가 이 필드 없이 존재해 필수화하면 둘의 tsc가 깨짐, `resolveHomePhase`는 항상 채움) · `HomeScreenProps.onPressTripHeroCta?`(옵셔널 콜백, `home-trip-hero-cta` 죽은 버튼 해소용). **TRIP-694 추가**: `hero: HomeMagazineHero`(단일) → `hero: readonly HomeMagazineHero[]`(5장 배열, discovery 캐러셀용). collecting·planning은 `hero[0]`로 첫 장만 쓴다(2곳 파급) — **주의**: 빈 배열이면 `hero[0]`가 `undefined`가 되어 `MagazineHero`가 크래시한다(코드에 방어 없음, 현 호출자는 항상 5장이라 미도달 — `traps-home.md` 참고, 서버 API 연동 시 재확인 필요). **TRIP-695 추가**: `HomeScreenProps.savedPlacesCount?`·`savedStaysCount?: number`(옵셔널 additive, test-designer가 컴파일용 타입 선언으로 선반영 — 02a §1-⑥) — 담은 곳 메뉴 배지 개수, 런타임은 `HomeScreen.tsx` `CountBadge` 소비. **TRIP-700 추가**: `HomeScreenProps.onPressMagazine?: () => void`(옵셔널 additive, 컴파일용 선반영) — discovery page0(`home-magazine-hero`) press가 최종 소비, 라우팅 자체는 `(tabs)/index.tsx`가 짐 |
| `src/features/home/model/homeFixtures.ts` | 고정 목업(Q2 — 신 프레임 Figma 표시값 그대로 상수화, TRIP-316 재작성). `HOME_DEFAULT_PROPS`·`HOME_LOADING_PROPS`. **TRIP-317 추가, TRIP-701로 축소**: `HOME_PLANNING_PROPS`·`HOME_POST_TRIP_PROPS` 2상수만 잔존 — discovery 기저(`hero`·`sections`) 위에 `phase`만 주입, 프리뷰(`_dev/preview.tsx`) 전용 진입점(실착지 `(tabs)/index.tsx`는 서버가 단계를 줄 계약이 없어 discovery/planning만 씀, postTrip은 가정 E). **TRIP-701(2026-09-17) 삭제**: `HOME_NO_TRIP_PROPS`는 `HOME_DEFAULT_PROPS`와 바이트 동일이라 후자로 병합(4곳 라우트 스왑, `(tabs)/index.tsx`) · `HOME_EMPTY_PROPS`·`HOME_COLLECTING_PROPS`·`HOME_UPCOMING_PROPS`는 프리뷰 전용 고아라 삭제. **TRIP-694 추가**: `MAGAZINE_HERO`(단일)→`MAGAZINE_HEROES`(5장 배열, page0=TRIP-316 정본값·page1~4는 발명 카피 — 전역 문자열 충돌 회피 확인 완료) + 컬렉션 3·스팟 4 카드에 `imageUrl` 채움. 내부 헬퍼 `toUri`(비export, `Image.resolveAssetSource(require(...)).uri ?? null`)를 3배열이 공유 — 개념 후보(다음 사이클 승격 검토): 번들 이미지 uri 변환 패턴 |
| `src/features/home/ui/HomeGlyphs.tsx` | 홈 전용 인라인 SVG **6종**(TRIP-316에서 10종→6종 — 신규 3: `SearchGlyph`·`SparkleGlyph`·`LocationPinGlyph`, 톤 변경 2: `HeartOutlineGlyph`·`PlusGlyph`, 유지 1: `BellGlyph`. 고아 7삭제: `ClockGlyph`·`PencilGlyph`·`ChevronRightGlyph`·`FlameGlyph`·`LikeHeartGlyph`·`CommentGlyph`·`RouteDotsGlyph`, home 밖 참조 0 확인 후 제거). raw hex 직박 — TRIP-173으로 `ui/`에서 `*Screen.tsx` 파일과 **같은 폴더가 됐다.** D-3 가드(`homeStructure.test.ts`)가 이제 디렉토리가 아니라 **`*Screen.tsx` 파일명 접미사로 필터**해 계속 미대상이다(`HOME_SCREEN_SOURCE_FILES` 동결목록으로 1건 고정, code-critic W-1 확인) — 필터가 조용히 넓어지면 이 파일도 스캔 대상이 될 수 있으니 그 필터를 건드릴 땐 이 파일부터 확인. **TRIP-694 추가**: `LocationPinGlyph`(컬렉션 카드 지역 핀) stroke `WHITE→PRIMARY`(Figma 2091:1357 정합). SVG stroke라 jest 사각 — 유일한 그물은 `src/__tests__/homeGlyphColorStructure.test.ts`(소스 스캔, 뮤테이션 실측 완료, 개념 [[글리프 fill 색 사각 (SVG 단일 노드는 값 변화를 못 잰다)]]). `WHITE` 상수는 하트·플러스·닫기 글리프가 계속 써서 미사용 안 됨. **TRIP-700 추가**: `BackChevronGlyph`(뒤로가기 꺾쇠, a02 앱바용) 신규 export — 다른 feature엔 있으나 features 간 import 금지라 색 맞춰 새로 그림 |
| `src/features/home/model/homePhase.ts` | **신규(TRIP-371) → TRIP-401로 지배 tripId 노출 → TRIP-472로 badge·ctaLabel 재계산.** 홈 라우트의 순수 판정 모듈. `formatDday(startDate, today)`(UTC epoch-day 산술, 'D-n'/'D-DAY'/'D+n', 형식 이상→`''`, `planState.utcDayTime`과 같은 규율이나 features 경계상 import 불가라 로컬 복제)와 `resolveHomePhase({trips, today, savedCount, formatTripMeta})`(비-ENDED 중 가장 이른 startDate가 지배 여행 → `planning`, 비-ENDED 0건 → `undefined`) 2개를 export. **TRIP-472**: badge는 더 이상 server status(ACTIVE) 기준이 아니라 신규 `isTraveling(startDate,endDate,today)` — 오늘이 `[startDate,endDate]` 안이면 `'여행 중'` 아니면 `'계획 중'`(dday와 같은 UTC epoch-day 소스라 "여행 중"+"D-1" 모순이 원천 제거됨). ctaLabel도 하드코딩 '일정 이어서 짜기'에서 신규 `ctaLabelForStatus(status)` 분기(`CONFIRMED`→'확정 일정 보기', `ACTIVE`→'여행 일정 보기', 그 외→'일정 이어서 짜기')로 교체 — 탭 목적지 자체는 여전히 라우트(`resolveItineraryDestination`)가 정하고 이 함수는 문구만 맞춘다. **경계 회피 DI**: `features/home`은 eslint zone + `importBoundary` 대상이라 `features/trip`(`formatTripRange`)·`features/itinerary`(`formatNightsLabel`)를 직접 import 못 해 — `formatTripMeta`를 4번째 인자로 **라우트가 주입**(`resolveNearby` 선례와 동형 DI). `HomeTripInput`은 서버 `Trip` 타입을 직접 import하지 않는 로컬 구조 타입(`title`·`startDate`·`endDate`·`party`·`status` + **TRIP-401 추가 `tripId`(필수)** 6필드, `Trip[]`이 구조적으로 대입 가능) — `homeStructure` D-1이 `@/shared/api` 문자열을 본문·주석 어디서도 금지하기 때문(raw `includes` 스캔, 주석도 걸림 — 02 실측). **TRIP-401 추가분**: `resolveHomePhase`가 `planning` 반환값에 `dominantTripId: dominant.tripId`를 실어 노출 — 이전엔 phase가 "어느 여행인지" 식별자를 안 날랐다(홈 CTA 배선의 숨은 선행작업이었음). 테스트는 co-located `homePhase.test.ts`(단위+PBT 500런, 주석이 아직 "5필드"로 적혀 스테일 — test 파일이라 미수정, 03 보고). |
| `src/features/home/ui/HomeScreen.tsx` | **2상태**(`ready`/`loading`, `empty`는 TRIP-701로 삭제) 발견·영감 피드 프레젠테이션 화면(TRIP-316 재작성, 구 4상태 대시보드 폐기). props만 받음 — `expo-router`·`@/shared/api`·타 feature import 0(homeStructure D-1이 기계 강제). softNote 배경 `bg-[#fff7f8]`는 raw-hex 가드(D-3) 13색 밖 사각지대(`repo-traps.md` 대상 아님 — 06-b 실기 스모크로만 담보, TRIP-316 04b SKIP 인수인계). **TRIP-317 추가, TRIP-701(2026-09-17)로 축소**: 내부 `PhaseBody` 순수 스위치 함수가 `phase.kind`(미전달/`discovery`→316 얼굴 그대로, 나머지 **2종**(planning·postTrip)→단계 얼굴. collecting·upcoming case는 TRIP-701로 삭제)로 조립 — 여행 데이터를 뜯어 단계를 스스로 도출하는 코드 0줄. 공유 부품(`GreetingHeader`·`SoftNote`·`CollectionCard`·`CollectionStrip`·`TripHero`·`MiniMapCard`·`DashRow`·`PastTripsSection`)을 파라미터화해 조립(`NextStopCard`는 TRIP-701로 삭제) — 얼굴별 신규 export 0, 전부 파일 내부 `function`(`HOME_SCREEN_SOURCE_FILES` 동결목록 1건 무변경). testID(TRIP-701 기준 2종 잔존): `home-trip-hero`·`home-recap-card`(과거 5종 중 `home-next-stop`·`home-saved-count-chip`은 TRIP-701로, `home-dash-itinerary`는 이미 TRIP-646으로 소멸). INV-3 렌더 가드 `DURATION_RENDER`(`HomeScreen.test.tsx`)는 "24시간 개방" 거짓매치는 막지만 "이동시간 15분"류 진짜 소요시간 일부를 놓치는 과교정 상태(현 동결 데이터엔 트리거 없음 — 후속 티켓 후보, 아래 W1). **TRIP-370 추가**: 위 3콜백을 `onPressCreateTrip→CreateTripFab.onPress`(1단), `onPressSavedPlaces→SoftNote.onPressCta`·`onPressSpotsMore→SpotsSection.onMore`(discovery 얼굴 안, `PhaseBody→DiscoveryBody`를 거쳐 스레딩)로 prop만 전달(스토어·컨텍스트 없음). 목적지 없는 컨트롤(검색바·컬렉션/일정 더보기·벨) 4종은 `accessibilityRole="button"` 제거. 공용 `SectionHeader`(collections/spots/itineraries 더보기 3인스턴스가 공유)는 `asButton`(role, 구조로 고정 — 콜백 유무로 파생 금지)·`onMore`(handler) 2-prop 분기로 spots-more만 버튼 유지 + 나머지 role 제거. discovery 버튼 집합 = 정확히 `{home-create-trip-fab, home-saved-places-cta, home-spots-more}`. 라우터·네트워크 import는 여전히 0건(D-1 유지, 콜백은 `(tabs)/index.tsx`가 문다). ⚠️ **경고-1(03b) 정정(역사적 기록 — collecting 자체는 TRIP-701로 코드째 삭제됨)**: implementer 노트(03)가 "collecting의 `SpotsSection` 더보기도 버튼 아니게 됐다"고 적었는데 코드는 반대 — `SpotsSection`은 `asButton`을 구조적으로 항상 넘겨(413행) collecting에서도 `home-spots-more`는 role="button" 유지(onPress만 undefined, 무동작 버튼). collecting/postTrip에서 role이 실제로 빠진 것은 `CollectionStrip` 쪽 더보기뿐. **TRIP-401 추가**: `SoftNote`가 role을 `onPressCta` 유무로 파생하던 것을 **명시 `asButton` prop**으로 굳힘(discovery 온램프만 `asButton=true`, planning 브릿지 "일정에 추가"는 `false`) — planning 얼굴 죽은 버튼 정리(370-AC-4 discovery-only 사각의 **planning 갈래만** 닫힘, `HomeScreen.test.tsx` T3 AC-6/AC-7이 협공 잠금). `TripHero`도 `onPress?` 옵셔널 콜백을 새로 받아 `home-trip-hero-cta`가 더는 하드코딩 `onPress={undefined}`가 아님(`(tabs)/index.tsx`의 `PlanningHome`이 주입). ⚠️ **postTrip은 여전히 무심판**(라이브 미도달, 가정 E) — collecting·upcoming은 TRIP-701(2026-09-17)로 코드째 삭제돼 더는 존재하지 않음 — repo-traps home 절 참고. **TRIP-470 추가**: `+` FAB 위에 담은 곳 하트 FAB(`home-saved-places-fab`, bottom-152, `onPressSavedPlaces` 재사용) 신설 — a01 전용이 아니라 **모든 phase에 렌더**(discovery·planning 버튼-집합 가드 둘 다 갱신). ⚠️ **TRIP-596 정정(위 TRIP-370 절 "discovery 버튼 집합" 문구는 스테일)**: discovery의 `<SoftNote onPressCta={onPressSavedPlaces}>` 인스턴스(하트 FAB과 중복 온램프) 삭제 + `DiscoveryBody`가 받던 `onPressSavedPlaces` 스레딩(파라미터·`PhaseBody`→`DiscoveryBody` 전달) 걷어냄 — `onPressSavedPlaces`는 이제 `HomeScreen`→`SavedMenuFab`(하트 FAB) 경로로만 소비된다. `SoftNote` 정의·planning 브릿지(`asButton=false`)·postTrip 공유 인스턴스는 무변경(3용도 공유 컴포넌트, discovery 인스턴스 1건만 삭제). **현재 discovery 버튼 집합 = `{home-create-trip-fab, home-saved-menu-toggle, home-spots-more, home-search-bar}`**(`home-saved-places-cta` 빠짐 — `home-saved-menu-toggle`·`home-search-bar`는 TRIP-494가 이미 추가했었고 이 절이 그때 안 갱신됐던 것도 함께 바로잡음, `HomeScreen.test.tsx` `WIRED_CTA_TEST_IDS` 실측 정본). **TRIP-694 추가(Figma 2091:1357 default 픽셀 정합)**: (1) `MagazineHero` 상단 하트(`HeartOutlineGlyph`) 제거. (2) 신규 내부 함수 `DiscoveryHeroCarousel`(`ScrollView horizontal pagingEnabled` + `useWindowDimensions().width` + `onMomentumScrollEnd`로 현재 page 추적, 기존 planning `HeroCarousel` 패턴 재사용) — 5장을 `home-hero-page-0..4` 래퍼로 감싸고, `MagazineHero`는 옵셔널 `testID`(기본값 `home-magazine-hero`)·`showDots` 2props 신규: page0만 기본 testID, page1~4는 `home-hero-slide-N`(다중 인스턴스 testID 충돌 회피, `traps-home.md` 참고). (3) `GreetingHeader` 벨 `bg-canvas rounded-full h-[40px] w-[40px]` + `softCardShadow`(기존 상수 재사용, 원래 `h-10`은 rem 환산 35px 함정이라 명시 px로 교정). (4) 히어로·컬렉션·스팟 카드에 `<Image>` 무조건 렌더(source uri null이어도 엘리먼트는 그려짐, h11 선례). (5) `DiscoveryBody`에서 `<ItinerariesSection>` 렌더 한 줄 제거(정의·planning 렌더는 유지 — `ItinerariesSection` 자체는 안 지움, 696~698 소관; collecting은 TRIP-701로 phase 자체가 삭제됨). 새 export 0(전부 파일 내부 함수, `HOME_SCREEN_SOURCE_FILES` 동결목록 무변경). **TRIP-701(2026-09-17) 삭제**: `SectionEmptyBlock`·`CollectingBody`·`UpcomingBody`·`NextStopCard`·`SavedCountChip` 정의 전부(§`{kind:'empty'}` 3분기[collections/spots/itineraries-empty]·`{kind:'collecting'}`/`{kind:'upcoming'}` case 소멸에 따른 tsc 강제 제거). `CollectionCard`의 `savedAtLabel` 삼항도 축약(항상 지역+핀 분기). **TRIP-695 추가(담은 곳 메뉴 개수 배지)**: 로컬 함수 `CountBadge`(비export, `(count ?? 0) < 1`이면 `null` — 표시 게이트) 신설, `SavedMenuFab`이 `savedPlacesCount?`·`savedStaysCount?` 2 prop을 받아 각 미니 FAB(위치핀·가방) 안에 `<CountBadge/>`를 얹는다(텍스트=`../lib/formatCountBadge` 호출값, `bg-primary` 토큰). **z-order**: 백드롭 블록을 `SavedMenuFab` 안에서 `HomeScreen` 레벨로 승격해 `CreateTripFab` 뒤·`SavedMenuFab` 앞에 조건부 렌더(문서순=쌓임, 개념 [[z-order = 렌더 순서 (RN absolute 형제는 나중 렌더가 위)]]) — 딤이 +FAB도 덮도록. ⚠️ 게이트 `< 1`의 정확한 경계값(count=1)은 초기 렌더 테스트가 {0,3,7,100}만 확인해 무심판이었다(code-critic 경고-1) — 5-c에서 `count=1` present 단언 추가로 봉합, 개념 [[개수 임계값(>=N) 구조가드의 사각]] 참고. **TRIP-700 추가**: `MagazineHero`에 `asButton`(role을 구조로 굳힘, 기본 false)·`onPress` 2prop 신규 — `DiscoveryHeroCarousel`이 **page0에만** `asButton={true}`+`onPress={onPressMagazine}`을 실어 a02(`/magazine`)로 push, page1~4·planning/postTrip 캐러셀은 기본 false라 비버튼 유지([[버튼셋 완전동치 게이트]] discovery 집합에 `home-magazine-hero` 추가, 협공 단언은 `HomeScreen.test.tsx` 참고). |
| `src/features/home/lib/formatCountBadge.ts` | **신규(TRIP-695) — `home` 슬라이스 첫 `lib` 세그먼트.** 순수 함수 `formatCountBadge(n: number): string` — `n<=0→''`(미표시, 배지 표시 게이트와의 이중 방어) · `1~99→String(n)` · `n>=100→'99+'`. co-located `formatCountBadge.test.ts`가 단위 경계표 6행 + PBT(fast-check) P1~P5를 잠근다(개념 [[순수 함수 + PBT (fast-check)]]). 이 파일의 존재 때문에 `fsdStructure.test.ts`의 home 세그먼트 완전일치가 `['model','ui']`→`['lib','model','ui']`로 확장됨(layer-test.md 참고). |
| `src/features/home/ui/MagazineScreen.tsx` | **신규(TRIP-700) — a02 "여행지 둘러보기" 매거진 목록 순수 뷰.** 앱바·인트로·필터 칩 5(정확히 1선택)·에디토리얼 카드·2열 매서너리 6장([[매서너리 고정높이 2열]])·FAB 2종(시각 전용, role 없음)을 props+콜백만으로 그린다. `HOME_SCREEN_SOURCE_FILES` 동결목록에 **반드시 추가돼야** D-3(토큰)·D-4(SafeArea)·TRIP-317 자기검사가 통과한다(누락하면 즉시 red — `traps-home.md`). 검색(`onSearch`)·카드 press(`onPressCard`)는 콜백만 받고 컨테이너가 아직 미배선(후속 티켓, role도 없음 — 죽은 버튼 회피 관례). |
| `src/features/home/model/magazineFixtures.ts` | **신규(TRIP-700).** `MAGAZINE_DEFAULT_PROPS`(칩5·에디토리얼·6카드, 카드별 고정 `height`) — 서버 매거진 API 없어 픽스처 구동(홈 선례 동형). 카드·에디토리얼 이미지는 `toUri` 헬퍼로 `@/assets/home/*` 재사용(실사진 없음, 후속). ⚠️ **주석에 `@/shared/api`·axios·`expo-router` 문자열을 쓰면 D-1 raw-스캔이 본문뿐 아니라 주석도 걸려 red**(TRIP-700 실측, `traps-home.md` 참고 — 완곡 표현으로 우회). |
| `src/features/home/model/magazineTypes.ts` | **신규(TRIP-700).** `MagazineScreen` prop 계약(순수 타입, `import type`이라 런타임 0) — `chips`·`selected`·`editorial`·`cards: {title,tag,height,imageUrl?}[]`·콜백 4종(전부 옵셔널). `homeTypes.ts`의 `HomeScreenProps.onPressMagazine?`(홈 캐러셀 page0 진입 콜백)도 이 티켓에서 컴파일용 선반영됨(homeTypes.ts 쪽 행 참고). |
