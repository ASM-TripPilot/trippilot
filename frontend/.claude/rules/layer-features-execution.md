---
paths:
  - "src/features/execution/**"
---
# `src/features/execution/` — 여행 중(execution) 현장 표면 (TRIP-395·397·398, i01·i02·i03·i05)

**신규 층 규칙 파일 — 이번 사이클(`20260820-trip395-397-figma-align`)에서 처음 만들었다.** `execution/` 자체는 TRIP-395 substrate(`c1f7217`)부터 여러 커밋에 걸쳐 자랐으나 층별 표는 지금까지 어느 `layer-*.md`에도 없었다(structure-index `--check` 실측 — 12파일 전부 "누락"). 아래 표가 정본이다. 여행 중 현장(오늘 일정 실시간 보기)을 타임라인(i01)·지도(i02 계획·i03 실제 GPS)로 보여준다. BR-U4-34~46·PBT-U4·INV-3(소요시간 비표시) 소관 — 시각은 계획값만 통과, 재추정 금지(`liveTimeStructure` 가드, `layer-test.md` 참고).

| 파일 | 역할 |
|---|---|
| `src/features/execution/model/slotProgress.ts` | **신규(TRIP-395) → TRIP-396으로 실제 배선(`progress` 인자를 처음 채움).** `projectSlotProgress(slots, {completedPoiIds?, activePoiId?}) → ProjectedSlot[]`. 슬롯에 진행 상태(`done`\|`active`\|`upcoming`)를 붙이는 순수 사영. **`startAt`/`endAt`을 읽지도 연산하지도 않고 슬롯을 그대로 통과**시킨다(PBT-U4-F1 — 시계로 도착 시각을 재추정하지 않는다, BR-U4-34·DEC-U4-9). 완료가 진행 중보다 우선. i01 "기록 없음" 기본(빈 progress)은 전부 `upcoming`. **파일 자체는 무변경**(재사용, 재구현 아님) — 인자만 `LiveItineraryPage`가 채우기 시작했다(아래 `useVisitCheck.ts`·`visitProgress.ts` 참고, `repo-traps.md`의 "i01 active 카드 도달성 없음" stale 항목이 이 배선으로 해소) |
| `src/features/execution/model/dwellMinutes.ts` | **신규(TRIP-396)** — `dwellMinutes(arrivedAt, completedAt): number\|null`. `completedAt − arrivedAt` 분(整數) 산출, 한쪽 null→null, 역전→`Math.max(0,·)`(정본 침묵, 자가판단). BR-U4-37·INV-U4-03(체류를 화면에 표시하지 않고 트리거 입력으로만). `liveTimeStructure` 가드 사정거리라 `"HH:mm:ss".split(':')`로 쪼갠 `hh`/`mm` 지역 변수로만 연산(`placeDetailView.toMinutes` 선례 동형, Date API·식별자 인접 산술 0). **⚠️ 소비처 0(데드코드)** — 서버가 `arrivedAt`/`completedAt`로 dwell을 스스로 도출하고 `POST /complete`·`VisitCheck` 어디에도 클라→서버 dwell 필드가 없다(API 계약 실측). AC-1 요구라 빌드하고 PBT로 정확성만 잠갔다 — `nextNav.ts`(TRIP-399, 호출자 0)와 같은 계열. 후속 티켓 후보: 클라 dwell 소비 창구 신설 또는 제거 판단(`.claude/rules/repo-traps.md` "여행 중 실행" 절 참고) |
| `src/features/execution/model/visitProgress.ts` | **신규(TRIP-396)** — `deriveVisitProgress(list: VisitCheckList): VisitProgress{completedPoiIds, activePoiId, visitCheckIdByPoiId}`. `GET /visits/days/{day}` 응답에서 완료(`completedAt≠null && skippedAt==null`)와 도착·미완료(`arrivedAt≠null && completedAt==null && skippedAt==null`, 완료 목록에 없는 것만)를 분류 — 완료가 진행 중을 이긴다. 즉석(slotKey=null)도 도착이면 active. `slotProgress.ts`의 `projectSlotProgress`에 주입되는 것이 유일한 소비 경로(페이지 1회 배선, FSD 구조가드) |
| `src/features/execution/model/useVisitCheck.ts` | **신규(TRIP-396) → TRIP-619로 도착(arrive) 낙관 리터럴에 `updatedAt: optimisticAt` 1필드 추가.** `useVisitCheck({tripId, day})`. 도착(`POST /visits`)·완료(`POST /visits/{id}/complete`) imperative 낙관 갱신(`savedPlaces.ts` 패턴, 생성 훅 대신 raw 함수를 직접 호출 — promise 반환 계약이 필요해서). **★ 롤백이 슬롯키(레코드) 단위**다 — `savedStays.ts`/`savedPlaces.ts`의 W-2(동시 두 카드 토글 시 통짜 스냅숏 롤백이 서로를 지움)를 이 파일에서 처음 슬롯키 단위로 고쳤다. 이 방어의 유일한 심판(`useVisitCheck.integration.test.tsx` U4)이 한때 react-query v5 tracked-props 문제로 심판 결함이었다가 앵커 한 줄로 소생됨(개념: [[react-query tracked-props — 관찰자가 읽은 속성만 재렌더된다]]). **TRIP-619**: `updatedAt`(낙관적 락 기준버전, BR-U5-22)이 codegen 재생성으로 required가 되며 arrive 낙관 리터럴에 자리표시자(`optimisticAt` 문자열, `new Date` 미사용 — `liveTimeStructure` 준수)로 채움. complete/skip은 기존 `{...v,...}` 스프레드라 서버 `updatedAt`이 자동 보존(개념: [[낙관적 락 기준버전 (updatedAt)]] · [[낙관적 갱신과 롤백 (optimistic update)]] TRIP-619 절) |
| `src/features/execution/model/visitConflict.ts` | **신규(TRIP-619)** — `resolveVisitConflict(error): {kind:'conflict'\|'alreadyRecorded'\|'unknown'}` + `VISIT_CONFLICT_CODES` 상수. 409 응답의 `error.code`를 openapi 문서화 리터럴(`VISIT_CONFLICT`·`VISIT_ALREADY_RECORDED`, `backend/docs/design/openapi.yaml:895·931·2513`)로 분류하는 순수함수 — `features/itinerary/model/slotSwapError.ts` **구조만 미러**(features 간 import 금지라 재사용 불가, `isAlreadyRegistered.ts`는 status만 봐 두 code를 못 가름). 비axios·네트워크·비409→`unknown`(한 줄로 접음), 409+미지 code→`unknown`(계약 공백 방어, INV-4), 409+실코드→`conflict`/`alreadyRecorded`. **소비처 0(미배선)** — 프로덕션 배선된 `classifyFailure`(이 파일 아래·`features/record/model/useVisitCheck.ts`)는 여전히 status만 보고 두 code를 못 가른다(03b 참고-2, TRIP-613 소관). 개념: [[409 수렴 (이미 있음은 실패가 아니다)]] TRIP-619 절 |
| `src/features/execution/model/visitConflict.test.ts` | **신규(TRIP-619)** — V1~V5. 409 code 값을 발명하지 않고 openapi 리터럴을 직접 넣어 매핑을 계약값에 못박음(상수 뒤바뀜 검출 별도 `it`). |
| `src/features/execution/model/actualDistance.ts` | **신규(TRIP-397)** — `accumulateDistanceKm(points)`(하버사인 누적, **걸음 수는 산출 안 함** — `noStepCountStructure` 가드 대상) + `resolveActualRoute({locationConsent, points}) → ActualRouteView`. 위치 동의 없으면 실제 경로 레이어를 **항상 비활성**+사유 동반, 거리 0(BR-U4-42·PBT-U4-F3, 리포 CI 차단 게이트) |
| `src/features/execution/model/useActualRoute.ts` | **신규(TRIP-397)** — `useActualRoute()`. 실제 GPS 점열 수집은 네이티브(expo-location 포그라운드 워치)가 필요해 이 substrate에서 미배선 — **항상 `{locationConsent:false, points:[]}` 반환**(정직한 degrade, 거짓 경로 안 그림). `ponytail:` 주석으로 네이티브 워치 배선 지점 명시 |
| `src/features/execution/model/liveState.ts` | **신규(TRIP-395) → 20260820 재정합으로 `notFound` kind 추가.** `resolveLiveState(input) → LiveState`(판별 유니온: `loading`\|`notFound`\|`error`\|`outsideToday`\|`active`). 우선순위 `loading > notFound > error > outsideToday > active`. `isNotFound?: boolean`은 **옵셔널 additive**(기본 false, 기존 7 테스트 불변 — 리포 `confirmLocked?` 패턴 동형). 404(결함#4)를 5xx·네트워크와 분리하는 판정 핵심 — page가 `isNotFound(query.error)`(`@/shared/api/isNotFound`)를 계산해 주입(순수성 유지, 판정 자체는 trip 조회를 안 봄) |
| `src/features/execution/model/useLiveItinerary.ts` | **신규(TRIP-395)** — `useLiveItinerary(tripId)`. `useGetTripsTripIdItinerary` 얇은 래퍼(로직 0줄) — 판정은 `liveState.ts`, 조립은 `pages/live-itinerary`가 맡는다 |
| `src/features/execution/model/liveViewStore.ts` | **신규(TRIP-395)** — `useLiveViewStore`(Zustand). 서버가 모르는 UI 상태만(`segment: 'itinerary'\|'map'`·`toggle: 'plan'\|'actual'`·`sheetOpen`), persist 없음. `create(변수)` 형태(스토어 구조가드 정규식 `/\bcreate\(/` 회피, `tripWizardStore.ts`·`preferenceStore.ts`와 동형) |
| `src/features/execution/model/placeDetailView.ts` | **신규(TRIP-398)** — `buildPlaceDetailView(slots, poiId) → PlaceDetailView \| null`(D8, poiId 매칭 실패 시 null) + `resolveSlackLabel(currentEndAt, nextFixed) → string`. slack(여유)은 **두 확정 시각의 차**(BR-U4-24, wall-clock `new Date` 미사용 — D1) — `next.startAt`과 `cur.endAt`을 `"HH:mm:ss".split(':')`로 쪼개 **다른 이름의 minute 변수**(`toMinutes`)로 옮긴 뒤 함수 호출 사이에서만 빼 `liveTimeStructure`(`SLOT_TIME_ARITH` — `startAt`/`endAt` 식별자 인접 산술 금지)를 우회한다. 라벨은 차의 **부호**로 정성 이진(`여유 있음`/`여유 없음`/다음 고정 없음→`미확인`) — 소요시간 단위 문자열이 원리적으로 안 나와 `executionDurationStructure`도 자연 통과(INV-3). 결측은 `mapPeek.ts`의 `nameKo ?? '미확인'` 선례를 그대로 따름(BR-U4-40), "위치"는 슬롯 합성 필드에 주소·region이 없어(계약 공백, D3) 항상 `'미확인'` |
| `src/features/execution/model/usePlaceDetail.ts` | **신규(TRIP-398)** — `usePlaceDetail(tripId)`. `useGetTripsTripIdItinerary` 얇은 래퍼(로직 0, `useLiveItinerary.ts` 동형) — poiId 매칭·조립은 page(`live-place`)와 `placeDetailView.ts`가 나눠 맡는다 |
| `src/features/execution/model/mapPeek.ts` | **신규(20260820 재정합, sub-cycle B)** — `buildMapPeek(slots: ProjectedSlot[]) → MapPeekView`(순수 도출). `remainingCount`=예정 슬롯 수, `now`=진행 중(없으면 첫 non-done), `next`=now 바로 다음, 이름 결측은 `nameKo ?? '미확인'`(BR-U4-40). `LiveMapScreen.slots`(raw)는 무변경 유지하고 **부모(`LiveItineraryScreen`)가 이 함수로 도출해 `peek` prop 주입**하는 설계 — 동결 `LiveItineraryScreen.test.tsx` S3(`segment='map'`을 `peek` prop 없이 렌더)를 안 깨는 유일 배선(02aB ★1). 시각 산술 0(`liveTimeStructure` 무관) |
| `src/features/execution/ui/LiveItineraryScreen.tsx` | **신규(TRIP-395) → 20260820 재정합(sub-cycle A)으로 헤더·레일·탭바 대폭 확장 → TRIP-396으로 방문 완료·수동 도착 콜백 배선.** i01 무상태 화면. 헤더(제목 `${tripTitle} · N일차`+부제 `subtitle` prop 그대로 렌더 — **execution 안 `new Date` 0줄**, `liveTimeStructure` 준수의 핵심 seam) + 일자칩(outline pill) + 세그먼트(선택=`bg-primary`, 결함#1 근본수정으로 `grow-0` 스톱갭 제거) + 좌측 타임라인 레일(시각+상태 점 글리프+세로선) + `LiveSlotCard` 행 + 바닥 `BottomTabBar`(activeKey='itinerary', 결함#3 탈출구 — StaySearchScreen 복제탭바 선례). `peek={buildMapPeek(slots)}`·`onPressFullItinerary={() => onSelectSegment('itinerary')}` additive 2줄로 `LiveMapScreen`에 배선(★ 이 배선 자체는 어느 jest도 안 잼 — 03bB 경고-1, 6-b 실기 전용). **TRIP-396 additive props**: `onPressComplete?()`·`onManualArrive?(poiId)` — active 카드에만 `onPressComplete`를, upcoming 카드에만 `() => onManualArrive?.(poiId)`를 조건부 주입(`projected.state` 분기, 카드는 이 상태를 몰라도 됨). page가 `onPressNextNav`와 같은 자리에서 배선(아래 `layer-pages.md` `live-itinerary` 참고). **TRIP-561 additive**: `triggerChip?: ReactNode`(헤더 아래·타임라인 위) · `renderSlotBanner?: (slotKey: string) => ReactNode`(슬롯 카드 아래) — 둘 다 기본 undefined, 동결 `LiveItineraryScreen.test.tsx` S1~S8 무회귀(선례 `peek`·`onPressComplete?`). **TRIP-562 additive**: `onPressWatchlist?: () => void` + 상시 렌더 원형 FAB(`execution-live-watchlist-fab`, `ShieldGlyph`) — `SafeAreaView` 직계 자식(지도 블록·탭바의 **형제**)으로 배치해 `KakaoMapView` 위 오버레이 터치 흡수(TRIP-397 함정)를 회피. press가 `onPressWatchlist?.()`(콜백 미제공이면 no-op, 개념 [[후방호환 옵셔널 파라미터 (additive prop)]]) — planb import 0(콜백만, 배선은 page) |
| `src/features/execution/ui/TriggerChip.tsx` | **신규(TRIP-561)** — i08 상단 상주 트리거 칩, 순수 프레젠테이션(props만, 조회·판정 0). 루트 testID `execution-live-trigger-chip`(`bg-primary-pale`) + 제목(`text-primary-text`, **정적 요지만** — `triggerLabel(kind).label`, 동적 `reason` 미포함)·부제 + chevron(대안 보기, press→콜백)·×(끄기, press→콜백) Pressable. `icon` prop 없으면 경고삼각형 폴백. `./ExecutionGlyphs`만 import — `features/planb/model`(`triggerLabel`·훅) 직접 import 금지(features 경계, `LiveItineraryPage`가 중개) |
| `src/features/execution/ui/TriggerBanner.tsx` | **신규(TRIP-561)** — i01 슬롯 내부 경고 줄, 순수 프레젠테이션. 루트 testID `execution-live-trigger-banner`(`bg-primary-pale`) + 본문 Text(`text-primary-text`, `${label} · ${reason}` — 조립은 페이지가 함). **chevron·× testID를 렌더 트리에 아예 안 만든다**(칩과 달리 액션 없음 — 부재 단언으로 잠김). `TriggerChip`과 동일하게 순수-렌더(훅 import 0) |
| `src/features/execution/ui/LiveSlotCard.tsx` | **신규(TRIP-395) → 20260820 재정합으로 상태별 렌더 분리 → TRIP-399로 "다음 예정지" 섹션 추가 → TRIP-396으로 방문 완료 기능화.** `done`=컴팩트(이름+시각범위 배지만, 상태텍스트·버튼 **부재** — Figma 정합) · `active`=핑크 테두리+"진행 중"+상태줄 `{HH:mm} 도착 · 방문 중`(계획값+정성, 숫자 없음) + **[방문 완료]**(`execution-arrive-complete`, 활성·`flex-1`) + [사진]/[메모](`execution-arrive-{photo,memo}`, press 시 로컬 state `soonHintVisible`→`execution-arrive-soon-hint` "준비 중" 힌트, 무동작·무오류·BR-U4-38) + `nextDest`가 있으면 거리·[다음 장소 길찾기] CTA(`execution-arrive-next-nav`, 117-A 소관 미배선) · `upcoming`=배지+거리행+수동 [도착](`execution-arrive-manual-{slotKey}`). testID `-slot-{status\|time\|name\|hours\|distance}-{key}`(slotKey 규약 `{date}#{poiId}`) — done의 status·버튼은 **조건부 부재**가 정합. **TRIP-396 동결 개봉**: `LiveSlotCard.test.tsx`의 C5(구 비활성 단언)·C7(구 부재 단언)을 신 계약(활성 완료버튼·컴팩트 done)에 맞춰 재작성(01b Q2, 선례 TRIP-401/456). additive 옵셔널 prop `onPressComplete?`·`onPressManualArrive?`·`nextDest?: NavDest\|null`·`onPressNextNav?: () => void`(기본 undefined, 카드는 `Linking`·router를 모르는 순수 뷰 — `../model/nextNav.ts` 참고). ⚠️ done 카드 시각범위 배지(`-slot-range-{key}`) 값은 어느 테스트도 안 잼(03b 경고-1, jest가 볼 수 있는 leaf인데 무심판) |
| `src/features/execution/ui/LiveMapScreen.tsx` | **신규(TRIP-397) → 20260820 재정합(sub-cycle B)으로 토글 형제화·250px·peek 대폭 확장.** i02(계획)·i03(실제) 공유 컴포넌트, `toggle` prop으로 갈리는 한 얼굴. 결함#2 근본수정: 계획\|실제 토글을 `KakaoMapView`(WebView) 위 `absolute` 오버레이에서 **형제 노드**로(지도 250px 고정 블록 + 토글·peek는 flow) — `liveMapStructure` 가드가 소스에 `absolute` 0건 강제. 토글 라벨 "계획 동선"/"실제 경로", 선택색 흰 pill(`bg-canvas`, 구 `bg-ink` 제거). peek 헤더 3분기 상호배타(plan→`-peek-remaining`/actual+동의→`-actual-distance`/actual+미동의→`-actual-disabled`) + 지금/다음 2행(양쪽 공통) + "전체 일정 ›" + 각주(Q5, BR-U4-41 유지). `slots`(raw, 핀 도출용)는 무변경 — peek는 별도 prop |
| `src/features/execution/ui/ExecutionGlyphs.tsx` | **신규(20260820 재정합, sub-cycle A) → TRIP-398로 `BackArrowGlyph`·`ShareGlyph` 2종 추가.** 인라인 SVG 10종: `RailDoneGlyph`·`RailActiveGlyph`·`RailUpcomingGlyph`(레일 상태 점)·`ClockGlyph`·`VisitCheckGlyph`·`CameraGlyph`·`MemoGlyph`·`RouteArrowGlyph`(카드 아이콘) + `BackArrowGlyph`·`ShareGlyph`(i05 헤더, TRIP-398 — `grep BackArrow\|Share` 0건 확인 후 신규 추가, features 간 import 금지라 auth/itinerary 글리프 재사용 불가). `features/itinerary/ui/ItineraryGlyphs.tsx`와 유사 개념이 있으나 features 간 import 금지라 패턴만 미러링(재사용 아님). `*Glyphs.tsx` raw hex 스캔 제외 관례(리포 전체) — 이 프레임은 Figma 변수 바인딩이 없어(`get_variable_defs={}`) 색·path는 스크린샷 관측 근사(6-b 실기 캘리브레이션 대상). **TRIP-561로 `WarningTriangleGlyph`·`WeatherCloudGlyph`·`ChevronRightGlyph`·`CloseGlyph` 4종 + `PRIMARY_TEXT` 상수 추가** — 트리거 칩·배너 아이콘. 동명 글리프가 `explore`(`WarningTriangleGlyph`·`CloseGlyph`)·`auth`(`BackArrowGlyph` 반전)에 있으나 features 간 import 금지라 벡터만 그대로 옮겨 미러 신설(재구현 아님, 비구름만 Figma 근사 새 그림). 색 기본 `PRIMARY_TEXT='#C13515'`(raw hex, `*Glyphs.tsx` 스캔 제외 관례). **TRIP-562로 `ShieldGlyph` 1종 추가**(watchlist FAB 아이콘, 방패+체크) — planb 행 아이콘 3종(`WeatherCloudGlyph`·`ClockGlyph`·`ShopGlyph`, `layer-features-planb.md` 참고)은 `PlanbGlyphs.tsx`에 넣고 이 FAB 아이콘만 여기 둔 이유는 FAB가 execution/ui 안에 살아 execution→planb import를 피해서(브리프 §함정 ★6 준수, 어느 파일이냐는 판정성 이견 아님) |
| `src/features/execution/model/nextNav.ts` | **신규(TRIP-399)** — i01 "다음 예정지" 딥링크 폴백 사다리. `buildAppNavUrl`(`kakaomap://route?ep={lat},{lng}`)·`buildWebNavUrl`(`https://map.kakao.com/link/to/{name},{lat},{lng}`, raw 무인코딩)·`resolveNextDest(slots)`(첫 `upcoming` 슬롯에서 좌표 둘 다 `Number.isFinite`면 `NavDest`, 아니면 null)·`openNextNav(dest, fallback)`(canOpenURL→앱 열기, 실패/부재→웹, 웹도 실패→`fallback(distanceRange)`) 4함수. `expo-router` import 0(AC-7, 라우터 미개입을 구조로 보장). ⚠️ **이 빌드에서 프로덕션 호출자 0(데드코드)** — `activePoiId` 미배선으로 active 카드 자체가 안 생겨 CTA에 안 닿는다(`repo-traps.md` "여행 중 실행" 절 참고, TRIP-395 선재 공백·이 티켓 회귀 아님). `liveTimeStructure`(execution/** 전수) 사정거리 안이지만 `new Date`류 0이라 자연 통과 |
| `src/features/execution/ui/PlaceDetailScreen.tsx` | **신규(TRIP-398)** — i05 현재 장소 상세 무상태 화면(`{view: PlaceDetailView, onPressItinerary?}` props만, 재판정 없음). 헤더(뒤로가기·공유 아이콘은 시각 요소만 — 핸들러 없음, 계약 prop에 콜백 자리가 없다)·hero(`imageUrl` NULL→기본 이미지 미발명 placeholder, D6·TRIP-219)·부제·태그칩·정보카드(영업시간·위치·다음 일정까지 — 결측은 `-unknown-{field}` testID로 값 자리 자체가 바뀜, BR-U4-40)·"지금 여기"(계획값 `arrival`만, "머무는 중" 활성 판정은 신호 미배선이라 정적 접미 — ★6 무심판)·미니맵 placeholder(`-map`, D5 후속 이연)·하단 CTA 2개(`일정에서 보기`=`onPressItinerary`, `길찾기`=onPress 없음, US-ONTRIP-03 소관). `executionDurationStructure`(`\d+분\|\d+시간\|소요` 0건) 사정거리 안 — slack이 정성 라벨이라 자연 통과 |

**구조가드**(`layer-test.md`에 정본 — `liveTimeStructure`·`executionDurationStructure`·`noStepCountStructure`·`liveMapStructure` 4종이 이 디렉토리를 스캔): 이 층에 코드를 추가할 때 `new Date`류·소요시간 표기·걸음수 심볼·지도 `absolute` 오버레이 4종을 피한다 — 피하는 방법은 위 각 파일 행의 "왜"를 참고.

**6-b 실기 전용(jest 원리적 무심판)**: 일자칩 outline pill·타임라인 레일·상태별 카드 픽셀·지도 250px 컬럼·토글 흰 pill·핀·"© Kakao"·peek 우측 디테일·탭바 실터치·토글 실터치·지도 제스처. `_dev/preview.tsx`의 `live-itinerary`·`live-map-{plan,actual,actual-off}` 키가 유일한 눈으로 보는 자리(실화면은 오늘 구간 안 여행+일정 실재가 필요해 도달 어려움).
