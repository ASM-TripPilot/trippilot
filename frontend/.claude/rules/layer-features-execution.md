---
paths:
  - "src/features/execution/**"
---
# `src/features/execution/` — 여행 중(execution) 현장 표면 (TRIP-395·397·398, i01·i02·i03·i05)

**신규 층 규칙 파일 — 이번 사이클(`20260820-trip395-397-figma-align`)에서 처음 만들었다.** `execution/` 자체는 TRIP-395 substrate(`c1f7217`)부터 여러 커밋에 걸쳐 자랐으나 층별 표는 지금까지 어느 `layer-*.md`에도 없었다(structure-index `--check` 실측 — 12파일 전부 "누락"). 아래 표가 정본이다. 여행 중 현장(오늘 일정 실시간 보기)을 타임라인(i01)·지도(i02 계획·i03 실제 GPS)로 보여준다. BR-U4-34~46·PBT-U4·INV-3(소요시간 비표시) 소관 — 시각은 계획값만 통과, 재추정 금지(`liveTimeStructure` 가드, `layer-test.md` 참고).

| 파일 | 역할 |
|---|---|
| `src/features/execution/model/slotProgress.ts` | **신규(TRIP-395) → TRIP-396으로 실제 배선(`progress` 인자를 처음 채움).** `projectSlotProgress(slots, {completedPoiIds?, activePoiId?}) → ProjectedSlot[]`. 슬롯에 진행 상태(`done`\|`active`\|`upcoming`)를 붙이는 순수 사영. **`startAt`/`endAt`을 읽지도 연산하지도 않고 슬롯을 그대로 통과**시킨다(PBT-U4-F1 — 시계로 도착 시각을 재추정하지 않는다, BR-U4-34·DEC-U4-9). 완료가 진행 중보다 우선. i01 "기록 없음" 기본(빈 progress)은 전부 `upcoming`. **파일 자체는 무변경**(재사용, 재구현 아님) — 인자만 `LiveItineraryPage`가 채우기 시작했다(아래 `useVisitCheck.ts`·`visitProgress.ts` 참고, `repo-traps.md`의 "i01 active 카드 도달성 없음" stale 항목이 이 배선으로 해소) |
| `src/features/execution/model/dwellMinutes.ts` | **신규(TRIP-396)** — `dwellMinutes(arrivedAt, completedAt): number\|null`. `completedAt − arrivedAt` 분(整數) 산출, 한쪽 null→null, 역전→`Math.max(0,·)`(정본 침묵, 자가판단). BR-U4-37·INV-U4-03(체류를 화면에 표시하지 않고 트리거 입력으로만). `liveTimeStructure` 가드 사정거리라 `"HH:mm:ss".split(':')`로 쪼갠 `hh`/`mm` 지역 변수로만 연산(`placeDetailView.toMinutes` 선례 동형, Date API·식별자 인접 산술 0). **⚠️ 소비처 0(데드코드)** — 서버가 `arrivedAt`/`completedAt`로 dwell을 스스로 도출하고 `POST /complete`·`VisitCheck` 어디에도 클라→서버 dwell 필드가 없다(API 계약 실측). AC-1 요구라 빌드하고 PBT로 정확성만 잠갔다 — `nextNav.ts`(TRIP-399, 호출자 0)와 같은 계열. 후속 티켓 후보: 클라 dwell 소비 창구 신설 또는 제거 판단(`.claude/rules/repo-traps.md` "여행 중 실행" 절 참고) |
| `src/features/execution/model/visitProgress.ts` | **신규(TRIP-396)** — `deriveVisitProgress(list: VisitCheckList): VisitProgress{completedPoiIds, activePoiId, visitCheckIdByPoiId}`. `GET /visits/days/{day}` 응답에서 완료(`completedAt≠null && skippedAt==null`)와 도착·미완료(`arrivedAt≠null && completedAt==null && skippedAt==null`, 완료 목록에 없는 것만)를 분류 — 완료가 진행 중을 이긴다. 즉석(slotKey=null)도 도착이면 active. `slotProgress.ts`의 `projectSlotProgress`에 주입되는 것이 유일한 소비 경로(페이지 1회 배선, FSD 구조가드) |
| `src/features/execution/model/useVisitCheck.ts` | **신규(TRIP-396) → TRIP-619로 도착(arrive) 낙관 리터럴에 `updatedAt: optimisticAt` 1필드 추가.** `useVisitCheck({tripId, day})`. 도착(`POST /visits`)·완료(`POST /visits/{id}/complete`) imperative 낙관 갱신(`savedPlaces.ts` 패턴, 생성 훅 대신 raw 함수를 직접 호출 — promise 반환 계약이 필요해서). **★ 롤백이 슬롯키(레코드) 단위**다 — `savedStays.ts`/`savedPlaces.ts`의 W-2(동시 두 카드 토글 시 통짜 스냅숏 롤백이 서로를 지움)를 이 파일에서 처음 슬롯키 단위로 고쳤다. 이 방어의 유일한 심판(`useVisitCheck.integration.test.tsx` U4)이 한때 react-query v5 tracked-props 문제로 심판 결함이었다가 앵커 한 줄로 소생됨(개념: [[react-query tracked-props — 관찰자가 읽은 속성만 재렌더된다]]). **TRIP-619**: `updatedAt`(낙관적 락 기준버전, BR-U5-22)이 codegen 재생성으로 required가 되며 arrive 낙관 리터럴에 자리표시자(`optimisticAt` 문자열, `new Date` 미사용 — `liveTimeStructure` 준수)로 채움. complete/skip은 기존 `{...v,...}` 스프레드라 서버 `updatedAt`이 자동 보존(개념: [[낙관적 락 기준버전 (updatedAt)]] · [[낙관적 갱신과 롤백 (optimistic update)]] TRIP-619 절) |
| `src/features/execution/model/liveState.ts` | **신규(TRIP-395) → 20260820 재정합으로 `notFound` kind 추가 → 20260923 경량 사이클(TRIP-751 계열, 티켓 없음)으로 `outsideToday` kind 삭제.** `resolveLiveState(input) → LiveState`(판별 유니온: `loading`\|`notFound`\|`error`\|`active`). 우선순위 `loading > notFound > error > active`(제품 규칙 변경 — 오늘이 일정 밖이어도 막지 않는다, 사용자 결정). `isNotFound?: boolean`은 **옵셔널 additive**(기본 false, 기존 7 테스트 불변 — 리포 `confirmLocked?` 패턴 동형). 404(결함#4)를 5xx·네트워크와 분리하는 판정 핵심 — page가 `isNotFound(query.error)`(`@/shared/api/isNotFound`)를 계산해 주입(순수성 유지, 판정 자체는 trip 조회를 안 봄). **`active`의 `todayIndex`**는 오늘이 `days`에 있으면 그 인덱스, `input.todayDate < (days[0]?.date ?? '')`(문자열 사전순 비교)면 여행 전이라 0, 아니면 여행 후라 마지막 인덱스(`days.length-1`, 빈 배열은 `Math.max(-1,0)=0`) — `'YYYY-MM-DD'` 사전순=시간순 성질을 그대로 씀([[UTC 사전식 날짜 경계 (타임존 안전)]] 참고). 2일 일정에서는 이 폴백 식과 `findIndex`가 항상 같은 답을 내므로(첫날 아니면 마지막날뿐), 이 분기를 지키는 심판은 **3일 이상 일정의 가운데 날**이어야 판정력이 있다(뮤테이션 실측, 개발로그 참고) |
| `src/features/execution/model/useLiveItinerary.ts` | **신규(TRIP-395)** — `useLiveItinerary(tripId)`. `useGetTripsTripIdItinerary` 얇은 래퍼(로직 0줄) — 판정은 `liveState.ts`, 조립은 `pages/live-itinerary`가 맡는다 |
| `src/features/execution/model/placeDetailView.ts` | **TRIP-398 신규 → TRIP-755(배치)로 i10 재작성 대응 확장 → 20260925 경량 사이클(`20260925-trip755-remove-slack-row`)로 slack 축소.** `buildPlaceDetailView(slots, poiId) → PlaceDetailView \| null`(D8, poiId 매칭 실패 시 null) + `buildPlaceShareMessage(view) → string`(주소 있으면 `장소명\n주소`, 없으면 장소명만 — 순수 함수로 뺀 이유는 운영 주소가 항상 null이라 페이지 테스트로는 이 분기에 못 닿기 때문, 단위 테스트 V-9 전담). **`resolveSlackLabel`·`PlaceDetailView.slackLabel`·`toMinutes`는 사용자 결정으로 삭제됨**("다음 일정까지" 행 제거, 위 `PlaceDetailScreen.tsx` 행 참고) — `liveTimeStructure` 우회 합법 형태 선례가 필요하면 `dwellMinutes.ts`·`features/planb/model/slackTime.ts`를 본다(동형 split 우회, 이 파일 자신은 더 이상 해당 없음). 결측은 `mapPeek.ts`의 `nameKo ?? '미확인'` 선례를 그대로 따름(BR-U4-40). **TRIP-755로 `location` 필드 삭제**(주소 행으로 대체, 값은 서버 계약 공백이라 운영에서 항상 null→화면이 "미확인" 표시) — `galleryUrls`·`photoTotal`·`pitchTitle`·`pitchBody`·`address`·`admissionFee`는 운영 null, 프리뷰 픽스처만 값을 채운다(INV-1, 지어내지 않는다) |
| `src/features/execution/model/usePlaceDetail.ts` | **신규(TRIP-398)** — `usePlaceDetail(tripId)`. `useGetTripsTripIdItinerary` 얇은 래퍼(로직 0, `useLiveItinerary.ts` 동형) — poiId 매칭·조립은 page(`live-place`)와 `placeDetailView.ts`가 나눠 맡는다 |
| `src/features/execution/ui/TriggerChip.tsx` | **TRIP-561 신설 → TRIP-748로 제자리 전면 재작성(i02 지도 위 알약).** props 5→2(`label`·`onPressAlternative`)로 축소 — `subtitle`·`icon`·`onDismiss` prop과 testID `execution-live-trigger-dismiss`(×)는 **삭제**. 흰 알약(`bg-canvas` + `border-hairline` + `rounded-pill`) + `WarningFilledGlyph`(SVG 채운 삼각) + 라벨 Text(`numberOfLines=1`) + `›` 텍스트, 그림자는 `BACK_SHADOW`와 값이 같은 로컬 상수(pages 상수를 features가 import 못 함 — 층 방향). testID `execution-live-trigger-chip`·`execution-live-trigger-alternative`는 **보존**. `./ExecutionGlyphs`만 import(기존 관례 유지) |
| `src/features/execution/ui/ExecutionGlyphs.tsx` | **… → TRIP-746 대폭 축소 → TRIP-748로 트리거 아이콘 교체 → TRIP-755로 히어로 원형 버튼용 글리프 2종 추가.** 현재 export: `RailDoneGlyph`·`RailActiveGlyph`·`RailUpcomingGlyph`·`BackArrowGlyph`·`ShareGlyph`·`WeatherCloudGlyph`·`WarningFilledGlyph`·**`HeroPinGlyph`**(신규 TRIP-755, 흰 핀 — `shared/location/LocationPinGlyph`가 있으나 벡터가 Figma와 달라 복제, 동명 회피)·**`HeroPhotoGlyph`**(신규 TRIP-755, 흰 카메라 — `entities/…/SlotGlyphs.PhotoGlyph`는 tone이 ink·disabled뿐이라 흰 tone 추가 대신 execution에 복제, 선 두께도 1.6 vs 2로 다름). **TRIP-755로 `BackArrowGlyph`·`ShareGlyph` 기본색이 `MUTED_SOFT`→`INK`로 변경**(원형 흰 버튼이 첫 실 소비처라 raw hex 대신 기본값을 바꿈, `MUTED_SOFT` 상수는 고아가 되어 삭제). **TRIP-748 삭제**: `WarningTriangleGlyph`·`ChevronRightGlyph`·`CloseGlyph`(execution판, 옛 칩·배너 전용 — 소비처 0 확인 후 정리. 동명 `CloseGlyph`는 `features/home`판·동명 `ChevronRightGlyph`는 `PlanbGlyphs`에 별도 존재, 무관). **⚠️ `WeatherCloudGlyph`는 프로덕션 소비처 0**(통합 테스트가 "0개 그려짐"을 단언하려고 import — 지우면 그 심판이 깨진다. 테스트 개정 시 함께 정리할 후보, 학습노트 새 티켓 후보). `*Glyphs.tsx` raw hex 스캔 제외 관례(리포 전체) — 색·path는 스크린샷 관측 근사(6-b 실기 캘리브레이션 대상). **흰 글리프(`HeroPinGlyph`·`HeroPhotoGlyph`) 색은 jest 사각** — SVG fill이라 자동 심판이 못 보고 6-b 실기 캡처로만 확인(2026-09-25 캡처 확인함) |
| `src/features/execution/model/nextNav.ts` | **신규(TRIP-399)** — i01 "다음 예정지" 딥링크 폴백 사다리. `buildAppNavUrl`(`kakaomap://route?ep={lat},{lng}`)·`buildWebNavUrl`(`https://map.kakao.com/link/to/{name},{lat},{lng}`, raw 무인코딩)·`resolveNextDest(slots)`(첫 `upcoming` 슬롯에서 좌표 둘 다 `Number.isFinite`면 `NavDest`, 아니면 null)·`openNextNav(dest, fallback)`(canOpenURL→앱 열기, 실패/부재→웹, 웹도 실패→`fallback(distanceRange)`) 4함수. `expo-router` import 0(AC-7, 라우터 미개입을 구조로 보장). ⚠️ **이 빌드에서 프로덕션 호출자 0(데드코드)** — `activePoiId` 미배선으로 active 카드 자체가 안 생겨 CTA에 안 닿는다(`repo-traps.md` "여행 중 실행" 절 참고, TRIP-395 선재 공백·이 티켓 회귀 아님). `liveTimeStructure`(execution/** 전수) 사정거리 안이지만 `new Date`류 0이라 자연 통과 |
| `src/features/execution/ui/PlaceDetailScreen.tsx` | **TRIP-398 신규(i05 옛 코드) → TRIP-755로 i10 현재 장소 상세 전면 재작성.** 무상태 화면(`{view: PlaceDetailView, onPressItinerary?, onPressBack?, onPressShare?}` props만, 재판정 없음). **풀블리드 갤러리 히어로**(`ScrollView horizontal pagingEnabled` + `useWindowDimensions().width`, `imageUrl` 없으면 `galleryUrls=[]`) 위에 원형 버튼 3개(뒤로·공유는 `onPressBack`/`onPressShare` 콜백 없으면 0개 렌더 — S6, 하트는 콜백 prop이 없고 로컬 `useState`로 "저장 기능은 준비 중이에요" 안내만 켠다 — D-a, 눌러도 SVG 서브트리는 상수라 한 글자도 안 바뀜 S7)·장소명+핀 아이콘+분류·"1 / N" 카운터 칩(앞 숫자 1 고정)·추천 카피(제목/본문, 둘 다 null이면 칸째 미표시)·해시태그 칩·정보 카드(`ValueOrUnknown` 헬퍼 — 영업시간→주소→입장료 **3행**, 결측은 `-unknown-{field}` testID로 값 자리가 바뀜 BR-U4-40. **"다음 일정까지" 행은 20260925 경량 사이클(`20260925-trip755-remove-slack-row`, 사용자 결정)로 제거** — Figma 3행 정합에 맞췄다. US-ONTRIP-02(여유 표시 요구)와는 정본 드리프트로 남고(사용자가 알고 수용), 되살리려던 심판(`PlaceDetailScreen.test.tsx` S1·S11·`devPreviewLiveHub.test.tsx` AC-11)이 있으니 다시 넣기 전에 먼저 확인할 것)·실지도(`MapView viewOnly showScaleBar`, `connectPins` 미전달 — 핀 1개라 선 없음)·"이곳의 사진" 섹션(사진 3장 이상일 때만, 썸네일 앞 3장 + `photoTotal−3>0`이면 마지막 칸에 `+N` 오버레이, `onPressSeeAll` 있을 때만 "모두 보기 ›"). "지금 여기"·하단 위치 행·`location`/`arrival` 필드는 **TRIP-755로 삭제**(정보 카드 주소 행으로 대체). `executionDurationStructure`(`\d+분\|\d+시간\|소요` 0건) 사정거리 안 — slack이 정성 라벨이라 자연 통과 |

**구조가드**(`layer-test.md`에 정본 — `liveTimeStructure`·`executionDurationStructure`·`noStepCountStructure` 3종이 이 디렉토리를 스캔): 이 층에 코드를 추가할 때 `new Date`류·소요시간 표기·걸음수 심볼 3종을 피한다 — 피하는 방법은 위 각 파일 행의 "왜"를 참고. **`liveMapStructure`는 TRIP-746으로 삭제됐다**(옛 `LiveMapScreen.tsx`의 `absolute` 오버레이 금지 가드 — 그 화면 자체가 사라져 사정거리가 없어짐). i01 허브의 삭제·이관 소스 층 가드는 이제 `liveHubStructure.test.ts`(전 src 스캔, `layer-test.md` 참고)가 맡는다.

**6-b 실기 전용(jest 원리적 무심판)**: 시트 헤더·타임라인 레일·상태별 카드 픽셀·핀 배치(fit-to-pins 부재, 새 티켓 후보)·네이버 기본 줌 컨트롤(노출 안 끔, 새 티켓 후보)·3스냅 실전환·수정 알약 딤 없음. `_dev/preview.tsx`의 `live-hub-{closed,half,expanded}` 3키가 유일한 눈으로 보는 자리(옛 `live-itinerary`·`live-map-*` 키는 TRIP-746으로 삭제).
