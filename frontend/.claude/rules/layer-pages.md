---
paths:
  - "src/pages/**"
---
# `src/pages/` — FSD pages 층 (TRIP-173 신설, 16슬라이스 — TRIP-181로 `stay-search`·TRIP-183으로 `region-picker`·TRIP-198로 `stay-register`·TRIP-205로 `trip-new-step1`·TRIP-221로 `place-explore`·TRIP-223로 `saved-places`·TRIP-225로 `trip-new-step2`·TRIP-296으로 `itinerary-mustvisit`·TRIP-297으로 `itinerary-draft`·TRIP-299로 `itinerary-plan`·**TRIP-302로 `itinerary-edit` 추가(슬라이스1)**·TRIP-335로 `itinerary-option-swap` 추가(슬라이스1, `itinerary-draft`엔 `SlotCandidateSheetContainer.tsx` 추가)·TRIP-338로 `itinerary-manual` 추가·TRIP-395로 `live-itinerary` 추가·TRIP-398로 `live-place` 추가)


구 `features/{auth,onboarding}/containers/*`(훅 ↔ 화면 배선)가 이주한 자리. **아직 방향 규칙 없음**(위 "FSD 층 방향 규칙" 참조) — 지금은 폴더 배치일 뿐이다.

| 슬라이스 | 파일 | 역할 |
|---|---|---|
| `login` | `src/pages/login/ui/LoginPage.tsx` | 로그인 훅 ↔ 화면 배선(구 `SocialLoginContainer.tsx`, 심볼도 `*Page`로 개명) |
| | `src/pages/login/index.ts` | 배럴 — `LoginPage` 재수출 |
| `onboarding-terms` | `src/pages/onboarding-terms/ui/TermsPage.tsx` | 약관 훅 ↔ 화면 배선(구 `TermsContainer.tsx`) |
| | `src/pages/onboarding-terms/index.ts` | 배럴 |
| `onboarding-nickname` | `src/pages/onboarding-nickname/ui/NicknamePage.tsx` | 닉네임 훅 ↔ 화면 배선(구 `NicknameContainer.tsx`) |
| | `src/pages/onboarding-nickname/index.ts` | 배럴 |
| `onboarding-pref1` | `src/pages/onboarding-pref1/ui/PrefStep1Page.tsx` | 취향 1/2 배선(구 `PrefStep1Container.tsx`) |
| | `src/pages/onboarding-pref1/index.ts` | 배럴 |
| `onboarding-pref2` | `src/pages/onboarding-pref2/ui/PrefStep2Page.tsx` | 취향 2/2 배선(구 `PrefStep2Container.tsx`) |
| | `src/pages/onboarding-pref2/index.ts` | 배럴 |
| `stay-search` | `src/pages/stay-search/ui/StaySearchPage.tsx` | **TRIP-181 신규 → TRIP-182로 확장**(22→61줄). `useLocalSearchParams`로 `region`(배열·빈 문자열 방어 후 `\|\|` 폴백 `'부산'`)에 더해 `amenity`·`stayType`도 읽어 `toParamList()`로 `string\|string[]→string[]` 정규화(빈 값은 파라미터 자체를 안 보냄) → `useStaySearch(...)` → **`resolveStaySearchState(...)`가 판정을 이 한 곳에서만 수행**(화면은 재판정 금지, 구조 가드로 잠금) → `<StaySearchScreen region items state onRetry={() => refetch()} />`. `data?.items?.length ?? 0`(03b W-3 수정 — `?.items.length`였다면 계약 위반 응답에서 크래시). **TRIP-413**: `onPressTab={(key) => router.replace(key === 'home' ? '/' : \`/${key}\`)}` 배선(하단 복제 탭바 실동작, `home`만 파일 규약상 `/`). **TRIP-414**: `onPressCreateTrip={() => router.push('/trips/new/step1')}`(탐색 랜딩 bridge CTA·PlaceExplore CtaBar와 동일 목적지). **TRIP-415**: 필터 시트 열림 상태·선택 초안을 페이지가 소유, 적용 시 `router.setParams`로 `amenity`·`stayType` 갱신해 재조회 트리거 — `StayFilterSheet`엔 완전 제어로 내려줌, 지역 칩은 `/explore/region` 재사용(새 UI 없음). **TRIP-416**: 0건 안내 카드 콜백 3개 배선 — `onPressChangeRegion={() => router.push('/explore/region')}`(상단 지역 칩과 동일 목적지 재사용) · `onRelaxFilters={() => router.setParams({amenity:[], stayType:[]})}`(empty 필터완화 + filter-zero 초기화 겸용, region 키 미포함 — merge라 넣으면 지역이 흔들림) · `onClearCulpritFilter={(reason) => router.setParams(relaxCulpritFilter(reason, {amenity, stayType}))}`(신규 `features/stay/model/relaxCulpritFilter` 재사용, 반환값을 그대로 넘겨도 키가 정확히 2개뿐이라 구조분해 재조립 불필요). **TRIP-417**: 저장 하트 실배선 — `isAuthed = getAccessToken() !== null`(렌더 시점 1회 동기, `PlaceExplorePage` seam 동형) → `useSavedStays({isAuthed})`(`features/stay/model`) → `attemptToggle(item)`가 key를 로컬 `pendingKeys`(`useState`)에 올리고 `isSaved(key) ? remove(item) : save(item)` 분기 → 응답 후 pending 해제 → `outcome.reason==='unauthenticated'`면 `router.push('/(auth)/login')`(정적 `router` 싱글턴, 동결 통합테스트 목 형태 유지). 화면에 `savedKeys`·`pendingKeys`·`onToggleSave` 배선 |
| | `src/pages/stay-search/index.ts` | 배럴(`StaySearchPage` 재수출) — 게이트②에서 배럴 유지 vs 딥임포트 선택지 중 **배럴 유지 채택**(6슬라이스 중 5개가 배럴 관례라 일관성) |
| `stay-register` | `src/pages/stay-register/ui/StayRegisterPage.tsx` | **신규(TRIP-198), TRIP-369로 뒤로가기 배선 추가** — e05 등록 배선(**실측 259줄**, TRIP-369 시점 — 이전 문서 182줄은 TRIP-199 핀 탭 확장분 미반영이던 것을 이번에 정정). 상태 8축(`query`/`submittedQuery` 분리·후보·좌표확정·지도시트·날짜범위·달력월·제출)을 전부 여기서 지고 무상태 화면에 `flow` 한 덩어리 + 콜백 13개로 내린다. `useGetStaysGeocode`는 `enabled: (submittedQuery ?? '').trim() !== ''`로 꺼 둔다(빈/공백 검색어가 서버 400 → 엉뚱한 "지도 장애" 문구가 되는 경로 차단, 5-b W-2). **재검색·후보 변경이 `coordConfirmed`를 반드시 푼다**(§3-2 가중치 1.0 — 'A의 좌표'에 'B의 이름'이 붙는 불일치 차단). 제출 진입에 `canSubmitStayRegister` 가드를 둔다(5-b B-1 — 버튼 `disabled`만으로는 실패 배너의 "다시 시도"가 통째로 우회했다). **TRIP-369**: 화면에 `onBack={() => router.back()}` 한 줄 배선(기존 `useRouter()` 재사용, 제출 성공 경로가 이미 쓰던 인스턴스) — `StayRegisterPage.back.integration.test.tsx`(SB-1)가 잠근다. ⚠️ 여기는 `useRouter()` 훅을 쓰는데 `StaySearchPage`는 정적 `router` 싱글턴이다 — 설계 비대칭이 아니라 **각 페이지 통합테스트 목의 차이**가 원인이다(그쪽 동결 목이 `useRouter` 미제공) |
| | `src/pages/stay-register/index.ts` | 배럴(`StayRegisterPage` 재수출) |
| `region-picker` | `src/pages/region-picker/ui/RegionPickerPage.tsx` | **신규(TRIP-183) → TRIP-445로 서버 카탈로그 전면 재작성.** URL `purpose`는 신뢰 경계로 취급(`'trip'`이 아니면 전부 `'stay'`로 결정론적 폴백) → `useRegions()`(전체 카탈로그) → `filterRegions(data??[], query)` → 화면에 `regions`·`isLoading`·`isError`·`onRetry=refetch` 전달. 카드 press는 서버 `Region` 객체 그대로 받아 stay는 `/stays?region={name}`(카탈로그 정식명, 구 큐레이션 짧은 이름에서 교체 — code-critic 확인-1: 백엔드 수용 여부 FE 검증 불가, 후속 확인 권고), trip은 `/explore/destination/{regionCode}`로 분기. **삭제**: `resolveNearby(...)` 호출·`firstConfirmedSavedStayCoords`·`getSavedStays`·`expo-location`·`/stays?lat=&lng=` 전부(내 주변 배선 완전 소멸, `regionCatalogStructure.test.ts` G4가 회귀 잠금) |
| | `src/pages/region-picker/index.ts` | 배럴 — `RegionPickerPage` 재수출 |
| `trip-new-step1` | `src/pages/trip-new-step1/ui/TripNewStep1Page.tsx` | **TRIP-205 신설 → TRIP-206으로 제출·오류 배선 추가 → TRIP-207로 예산 배선 추가 → TRIP-208로 숙소 날짜 가져오기 조회·판정 배선 추가(+43/-6) → TRIP-209로 꼭 갈 곳 시드·등록 배선 추가(+168줄, 이 파일의 핵심 변경) → TRIP-288로 재시드 가드 해제 + 등록 직전 재조회(+18/-9) → TRIP-367로 '더 담기' 목적지 분기(+8/-1) → TRIP-368로 날짜 시트 배선(+13/-1) → TRIP-389로 종료일 파생·마커·제출 정합 배선.** `useSavedStays()` → `resolveStayImport({stays, loading, periodFilled})`(`features/trip/model/stayDateImport.ts`) → 완성형 props를 화면에 내림. `importStayDates()`가 `stayImport.kind !== 'ready'`면 즉시 반환 — 화면의 `disabled`가 접근성 상태만으로도 통과할 수 있어 배선이 한 번 더 잠그는 것이지만, 실제로 이 줄을 지키는 건 jest가 아니라 `pnpm tsc`의 타입 좁히기다(지워도 jest는 14/14 green, tsc만 잡음). g01 위저드 1/2 배선. `destinations.length>0 && startDate/endDate 둘 다 있음 && validateTripDraft(...).length===0`로 `canProceed` 계산(TRIP-205). `baseDate` prop 미지정 시 `todayIso()`로 폴백(⚠️ 이 폴백이 실제 프로덕션의 유일한 경로인데 그 반환값을 검증하는 테스트가 없다 — 문제로그 참고). `useTripWizardStore` 셀렉터 6개로 액션을 개별 구독. **TRIP-206 추가분**: `submit()`이 `touched` 게이팅으로 클라 위반을 문구화(여행지·기간 두 축 다 touched일 때만 박수 초과 문구 — `NaN` 방지) → `createTrip.mutateAsync({ data })` 성공 시 `setCreatedTripId`+`router.push('/trips/new/step2')`(둘 다 **try 밖** — 5-c 수정, 문제로그 참고) → 실패 시 `classifyServerFailure`가 `overseas`/`banner` 둘로만 분류(모르는 코드·필드는 전부 배너, INV-4). `PERIOD_ERROR_FIELDS` 갈래는 5-c에서 삭제(심판 0건이라 서버 기간 오류도 배너로 통일, 문제로그 참고). **TRIP-207 추가분**: 예산 프리필은 **파생값**(`budgetTouched ? budgetText : prefillText` — 스토어에 안 씀), `isPrefillableBudget`(음수 아닌 정수)로 신뢰 경계(취향 응답)에서 프리필 진입을 가드(5-c, code-critic W-2). `canProceed`에 `parsedBudget.kind !== 'invalid'` 추가. `handleBlurBudget`이 `budgetTouched && Number.isSafeInteger(amount)`일 때만 콤마 재포맷(5-c, W-1). `submitError` 배너를 걷는 `useEffect` 의존 배열에 `budgetText` 추가(TRIP-206 N-2 규칙의 예산 축 확장, 심판 0건 — code-critic N-1). **TRIP-209 추가분**: `useSavedPlaces()` 조회 → `로그인했나 && 조회 아직인가`(`savedPlacesLoading`) 곱셈 한 줄로 게스트 접기(비회원은 요청 자체가 안 나가 이 값이 항상 false — 잠그면 비회원이 영영 여행을 못 만든다) — 이 값이 §1 셋(비회원 화면 둘 + 승인 동결 테스트 3파일)을 함께 지킨다. `useEffect`에서 **그린 다음** `initMustVisits`로 1회성 시드 채움(그리는 도중 스토어를 고치면 React 경고 + "그릴 때마다 다시 만드는" 구현이 되어 복사본 독립성이 깨진다). `canProceed`에 `savedPlacesLoading` 항목 추가(담은 목록 도착 전 `[다음]` 잠금, 정본 AC 없음 — **게이트①-2 사용자 결정**, `aidlc-docs/construction/u1-accommodation-trip/functional-design/frontend-components.md` §4 반영). 제출은 **`try`가 둘**(여행 생성 `try` 안 / must-visit 등록은 그 **밖**, 문제로그 [[2026-08-02 성공 후 부작용을 try가 감싸면 성공이 실패로 둔갑한다]]) — 등록은 `Promise.allSettled`로 N건 동시 발사, 409는 실패에서 뺀다(INV-U1-18). 실패 0 → step2 이동(**잠금을 안 풂** — 되돌아와 재클릭해도 맨 위에서 걸림) / 실패>0 → step1 유지 + 등록 실패 배너 + 잠금 해제(재시도가 다시 타야 하므로). 제출 잠금은 `submitLockedRef`(`useRef` — 상태와 달리 **같은 틱에 즉시 읽힌다**, `useState`면 연타 두 번째가 옛 값을 읽어 못 막는다). 이미 여행이 만들어져 있고 등록만 남았으면(`createdTripId` 존재) 여행을 또 안 만들고 남은 등록만 이어간다. **TRIP-288 추가분**: 시드 채우기 효과의 `mustVisitsInitialized` 가드를 풀고 `initMustVisits` → `addMustVisits`로 교체 — 담은 목록이 도착·갱신될 때마다 재시드가 돌되, `mergeMustVisitSeeds`가 참조를 지켜 무한 루프는 안 난다(개념 [[참조 동일성과 렌더 루프]]). `canProceed`·조회 실패 가드는 한 글자도 안 바꿈. 등록 직전 `useTripWizardStore.getState().mustVisits`로 **다시 읽어** `registerMustVisits`에 넘긴다(code-critic 경고-1 봉합 — `await` 대기 중 시드가 늘어도 그때 화면에 보이는 시드를 싣는다, 심판 0건). **TRIP-367 추가분**: `onPressMoreMustVisits`가 `() => router.push('/explore/places')` 고정에서 `savedPlaceList.length > 0 ? '/explore/saved-places' : '/explore/places'` 삼항 분기로 바뀜 — 담은 곳이 있으면 담은 장소 화면(d02)으로, 없으면 탐색 유지(재시드 동작 TRIP-288 무변경). **TRIP-368 추가분**: `dateSheetOpen` 로컬 `useState` 신설(시트 열림을 배선이 소유, 숙소 등록 `CalendarSheet` 배치와 동형) + `onPressPeriod={() => setDateSheetOpen(true)}`(기존 빈 함수 `() => {}` 대체)·`onCloseDateSheet`·`onConfirmDates={(start,end) => setPeriod(undefined, start, end)}`(프리셋 칩 해제 공존, `stayImport.ts` 패턴 재사용)·`baseDate={resolvedBaseDate}` 배선. **TRIP-389 추가분(안전 슬라이스만 — 프리셋 4칩·여행지0개 UX·기간연장은 deferred)**: `derivedEnd`를 **render마다 계산**해 표시(`date-field`)·검증(`validateTripDraft`)·`canProceed`·제출(`buildCreateTripRequest`)의 **단일 출처**로 삼는다(개념 [[파생값]]) — 페이지 로컬 마커 `periodFromCalendar`(useState, 3개 `setPeriod` 호출부 `handleSelectPreset`=false·`importStayDates`=false·`onConfirmDates`=true가 동기)가 `true`일 때만 `derivedEnd = deriveEndDate(startDate, nightsSum(destinations))`, 그 외(프리셋·등록숙소 가져오기)는 store의 `endDate`가 그대로 권위. ⚠️ **브리프가 제시한 조건 `presetCode===undefined`을 마커로 바꾼 이유**: 등록 숙소 가져오기도 `setPeriod(undefined, checkIn, checkOut)`이라 `presetCode`가 undefined라서 그 경로까지 파생으로 오인해 실제 체크아웃을 덮어썼다(동결 `stayImport.test.tsx`가 red로 잡음, code-critic 확인① — 마커는 양방향 뮤테이션으로 잠김 검증됨). `onConfirmDates`가 `(start)` 1인자로 받아 `setPeriod`로 세우고, **제출 직전** 달력 경로면 `setPeriod(undefined, startDate, derivedEnd)`로 store를 파생값에 맞춰(step2가 store를 직접 읽는 회귀 봉합, 브리프 밖 추가·02a ★9가 "제출 바디 반영은 자동 심판 밖"으로 예고한 지점 — 게이트①-2 `TripNewStep1Page.integration.test.tsx`의 I-9가 이 정합을 잠금). 재진입(remount)은 마커가 `false`로 리셋돼 마지막 확정값(store 캐시)이 고정으로 보이는 안전한 degrade(박수 늘면 `NIGHTS_EXCEED_PERIOD`가 막음, 박수 줄면 조용히 낡은 긴 기간이 남음 — 하드 불변식 위반 아님, 참고 등급 결함으로 미룸). **TRIP-445 추가분**: 여행지 시트가 이제 `useRegions()`(react-query, 서버 카탈로그)를 문다 — `selectable !== false`만 남기고 `{code: regionCode, name}`로 어댑트해 `regions`(full)·`sheetRegions`(검색 좁힘) 두 갈래로 화면에 내림(위저드 화면 prop 계약 `{code,name}`·TRIP-387 성질은 어댑터가 흡수해 한 글자도 안 바뀜, 개념 [[좁힌 목록과 원본 목록의 소비처 분리]]). ⚠️ 서버 소스라 통합 버킷에서 지역 칩이 **본질적으로 비동기**(react-query 마운트 effect) — node 버킷 sibling 테스트는 `useRegions` 목(동기)이 필요하고, 통합 버킷 헬퍼는 press 전 `await findBy*`가 필요(레포 트랩 참조) |
| | `src/pages/trip-new-step1/index.ts` | 배럴 — `TripNewStep1Page` 재수출 |
| `place-explore` | `src/pages/place-explore/ui/PlaceExplorePage.tsx` | **신규(TRIP-221) → TRIP-222로 상태 판정·담기 실패 배선 확장**(66줄→더 큼). `useGetPlaces({region?,category?})` + `useSavedPlaces({isAuthed})`로 두 서버 상태를 받아 `visiblePlaces(data, searchText)`(→`itemCount`)와 `isPending`·`isError`·`hasQuery`·`hasCategory`를 `resolvePlaceListState(...)`에 넘겨 `state: PlaceListState`를 얻는다(판정은 이 한 곳에서만, AC-G3). **`attemptToggle(place)`로 개명·확장**(구 `handleToggleSave`): `saveError` 비움 → 해제가 아니면 `hasUsableCoords`로 좌표 방어 → 통과하면 `pendingPoiIds`에 poiId를 올려 화면이 그 하트를 `disabled`로 만들게 함(Seed Q7 ⓑ) → `save`/`remove`가 돌려주는 `SavedPlacesOutcome`을 **처음으로 읽어**(TRIP-221까지는 `void`로 버렸다 — 이 이관 결함이 TRIP-222에서 닫혔다) 실패면 `SAVE_FAILURE_NOTICE[reason]`을 배너로 세운다. 미로그인 담기는 로그인 유도 배너 + `router.push('/(auth)/login')`(Seed Q6 ⓒ, `useBootstrapGate.ts` 5-c 수정과 짝 — 위 `features/auth` 절 참고). `onBack={() => router.back()}`(TRIP-221 5-c) |
| | `src/pages/place-explore/index.ts` | 배럴 — `PlaceExplorePage` 재수출(1줄) |
| `saved-places` | `src/pages/saved-places/ui/SavedPlacesPage.tsx` | **신규(TRIP-223)** — d02 담은 장소 배선(95줄). `useSavedPlaces({isAuthed})`(반환 확장분: `savedPlaces`·`isPending`·`isError`·`refetch`) → `orderSavedPlaces(savedPlaces)`(정렬) → `resolvePlaceListState(...)`(TRIP-222 판정 함수 재사용, 판정을 다시 만들지 않음)로 `state`를 얻고, `isGuest={!isAuthed}`를 **그 판정과 독립적으로** 화면에 내려보낸다(게스트가 항상 이김 — `resolveFace`가 화면 쪽에서 다시 합성, `SavedPlaceListScreen.tsx` 행 참고). 하트 해제는 `remove(savedPlaceId)` → 실패 시 `REMOVE_FAILURE_NOTICE[reason]` 배너. `isAuthed`는 `getAccessToken() !== null`을 **렌더 시점에 1회 읽는다**(토큰 변화 구독 없음 — `PlaceExplorePage.tsx`와 같은 기존 패턴, code-critic N-2 참고 필요·심판 없음). **TRIP-449 추가분**: 숙소(SavedStay) 축을 새로 물림 — `useSavedStays({enabled:isAuthed})`(`features/trip/model/useSavedStays.ts` 읽기전용 재수출, **토글 훅 `features/stay/model/savedStays.ts`의 `remove`는 안 씀** — 그건 외부키 역인덱스라 핀·수동 등록 숙소를 못 지움) → 같은 `resolvePlaceListState`(itemCount=숙소 수, hasQuery/hasCategory=false)로 `stayState` → `StayRowVM[]`로 매핑(`dateLabel`은 `checkIn`·`checkOut` 둘 다 있을 때만 `formatStayDateRange` 재사용 조립, INV-3 — 소요시간 아니고 체크인/아웃 날짜). 숙소 해제는 생성 mutation 훅 `useDeleteSavedStaysSavedStayId`(raw 함수 대신 — react-query가 `isPending`/`variables`를 관리해 `removingStayIds`를 페이지 `useState` 없이 파생, ponytail lite 채택) `.mutate({savedStayId})` → `onSuccess`에서 `getGetSavedStaysQueryKey()` 무효화. **통합 `showEmpty`는 페이지가 파생**(`isAuthed && 장소=empty && 숙소=empty`일 때만 true — 화면 순수성 강제 `pagesLayerStructure.test.ts` TE-3 때문에 판정을 화면에 못 둠). 얼굴 파생 우선순위 `guest > loading > error > results > empty`(양 축 조합). ⚠️ **숙소 해제 실패 경로 없음**(`onError` 미배선, DELETE 실패 시 무피드백 — 장소 축 `REMOVE_FAILURE_NOTICE`와 비대칭, INV-4 gap·03b 경고-2·후속 티켓 후보). 상세는 [[2026-08-20 20260820-trip449-saved-split]] |
| | `src/pages/saved-places/index.ts` | 배럴 — `SavedPlacesPage` 재수출(1줄) |
| `trip-new-step2` | `src/pages/trip-new-step2/ui/TripNewStep2Page.tsx` | **신규(TRIP-225, 239줄) → TRIP-226으로 +322/-23.** `useSavedStays({enabled})`·`useTripBases(tripId)`·`useTripCoverage(tripId)` 세 조회를 `tripId(=createdTripId) !== undefined`로 함께 껐다 켠다 → `toBaseSections(assignments, savedStayList, {startDate})`(TRIP-224 순수 함수, 정렬·박 번호 재계산 0건)로 구간 목록을 만들고 `formatSectionRange`·`unresolvedDaysView`(`features/trip/model/baseScreen.ts`)로 표시 문자열을 입혀 화면에 완성된 값만 내린다. **판정은 이 파일에만 산다** — `blocked = coverageData?.blocked` → `generateDisabled = blocked !== false`(fail-closed, 모르면 막는다). `resolveVariant`가 `notrip→error→loading→empty→default` 순으로 다섯 얼굴을 고른다(앞이 이긴다, `empty`는 저장 숙소 0 **그리고** 구간 0일 때만 — 배정이 남았는데 empty로 갈아 끼우면 이미 지정한 거점이 사라진다). 지정·해제는 `beginLock`/`endLock`(`useRef` Set — 같은 틱 두 번째 누름을 즉시 반영으로 막는다, `useState`로는 다음 렌더에야 반영돼 못 막음) + `useInvalidateBases`(성공 시 `bases`·`coverage` 두 쿼리키만 무효화, `saved-stays`는 그대로 둠) + 409는 성공으로 접어 재조회(D13-b) + POST/DELETE 500은 전면 error 대신 그 카드/행 자리에 인라인 오류+다시 시도. `coverage`만 실패하면 목록은 살리고 주 CTA만 fail-closed로 막는다(D15, 보조 CTA는 모든 실패에서 활성 유지). **TRIP-226 추가분**: 좌표 미확정·기간 밖 숙소를 고쳐 거점으로 지정 가능하게 만드는 `fixSheet` 조립 블록(`:435-500`) — `baseGate.ts`의 `baseBlockReason`으로 카드·시트 저장 게이트를 **같은 `gateInput`**에서 계산(D3, 갈라질 통로 없음) + `useBaseFix.ts`의 `useFixSavedStay`/`useExtendTripPeriod`를 물어 `TripBaseFixSheet`에 19개 필드로 내린다. `runAssign`은 기간 확장(`PATCH /trips`)과 배정(`POST /bases`)을 **순차 `await`**로 묶는다(나란히 보내면 서버가 옛 기간 기준으로 400을 준다, 이해부채 [[순서 보장과 실패 시 중단]]). 기간 확장 성공 시 `setPeriod`로 위저드 스토어까지 갱신(서버 갱신과 화면 기억은 따로 고쳐야 한다, [[동기화의 차집합 계산]]). ⚠️ **경고(미룸, devlog 인수인계)**: 확장 `PATCH /trips`의 404를 숙소 404로 오인해 거짓 문구 표시(W-2) · `assignable`이 화면과 별개로 페일클로즈 2선을 스토어 통째 구독으로 재는 리렌더 레버(W-1, 리포 유일 형태) · `onPickDay`가 ★9(정렬 금지)의 구조 심판 사정거리 밖(W-4) |
| | `src/pages/trip-new-step2/index.ts` | 배럴 — `TripNewStep2Page` 재수출(신규, TRIP-225) |
| `itinerary-mustvisit` | `src/pages/itinerary-mustvisit/ui/MustVisitListPage.tsx` | **신규(TRIP-296) → TRIP-326으로 +159줄.** h05 필수 방문지 목록 배선. `GET /trips/{id}/must-visits` + `useSavedPlaces()`(`features/explore`, features 간 직접 import 금지라 조합은 이 페이지가 전담) → `joinMustVisits`로 합치고 `resolveMustVisitListView`(순수, `features/itinerary/model/mustVisitList.ts`)에 넘겨 얼굴 판정. `removeMustVisit` 해제 실패도 `staleFailed` 축에 태운다(침묵 금지). `다시 시도`는 `savedPlaces.isError`일 때만 `savedPlaces.refetch()`도 함께 부른다(5-c, W-2 — 안 그러면 이름 자리가 영영 안 돌아온다). **TRIP-326 추가분**: `useSavedPlaces()` 결과와 `buildMustVisitPins`(`features/itinerary/model/mustVisitList.ts`)로 지도 핀 조립(좌표 조인이 여기서만 일어남, 화면은 완성된 `pins`만 받음) + 강등(FIXED→ANYTIME) `DELETE /must-visits/{id}` → `POST /must-visits` 2단(원자성 없음, 실패 3종 `lost`/`kept`/409 판별) + `sendDemotePost` 실패 상태를 함수형 갱신으로 정리(다른 항목 성공에 안 지워짐, 5-c I14) + `handleDemote` 반환값 `boolean`으로 잠금 중 거절을 화면에 알림(5-c I15) |
| | `src/pages/itinerary-mustvisit/ui/MustVisitTimePage.tsx` | **신규(TRIP-296)** — h07 방문 시각 지정 배선. 계약에 수정 오퍼레이션이 없어 **DELETE→POST 2단**으로 "이미 등록된 항목을 FIXED로 바꾸는" 것을 흉내(`PromoteFailure` 판별 유니온으로 실패 시 "다시 낼 요청"을 들고 있는다 — 원자성 없음, `ponytail:` 주석으로 천장 명시). 409는 `이미 추가된 곳이에요` 안내만 내고 **되돌아가지 않는다**(게이트①-3 개정 — 안내를 세운 렌더에서 바로 떠나면 사용자가 그 문장을 못 본다). `blockReason`은 폼을 한 번이라도 건드린 뒤(`edited !== null`)에만 뜬다(5-c, W-3) |
| | `src/pages/itinerary-mustvisit/index.ts` | 배럴 — `MustVisitListPage`·`MustVisitTimePage` 재수출(신규, TRIP-296) |
| `itinerary-draft` | `src/pages/itinerary-draft/ui/DraftPage.tsx` | **신규(TRIP-297) → TRIP-337로 `generating` 배선 추가.** h11 배선. `useGetTripsTripId`+`useGetTripsTripIdItinerary`(폴링 옵션 포함) 두 조회 + `usePostTripsTripIdItinerary`(재생성) 하나를 잇는다. 탭은 여행 기간(`buildDraftDayTabs`)이 출처, 폴링은 `refetchInterval` 함수형 인자의 `query.state.dataUpdateCount`(반환값엔 없음) 하나로 자체 타이머 없이 센다. 재생성 카운터 되감기는 `useRef` 기준선(`pollBaseline`) + `invalidateQueries`만(`resetQueries`는 데이터까지 버려 미채택, 개념 [[invalidateQueries (쿼리 무효화)]]). 재생성은 `status !== 'CONFIRMED'`일 때만(1차 방어선 `canRetry`) + `handleRetry` 내부 `isNotFound`/`status==='CONFIRMED'` 재확인(2차 방어선, 자동 회귀 테스트 없음 — 후속 티켓 후보). **TRIP-337 추가분**: `resolveDraftView` 결과(listed)에 `generating: itinerary.data?.generationState==='PARTIAL'`를 얹어 `view`로 `DraftScreen`에 전달(view-편승 — S3 프롭 10필드 동결 우회, `resolveDraftView` 자체는 무수정이라 `draftView.test.ts` 불변) |
| | `src/pages/itinerary-draft/ui/SlotCandidateSheetContainer.tsx` | **신규(TRIP-335, 슬라이스1)** — h12 배선. 마운트 시 `usePostTripsTripIdItinerarySlotCandidates` POST 1건(slotKey만, 제외목록 없음 — BR-U3-24) → "선택" 탭 → `swapSlotPoi`+`buildEditItineraryRequest` → `usePutTripsTripIdItinerary`. 성공 = `onClose`+`invalidateQueries`(재조회), 실패 = 인라인 유지. `firedRef`(useRef)로 동기 이중탭 방지(게이트①-2 C4가 뮤테이션 실측). days 출처는 `useGetTripsTripIdItinerary` **GET 캐시**(별도 스토어 시드 없음) — candidates POST와 itinerary GET은 순서 보장이 없어 GET 미도착 중 확정하면 `swapSlotPoi([], ...)`로 **빈 days PUT**(데이터 손실)이 나갈 수 있었다(경고3). 5-c가 콜드캐시 가드(로딩 중 확정 차단)로 봉합, 게이트①-2 C9가 뮤테이션 실측 |
| | `src/pages/itinerary-draft/index.ts` | 배럴 — `DraftPage` 재수출(신규, TRIP-297) |
| `itinerary-plan` | `src/pages/itinerary-plan/ui/ItineraryPlanPage.tsx` | **신규(TRIP-299) → TRIP-337로 `confirmLocked` 배선 추가.** — h25 완성 일정 시간표 배선(지배인). `useGetTrips`+`useGetTripsTripIdItinerary` 두 조회를 `resolvePlanState({loading,notFound,failed,days})`(`features/itinerary/model/planState.ts`)로 하나의 판별 유니온에 접어 얼굴을 고른다. `listed`면 두 조회를 조립해 헤더(제목·기간은 trip, 곳 수는 itinerary 슬롯 합계)를 만들고, 세그먼트(시간표↔지도)·활성 날짜 인덱스는 **페이지 로컬 `useState`**로 들어 쿼리 키를 안 바꾼다(전환 시 재조회 0, `ItineraryPlanPage.integration.test.tsx` I1이 HTTP 실건수 1→1로 확인). 확정 CTA는 `<Pressable disabled>`에 onPress를 아예 안 준다(회색인데 눌리는 함정 회피). ⚠️ 슬롯 카드 왼쪽 절반(장소명·사진·영업시간)은 TRIP-301 몫 — 아래 `src/features/itinerary/` 절 `TimelineScreen.tsx` 행 참고. **TRIP-402 추가**: 4얼굴(loading·notFound·failed·listed) 공통 `handleBack`(`canGoBack()?back():replace(HOME_FALLBACK)`, `HOME_FALLBACK='/(tabs)'` — 리포 최초 `canGoBack` 도입, 개념 [[router.canGoBack() — 딥링크 히스토리 없음 폴백]]) + 파일 로컬 `PlanAppBar`(loading·`PlanFace` 공유, 세 번째 호출자 생기기 전엔 `TimelineScreen` 앱바와 통합 안 함 — ponytail lite) + notFound `StateNotice.actions`에 "일정 만들기"(`itinerary-plan-create-cta`→h04 method) 추가. listed 기존 `onBack={()=>router.back()}`도 `handleBack`으로 교체(딥링크 사각 봉합). 신규 export 0(`PlanAppBar`·`PlanFace`·`HOME_FALLBACK` 전부 모듈 로컬). **TRIP-337 추가분**: `confirmLocked={isConfirmLocked(itinerary.data?.generationState)}`를 `TimelineScreen`에 배선 — PARTIAL이면 확정이 예방 잠긴다(계약 409의 클라 사본) |
| | `src/pages/itinerary-plan/index.ts` | 배럴 — `ItineraryPlanPage` 재수출(신규, TRIP-299 — [테스트 작성] 단계에서 스텁으로 이미 완성돼 구현자가 안 채움) |
| `itinerary-edit` | `src/pages/itinerary-edit/ui/ItineraryEditPage.tsx` | **신규(TRIP-302, 슬라이스1) → 저장 배선 추가(TRIP-302, 슬라이스2) → 시각조정 시트 배선 추가(TRIP-302, 슬라이스3).** h24 편집 화면 배선(배선판). `useGetTripsTripIdItinerary`로 조회 → `resolvePlanState`(h25 재사용, 재판정 없음) 얼굴 판정 → 도착 데이터를 편집 드래프트 스토어에 **1회만 시드**(`useEffect([itinerary.data])` — 편집으로 리렌더돼도 조회 데이터 참조가 그대로라 재시드 없음). 삭제·재정렬은 `activeDate = days[activeDayIndex].date` 기준으로 `store.deleteSlot`/`reorderSlots`에 배선. **슬라이스2 추가분**: `handleSave()` — `buildEditItineraryRequest(store.days)`로 조립 → `usePutTripsTripIdItinerary<unknown>().mutate`. 성공(200)은 `setQueryData`로 조회 캐시에 직접 써넣어(재조회 0) 위 시드 `useEffect`를 다시 돌려 재-시드(배지 지속, BR-U3-13 — 새 코드 아니라 슬라이스1 고리 재사용). 409는 `fetchQuery`로 재조회해 `generationState==='PARTIAL'`→"만드는 중" / `status==='CONFIRMED'`→"확정" 문구(신호 B, 서로 배타) · 그 외(5xx·네트워크)는 generic 안내(INV-4). 저장은 위반 유무를 안 보고 곧장 mutate(비차단, BR-U3-13). SaveConflictSheet는 **이연**(Figma 미설계, 아래 경고 절). 트립 조회(`useGetTripsTripId`)는 안 부름(제목 헤더가 없어 YAGNI, ponytail 채택). **슬라이스3 추가분**: 로컬 상태 `editingSlotKey`(어느 슬롯의 시각조정 시트가 열렸나) — 화면의 `onEditSlotTime(slotKey)` 콜백이 세팅하면 `<SlotTimeSheet>`를 **조건부 마운트**(닫힘 = 트리에서 제거, 개념 [[목 테스트의 이음매 사각지대]]). `onApply`는 `adjustSlotTime(date, poiId, patch)` 호출 + `editingSlotKey` 클리어, `onCancel`은 클리어만. 시각조정은 로컬 편집이라 서버 호출 0(INV-2) — 저장은 여전히 슬라이스2의 PUT 경로 그대로(새 저장 코드 0) |
| | `src/pages/itinerary-edit/index.ts` | 배럴 — `ItineraryEditPage` 재수출(신규, TRIP-302) |
| `itinerary-option-swap` | `src/pages/itinerary-option-swap/ui/OptionSwapPage.tsx` | **신규(TRIP-335, 슬라이스1)** — h18 배선(BR-U3-23의 h12 짝, 단독 라우트). 마운트 시 slot-candidates POST → 라디오 로컬상태 → "B로 교체" 확정 → `swapSlotPoi`+`buildEditItineraryRequest` → PUT → 성공 시 `router.back()`. `firedRef`로 동기 이중탭 방지(게이트①-2 C4가 신규 심판으로 뮤테이션 실측 — 5-b 경고1 봉합) + 콜드캐시 가드(경고3 봉합, C5). **라우트 파일 없음** — 컨테이너 컴포넌트만(슬라이스2 위저드와 라우트 공유 결정 대기, 브리프 §8 Q4) |
| | `src/pages/itinerary-option-swap/index.ts` | 배럴 — `OptionSwapPage` 재수출(신규, TRIP-335) |
| `itinerary-manual` | `src/pages/itinerary-manual/ui/ManualPlanPage.tsx` | **신규(TRIP-338)** — h19 빈 일정 배선. 마운트 시 `usePostTripsTripIdItinerary({generationMode:'MANUAL'})`를 `firedRef` 가드로 1회만 쏘고(GeneratingPage 선례) `useGetTripsTripIdItinerary`로 받은 `days`를 그대로 `ManualPlanScreen`에 내린다. h09(생성 중 폴링)는 건너뜀 — MANUAL은 AI 미호출·즉시 빈 일자라 2단계 폴링이 불필요(01b Q2). 폴백 배너 코드 경로 자체가 없다(MANUAL=`solveMode=MINIMAL`+`isFallback=false`, 폴백이 아니라 선택 — AC-2) |
| | `src/pages/itinerary-manual/ui/PlaceAddPage.tsx` | **신규(TRIP-338) → 5-c로 캐시 무효화·notReady 도출 추가.** h20 장소 추가·검색 배선. `useGetPlaces({category?})`(검색어는 서버 계약에 키가 없어 안 보냄) + `visiblePlaces`로 이름 클라 필터 → "추가" → `SlotTimeSheet`(h24 재사용)로 시각 입력 → `addSlot`+`buildEditItineraryRequest` → PUT. **5-c 추가분(W-1)**: PUT 성공 시 `invalidateQueries(getGetTripsTripIdItineraryQueryKey)`로 h19 캐시 무효화(안 하면 추가한 장소가 h19에 영영 안 보임 — `SlotCandidateSheetContainer.tsx` 선례와 동형). **5-c 추가분(W-2)**: `notReady = itinerary.data?.days?.[0]?.date===undefined`를 도출해 `PlaceAddScreen`에 내리고 press를 가드(콜드/에러 캐시에서 add가 조용히 소실되는 것을 안내 배너+press 차단으로 막음) |
| | `src/pages/itinerary-manual/index.ts` | 배럴 — `ManualPlanPage`·`PlaceAddPage` 재수출(신규, TRIP-338) |
| `live-itinerary` | `src/pages/live-itinerary/ui/LiveItineraryPage.tsx` | **신규(TRIP-395, `c1f7217`) → 20260820 Figma 재정합(TRIP-395·397, sub-cycle A)으로 헤더·404 배선 확장.** i01·i02·i03 여행 중 배선. `useLiveItinerary(tripId)`(일정 조회, 판정 재료) + `useGetTripsTripId(tripId)`(**판정 무관** — 헤더 제목 `tripTitle`만, trip 로딩이 얼굴을 안 막게 분리) → `resolveLiveState({isLoading, isNotFound: isNotFound(query.error), isError, itinerary, todayDate})`(우선순위 `loading > notFound > error > outsideToday > active`) → active면 `subtitle = formatKoreanDate(activeDate) + ' · 오늘 일정'`(`shared/date`, execution 밖 조립 — ★ 아래 `features/execution` 절 참고)·`onPressTab={(key) => router.replace(key==='home'?'/':`/${key}`)}`(StaySearchPage TRIP-413 선례 동형)를 `LiveItineraryScreen`에 주입. 404(`isNotFound`)는 `error`보다 먼저 갈려 `execution-live-notfound`("아직 일정이 없어요") 얼굴로, 5xx·네트워크는 기존 `execution-live-error` 유지 |
| | `src/pages/live-itinerary/index.ts` | 배럴 — `LiveItineraryPage` 재수출(신규, TRIP-395) |
| `live-place` | `src/pages/live-place/ui/LivePlacePage.tsx` | **신규(TRIP-398)** — i05 현재 장소 상세 배선. `usePlaceDetail(tripId)`(=`useGetTripsTripIdItinerary` 얇은 래퍼) → `isPending`이면 **먼저** `-loading` 중립 뷰(★8, I5가 잠금) → `days.find(day => day.slots.some(slot => slot.poiId === poiId))?.slots ?? []`로 **poiId가 속한 당일 슬롯만** 골라 `buildPlaceDetailView(daySlots, poiId)`(5-c 수정 — 원래 `flatMap`으로 여행 전체를 평탄화해 익일 고정 슬롯이 slack 부호를 반전시키던 경고-1을 당일 스코프로 봉합, I4가 잠금) → `null`이면 `-notfound`("장소를 찾을 수 없어요") 얼굴, 아니면 `PlaceDetailScreen`에 뷰 + `onPressItinerary={() => router.back()}` 주입. **조회 5xx/네트워크 오류 전용 얼굴은 없음**(★9 — data 미도착도 notFound로 접힘, AC 없어 범위 밖 미룸, repo-traps 참고) |
| | `src/pages/live-place/index.ts` | 배럴 — `LivePlacePage` 재수출(신규, TRIP-398) |

> **⚠️ 배럴 경유는 지금 관행일 뿐 강제되지 않는다** — 라우트 5개는 전부 배럴을 경유하지만(위반 0건), 승인 테스트(`fsdStructure.test.ts`)의 단언이 `toContain`(부분 문자열)이라 딥 임포트로 바꿔도 잡히지 않는다(code-critic 참고-1 실측). 회귀 방지는 승인 테스트 수정이 필요해 사이클 3 몫으로 이관됨. `region-picker`도 이 관행을 따른다(강제는 `fsdStructure.test.ts`의 pages 슬라이스 **7개 완전일치** 집합 단언뿐 — `PAGE_SLICES` forEach 상수 자체는 여전히 로그인·온보딩 5개만 대상, region-picker·stay-search는 그 forEach 밖).
