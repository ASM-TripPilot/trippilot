---
paths:
  - "src/features/itinerary/**"
---
# `src/features/itinerary/` — 순수 함수 계층 (TRIP-295) → 첫 화면 표면 (TRIP-296) → h11 초안 화면 (TRIP-297) → 강등 안내 + h35 후보 0건 화면 (TRIP-298) → h25 완성 일정 시간표 화면 (TRIP-299) → h24 편집 화면 슬라이스1 (TRIP-302) → 슬라이스2 저장 코어 (TRIP-302) → h26 지도·h23 핀상세·h31 폴백 (TRIP-301) → h24 편집 슬라이스3 시각조정 시트 (TRIP-302) → h25/h34 풀디자인 정합·인라인 지도 (TRIP-354) → h25 사진 없는 슬롯 카테고리 플레이스홀더 (TRIP-465)


TRIP-173에서 빈 배럴과 함께 디렉토리째 삭제됐다가(아래 "아직 시작 안 한 도메인" 절 참고), TRIP-295가 처음 재등장시켰다(순수 함수 2개, 화면·소비자 0). TRIP-296이 첫 화면(h05·h07)을 붙였다 — `ui/` 서브디렉토리 신설. TRIP-297이 두 번째 화면(h11)을 붙였다. **TRIP-298이 세 번째 화면(h35, 후보 0건)을 붙이고 h11에 강등 안내 배너 1블록을 더했다** — 티켓 요구 3표면 중 2표면만(h11 슬롯 카드의 추천 이유 줄은 D1로 범위 밖, 후속 티켓 후보). **TRIP-299가 네 번째 화면(h25, 완성 일정 시간표)을 붙였다** — 골격만(장소명·사진·영업시간 등 POI 표면은 아예 미참조, TRIP-301 몫). **TRIP-302 슬라이스1이 다섯 번째 화면(h24, 일정 편집)을 붙였다** — 조회 렌더+삭제+드래그 재정렬 뼈대만(저장·시트·실제 지도는 슬라이스2·3). **TRIP-302 슬라이스2가 무동작 `[저장하기]`를 `PUT` 실배선으로 채웠다** — 조립 순수함수(`buildEditItineraryRequest.ts`) + 성공 캐시 직접 갱신(재조회 0)·재-시드로 배지 지속 + 409 2갈래(재조회 신호 B). **SaveConflictSheet 시트는 이연**(Figma h24에 시트 프레임 미설계 — 아래 경고 절). **TRIP-301이 h25 지도 세그먼트의 placeholder를 채웠다** — 실지도(h26)·핀 상세 읽기전용 시트(h23)·지도 실패 폴백(h31) 세 표면 + 그 밑을 받치는 통일 POI 카드(`PoiSlotCard`, peek/list 겸용). `shared/map`은 additive 확장만(PIN_TAP·onLoadFailed·maxLevel), `TimelineScreen.tsx`의 골격·시간표 세그먼트·확정 흐름은 무수정. 범위 좁힘: 시간표 카드 업그레이드·인라인 지도는 TRIP-354 이관, 길찾기·스크러버는 드롭. 배럴(`index.ts`) 여전히 없음(`auth` 선례대로 딥 임포트, `pages/itinerary-mustvisit/`·`pages/itinerary-draft/`·`pages/itinerary-plan/`·`pages/itinerary-edit/`이 조합).

| 파일 | 역할 |
|---|---|
| `src/features/itinerary/model/timeBandLabel.ts` | `timeBandLabel(startAt: string): TimeBandLabel` — `HH:mm:ss` 문자열 → `'오전'\|'점심'\|'오후'\|'저녁'` 사영(BR-U3-07·PBT-U3-2). 경계는 **사용자가 이 사이클에서 동결**한 값(정본 부재, C-1 — `05:00`·`11:00`·`14:00`·`17:00`)이고 문자열 사전순 비교로 판정(0채움 `HH:mm:ss` 전제). 저녁만 자정을 넘어 두 조각(`17:00:00~23:59:59` + `00:00:00~04:59:59`)이라 첫 줄과 마지막 줄이 같은 라벨을 낸다. **입력 형식을 검증하지 않는다** — 0채움 아닌 문자열(`'9:00:00'` 등)은 조용히 `'저녁'`이 된다(심판 0, 동결 시그니처에 실패 갈래가 없음). 성격 축(`활동·식사·전시·숙소`) 인자는 이 칸에서 **열지 않음**(정본 매핑 부재, 범위 축소) |
| `src/features/itinerary/model/legDistance.ts` | **신규(TRIP-354)** — `legDistance(distanceRanges: (string\|null)[]): string\|null` 순수함수. 날짜헤더 "이동 X" 합계 조립: 각 슬롯의 서버 `distanceRange` 문자열에서 **느슨한 정규식**(`km`을 `m`보다 먼저 시도)으로 숫자+단위만 뽑아 미터로 정규화·합산 → `<1000m`은 10m 단위 반올림("820m"), `≥1000m`은 소수 1자리("3.2km")로 표시 환원(round-half-up). present-but-broken 값 하나라도 있으면 그날 줄 전체를 접어 `null`(실제보다 적은 틀린 숫자 금지), `null`/빈 문자열은 조용히 스킵, 합계 0도 `null`. `TimelineScreen.tsx`가 날짜헤더에서 `legDistance(slots.slice(1).map(s=>s.distanceRange))`로 호출(첫 슬롯 제외 — 구간행 leg 모집단과 일치, 03b W-1 처리). INV-3(소요시간 미표시) 준수 — duration 산출 없음 |
| `src/features/itinerary/model/mustVisitList.ts` | **신규(TRIP-296) → TRIP-326으로 +25줄.** `joinMustVisits`(등록 건 ↔ 담은 장소를 `sourcePoiId`로 합침) · `resolveMustVisitListView`(순수 얼굴 판정, items를 loading·failed보다 먼저 봄 — `staleFailed`로 잔존 목록과 실패 사실을 한 값에 동시에 싣는다, TRIP-222·223 재발 방지 세 번째 자리) · `MUST_VISIT_NAME_PLACEHOLDER`(이름 조인 실패 플레이스홀더) · **`buildMustVisitPins`(TRIP-326 신규)** — 담은 장소 좌표로 지도 핀 배열 조립. `items` 순서대로 번호를 매기되 좌표 없는 항목은 건너뛰고 뒤 번호를 당기지 않는다(`MapPin`은 타입 전용 import, 런타임에 `shared/map`을 안 끌어온다) |
| `src/features/itinerary/model/mustVisitTimeForm.ts` | **신규(TRIP-296)** — `tripDayChips`(날짜 칩, 요일은 계산이지 Figma 목업 베끼기 아님) · `startTimeOptions`·`startTimeLabel`(30분 간격 48개, 12/24시간제 병기) · `DWELL_OPTIONS`·`DEFAULT_DWELL_KEY`(체류시간 3단 30·60·120분 — **발명값**, 정본 부재) · `mustVisitTimeBlockReason`·`canSubmitMustVisitTime`(폼 검증) · `buildFixedMustVisitRequest`(요청 본문 조립, 검증 실패 시 `null` — 되돌릴 수 없는 DELETE를 검증 전에 못 내보내는 방어). **TRIP-786 추가**: `canSubmitMustVisitTime`에 OFF 갈래 신설(`!form.fixed \|\| isCompleteFixedForm(form)` — OFF는 값 무관 `true`, BR-U1-48 "기본은 ANYTIME") + 신규 `buildAnytimeMustVisitRequest({poiId})` → `{poiId, type:'ANYTIME'}` 최소본(날짜·시각·체류 안 실음, 여분 키 0으로 여분이 곧 솔버 힌트가 되는 것을 막음) |
| `src/features/itinerary/ui/MethodPickerScreen.tsx` | **신규(TRIP-303, 문서 소급 반영 이번 사이클 최초).** h04 생성 방식 3카드(완전AI·같이 짜기·직접) 화면(props+콜백만). 완전AI는 `onPressFullAi` 필수, 같이 짜기·직접은 각각 `onPressCoPick?`·`onPressManual?` **옵셔널**(둘 다 후방호환 — 개념 [[후방호환 옵셔널 파라미터 (additive prop)]]) — 콜백이 오면 그 카드 press가 navigate, 안 오면(프리뷰 등) 카드 내부 `showSoon`("준비 중" 안내)으로 폴백. **TRIP-460 추가**: `onPressManual?` 신설(직접 짜기 게이트 해제 대상). **TRIP-462 추가**: `onPressCoPick?` 신설 — `onPress={onPressCoPick ?? showSoon}`(같이 짜기 카드), `onPressManual?` 선례와 완전 동형 패턴. **TRIP-784 추가**: h01로 재편(Figma 3824:2128) — 앱바 우측 진행 표시(점 4개+`current / total`, config 소비) 신설, 서브카피·하단안내를 config 카피로 교체, 추천 배지(`badge` prop 포함) 삭제, `onPressManual`·`onPressCoPick` **옵셔널→필수**(`?` 제거로 `showSoon`/`SOON_LABEL`/`useState` 폴백 전부 소멸 — 개념 [[필수 필드 추가의 컴파일 폭발 반경]]). `activeGeneration`·`showRegenerateConfirm` 계열(서버 판정면)은 한 줄도 무변경 |
| `src/features/itinerary/config/methodPicker.ts` | **신규(TRIP-784)** — itinerary 피처의 첫 `config` 세그먼트. `METHOD_PROGRESS={current:3,total:4}` + 서브카피 + 하단안내 상수 3개만 export, 배럴·api 세그먼트·색 토큰·소요시간 없음. **raw-hex/INV-3 소스 스캔(`itineraryTimeStructure.test.ts`·`itineraryMethodStructure.test.ts` 등) 사정거리 밖**(전부 `features/itinerary/ui`만 훑음, 현재 결함 0) — 개념 [[가드의 사정거리]] "새 세그먼트 폴더 하나가 통째로 기존 소스 스캔 모집단 밖" 절 참고 |
| `src/features/itinerary/config/placeCategoryChips.ts` | **신규(TRIP-798)** — `PLACE_CATEGORY_CHIPS` 6종 `{testId,label,category}`. **전시 칩만 `label='전시'`·`category='문화'`로 갈린다**(`PoiCategory` enum에 '전시' 없음, '문화'만 실존) — testID·라벨·전송값 3-way 명시 분리가 이 파일의 존재 이유(라벨이 서버 파라미터로 새면 400/빈목록). 같은 소스 스캔 사정거리 밖 함정은 `methodPicker.ts`와 동일(위 행 참고) |
| `src/features/itinerary/ui/MustVisitPickerScreen.tsx` | **신규(TRIP-296) → TRIP-326으로 +346줄(대부분).** h05 화면(props만). FIXED 카드는 시작 시각만 표시하고 끝 시각은 안 그린다(D12, INV-2·INV-3와 충돌하는 Figma 표기를 의도적으로 안 따름). **TRIP-326 추가분**: 지도 카드(`screen-map`, `pins` prop을 `key={JSON.stringify(mapPins)}`로 실물 `KakaoMapView`에 전달 — 지도는 마운트 시 얼어붙어 `key` remount 없인 안 바뀜) · timeMode 칩 2종(`아무 때나`/`시간 정해두기`, `-timemode-` 접두로 기존 카드 셀렉터 제외 목록과 충돌 회피) · FIXED→ANYTIME 강등 확인 시트 · CTA/건너뛰기 비활성+사유(`proceedBlockedReason`, 색 토큰까지 상태별로 다르게 — TRIP-225 미해결 함정 회피) + `onDemote` 반환값이 `false`면 시트를 안 닫음(5-c). **제거**: `시각 고정` 단일 칩(`chip-time-{id}`)과 그 `CheckGlyph` import(AC-11). 검색 필·`＋ 필수 방문지 추가` 타일·`지도에서 지정` 칩·`© Kakao`·축척 바는 여전히 **안 그림**(D4·D6·D8, 정직한 스텁도 금지 — API 자체가 없거나 SDK가 이미 그림). **TRIP-339로 `KakaoMapView`에 `viewOnly`·`connectPins={false}` 두 줄 + 근거 주석 3줄 추가**(+5) — 보여주기 전용이라 지도 고정 + 핀 번호가 "담은 순서"일 뿐 확정 동선이 아니라 h05만 연결선을 끈다(h11은 그대로 둠, Figma h05map 연결선 노드 실측 0개). **TRIP-785로 h05→h02 Figma 재번호에 정합**(재번호 자체는 프리뷰 키만 반영, 코드 주석 h05 표기는 범위 밖 유지) — 헤드라인 제목/앱바 건너뛰기를 `view.kind`로 조건부 렌더(제목 `!=='listed'`·건너뛰기 `==='failed'`)·CTA 비활성 판정에 `view.kind!=='listed'`를 1차 기준으로 추가(`proceedBlockedReason`은 보조 사유로 격하)·**loading 얼굴 신설**(`LoadingFace` 확장 — 지도 자리 회색 스켈레톤 `-map-skeleton` + 카드 스켈레톤 3장 각 원+사각+바2, `bg-surface-soft`류 토큰만)·**error 얼굴 신설**(로컬 `FailedFace`, StateNotice 재사용 불가 — icon XOR illustration 필수+outline 회색이라 "아이콘 없는 빨강 아웃라인" 불가)·**강등 확인 시트·staleFailed AlertRow 렌더 제거**(h03/TRIP-786이 흡수, `onDemote`/`demoteErrorText`/`onRetryDemote` prop 삭제 — `staleFailed`는 `mustVisitList.ts`가 여전히 산출하나 이 화면은 안 그림)·`TimeModeChip`을 `onPress` 유무로 `View`/`Pressable` 분기(5-c, 표시 전용 칩의 침묵 실패 봉합 — `아무 때나` 칩은 이 화면에서 다시는 onPress를 안 받는 영구 종료 상태라 콜백 파생이 예외적으로 안전, 개념 [[명시 prop으로 role 고정 (콜백 유무 파생 금지)]] 반례)·카피 교정(`FAILED_TITLE`·`FAILED_NOTE`) |
| `src/features/itinerary/ui/MustVisitTimeScreen.tsx` | **신규(TRIP-296) → h03로 Figma 재번호(TRIP-786, 구 h07).** h03 화면(props만 + 시트 개폐 상태 1개). 시각 선택은 `@gorhom/bottom-sheet`(리포 기존 설치·목 재사용). **TRIP-786으로 옛 "토글 OFF=opacity-40+비활성(D8, Figma에 OFF 프레임 없음)" 서술은 절반만 유효** — 날짜·시각·체류 3섹션은 여전히 `opacity-40`+잠금이지만 **저장(CTA)만 열렸다**(BR-U1-48: OFF=ANYTIME 제출 허용, CTA 라벨 `아무 때나로 두기`). Figma에 OFF 프레임이 이제 있다(3826:2127 옆 4294:8400). 에러 행도 이 사이클에서 안내 배너 **아래**로 이동 + 흰 배경(`bg-canvas`)+hairline로 재스타일(신규 testID `-error-row`가 컨테이너 View, className은 메시지 Text가 아니라 이 컨테이너에만 산다). **TRIP-599(2026-08-28)**: 시작 시각 시트 본문 flat 48-옵션 리스트 → `shared/ui/WheelPicker` 소비로 교체. 시트 컨테이너 `BottomSheetScrollView`→`BottomSheetView`+`enableContentPanningGesture={false}`(휠이 자기 세로 스크롤을 쥐므로 시트 콘텐츠 pan과 축 충돌 — TRIP-455와 동형, jest 사각·6-b 전용). `press→onPickStart` 계약·testID(`-start-option-{HH:mm}`) 불변 |
| `src/features/itinerary/ui/ItineraryGlyphs.tsx` | **TRIP-296 신규 → TRIP-297로 수정 → TRIP-298로 2종 추가 → TRIP-302로 3종 추가 + `LockGlyph` 확장 → TRIP-301로 2종 추가 → TRIP-338로 `SearchGlyph` 신규 + `PlusGlyph`/`PencilGlyph` tone 확장 → TRIP-465로 카테고리 아이콘 8종 + 색 상수 3종 추가 → TRIP-809로 그 8종 + 3색상수를 `entities/itinerary-slot/ui/SlotGlyphs.tsx`로 이관(재수출 shim만 남김) → TRIP-810으로 그 재수출 8종 줄을 제거.** 인라인 SVG 글리프 19종 잔존(`BackChevronGlyph`·`ChevronRightGlyph` 등 비-카테고리분, 무변경). **TRIP-810 변경분**: 유일한 옛 자리 소비처 `ConceptPickerScreen.tsx`(브리프 §③ "importer 0" 오측 — 실측은 6종 직접 import)를 카테고리 6종만 `@/entities/itinerary-slot/ui/SlotGlyphs`로 split, 비-카테고리 2종(`BackChevronGlyph`·`ChevronRightGlyph`)은 여전히 이 파일에서 import. **TRIP-809 변경분**: `CategoryPinGlyph`·`CategoryForkKnifeGlyph`·`CategoryCupGlyph`·`CategoryNightGlyph`·`CategoryTreeGlyph`·`CategoryShoppingBagGlyph`·`CategoryBuildingGlyph`·`CategoryImageGlyph`(폴백) 8종 + `INFO`·`SUCCESS`·`PRESENCE_BLUE` 상수가 `SlotGlyphs.tsx`로 바이트 이관(SVG path 좌표까지 IDENTICAL). **`ICON_BY_KEY` 룩업 사각은 이관 후에도 그대로**(`layer-entities.md`의 `entities/itinerary-slot/` 절 참고 — 이관이 만든 신규 사각 아님). **TRIP-338 추가분**: `WHITE` 상수 + `SearchGlyph`(신규, 돋보기 — h20 검색창. `ExploreGlyphs.tsx`에 동명이 있으나 features 간 import 금지라 로컬 중복이 이 파일의 확립된 관례) · `PlusGlyph`에 `tone?: 'ink'\|'primary'\|'white'`, `PencilGlyph`에 `tone?: 'mutedSoft'\|'white'` 선택 인자 추가(기본값이 기존 렌더 불변 — `LockGlyph`/`FullAiGlyph`의 tone 패턴 계승, 후방호환). `PencilGlyph`·`CheckCircleGlyph`는 이미 있어 h19/h20이 재사용(브리프 §4-4가 "없음"이라 짐작했으나 실측 결과 있었음, 03 §브리프와 달라진 점). `AlertCircleGlyph`만 Figma 실측이 아니다(h05·h07에 실패 프레임 자체가 없어 같은 파일의 `InfoCircleGlyph` 원에 느낌표만 뒤집어 그림 — `03_implementer_notes.md` §3-10). **TRIP-297 수정분**: h11 소비(구체 내용은 `DraftScreen.tsx` 행 참고). **TRIP-298 추가분**: `ChevronRightGlyph`(`TripGlyphs.tsx`·`StayGlyphs.tsx`·`HomeGlyphs.tsx`와 이름이 겹치는 4번째 벌 — `features` 간 import 금지 규약이 만드는 의도된 중복) · `LocationOffGlyph`(Figma 실측이 아니라 스크린샷 재구성. ⚠️ `shared/location/LocationGlyphs.tsx`에 **같은 이름·같은 그림**이 이미 있고 색만 다르다 — 아래 경고 절 참고). **TRIP-302 추가분(슬라이스1)**: `DragHandleGlyph`·`TrashGlyph`·`PlusGlyph`(휴지통·플러스는 `StayGlyphs`/`TripGlyphs`에 이미 있으나 features 간 import 금지라 의도된 중복) · `LockGlyph`에 `tone?: 'primaryText'\|'muted'` 선택 인자 추가(기본값 `'primaryText'`로 h25 렌더 불변 — h24 고정칩의 회색 `surface-strong` 배경에 맞춰 `'muted'` 톤으로 씀, 후방호환 확장). **TRIP-301 추가분**: `WarningTriangleGlyph`(h31 폴백용 삼각형 경고 — `TripGlyphs.tsx`에 동명 심볼이 있으나 features 간 import 금지라 별도로 그림, ponytail N1로 기존 `AlertCircleGlyph`(경고 원) 재사용 대안이 나왔으나 h31 디자인 정합 위해 미채택) · `RefreshGlyph`(h31 재시도 버튼용 새로고침). **TRIP-788 추가분**: `CalendarGlyph`(신규, viewBox `0 0 30` sw2 round stroke `#9AA1AB`=muted-soft, h06 empty 얼굴 아이콘) — stay·trip·record에 동명 글리프가 있으나 features 간 import 금지라 itinerary 로컬 사본(이 파일의 확립된 관례). |
| `src/features/itinerary/model/draftView.ts` | **신규(TRIP-297) → TRIP-298로 `zero` 얼굴 + `isCandidatesDemoted` 추가 → TRIP-337로 h10 게이지 도출 3종 + `listed.generating?` 필드 추가.** h11·h35 순수 판정 함수 + 상수 2개. `DRAFT_POLL_INTERVAL_MS=2000`·`DRAFT_POLL_MAX_COUNT=30`(정본 부재, 발명값) · `buildDraftDayTabs`(탭은 여행 기간이 출처, `days`에 있는 날짜만 `hasData:true`) · `formatDraftDayHeader`(`'2026-06-10'→'6월 10일 · 수'`) · `buildDraftPins`(좌표 없는 슬롯은 건너뛰되 카드 번호는 배열 인덱스+1 그대로, 지도 핀만 결번 — 재번호 안 함) · `shouldKeepPollingDraft`(경계값 순수 판정, 통합 테스트는 "실제로 도는가"만 1틱 잰다 — 개념 [[폴링 (polling)]] ★7) · `resolveDraftView`(판별 유니온 **5종**: loading·failed·zero·empty·listed — TRIP-298이 `zero` 추가 + 판정축을 `days.length`에서 **슬롯 합계**로 변경, 얼굴 우선순위는 슬롯>0 → loading → failed → zero → empty, **TRIP-337 무수정**) · **`isCandidatesDemoted(summary)`(신규, TRIP-298)** — `candidatesSummary.level`을 조용할 값 화이트리스트(`['OK','HIGH']`, `trim().toUpperCase()` 정규화)로 판정, 모르는 값·`undefined`/`null`은 안내 켜는 쪽(INV-4). `level`을 enum으로 안 좁힌다(계약이 문자열로 열어 둠 — 근거: `backend/docs/design/openapi.yaml:1183-1195`). **TRIP-337 추가분**: `GenerationDayState`(`'done'|'active'|'waiting'`) 타입 + `GenerationGaugeCell` 인터페이스 + `buildGenerationGauge(tabs)` 순수 함수(day별 3상태를 `tabs.hasData`에서 도출 — 주입이 아니라 도출이라 "전부 done" 가짜 진척이 원천 불가, 개념 [[파생값 (derived value)]] 사례 추가) · `DraftView`의 `listed` 변형에 `generating?: boolean` 옵션 필드 추가(프롭 동결 S3 우회 — `DraftPage.tsx`가 `{...baseView, generating}`으로 얹는다, `resolveDraftView` 런타임 출력 자체는 불변) |
| `src/features/itinerary/ui/DraftScreen.tsx` | **신규(TRIP-297) → TRIP-298로 `demoted?` prop + 배너 1블록 추가 → TRIP-337로 h10 "만드는 중" 얼굴 추가(프롭 미추가).** h11 화면(props만). 시각 노출은 `isFixed`로만 갈린다(D5) — `isFixed:false`는 시간대 라벨(`timeBandLabel`)만, `isFixed:true`는 `startAt.slice(0,5)`로 절삭 표시(발명값, 정본 공백). `AI 추천` 배지 근거도 `placementReason`이 아니라 `isFixed`(★14). 지도는 `key={\`${selectedDate}|${JSON.stringify(pins)}\`}`로 날짜·핀 조합이 바뀔 때만 강제 리마운트(React `key`는 렌더 트리에 안 보임 — 소스 정규식으로만 계약, 개념 [[목 테스트의 이음매 사각지대]]). 카드 루트 testID(`itinerary-draft-slot-{slotKey}`) 하위에 번호·라벨·배지·사진·태그·이름이 같은 접두를 쓰므로 제외 목록(`no-`·`band-`·`badge-`·`fixed-`·`image-`·`tags-`·`name-`)으로 트리를 좁힌다. **TRIP-339로 `KakaoMapView`에 `viewOnly` 한 줄 추가**(+1) — 보여주기 전용이라 지도 고정. `connectPins`는 안 넘김(기본값 `true`가 h11이 원하는 값과 이미 같음). **TRIP-298 추가분**: `demoted?: boolean`(옵셔널 + 기본값 `false` — 필수화하면 동결 심판·프리뷰 5상태가 tsc를 깬다) prop과 그 값이 참일 때 카드 목록 위·서브트리 밖에 뜨는 강등 배너(`itinerary-draft-fallback-banner`와 같은 성격, 숫자·시간 단위 0자). **TRIP-337 추가분**: 새 프롭 0개 — `view.generating===true`(model층 `DraftView.listed.generating`)를 게이트로 `GaugeCell`·`GenerationGauge`·`GenerationSkeleton` 모듈-로컬 컴포넌트를 렌더. 제목이 "일정 만드는 중" 계열로 스왑되고 일자별 게이지(`itinerary-generating-day-{n}-{done|active|waiting}`, `buildGenerationGauge`가 `tabs`에서 도출)와 미도착 일자 스켈레톤(`itinerary-generating-skeleton-{n}`)이 얹힌다 — day1 카드·핀 지도·날짜 탭 3개는 그대로 공존(additive, I1 비파괴). 퍼센트·소요시간 렌더 0(계약에 진행 수치 없음, INV-3). `DraftScreenProps`는 동결 심판 S3(`itineraryMapSurfaceStructure.test.ts`)가 10필드로 박제해 새 프롭을 못 받는다 — 그래서 `generating`은 프롭이 아니라 `view` 안에 얹힌다(위 model 행 참고, 개념 [[파생값 (derived value)]]). **TRIP-454 추가**: `onComplete?: () => void`(프로퍼티형·인터페이스 마지막 필드 — S3 `interfaceFields`가 11번째로 캡처, 메서드 단축형이면 미캡처) prop + `COMPLETE_LABEL='이 일정으로 완성'`(Figma 공백 발명값) + `listed` 얼굴 스크롤 마지막에 완성 CTA(`itinerary-draft-complete`, PARTIAL 포함 항상 활성 — `isConfirmLocked`류 잠금 없음, h25가 자체 상태 처리). **TRIP-467 추가**: 죽은 코드 `SlotCandidateSheetContainer`(TRIP-335)를 여는 트리거 배선 — `onPressSlot?: (slotKey: string) => void`(프로퍼티형·인터페이스 마지막 필드, S3 `interfaceFields`가 12번째로 캡처, 11→12 동결 갱신) + `ALT_LABEL='다른 후보 ›'`(`ItineraryEditScreen.tsx`의 동명 미export 상수를 로컬 복제 — cross-screen 드리프트 무심판, repo-traps 참고-1). 비고정(`!slot.isFixed`) 슬롯 카드에만 `onPressSlot`이 주어졌을 때 트리거(testID `itinerary-draft-alt-{slotKey}`, 카드 접두 `itinerary-draft-slot-` 밖이라 `cardTestIds()` 오계수 없음)를 그려 press 시 `onPressSlot(buildSlotKey(date, poiId))`를 부른다. 컨테이너 자체는 무수정, `DraftPage.tsx`가 조건부 마운트로 소유(아래 pages 행 참고). **TRIP-483 추가(additive)**: `onPressSlot` 뒤 3필드 더 추가해 S3 동결 12→15 — `expandedSlotKey?: string | null`(어느 슬롯이 펼쳐졌나)·`renderSlotPanel?: (slotKey: string) => ReactNode`(그 자리에 그릴 것을 조립)·`onManualPlan?: () => void`(수동 짜기 콜백). 화면은 슬롯을 그리다가 `expandedSlotKey === slotKey`인 카드 **바로 다음**에 `renderSlotPanel(slotKey)`를 호출(Fragment) — 컨테이너(`SlotCandidatePanelContainer`)는 배선(`DraftPage.tsx`)이 함수로 내려줌, 화면은 위치만 안다(개념 [[바텀시트→인라인 패널 전환 (통과형 목 사각 해소)]]). 트리거는 재press 토글(`prev===k?null:k`)로 접힘. 하단 CTA를 2버튼 행으로 확장 — 좌 「처음부터 직접」(`onManualPlan` gated)·우 「이대로 확정」(`onComplete` 계승, 라벨만 `이 일정으로 완성`→`이대로 확정`, 동결 `DraftScreen.test.tsx` 문자열 갱신). 우상단 「직접 고르기」 링크도 `onManualPlan`(기존 「다시 만들기」 retry와 공존, Figma엔 후자 없지만 무회귀·INV-4라 유지). reason 블록에 정적 부제 1줄 추가(`itinerary-draft-reason-subtitle`) — **5-c로 `view.kind==='listed'` 게이트**(그 전엔 `generating===false`인 모든 얼굴(loading/failed/empty 포함)에 누출, 03b 참고-1 — 이 게이트를 지키는 회귀 심판은 여전히 없다, repo-traps 후보) |
| `src/features/itinerary/ui/ZeroCandidateScreen.tsx` | **신규(TRIP-298)** — h35(후보 0건) 화면. `ZeroCandidateScreenProps`·`ZeroCandidateScreen`. `DraftScreen`의 상태가 아니라 **별도 완결 화면**(앱바를 자기 안에 가짐) — 승인 심판이 "화면 전체 버튼 정확히 2개"(뒤로·완화 1행)를 재기 때문. `shortfallCategories`를 `<Text>`로 그대로 노출(사전 매핑 없음), 완화는 **필수 방문지 줄이기 1행뿐**(`/trips/[tripId]/itinerary/must-visits`로 라우팅 — D7, 예산·조건 완화 카드·이동 반경 카드·하단 CTA는 전부 제외. 근거: `GenerateItineraryRequest`에 완화 파라미터가 없어 갈 곳도 보낼 값도 없음) |
| `src/features/itinerary/model/planState.ts` | **신규(TRIP-299) → TRIP-401로 목적지 판정 2함수 추가 → TRIP-337로 `isConfirmLocked` 추가.** h25 순수 판정 3함수: `resolvePlanState({loading,notFound,failed,days})`(판별 유니온, 우선순위 loading>notFound>failed>listed — 404가 "없음"이자 "실패"이기도 한 겹침을 순서로 가름) · `formatNightsLabel`(UTC 날짜 산술로 "N박 M일", 끝<시작이면 빈 문자열) · `buildPlanDayTabs`(날짜탭 메타, 1부터·곳 수=슬롯 길이). `DraftPage`의 `draftView.ts`와 같은 3층 배치(판정=model·조립=page·그림=screen)를 따른다 — `utcDayTime`/`MS_PER_DAY` UTC 산술은 `draftView.ts`의 private 헬퍼와 형태가 같지만 **지역 복제**(cross-file 리팩토링은 이 칸 범위 밖, 세 번째 호출자 생기면 추출 — ponytail lite 대안, 03 §트레이드오프). **TRIP-401 추가분**: `resolveItineraryDestination({notFound, generationState?, status?}) → 'method'\|'generating'\|'draft'\|'plan'`(순수, `resolvePlanState`와 **별개 관심사** — 이건 "어느 화면으로 보낼지"고 `resolvePlanState`는 "화면 안에서 어떤 얼굴을 그릴지", 서로 무수정) 우선순위 `notFound→PARTIAL→FAILED→CONFIRMED→else draft`(FAILED·COMPLETE+PLANNED가 같은 `draft`로 접힘, 정본 BR-U3-*엔 상태→화면 표가 없어 프론트 합성 — 결정1, 사용자 검토 대상) · `itineraryDestinationHref(tripId, dest): ItineraryDestinationHref`(4라우트 템플릿 리터럴 유니온 반환 — `typedRoutes:true`가 순수 `string`을 `Redirect href`/`router.push`에서 거부해 `expo-router` import 없이 라우트 모양만 타입에 실음, `plan`만 접미 없음 특례가 한 곳에만 삶). 두 진입점(`(tabs)/itinerary.tsx`·`(tabs)/index.tsx`)이 이 둘을 공유해 목적지 규칙이 두 번 살지 않는다. **TRIP-337 추가분**: `isConfirmLocked(generationState?): boolean`(1줄, `generationState==='PARTIAL'`) — `resolvePlanState`는 무수정(얼굴 판정과 잠금 판정은 별개 관심사, `resolveItineraryDestination`과 같은 결의 이웃 export) |
| `src/features/itinerary/model/tripCardFace.ts` | **신규(TRIP-788)** — `deriveTripCardFace(status?, generationState?) → {statusLine, badge:'done'\|'draft', resume:boolean}`(순수). h05/h06 "내 여행" 카드 상태문 3종(생성중/초안/완성)을 한 함수에 몰아 기록 — u3 FD에 상태→상태문 표가 없어 **정본 공백·프론트 합성**(01b Q1 Mapping A, 틀리면 이 함수 3줄만 수정). 우선순위 `PARTIAL→CONFIRMED→else`(`resolveItineraryDestination`과 정렬) — PARTIAL='AI가 일정을 짜는 중'+draft+resume:false(생성중) · CONFIRMED='추천안이 준비됐어요'+done+resume:false(완성, 구 '확정 장소 N곳' 슬롯합계 **대체** — 정보 상실은 의도된 Figma 정합, 01b Q2) · else(COMPLETE/FAILED+PLANNED)='추천안 준비 중'+draft+resume:true(초안, FAILED도 여기로 접힘 — 01b Q3). "어느 화면으로"(`resolveItineraryDestination`)와 "어떤 얼굴로"(이 함수)는 형제 관심사로 같은 model에 이웃. 소비 = `pages/itinerary-list/ui/TripCardContainer.tsx`뿐(조립은 pages 행 참고). |
| `src/features/itinerary/model/itineraryEditStore.ts` | **신규(TRIP-302, 슬라이스1) → `adjustSlotTime` 액션 추가(TRIP-302, 슬라이스3) → `addSlot` 액션 충전(TRIP-338) → `insertSlotAt` + 로컬 확장 타입 추가(TRIP-797).** h24 편집 드래프트 스토어(Zustand, `create(변수)` 표기). 순수 헬퍼 `removeSlot(slots,poiId)`(`findIndex`+`slice`, 중복 poiId는 첫 자리만 제거·비파괴) · `reorderKeepingFixed(original,reordered)`(고정 슬롯을 원래 절대 인덱스에 재고정, 비고정만 lib이 준 순서로 채움 — INV-U3-02)를 별 파일로 안 빼고 스토어 파일에 함께 export(ponytail lite 채택, 재사용이 늘면 그때 분리). `seed`는 `[...day.slots]` 얕은 복사라 시드 원본 배열 불변. **슬라이스3 추가분**: `adjustSlotTime(date, poiId, {startAt, endAt, endsNextDay})` — 대상 슬롯의 시각 3필드만 갈고 배열 순서·이웃·타 일자·시드 원본은 불변(`{...slot}` 새 객체, 비파괴 — `deleteSlot`·`reorderSlots`와 같은 "해당 날짜에만 얹는 얇은 래퍼" 패턴). **TRIP-338 추가분**: 스텁이던 `addSlot(slots, slot)`을 `return [...slots, slot]`로 충전 — 배열 끝에 append(순서=슬롯 순서, INV-U3-02) + 스프레드라 원본 불변(`removeSlot`과 같은 비파괴 패턴). h20 장소 추가가 이 액션을 씀. **TRIP-797 추가분**: `insertSlotAt(slots, slot, index)` — `addSlot`(말미 전용)과 달리 **지정 index 자리**에 삽입(`Math.max(0, Math.min(index, len))`로 `splice` 클램프 준용, 비파괴 스프레드). h12 편집기 "카드 사이 +"가 넘긴 index를 h13(TRIP-798)이 이 함수로 소비할 예정(이 티켓 시점엔 소비처 0 — 순수 단위·insert+build 조합 테스트만 잠금, 개념 [[인덱스 클램프 삽입 (splice 준용)]]). **로컬 확장 타입** `EditorSlot = Omit<ItineraryDaysItemSlotsItem,'startAt'> & { startAt: string | null }` · `EditorDaysItem`도 함께 신설 — 서버 계약 `startAt`이 non-nullable(openapi 2065/2559)이라 "시간대 미지정" 편집 중간 상태를 서버 생성 타입으로 표현 못 해 로컬에서만 넓힌다(개념 [[로컬 확장 타입 — 서버 코드젠 타입 무오염]]) |
| `src/features/itinerary/model/buildEditItineraryRequest.ts` | **신규(TRIP-302, 슬라이스2) → 입력 확장 + 미지정 제외 필터(TRIP-797)** — `buildEditItineraryRequest(days): EditItineraryRequest` 순수 조립함수. 편집 스토어 `days`를 PUT 요청 봉투로 바꾼다: 슬롯당 **5필드만 픽**(`poiId·startAt·endAt·isFixed·endsNextDay`, 위반·태그 등 읽기전용 필드는 버림) · 시각은 **원본 그대로**(표시용 `slice(0,5)` 절단 금지, 초 보존) · `endsNextDay`는 조회값 그대로 실어나름(HC4 자정 넘김 소실 방지) · 배열 순서 = 슬롯 순서(INV-U3-02) · 다일자·date 보존. `features/trip`의 `buildCreateTripRequest`와 같은 자리(요청 조립 전담 순수함수)이지만 도메인이 달라 **지역 복제**(features 간 import 금지 규약). **TRIP-797 추가분**: 입력 타입을 `ItineraryDaysItem[]`의 상위집합인 `EditorDaysItem[]`(`startAt: string|null` 허용)로 넓히고, `startAt === null` 슬롯을 요청 조립에서 제외(AC-6, 나머지 순서는 INV-U3-02 그대로). 상위집합 확장이라 기존 소비처(`ItineraryEditPage.handleSave`·`PlaceAddPage`)는 무변경 컴파일. **미지정 슬롯을 실제로 만드는 프로덕션 경로는 이 티켓 시점 0건**(필터는 no-op 대기 상태) — 미지정 제외를 사용자에게 알리는 INV-4 안내 렌더는 이 함수 밖(페이지 재조립, 묶음 C 몫), 03b 참고-1 |
| ~~src/features/itinerary/ui/ItineraryEditScreen.tsx~~ | **삭제됨(TRIP-797 묶음 C, 2026-09-21)** — h24 편집기가 `pages/itinerary-edit/ui/EditorView.tsx`(pages 순수 뷰) 조립으로 대체, co-located test 2개(`.test.tsx`·`.slot-time.test.tsx`)와 함께 `git rm`(삭제도 계약 — 구조가드 `itineraryEditStructure` E2 부재 플립과 짝). 이하는 삭제 전 이력(참고용): **신규(TRIP-302, 슬라이스1) → `onSave?`·`saveError?` prop 추가(TRIP-302, 슬라이스2) → `onEditSlotTime?` prop 추가(TRIP-302, 슬라이스3).** h24 편집 화면(순수, props+콜백만, 스토어를 모름). 카드 2형태(고정: 고정칩·휴지통 없음·다른후보 없음 / 비고정: 휴지통·"다른 후보 ›"(무동작)·해시태그). 카드 순서 앵커 testID(`itinerary-edit-slot-{slotKey}`)는 **카드 래퍼가 아니라 부제 leaf**에 붙는다(02a 스펙표는 "카드 래퍼"라 적었지만 프로즌 테스트의 `toHaveTextContent(문자열)` 완전일치 단언이 부제로 강제 — 스펙표↔프로즌 테스트 드리프트, 03b code-critic이 "오히려 더 강한 정확일치"로 반증·해소). 자리만 어포던스(장소추가·다른후보)는 `onPress` 자체를 안 받아 press가 무해(비활성화가 아니라 콜백 부재). 위반 배지는 h25 `TimelineScreen`(:208~221)과 **개명 없이 같은 계약** 재현(`itinerary-edit-violation-{slotKey}`, 슬라이스2에서 저장 후 재-시드로 지속). **슬라이스2 추가분**: 저장 버튼 `onPress={onSave}`(선택 prop, undefined면 무해 no-op — 슬라이스1 무동작 프로즌 보존) · `saveError` non-null이면 `itinerary-edit-save-error` 인라인 노트(카드 리스트 **밖**, 저장 버튼 위 — h11 `confirmError` 선례 재사용, `cardOrder()` 정규식 접두가 달라 카드 순서 심판을 안 흔듦). **슬라이스3 추가분**: 선택 prop `onEditSlotTime?: (slotKey: string) => void`(undefined→무해 no-op, `onSave?` 선례) — 비고정 시각칩(`itinerary-edit-slot-time-{slotKey}`, 기존 무동작 `Pressable`)의 `onPress`에서 호출. **고정 슬롯은 onPress를 안 붙인다**(INV-U3-03, 시각 불변) |
| ~~`SlotTimeSheet.tsx`~~ (h24 시각조정 시트) | **TRIP-805로 `widgets/time-sheet/ui/TimeSheet.tsx`에 흡수·삭제** — i15/i22 `ManualTimeSheet`와 props 계약이 완전 동일한 쌍둥이라 공용 위젯으로 통일했다(`testIDPrefix`·`labels`·`title`만 소비처 주입). h24 소비처 `ItineraryEditPage`·`PlaceAddPage`가 이제 `@/widgets/time-sheet`를 접두 `itinerary-edit-time`로 소비한다. 파일 표는 `layer-widgets.md` 참고. INV-3(분 셀 bare 숫자)·INV-2(엔진 유도)·`@gorhom/bottom-sheet` 통과형 목 사각은 위젯으로 옮겨도 무변경 — INV-3 소스 가드는 `planbManualStructure` G3(widgets/time-sheet 재편입)·`itineraryEditSheetStructure` E3가 잠근다 |
| `src/features/itinerary/model/swapSlotPoi.ts` | **신규(TRIP-335, 슬라이스1)** — `swapSlotPoi(days, {date,poiId}, nextPoiId): ItineraryDaysItem[]` 순수함수. days 전체에서 `(date,poiId)` 첫 일치 슬롯의 poiId만 치환(비파괴, 이웃·타 일자·시각 3필드 불변). h12·h18 공통 코어(BR-U3-23) — `buildEditItineraryRequest`와 조립해 PUT 바디를 만든다. 스텁 아님(테스트 8케이스로 완결) |
| `src/features/itinerary/model/slotSwapError.ts` | **신규(TRIP-335, 슬라이스1)** — `resolveSlotSwapError(error): SlotSwapError`·`SLOT_SWAP_CONFLICT_CODES`(모듈 소유 409 code 3상수, 계약 미문서화라 대표값). `error.response.data.error.code`로 확정일정/생성중/슬롯특정불가 3분기 + 미지·부재 code·비axios·네트워크는 전부 폴백(침묵 없음, INV-4). 스텁 아님 |
| `src/features/itinerary/model/coPickSlots.ts` | **신규(TRIP-504)** — `firstCoPickSlotKey(days): SlotKey\|null`·`nextCoPickSlotKey(days, currentKey): SlotKey\|null` 순수함수. copick(h13→h14) 선형 순회 "다음 갈 슬롯은?"을 화면·라우터와 분리. day→슬롯 순서로 훑어 비고정(`isFixed=false`)만, 고정 슬롯·일자 경계는 건너뜀·넘김. 없으면 `null`(허브 대신 h17 complete로 폴백). 키는 `buildSlotKey` 재사용(`#` 하드코딩 금지). ⚠️ 호출측(`SlotFillPage.tsx`)은 **스왑 후가 아니라 스왑 전 원본 `days`·원본 `currentKey`**로 불러야 한다(스왑 후 days에선 원본 키를 못 찾음) — 개념 [[스왑 전 원본으로 다음 값을 구한다]] |
| `src/features/itinerary/ui/SlotCandidateCard.tsx` | **신규(TRIP-335, 슬라이스1)** — h12 시트·h18 화면 공용 후보 카드. 배지(A/B/C)+회색 사진자리("이름 준비 중" 미확보 표기)+`distanceRange`·`rationale` 실데이터 leaf, `trailing` 슬롯으로 "선택" 버튼/라디오만 다르게 조합. 이름·사진은 candidates 응답에 필드 자체가 없어(BE 후속 대기) 항상 플레이스홀더 — 정직한 스텁(코드 결함 아님) |
| `src/features/itinerary/ui/SlotCandidatePanel.tsx` | **신규(TRIP-335, 슬라이스1) → TRIP-483로 `SlotCandidateSheet.tsx`에서 git mv + 재작성(바텀시트→인라인 패널, Figma 정합).** h12 순수 패널(props+콜백만). `BottomSheet` 루트 제거·일반 `<View testID="itinerary-candidate-panel">`(구 `-sheet`). 후보 카드·현재 행·선택/펜딩/오류/0건 로직은 **재사용**(재작성 아님). "선택" 탭 즉시 `onSelectCandidate`, `isPending`이면 버튼 disabled+펜딩 문구. 현 슬롯 행은 "현재" 칩+탭 불가(집합에서 제외), Figma대로 primary 배지+1.5px primary 테두리 강조(구 `bg-muted-soft`에서 교체, 패널 소유 픽셀·심판 없음). **TRIP-483 신규 관통 2종**: 헤더 `{timeBand} — 다른 후보로 바꾸기`(컨테이너가 계산해 내림)·`degraded===true`면 강등 고지("AI 추천 준비 중, 가까운 순", `itinerary-candidate-degraded`, 상단 `fallbackNotice`와 다른 표면 — [[degraded ≠ fallbackNotice (두 강등 신호 분리)]]). 후보 배지 색(surface-strong)은 `SlotCandidateCard` 공유 컴포넌트라 미변경(h18과 공유, 심판 없는 픽셀 축이라 외과적 보류). 개명 자체는 [[바텀시트→인라인 패널 전환 (통과형 목 사각 해소)]] 참고 |
| `src/features/itinerary/ui/OptionSwapScreen.tsx` | **신규(TRIP-335, 슬라이스1)** — h18 순수 화면(props+콜백만). 라디오 단일선택(controlled, `selectedPoiId` prop) + "B로 교체" 2단계 CTA(선택 전/펜딩 disabled). 확정 라벨 조립(`${badge}로 교체`)이 깨져도 `/교체/` 느슨 정규식이 못 잡음(참고1, 미룸) |
| ~~src/features/itinerary/ui/ManualPlanScreen.tsx~~ | **삭제됨(TRIP-797 묶음 C, 2026-09-21)** — h19 빈 편집기가 `pages/itinerary-manual/ui/ManualPlanPage.tsx`의 `MapSheetShell` 직접 조립으로 대체, co-located test(`.test.tsx`)와 함께 `git rm`(구조가드 `itineraryManualStructure` 부재 앵커와 짝). 이하는 삭제 전 이력(참고용): **신규(TRIP-338)** — h19 빈 일정 화면(props만). 활성 일자 `slots.length===0`이면 4시간대 밴드(`itinerary-manual-band-{morning\|lunch\|afternoon\|evening}`, "이 시간에 장소 추가")를, 채워지면 슬롯 카드(`itinerary-manual-slot-{slotKey}` + `hasViolation`일 때만 `itinerary-manual-violation-{slotKey}` 배지)를 그린다. 시각 표시는 `slot.isFixed ? startAt.slice(0,5) : timeBandLabel(startAt)`. 서버가 준 `days`만 받는 구조라 미저장 시각이 확정 슬롯으로 샐 길이 없다(INV-2 — 구조 보장, 로컬 슬롯 prop 자체가 없음). 하단 CTA "장소 검색해서 추가" → h20. 다일자 day 전환 없음(활성 일자=index 0 고정, `onSelectDay` 콜백이 계약에 없음 — 3일 여행이면 Day 1만 보임, 03 §트레이드오프3) |
| `src/features/itinerary/ui/SlotDropZone.tsx` | **신규(TRIP-797)** — h12 통일 편집기의 드래그 삭제 드롭존(`itinerary-edit-dropzone` + "여기에 놓으면 삭제돼요" 문구). `isActive?: boolean`(기본 미부착) — 참이면 `itinerary-edit-dropzone-active` 활성 표식(색은 jest 사각, 표식 View 존재로만 계약). `EditorView`(pages)가 `isDragging` 시 형제 절대배치로 얹는다. **드래그 실동작·실제 드롭 삭제·`isActive` 토글 배선은 이번 티켓 밖** — `EditorView`가 `isDragging`일 때 `isActive`를 상수 `true`로만 넘겨(03b 참고-3) 드롭 위치별 비활성 경로가 프로덕션에 없다. 롱프레스→끌기 실동작은 `react-native-draggable-flatlist` 목이 원리적 사각이라 6-b(`h12-editor-dragging` 프리뷰) 전용 |
| `src/features/itinerary/ui/PlaceAddScreen.tsx` | **TRIP-338(신규) → 5-c `notReady?` → TRIP-798(h13 정합, 순수 시트콘텐츠 뷰) → TRIP-798 묶음 C(2026-09-21, MapSheetShell 조립으로 재분해).** `PlaceAddScreen` monolithic 컴포넌트 자체는 **제거**됐고, 대신 두 순수 조각을 export한다 — `PlaceAddHeader`(검색바+`config/placeCategoryChips.ts` 칩6종, `MapSheetShell.children`=ListHeaderComponent 자리에 얹힘)·`PlaceAddRow`(`entities/place/ui/PlaceRowCard` 채택 1건 조립, `MapSheetShell.list.renderItem`으로 소비). 두 조각 모두 `@/shared/api` import 0(순수, devPreviewMap 프리뷰 목 회피). foundation testID 전량 보존(`itinerary-place-search`·`-category-*`·카드 접두 `itinerary-place-card`·`-add`·`-added`·`-distance`). 거리줄은 옵셔널 `distanceByPoiId?`(계약 공백, BE 선행 TRIP-823, 픽스처 전용) 그대로. 옛 `ListEmptyComponent`(빈 목록 안내)는 `MapSheetListSlot` 계약에 슬롯이 없어 드롭(후속 티켓 후보, 03b 참고-1) |
| ~~src/features/itinerary/ui/PlaceAddCard.tsx~~ | **삭제됨(TRIP-798 묶음 C, 2026-09-21)** — `PlaceAddScreen`이 `PlaceRowCard`로 전환한 뒤 런타임 소비처 0(고아)이던 상태(구 서술: 동결 구조테스트 2곳이 존재를 강제해 무변경 존치)를, test-designer가 `entitiesPlaceConsumers.test.ts`·`itineraryManualStructure.test.ts`의 앵커를 **먼저 부재 확정(`REMOVED_CARD`)으로 뒤집은 뒤** implementer가 `git rm`으로 정식 제거(앵커 선-갱신 → 삭제 순서라 심판 변조 아님, [[고아 export — 정의만 있고 부르는 곳 0]] 다섯 번째 사례) |
| `src/features/itinerary/ui/MyTripsListScreen.tsx` | **신규(TRIP-468)** — h37 일정 탭 순수 화면(3얼굴: default·empty·loading, props만). 앱바 "내 여행" + "최신순" 정렬 라벨(단일 옵션이라 드롭다운 메뉴는 미구현 — ponytail lite, 01b Q1) + `sorted.map`으로 **모든** 여행을 `MyTripCard`로 렌더(첫 여행 하나로 리다이렉트하던 구 결함의 수정 헤드라인, TRIP-401 홈 카드 CTA와 자매). empty는 기존 `StateNotice`(문구·아이콘만 교체) 재사용, loading은 `my-trip-skeleton-{n}` 스켈레톤 카드 2장. 판정 0(리스트업만) — 배지·목적지·부가정보는 `TripCardContainer`(pages/itinerary-list)가 이미 조립한 VM을 그대로 그린다. **TRIP-788 추가**: empty 얼굴이 `StateNotice`의 `icon` 슬롯(하드코딩 핑크 원)에서 **`illustration` 슬롯**으로 옮겨 회색 원(`bg-surface-soft` 72px)+`CalendarGlyph`(신규 30px, ItineraryGlyphs)를 주입(StateNotice 자체는 무수정 — stay·explore 소비처 회귀 방지). 부제 상수(`EMPTY_DESCRIPTION`)의 공백 하나를 `\n`으로 바꿔 2줄 강제. testID(`itinerary-tab-empty`·`itinerary-tab-create-trip`) 보존. |
| `src/features/itinerary/ui/MyTripCard.tsx` | **신규(TRIP-468)** — h37 카드 1장(순수, `MyTripCardVM`+`onPress`만). leaf마다 값 하나(`toHaveTextContent` 완전일치 계약, `ItineraryEditScreen`·`PoiSlotCard` 선례와 동형)로 제목·메타줄(`~` 구분자·박수·인원)·배지("완성"/"작성중" 텍스트 판별, 색 아님 — 글리프 fill 함정 회피)·부가정보를 그린다. `badge==='draft'`일 때만 스스로 resume CTA("일정 이어서 짜기")를 그린다(별도 VM 필드 없는 파생, 개념 [[파생값 (derived value)]]). `badge===null`(itinerary 미도착)이면 배지·resume 없이 카드·제목만 뜬다(degrade). testID는 `my-trip-{part}-{tripId}`(루트만 `my-trip-card-`라 `getAllByTestId(/^my-trip-card-/)` 정렬 정규식이 자식과 안 겹침) |
