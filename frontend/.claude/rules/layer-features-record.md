---
paths:
  - "src/features/record/**"
---
# `src/features/record/` — j01 방문 기록 default (TRIP-565 신설)

**이 폴더는 이번 사이클(TRIP-565)에서 처음 생겼다.** `features/execution`과 목적이 겹쳐 보이지만(둘 다 `visit_check` 실적을 다룸) 페이지 shape가 다르다 — execution의 `deriveVisitProgress`는 poi 단위 **집계**(i01 실행 카드용), 이쪽은 per-record 4상태다. 그래서 재사용하지 않고 신규 구현했다(01b Q2, YAGNI — 3번째 소비자가 나올 때까지 shared 승격 보류).

**경계**: `features/record`는 **다른 `features/*`를 import할 수 없다**(특히 `execution` — 이름이 같은 `useVisitCheck`가 있어 혼동하기 쉽다). `eslint.config.js`의 `FEATURES` 배열엔 `record`가 없어 여전히 기계 강제 밖 — `recordsStructure.test.ts`의 G2(소스 재귀 스캔, `settingsBoundary.test.ts`·`notificationStructure.test.ts` 동형)가 이 경계의 유일한 그물이다(개념 [[기능 간 조합은 pages 층 전담 (기계 강제 없음)]]).

| 파일 | 역할 |
|---|---|
| `model/visitStatus.ts` | **신규.** `deriveVisitStatus({arrivedAt, completedAt, skippedAt}) → 'UPCOMING'\|'IN_PROGRESS'\|'COMPLETED'\|'SKIPPED'`. 서버·클라 어디에도 `status` 컬럼/필드를 두지 않고 세 timestamp의 유무만으로 매번 계산(INV-U5-01) — 입력 타입에 `status` 자리 자체가 없어 "저장된 상태를 실수로 읽는" 경로가 구조적으로 봉쇄된다. 우선순위 `skipped > completed > arrived > upcoming`은 execution의 `deriveVisitProgress`와 규칙은 같되 shape(집계 vs per-record)가 달라 재사용하지 않았다. 개념 [[per-record 상태 파생 (세 timestamp에서 우선순위로 계산)]]. |
| `model/visitStatus.test.ts` | PBT(fast-check) + 진리표 + INV 잠금(가짜 `status` 필드를 섞어도 무시됨을 확인). **02b test-fix 1건**: `fc.date({min,max})`가 fast-check v4.9.0에서 Invalid Date를 생성해 `.toISOString()`이 구현 호출 전에 throw하는 심판 결함(~25% flaky) — `noInvalidDate: true` 한 줄로 수정, 뮤테이션 실측(우선순위 swap → 2 failed)으로 커버리지 불변 확인(개념 [[뮤테이션 테스팅]] TRIP-565 절). |
| `model/useVisitCheck.ts` | **신규 → TRIP-619로 arrive 낙관 리터럴에 `updatedAt` 1필드 추가.** `arrive`/`complete`/`skip` 3종 낙관 갱신 + 레코드(visitCheckId) 단위 롤백(통짜 스냅숏 복원 아님, execution 선례 동형, W-2 회귀 방지). 판별 유니온 `VisitCheckOutcome`(`{kind:'completed'}\|{kind:'failed',reason}` 등)으로 실패를 조용히 삼키지 않는다(INV-4, 개념 [[판별 유니온]]). **`settleRollback()`**(:54-55, `await new Promise(r => setTimeout(r,0))`) — react-query v5 `notifyManager`의 배치 통지가 매크로태스크로 밀려 롤백 직후 관찰자 스냅숏이 stale COMPLETED를 보이는 것을 한 틱 양보해 브리지(개념 [[낙관적 갱신과 롤백 (optimistic update)]] TRIP-565 절, code-critic 참고-1 — 실효 소비자는 테스트뿐, 프로덕션 `TripRecordsPage`는 `void`로 fire-and-forget이라 무해하지만 5-c 후보로 test-infra 이관 미룸). arrive는 complete/skip과 달리 **무효화 대신 응답 레코드로 낙관 레코드를 교체**한다(재조회가 즉석 삽입을 덮어 AC-5가 깨지는 것을 피함, `useVisitCheck.ts` 내부 주석 명시). **TRIP-619**: codegen 재생성으로 `VisitCheck.updatedAt`이 required가 되며 arrive 낙관 리터럴에 자리표시자 채움(성공 시 서버 응답 레코드로 통째 교체돼 무해, execution 동형 — 개념 [[낙관적 락 기준버전 (updatedAt)]]). |
| `model/useVisitCheck.integration.test.tsx` | msw + 실 QueryClient. AC-1(plan 미접촉)·AC-2(409 롤백·불변)·AC-5(즉석 2건 append)·skip·★W-2(동시 두 도착 중 하나 실패해도 다른 낙관 생존). ⚠️ AC-1의 "plan 미접촉" 단언(`/itinerary` 히트 0건)은 이 셋업에서 항상 참(페이지 미마운트, 호출 경로 자체가 없음) — BR-U5-01의 진짜 보장은 구조적(G2 소스 스캔이 execution import 0을 보조), 03b 참고-3. |
| `model/useTripRecords.ts` | **신규 → TRIP-569로 `useRecordBases`·`useRecordSavedStays` 2개 얇은 래퍼 추가.** 원래는 `useGetTripsTripIdVisitsDaysDay` 얇은 래퍼(로직 0, `useLiveItinerary.ts` 선례)뿐이었다 — 동작 테스트 없음, 구조 가드가 존재만 확인. **TRIP-569 추가분**: `useRecordBases`(`useGetTripsTripIdBases`)·`useRecordSavedStays`(`useGetSavedStays`) — `features/trip`이 이미 감싼 같은 생성 훅을 `record`가 features 경계(G2) 때문에 못 가져와 자기 안에서 다시 감쌌다(승격 아님, 개념 [[생성훅 직접 사용 (cross-feature 우회)]]). |
| `model/stayAttribution.ts` | **신규(TRIP-569)** — `deriveStayAttribution({visits, bases, savedStays?}) → DayAttribution[]`(무게중심 모델). visits를 날짜별로 묶고, 그 날짜를 덮는 base(`dateFrom≤D<dateTo`, 개념 [[base 구간 커버리지 판정]])로 숙소명을 해소한다. 반환 타입에 "저장된 귀속" 자리를 두지 않아 매 호출 재계산됨을 구조로 강제(BR-U5-25, 개념 [[조회시점 파생·미저장]]). 날짜는 `new Date`/`Date.UTC` 없이 `'YYYY-MM-DD'` 문자열 슬라이스+사전식 비교만 사용(개념 [[UTC 사전식 날짜 경계 (타임존 안전)]]) — ⚠️ 즉석 방문(`slotKey==null`)의 `arrivedAt` 슬라이스는 "기기 타임존 무관"만 보장하고 "여행지 기준 날짜 정확"은 백엔드 `arrived_at` 직렬화(UTC vs 여행지 로컬)에 달려 있어 미확인(03b 경고-1, 후속 티켓 후보). |
| `model/stayAttribution.test.ts` | **신규(TRIP-569)** — AC-1~AC-6 행동 테스트 + PBT(임의 bases 조합에서 `baseStay`가 항상 그 날짜를 덮는 base의 함수임을 전수, tautology 아님 — code-critic 참고-1 확인). dateTo 경계값 3종(dateFrom·dateTo-1·dateTo) 명시 커버. |
| `model/adjustTimesDraft.ts` | **신규(TRIP-613)** — `adjustTimesDraft({arrivedAt, completedAt, now}) → {ok:true}\|{ok:false, reason}`(판별 유니온, 개념 [[판별 유니온]] TRIP-613 절). 순수 검증 — 시계를 안 읽고 **`now`를 인자로 받는다**(개념 [[의존성 주입 (DI)]] TRIP-613 절, `useSocialLogin`의 `authorize` DI와 같은 원리를 시각에 적용). 규칙 순서: ①완료-only(도착 없이 완료) 거부 `'completed-without-arrived'` ②순서 역전(완료<도착) 거부 `'order'` ③미래시각 거부 `'future'`(BR-U5-05). PBT-U5-2가 order 속성을 fast-check로 전수. |
| `model/useAdjustVisitTimes.ts` | **신규(TRIP-613)** — `useAdjustVisitTimes()`. `PATCH /trips/{tripId}/visits/{visitCheckId}` 낙관 뮤테이션: ①캐시에서 그 레코드를 `before`로 스냅숏 ②낙관 반영(바뀐 필드만) ③`expectedUpdatedAt=before.updatedAt` 포함 전송([[낙관적 락 기준버전 (updatedAt)]]) ④200→서버 레코드로 그 자리 통째 교체 `{kind:'adjusted'}` ⑤409 `VISIT_CONFLICT`(`resolveVisitConflict`, `@/shared/api/visitConflict` 딥임포트)→그 레코드만 롤백+그 날 캐시 재조회 `{kind:'conflict', message}` ⑥그 외(404/네트워크)→롤백만 `{kind:'failed', reason}`. 롤백은 **레코드 단위**(통짜 스냅숏 아님, `useVisitCheck.ts` 규율 계승 — 개념 [[낙관적 갱신과 롤백 (optimistic update)]] TRIP-613 절). 성공 경로에도 `settle()`(react-query 배치 통지 매크로태스크 브리지) 필요 — 없으면 AC-5만 조용히 red(동기 캐시 읽기가 배치 flush보다 먼저 돎). ⚠️ `alreadyRecorded`·`not-found`·`network` 세 결과는 통합테스트가 안 밟아 무심판(03b 참고-1, 후속 티켓 후보). |
| `ui/RecordGlyphs.tsx` | **신규.** 체크서클·즉석추가 등 벡터. `*Glyphs.tsx` 관례(raw hex 스캔 제외, features 간 import 금지라 execution/auth 글리프 재사용 불가 — 벡터만 Figma 실 에셋에서 옮겨 미러 신설). |
| `ui/VisitRecordCard.tsx` | **신규.** `deriveVisitStatus(card)`로 내부 파생(status를 prop으로 안 받음). **★fill 함정 방어**: 완료↔미완료를 fill 색만 바꾸지 않고 상태별 **서로 다른 testID**(`record-visit-check-{done\|active\|upcoming\|skipped}-{id}`)로 렌더 — 완료 발화 Pressable은 IN_PROGRESS에만 존재해 UPCOMING press가 **구조적으로** 0회(`fireEvent.press`는 `disabled`를 안 막는다는 함정을 disabled가 아니라 "핸들러 부재"로 회피, 개념 [[글리프 fill 색 사각 (SVG 단일 노드는 값 변화를 못 잰다)]] TRIP-565 절). 건너뜀 컨트롤(`record-visit-skip-{id}`)은 j01 default 프레임에 시각 정본이 없어 헤더 우측에 자율 배치(01b 맹점3, 6-b/후속에서 위치가 바뀔 수 있음). raw-hex 무가드(03b 참고-5, 현재 0 hits이나 신규 심판 공백 — 후속 티켓 후보). **TRIP-613 additive**: `onPressEditTime?` prop — 주면 헤더 우측에 "시각 수정" Pressable(`record-trip-visit-time-edit-{id}`), 안 주면 부재(후방호환, 565 `onPressComplete?`·`onPressSkip?`와 같은 패턴). |
| `ui/VisitRecordCard.test.tsx` | AC-1(UI 반)·AC-3(upcoming 완료 불가)·★fill 함정 잠금(4상태 it.each present/absent 짝)·skip UI. **TRIP-613**: AC-8 describe 3건(onPressEditTime 부재/제공/press) 추가, 기존 5블록 무변경(무회귀 증거). |
| `ui/VisitTimeSheet.tsx` | **신규(TRIP-613)** — j01 방문 시각 수정 바텀시트(`@gorhom/bottom-sheet`). 시·분을 `ScrollView`+`map`으로 전 값 렌더 + press로 선택(휠 대신 **셀-press** — 이 리포에 휠 라이브러리가 없고 jest가 스크롤-스냅을 구동 못 하며, 시트 안에 휠을 넣으면 `enableContentPanningGesture` 회귀 재발 위험, repo-traps TRIP-599. 개념 [[enableContentPanningGesture]] TRIP-613 절). 시각 문자열은 `slice`로만 다룸(`new Date` 파싱 금지 — 타임존 이동 방지, `SlotTimeSheet` 선례). 저장은 "바뀐 필드만"(diff) `onSave`에 전달(BR-U5-04 "안 보내면 유지"). 완료 컬럼은 `arrivedAt==null`이면 `accessibilityState.disabled`(AC-3) — ⚠️ 이 조건은 **도착 유무만** 봐서, 도착O·완료null(IN_PROGRESS)에서는 컬럼이 활성인데 저장 시 완료 입력이 조용히 폐기된다(03b 경고-1, 후속 티켓 후보 — 시트가 아직 페이지에 미배선이라 오늘 프로덕션 피해 0). ⚠️ 합성값↔원본 diff가 **raw 문자열 완전비교**라 서버가 초/Z 포함 포맷을 주면 무변경 저장도 스퍼리어스 PATCH를 낼 수 있음(03b 경고-2, 백엔드 실포맷 확인 선행 필요 — 후속 티켓 후보). INV-3 삼중 방어(소스 스캔 G2·렌더 스캔 S5·`recordsStructure` G6). |
| `ui/VisitTimeSheet.test.tsx` | **신규(TRIP-613)** — S1~S7. 셀 선택·diff 저장·완료 비활성(AC-3)·인라인 오류·INV-3 렌더 스캔(동적 `${m}분` 사각까지). |
| `ui/SpontaneousVisitButton.tsx` | **신규.** testID `record-trip-spontaneous-add`. |
| `ui/SpontaneousVisitButton.test.tsx` | AC-5 UI 반. |
| `ui/TripRecordsScreen.tsx` | **신규 → TRIP-569로 `DayAttributionHeader` 추가.** 무상태 프레젠테이션 — appbar·일자 탭·지도 히어로(`KakaoMapView viewOnly`)·카드 목록·즉석 추가·탭바·저장 FAB. 지도는 250px 고정 블록, 즉석 추가 버튼·카드는 지도 아래 flow 형제(**KakaoMapView 위 인터랙티브는 형제 노드로** — WebView가 자기 위 absolute 오버레이의 터치를 흡수하는 함정, repo-traps 지도 절). `itineraryMapSurfaceStructure.test.ts`의 `LOCKED_CALLERS`에 opt-in 등재(viewOnly/connectPins 심판을 확장, 커버리지 약화 아님 — 개념 [[가드의 사정거리 (opt-in 등재는 넓히되 기존 사각은 그대로다)]]). **TRIP-569 additive**: optional prop `attribution?: {dayLabel; stayName?}`(안 주면 헤더 미표시, 후방호환) — `stayName` truthy면 `record-trip-attribution-stay`, null이면 `record-trip-attribution-date`. j02(기록 비교)엔 이 UI가 정본에 없어(01 브리프 맹점4) 기존 record 토큰만으로 최소 표시, ScrollView 최상단 배치. **fill 함정 방어**: 숙소있음/없음을 색이 아니라 상호배타 testID(별개 Text leaf, `getByText` 완전일치)로 갈랐다. |
| `model/syncQueue.ts` | **신규(TRIP-568)** — `enqueue`·`replayQueue(queue, execute)` 순수 큐 + 멱등 재생. HTTP는 `execute`로 **주입**(DI, PBT가 네트워크 없이 재생 — 개념 [[의존성 주입 (DI)]] TRIP-568 절). 터미널 상태(`SYNCED`·`CONFLICT`)는 재실행하지 않는다(`TERMINAL.includes` 가드) — 이 skip이 멱등의 실체(상태 스냅숏만으론 안 잡히고 **호출수 짝**이 잡는다, 개념 [[오프라인 큐 + 멱등 재생 (idempotent replay)]]). 409는 `resolveVisitConflict`(`@/shared/api/visitConflict`, 재사용·새 HTTP 0건·G5)로 `alreadyRecorded→SYNCED`·`conflict→CONFLICT` 두 갈래(raw `status===409`로 뭉치면 진짜 충돌이 조용히 SYNCED로 덮인다 — 개념 [[409 수렴 (이미 있음은 실패가 아니다)]] TRIP-568 절). caller-owned in-memory 순수 값 — 내구 영속화·NetInfo 실배선은 후속 티켓(부모 TRIP-129, ponytail 주석). |
| `model/syncQueue.test.ts` | S-enqueue(비파괴)·S1 예제(4항목 확정 상태)·S2 PBT-U5-F2(블로킹, `statusSnapshot(twice)===statusSnapshot(once)` + 2차 호출 id ⊆ unknown)·S3 두 갈래(`already !== conflict`). |
| `model/conflict.ts` | **신규(TRIP-568)** — `isVisitConflict(localBaseUpdatedAt, serverUpdatedAt)`: 로컬이 딛고 선 base 시각이 서버 `updatedAt`보다 이르면 충돌(BR-U5-22, 개념 [[낙관적 락 기준버전 (updatedAt)]] TRIP-568 절). 구현은 ISO 문자열 사전식 비교 한 줄 — ⚠️ **같은 포맷(같은 소수 자리·`Z`)일 때만 안전**, 포맷이 다르면(`:00Z` vs `:00.001Z`) 판정이 뒤집혀 진짜 충돌을 놓치거나 없는 충돌을 만든다(03b 경고-1, 개념 [[UTC 사전식 날짜 경계 (타임존 안전)]] TRIP-568 절 — 소비자 0이라 오늘 피해 없음, 배선 전 백엔드 실포맷 확인 선행 필요, 후속 티켓 후보). `ConflictVisitVM`·`ConflictRow`·`ConflictSelection`·`ConflictChoice` 타입도 여기 산다. |
| `model/conflict.test.ts` | C1(it.each 3행): base<server→true, ==→false, base>server→false. |
| `ui/SyncBadge.tsx` | **신규(TRIP-568)** — 클라 소유 4상태(`LOCAL\|PENDING\|SYNCED\|CONFLICT`, BR-U5-19)를 배지 3표기(`동기화 대기`·`동기화 완료`·`충돌`)로 접는다. `SYNC_BADGE_LABEL: Record<SyncStatus,string>` 전수라 상태가 늘면 tsc가 먼저 깨진다. testID `record-trip-sync-badge`. 색은 무심판(fill jest 사각) — 표기 문자열만 계약. `SyncStatus`를 type-only import로 가져와 syncQueue의 런타임(axios)을 안 끌어온다(프리뷰·격리 렌더 안전). |
| `ui/SyncBadge.test.tsx` | B1: `Object.keys().sort()` + 값 4키 잠금. |
| `ui/ConflictSheet.tsx` | **신규(TRIP-568)** — 충돌 해소 **전체화면 조건부 렌더 뷰**(이름은 "Sheet"지만 `@gorhom/bottom-sheet` 아님 — Figma `1563:1842`가 딤·핸들 없는 전체 본문, 개념 [[바텀시트→인라인 패널 전환 (통과형 목 사각 해소)]] TRIP-568 절). 방문 단위 카드 · `내 기기(오프라인)`vs`서버` 2열 · 열당 라디오 1개 · **미선택 시작**(BR-U5-21 침묵 승자 금지) · 모두 고르기 전 적용 비활성(`allChosen`, `conflicts.length>0 &&` 가드 — 5-c 참고-1: 빈 배열이면 `every` 공허 참으로 활성돼 `onApply([])` 발화하던 결함 수정) · 선택은 색이 아니라 `accessibilityState.selected`(글리프 fill 함정 회피) · 적용 press → `onApply` 정확히 1회(방문별 배열). |
| `ui/ConflictSheet.test.tsx` | A1(leaf 완전일치 렌더)·A2/A3(`toBeSelected`/`not.toBeSelected`)·A4(콜백 1회·정확 배열)·`toBeDisabled`(미선택 시 적용 비활성). |
| `model/photoAttach.ts` | **신규(TRIP-566)** — 순수 함수. `photoAttach(asset: PhotoAssetMeta, gpsConsent: boolean) → AddPhotoRequest`. `gpsConsent=false`거나 좌표가 없으면 `exifLat`/`exifLng` **키 자체를 안 만든다**(조건부 대입, `undefined`로도 안 싣는다 — `not.toHaveProperty`가 값 무관 키 존재만 판정하는 strict 매처라 이 구분이 실체, 개념 [[키 부재 vs 값 undefined]]). 동의는 직접 안 읽고 boolean **DI**로 받는다(순수 유지, tsc/jest 안 깨는 조건 — expo 타입/모듈 import 0). `PhotoAssetMeta`는 `shared/photo`에서 import해 `export type`로 재수출. |
| `model/photoAttach.test.ts` | PBT(fast-check, exif 양쪽 생성) + 긍정 짝(consent=true→실림) — AC-2 P1/P3. |
| `model/photoAvailability.ts` | **신규(TRIP-566)** — 판별 유니온 `photoAvailability(photo, currentDeviceId, assetOk) → 'available'\|'other-device'\|'unavailable'`. `deviceId` 불일치면 `assetOk` 무관하게 `other-device`(BR-U5-15 — 그 기기엔 애초에 자산이 없다), 일치하면 `assetOk`로 available/unavailable — **deviceId가 assetOk를 이긴다**(순서 의존, 뮤테이션 진리표 4행이 이 우선순위를 잠근다). |
| `model/photoAvailability.test.ts` | 진리표 4행(deviceId×assetOk 조합). |
| `model/useVisitAttachments.ts` | **신규(TRIP-566)** — 생성 클라이언트 3함수(GET/POST photos·PUT memo)만 재사용(새 HTTP 0, `recordsStructure.test.ts` G5). `addPhoto`는 `photoAttach` 경유 후 POST → `invalidateQueries`로 재조회(낙관 캐시 조작 아님 — 서버 재조회라 `settle()` 매크로태스크 브리지 불필요, `useVisitCheck.ts`의 낙관 갱신과 다른 패턴). `saveMemo`는 `text.trim()===''`면 **PUT 0회**(무의미 PUT 방지, 비즈니스 판정 아님). |
| `model/useVisitAttachments.integration.test.tsx` | I1~I4, msw. addPhoto no-op→GET 재조회 · saveMemo 공백 무시. |
| `ui/PhotoThumbStrip.tsx` | **신규(TRIP-566)** — 상태를 fill 색이 아니라 **상태별 distinct testID**(`record-photo-{available\|other-device\|unavailable}-{id}`, T5 체크서클 선례 동형)로 렌더. 실 `<Image>`(`record-photo-thumb-image-{id}`)는 **available+uri 있을 때만** 존재 — 나머지 상태엔 Image 자체가 부재해 "깨진 썸네일 0"을 present/absent 짝으로 잠근다(fill 함정 회피, 개념 [[글리프 fill 색 사각 (SVG 단일 노드는 값 변화를 못 잰다)]] 계열과 동형이나 이쪽은 Image 컴포넌트 유무). `+` 추가 타일(`record-trip-photo-add`) press → `onPressAdd`. |
| `ui/PhotoThumbStrip.test.tsx` | 상태 4종(present/absent 짝) + add + 다건 렌더. |
| `ui/MemoInline.tsx` | **신규(TRIP-566)** — `TextInput maxLength=2000`(서버 권위 `PutMemoRequest.text` 1~2000의 UX 사본, jest `fireEvent.changeText`는 maxLength를 우회하므로 prop 값으로만 잠금). `onSubmitEditing` → trim 후 비면 무발화. ⚠️ **seed-once 파생 상태**(`:22` `useState(text ?? '')`) — `text` prop을 초기값으로만 써 마운트 뒤 prop이 바뀌어도 입력이 고정된다. 리스트에서 `key` 없이 인스턴스가 재활용되면 다른 방문 카드의 메모가 잔류(B 카드에 A 메모). **현재 프로덕션 소비자 0**(페이지 배선 후속) — 후속 페이지 배선 티켓에 `key={visitCheckId}` 필수 + text-변경 재렌더 테스트 1건을 명시할 것(03b 참고-1, 오늘 피해 없음). 개념: [[seed-once 파생 상태]]. |
| `ui/MemoInline.test.tsx` | 6건 — maxLength 잠금(4000 뮤테이션 red)·공백 게이트·onSubmitEditing. |
| `ui/VisitRecordCard.tsx` | **TRIP-566 additive** — 옵셔널 `photoSlot?`/`memoSlot?`(ReactNode) 추가. 주면 그 자리에 slot을 surface, **안 주면 기존 정적 스캐폴딩(`+`타일·"메모를 남겨보세요") 그대로 유지**(565/613 옵셔널 prop 동형 후방호환 — 안 주는 기존 호출부가 사진/메모 자리를 잃지 않는다). T5 무회귀 = 이 파일의 기존 `VisitRecordCard.test.tsx` **git diff 0**(신규 슬롯 테스트는 `.wiring.test.tsx`로 별도 분리 — 새 컴포넌트 import를 기존 파일에 더하면 import 실패가 T5 5블록까지 오염시키므로 의도적 격리). |
| `ui/VisitRecordCard.wiring.test.tsx` | **신규(TRIP-566)** — W1~W3, 슬롯은 계약 testID만 실은 마커 노드로 잠금(신규 컴포넌트 부재에도 W2 선제 green — 실 조립은 각 컴포넌트 테스트+페이지 배선(범위 밖) 소관). |
| `__tests__/recordPhotoBinaryGuard.test.ts` | `src/__tests__/`(전역 소스 스캔 관례) 소재 — 항목·상세는 `layer-test.md` 참고. INV-U5-03(사진 바이너리 서버 미전송)은 `AddPhotoRequest`에 storage_key·바이너리 필드 자체가 없어 **계약이 이미 구조적으로 만족**, 이 가드는 "코드가 새 업로드 경로를 짓지 않게" 소스 그래프를 잠그는 역할(개념 [[계약이 구조적으로 막는다]]). |
| `model/compareRows.ts` | **신규(TRIP-570)** — j02 기록 비교 순수 조립. `buildCompareRows(record: TripRecord, nameByPoi?) → CompareRow[]`(판별 유니온 `'actual'\|'unvisited'\|'change'`). 미방문은 **서버 `unvisitedSlotKeys` 직독**(planned vs actual 재비교로 재판정하지 않음, BR-U5-28 서버 소유 — CR-2b가 "서버 목록에 없으면 0건"으로 이 재판정 금지를 잠금). `record?.days ?? []`·`day?.actual ?? []`·`change?.before?.days?.[0]?.slots?.[0]?.poiId`처럼 **매 중첩 단계**를 방어(571·572·574 재발 방지, 개념 [[반쪽 방어 (half-applied guard)]]). `timeLabel`은 ISO `slice(11,16)`(`new Date` 금지, INV-3·타임존 안전). change의 before/after poiId는 계약이 full 스냅숏만 줘 단건 diff가 없으므로 **첫 슬롯**으로 구조만 잠금(정확 diff는 Blocker C 후속). before-POI 이름은 `nameByPoi[poiId] ?? poiId`(best-effort, 스냅숏 결측이면 `'이전 장소'`/`'변경된 장소'` 폴백). |
| `model/compareRows.test.ts` | CR-1~5 — 실제 행(AC-4)·미방문 파생+재판정 금지(AC-2, CR-2b)·변경 행 전후·사유·시각(AC-3)·중첩 결측 4변형(`it.each`, CR-4)·INV-3 직렬화(`JSON.stringify` 후 `duration` 키·소요시간 문자열 0, CR-5). |
| `model/useCompareRecords.ts` | **신규(TRIP-570)** — `useGetTripsTripIdRecords`(orval 생성, `GET /trips/{tripId}/records`) 얇은 래퍼. **동명 `useTripRecords`(`/visits/days/{day}` 감쌈, j01)와 별개** — 혼동 방지 주석 명시(개념 병기, 구조가드 G2가 페이지의 `useCompareRecords` 참조를 긍정 앵커로 잠금). 변경 이력(`changes[]`)이 이 응답에 임베드돼 내려온다 — standalone `GET /change-log`는 여전히 미개통(생산자 없어 항상 빈 목록, 티켓 원문의 드리프트, `traps-record.md` 참고). |
| `ui/CompareSegment.tsx` | **신규(TRIP-570)** — j02 3탭 세그(testID `record-compare-segment`, 탭별 `record-compare-tab-{planned\|actual\|change}`). `FormatSegment`(j06)·`TripStatusSegment` 패턴 참고(features 경계로 **import 금지**, 패턴만). 활성 표기는 색이 아니라 `accessibilityState.selected`(fill 함정 회피). 세그는 리스트 필터가 아니라 **지도 레이어 강조 토글**(01b·Figma 관측) — 화면은 활성 탭과 무관하게 행 전부를 그린다. |
| `ui/CompareSegment.test.tsx` | CS-1(렌더)·CS-2(`toBeSelected`/`not.toBeSelected`)·CS-3(press→`onSelect` 콜백). |
| `ui/CompareRow.tsx` | **신규(TRIP-570)** — testID `record-compare-row`(kind 무관 공통 루트). kind별 라벨(`[실제]`/`[계획]`/`[변경]`)·상태 pill(`미방문`/`휴무`)은 색이 아니라 **distinct 텍스트 leaf**로 구분(fill 함정 회피). 변경 행은 before·after·구분자를 **각각 별 `<Text>`**로 그려 `getByText` leaf 완전일치가 개별로 잡히게 했다(Figma는 한 줄 표기지만 테스트가 별개 leaf를 요구, 03 트레이드오프). |
| `ui/CompareRow.test.tsx` | RW-1(실제)·RW-2(미방문)·RW-3(변경, before/after/사유/시각)·RW-3b(poiId 폴백에도 구조 유지). |
| `ui/RecordsCompareScreen.tsx` | **신규(TRIP-570)** — 무상태 프레젠테이션(VM 주입, `TripRecordsScreen`·`TripSummaryScreen` 동형 규율). appbar·세그·귀속 헤더(상호배타 testID `record-compare-attribution-{stay\|date}`, j01 선례 미러)·범례·**지도 degrade 자리표시**(`record-compare-map-pending`, `KakaoMapView`를 아예 안 문다 — 좌표 계약 자체가 없어 `itineraryMapSurfaceStructure`의 `LOCKED_CALLERS` 등재가 불필요, 442·563·571의 3연속 6-a FAIL을 원천 회피. 개념 [[degrade 스텁 — 못 켜는 기능은 정직하게 꺼둔다]] TRIP-570 절)·라벨 행 목록(활성 탭 무관 전체). |
| `ui/RecordsCompareScreen.test.tsx` | SC-1(세그+행+map-pending 렌더)·SC-2(활성 탭 무관 전체 행, 필터 아님)·SC-3/4(귀속 상호배타 있음/null)·SC-5(뒤로 콜백). |

## 관련

- 페이지 배선: `frontend/.claude/rules/layer-pages.md`(`trip-records`·`records-compare` 행). ConflictSheet·SyncBadge는 실 라우트 미배선(프리뷰 `records-conflict`·`records-sync-badge`만, 후속 티켓).
- 개념: [[per-record 상태 파생 (세 timestamp에서 우선순위로 계산)]] · [[낙관적 갱신과 롤백 (optimistic update)]] · [[판별 유니온]] · [[글리프 fill 색 사각 (SVG 단일 노드는 값 변화를 못 잰다)]] · [[뮤테이션 테스팅]] · [[가드의 사정거리 (opt-in 등재는 넓히되 기존 사각은 그대로다)]] · [[기능 간 조합은 pages 층 전담 (기계 강제 없음)]] · [[낙관적 락 기준버전 (updatedAt)]] · [[의존성 주입 (DI)]] · [[409 수렴 (이미 있음은 실패가 아니다)]] · [[enableContentPanningGesture]] · [[조회시점 파생·미저장]] · [[UTC 사전식 날짜 경계 (타임존 안전)]] · [[생성훅 직접 사용 (cross-feature 우회)]] · [[base 구간 커버리지 판정]] · [[오프라인 큐 + 멱등 재생 (idempotent replay)]] · [[바텀시트→인라인 패널 전환 (통과형 목 사각 해소)]] · [[네이티브 의존성 분리 (순수 + degrade 스텁)]] · [[키 부재 vs 값 undefined]] · [[계약이 구조적으로 막는다]] · [[seed-once 파생 상태]] · [[반쪽 방어 (half-applied guard)]] · [[degrade 스텁 — 못 켜는 기능은 정직하게 꺼둔다]] · [[페이지 조립은 jest 무심판]].
- 개발로그: [[2026-08-31 20260831-trip565-visit-records]] · [[2026-08-31 20260831-trip619-visitcheck-updatedat]] · [[2026-08-31 20260831-trip613-visit-time-sheet]] · [[2026-08-31 20260831-trip569-stay-attribution]] · [[2026-08-31 20260831-trip568-offline-sync-conflict]] · [[2026-09-01 20260831-trip566-photo-memo-attach]] · [[2026-09-01 20260901-trip570-record-compare]].
