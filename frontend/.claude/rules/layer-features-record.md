---
paths:
  - "src/features/record/**"
---
# `src/features/record/` — j01 방문 기록 default (TRIP-565 신설)

**이 폴더는 이번 사이클(TRIP-565)에서 처음 생겼다.** `features/execution`과 목적이 겹쳐 보이지만(둘 다 `visit_check` 실적을 다룸) 페이지 shape가 다르다 — execution의 `deriveVisitProgress`는 poi 단위 **집계**(i01 실행 카드용), 이쪽은 per-record 4상태다. 그래서 재사용하지 않고 신규 구현했다(01b Q2, YAGNI — 3번째 소비자가 나올 때까지 shared 승격 보류).

**경계**: `features/record`는 **다른 `features/*`를 import할 수 없다**(특히 `execution` — 이름이 같은 `useVisitCheck`가 있어 혼동하기 쉽다). `eslint.config.js`의 `FEATURES` 배열엔 `record`가 없어 여전히 기계 강제 밖 — `recordsStructure.test.ts`의 G2(소스 재귀 스캔, `settingsBoundary.test.ts`·`notificationStructure.test.ts` 동형)가 이 경계의 유일한 그물이다(개념 [[기능 간 조합은 pages 층 전담 (기계 강제 없음)]]).

| 파일 | 역할 |
|---|---|
| `model/visitStatus.ts` | **신규.** `deriveVisitStatus({arrivedAt, completedAt, skippedAt}) → 'UPCOMING'\|'IN_PROGRESS'\|'COMPLETED'\|'SKIPPED'`. 서버·클라 어디에도 `status` 컬럼/필드를 두지 않고 세 timestamp의 유무만으로 매번 계산(INV-U5-01) — 입력 타입에 `status` 자리 자체가 없어 "저장된 상태를 실수로 읽는" 경로가 구조적으로 봉쇄된다. 우선순위 `skipped > completed > arrived > upcoming`은 execution의 `deriveVisitProgress`와 규칙은 같되 shape(집계 vs per-record)가 달라 재사용하지 않았다. 개념 [[per-record 상태 파생 (세 timestamp에서 우선순위로 계산)]]. |
| `model/visitStatus.test.ts` | PBT(fast-check) + 진리표 + INV 잠금(가짜 `status` 필드를 섞어도 무시됨을 확인). **02b test-fix 1건**: `fc.date({min,max})`가 fast-check v4.9.0에서 Invalid Date를 생성해 `.toISOString()`이 구현 호출 전에 throw하는 심판 결함(~25% flaky) — `noInvalidDate: true` 한 줄로 수정, 뮤테이션 실측(우선순위 swap → 2 failed)으로 커버리지 불변 확인(개념 [[뮤테이션 테스팅]] TRIP-565 절). |
| `model/useVisitCheck.ts` | **신규 → TRIP-619로 arrive 낙관 리터럴에 `updatedAt` 1필드 추가.** `arrive`/`complete`/`skip` 3종 낙관 갱신 + 레코드(visitCheckId) 단위 롤백(통짜 스냅숏 복원 아님, execution 선례 동형, W-2 회귀 방지). 판별 유니온 `VisitCheckOutcome`(`{kind:'completed'}\|{kind:'failed',reason}` 등)으로 실패를 조용히 삼키지 않는다(INV-4, 개념 [[판별 유니온]]). **`settleRollback()`**(:54-55, `await new Promise(r => setTimeout(r,0))`) — react-query v5 `notifyManager`의 배치 통지가 매크로태스크로 밀려 롤백 직후 관찰자 스냅숏이 stale COMPLETED를 보이는 것을 한 틱 양보해 브리지(개념 [[낙관적 갱신과 롤백 (optimistic update)]] TRIP-565 절, code-critic 참고-1 — 실효 소비자는 테스트뿐, 프로덕션 `TripRecordsPage`는 `void`로 fire-and-forget이라 무해하지만 5-c 후보로 test-infra 이관 미룸). arrive는 complete/skip과 달리 **무효화 대신 응답 레코드로 낙관 레코드를 교체**한다(재조회가 즉석 삽입을 덮어 AC-5가 깨지는 것을 피함, `useVisitCheck.ts` 내부 주석 명시). **TRIP-619**: codegen 재생성으로 `VisitCheck.updatedAt`이 required가 되며 arrive 낙관 리터럴에 자리표시자 채움(성공 시 서버 응답 레코드로 통째 교체돼 무해, execution 동형 — 개념 [[낙관적 락 기준버전 (updatedAt)]]). |
| `model/useVisitCheck.integration.test.tsx` | msw + 실 QueryClient. AC-1(plan 미접촉)·AC-2(409 롤백·불변)·AC-5(즉석 2건 append)·skip·★W-2(동시 두 도착 중 하나 실패해도 다른 낙관 생존). ⚠️ AC-1의 "plan 미접촉" 단언(`/itinerary` 히트 0건)은 이 셋업에서 항상 참(페이지 미마운트, 호출 경로 자체가 없음) — BR-U5-01의 진짜 보장은 구조적(G2 소스 스캔이 execution import 0을 보조), 03b 참고-3. |
| `model/useTripRecords.ts` | **신규 → TRIP-569로 `useRecordBases`·`useRecordSavedStays` 2개 얇은 래퍼 추가.** 원래는 `useGetTripsTripIdVisitsDaysDay` 얇은 래퍼(로직 0, `useLiveItinerary.ts` 선례)뿐이었다 — 동작 테스트 없음, 구조 가드가 존재만 확인. **TRIP-569 추가분**: `useRecordBases`(`useGetTripsTripIdBases`)·`useRecordSavedStays`(`useGetSavedStays`) — `features/trip`이 이미 감싼 같은 생성 훅을 `record`가 features 경계(G2) 때문에 못 가져와 자기 안에서 다시 감쌌다(승격 아님, 개념 [[생성훅 직접 사용 (cross-feature 우회)]]). |
| `model/stayAttribution.ts` | **신규(TRIP-569)** — `deriveStayAttribution({visits, bases, savedStays?}) → DayAttribution[]`(무게중심 모델). visits를 날짜별로 묶고, 그 날짜를 덮는 base(`dateFrom≤D<dateTo`, 개념 [[base 구간 커버리지 판정]])로 숙소명을 해소한다. 반환 타입에 "저장된 귀속" 자리를 두지 않아 매 호출 재계산됨을 구조로 강제(BR-U5-25, 개념 [[조회시점 파생·미저장]]). 날짜는 `new Date`/`Date.UTC` 없이 `'YYYY-MM-DD'` 문자열 슬라이스+사전식 비교만 사용(개념 [[UTC 사전식 날짜 경계 (타임존 안전)]]) — ⚠️ 즉석 방문(`slotKey==null`)의 `arrivedAt` 슬라이스는 "기기 타임존 무관"만 보장하고 "여행지 기준 날짜 정확"은 백엔드 `arrived_at` 직렬화(UTC vs 여행지 로컬)에 달려 있어 미확인(03b 경고-1, 후속 티켓 후보). |
| `model/stayAttribution.test.ts` | **신규(TRIP-569)** — AC-1~AC-6 행동 테스트 + PBT(임의 bases 조합에서 `baseStay`가 항상 그 날짜를 덮는 base의 함수임을 전수, tautology 아님 — code-critic 참고-1 확인). dateTo 경계값 3종(dateFrom·dateTo-1·dateTo) 명시 커버. |
| `model/adjustTimesDraft.ts` | **신규(TRIP-613)** — `adjustTimesDraft({arrivedAt, completedAt, now}) → {ok:true}\|{ok:false, reason}`(판별 유니온, 개념 [[판별 유니온]] TRIP-613 절). 순수 검증 — 시계를 안 읽고 **`now`를 인자로 받는다**(개념 [[의존성 주입 (DI)]] TRIP-613 절, `useSocialLogin`의 `authorize` DI와 같은 원리를 시각에 적용). 규칙 순서: ①완료-only(도착 없이 완료) 거부 `'completed-without-arrived'` ②순서 역전(완료<도착) 거부 `'order'` ③미래시각 거부 `'future'`(BR-U5-05). PBT-U5-2가 order 속성을 fast-check로 전수. |
| `model/useAdjustVisitTimes.ts` | **신규(TRIP-613)** — `useAdjustVisitTimes()`. `PATCH /trips/{tripId}/visits/{visitCheckId}` 낙관 뮤테이션: ①캐시에서 그 레코드를 `before`로 스냅숏 ②낙관 반영(바뀐 필드만) ③`expectedUpdatedAt=before.updatedAt` 포함 전송([[낙관적 락 기준버전 (updatedAt)]]) ④200→서버 레코드로 그 자리 통째 교체 `{kind:'adjusted'}` ⑤409 `VISIT_CONFLICT`(`resolveVisitConflict`, `@/shared/api/visitConflict` 딥임포트)→그 레코드만 롤백+그 날 캐시 재조회 `{kind:'conflict', message}` ⑥그 외(404/네트워크)→롤백만 `{kind:'failed', reason}`. 롤백은 **레코드 단위**(통짜 스냅숏 아님, `useVisitCheck.ts` 규율 계승 — 개념 [[낙관적 갱신과 롤백 (optimistic update)]] TRIP-613 절). 성공 경로에도 `settle()`(react-query 배치 통지 매크로태스크 브리지) 필요 — 없으면 AC-5만 조용히 red(동기 캐시 읽기가 배치 flush보다 먼저 돎). ⚠️ `alreadyRecorded`·`not-found`·`network` 세 결과는 통합테스트가 안 밟아 무심판(03b 참고-1, 후속 티켓 후보). |
| `ui/RecordGlyphs.tsx` | **신규.** 체크서클·즉석추가 등 벡터. `*Glyphs.tsx` 관례(raw hex 스캔 제외, features 간 import 금지라 execution/auth 글리프 재사용 불가 — 벡터만 Figma 실 에셋에서 옮겨 미러 신설). |
| `ui/VisitRecordCard.tsx` | **신규.** `deriveVisitStatus(card)`로 내부 파생(status를 prop으로 안 받음). **★fill 함정 방어**: 완료↔미완료를 fill 색만 바꾸지 않고 상태별 **서로 다른 testID**(`record-visit-check-{done\|active\|upcoming\|skipped}-{id}`)로 렌더 — 완료 발화 Pressable은 IN_PROGRESS에만 존재해 UPCOMING press가 **구조적으로** 0회(`fireEvent.press`는 `disabled`를 안 막는다는 함정을 disabled가 아니라 "핸들러 부재"로 회피, 개념 [[글리프 fill 색 사각 (SVG 단일 노드는 값 변화를 못 잰다)]] TRIP-565 절). 건너뜀 컨트롤(`record-visit-skip-{id}`)은 j01 default 프레임에 시각 정본이 없어 헤더 우측에 자율 배치(01b 맹점3, 6-b/후속에서 위치가 바뀔 수 있음). raw-hex 무가드(03b 참고-5, 현재 0 hits이나 신규 심판 공백 — 후속 티켓 후보). **TRIP-613 additive**: `onPressEditTime?` prop — 주면 헤더 우측에 "시각 수정" Pressable(`record-trip-visit-time-edit-{id}`), 안 주면 부재(후방호환, 565 `onPressComplete?`·`onPressSkip?`와 같은 패턴). |
| `ui/VisitRecordCard.test.tsx` | AC-1(UI 반)·AC-3(upcoming 완료 불가)·★fill 함정 잠금(4상태 it.each present/absent 짝)·skip UI. **TRIP-613**: AC-8 describe 3건(onPressEditTime 부재/제공/press) 추가, 기존 5블록 무변경(무회귀 증거). |
| `ui/VisitTimeSheet.tsx` | **신규(TRIP-613)** — j01 방문 시각 수정 바텀시트(`@gorhom/bottom-sheet`). 시·분을 `ScrollView`+`map`으로 전 값 렌더 + press로 선택(휠 대신 **셀-press** — 이 리포에 휠 라이브러리가 없고 jest가 스크롤-스냅을 구동 못 하며, 시트 안에 휠을 넣으면 `enableContentPanningGesture` 회귀 재발 위험, repo-traps TRIP-599. 개념 [[enableContentPanningGesture]] TRIP-613 절). 시각 문자열은 `slice`로만 다룸(`new Date` 파싱 금지 — 타임존 이동 방지, `SlotTimeSheet` 선례). 저장은 "바뀐 필드만"(diff) `onSave`에 전달(BR-U5-04 "안 보내면 유지"). 완료 컬럼은 `arrivedAt==null`이면 `accessibilityState.disabled`(AC-3) — ⚠️ 이 조건은 **도착 유무만** 봐서, 도착O·완료null(IN_PROGRESS)에서는 컬럼이 활성인데 저장 시 완료 입력이 조용히 폐기된다(03b 경고-1, 후속 티켓 후보 — 시트가 아직 페이지에 미배선이라 오늘 프로덕션 피해 0). ⚠️ 합성값↔원본 diff가 **raw 문자열 완전비교**라 서버가 초/Z 포함 포맷을 주면 무변경 저장도 스퍼리어스 PATCH를 낼 수 있음(03b 경고-2, 백엔드 실포맷 확인 선행 필요 — 후속 티켓 후보). INV-3 삼중 방어(소스 스캔 G2·렌더 스캔 S5·`recordsStructure` G6). |
| `ui/VisitTimeSheet.test.tsx` | **신규(TRIP-613)** — S1~S7. 셀 선택·diff 저장·완료 비활성(AC-3)·인라인 오류·INV-3 렌더 스캔(동적 `${m}분` 사각까지). |
| `ui/SpontaneousVisitButton.tsx` | **신규.** testID `record-trip-spontaneous-add`. |
| `ui/SpontaneousVisitButton.test.tsx` | AC-5 UI 반. |
| `ui/TripRecordsScreen.tsx` | **신규 → TRIP-569로 `DayAttributionHeader` 추가.** 무상태 프레젠테이션 — appbar·일자 탭·지도 히어로(`KakaoMapView viewOnly`)·카드 목록·즉석 추가·탭바·저장 FAB. 지도는 250px 고정 블록, 즉석 추가 버튼·카드는 지도 아래 flow 형제(**KakaoMapView 위 인터랙티브는 형제 노드로** — WebView가 자기 위 absolute 오버레이의 터치를 흡수하는 함정, repo-traps 지도 절). `itineraryMapSurfaceStructure.test.ts`의 `LOCKED_CALLERS`에 opt-in 등재(viewOnly/connectPins 심판을 확장, 커버리지 약화 아님 — 개념 [[가드의 사정거리 (opt-in 등재는 넓히되 기존 사각은 그대로다)]]). **TRIP-569 additive**: optional prop `attribution?: {dayLabel; stayName?}`(안 주면 헤더 미표시, 후방호환) — `stayName` truthy면 `record-trip-attribution-stay`, null이면 `record-trip-attribution-date`. j02(기록 비교)엔 이 UI가 정본에 없어(01 브리프 맹점4) 기존 record 토큰만으로 최소 표시, ScrollView 최상단 배치. **fill 함정 방어**: 숙소있음/없음을 색이 아니라 상호배타 testID(별개 Text leaf, `getByText` 완전일치)로 갈랐다. |

## 관련

- 페이지 배선: `frontend/.claude/rules/layer-pages.md`(`trip-records` 행).
- 개념: [[per-record 상태 파생 (세 timestamp에서 우선순위로 계산)]] · [[낙관적 갱신과 롤백 (optimistic update)]] · [[판별 유니온]] · [[글리프 fill 색 사각 (SVG 단일 노드는 값 변화를 못 잰다)]] · [[뮤테이션 테스팅]] · [[가드의 사정거리 (opt-in 등재는 넓히되 기존 사각은 그대로다)]] · [[기능 간 조합은 pages 층 전담 (기계 강제 없음)]] · [[낙관적 락 기준버전 (updatedAt)]] · [[의존성 주입 (DI)]] · [[409 수렴 (이미 있음은 실패가 아니다)]] · [[enableContentPanningGesture]] · [[조회시점 파생·미저장]] · [[UTC 사전식 날짜 경계 (타임존 안전)]] · [[생성훅 직접 사용 (cross-feature 우회)]] · [[base 구간 커버리지 판정]].
- 개발로그: [[2026-08-31 20260831-trip565-visit-records]] · [[2026-08-31 20260831-trip619-visitcheck-updatedat]] · [[2026-08-31 20260831-trip613-visit-time-sheet]] · [[2026-08-31 20260831-trip569-stay-attribution]].
