---
paths:
  - "src/shared/**"
---
# `src/shared/`


| 파일 | 역할 |
|---|---|
| `src/shared/api/index.ts` | **구현됨** — axios 인스턴스 · 인터셉터 · 토큰 갱신 · 서버 호출 전체. `authedClient` 인스턴스가 온보딩 5종 + `fetchBootstrap`(TRIP-172 결함 A-2, 이전엔 무인증 `baseClient`였음)을 인증 경로로 보낸다. `SERVER_ERROR_CODE_TRANSLATIONS`가 서버 실코드→프론트 계약 코드 번역표(현재 `AGE_REQUIREMENT_NOT_MET`→`AGE_NOT_MET` 1건, `normalizeSocialError`의 `??` 결과값에 적용 — 승인 테스트가 이 표를 겨냥하지 않아 미검증). `authedClient`는 **TRIP-179에서 처음 export**됐다(가시성만 변경, 새 심볼 아님 — `mutator.ts` 전용). **TRIP-248** — `normalizeSocialError`가 서버 에러 응답의 `fields`(어느 필드가 왜 틀렸는지 목록)를 더 이상 버리지 않고 `NormalizedApiError`에 선택 필드로 실어 나른다(원소 타입은 openapi 생성 `ErrorResponseErrorFieldsItem` 재사용 — 새 타입 선언 0, 새 export 0) |
| `src/shared/api/mutator.ts` | **신규(TRIP-179)** — orval이 생성한 클라이언트(`generated/**`)가 실제 HTTP 호출에 쓰는 단일 범용 함수(`customInstance`). `authedClient`를 그대로 얹고(새 인증 코드 0), `paramsSerializer: { indexes: null }`로 배열 쿼리를 브래킷 없이 직렬화(`amenity=A&amenity=B` — Spring `@RequestParam List<String>` 계약), `response.data`만 반환(AxiosResponse 껍질 벗기기). 오류 정규화는 안 거친다(그대로 위로 흘려보냄 — D6 이연) |
| `src/shared/api/tokenManager.ts` | **구현됨** — 동기 in-memory 액세스토큰 홀더. SecureStore(비동기)와 공존, 인터셉터가 동기로 읽는다. `subscribeAccessToken`으로 토큰 변화를 구독 가능(TRIP-172, 값이 실제로 바뀔 때만 통지 — 동등비교 가드) |
| `src/shared/api/isAlreadyRegistered.ts` | **신규(TRIP-296)** — 409(이미 등록됨) 판정 승격(D9, 몸통 1줄). `TripNewStep1Page.tsx`의 지역 함수를 이 자리로 승격해 소비 — 단 `features/explore/model/savedPlaces.ts`의 인라인 409 판정은 그대로 남아 **판정이 아직 두 벌**(N-2, 후속 후보) |
| `src/shared/api/isNotFound.ts` | **신규(TRIP-297)** — 404 판정(몸통 1줄, `isAlreadyRegistered.ts`와 동형). `isNotFound(error): boolean` — 네트워크 오류(응답 자체 없음)는 **`false`**(= "모른다"를 "아니다" 쪽으로 접는 fail-closed, 개념 [[fail-closed — 모를 때는 막는다]]). `DraftPage.tsx`의 `handleRetry`가 유일한 소비자 — 확정 일정에 잘못 POST가 나가는 것(되돌리는 API 없음)을 막는다 |
| `src/shared/storage/index.ts` | **구현됨** — expo-secure-store 토큰 저장소. `hasStoredToken`은 **accessToken 존재만 판정**한다(`getItemAsync(ACCESS_TOKEN_KEY) != null` — refreshToken만 없는 부분 저장도 true). TRIP-173 재작성 3/8에서 실수로 "두 토큰 다 있어야 true"로 좁혀졌다가(콜드스타트 폴백이 HOME→LOGIN으로 뒤집힘) code-critic이 잡아 원복됨 — 이 함수를 실제로 도는 테스트가 리포에 없어 재발해도 362건이 그대로 green이니 다음에 손댈 때 주의 |
| `src/shared/version/compareVersion.ts` | **구현됨** — 버전 비교(강제 업데이트 판정) |
| `src/shared/date/formatKoreanDate.ts` | **신규(TRIP-395, 20260820 재정합)** — `formatKoreanDate(isoDate): "M월 D일 요일"`(leading-zero 제거·요일 계산). **`liveTimeStructure` 가드(`features/execution/**`의 `new Date`류 금지) 회피가 존재 이유** — 날짜 파싱은 execution 밖에서만 허용돼 이 자리에 뺐다. 에포크 일수(UTC 정수) 산술만 써 TZ-safe(`tripDatePicker.ts` 관례). 배럴 없이 직접 import(`compareVersion.ts`와 동형). 어느 구조가드 모집단에도 없음(자유 재량). `pages/live-itinerary/ui/LiveItineraryPage.tsx`가 유일 소비자(부제 조립 `formatKoreanDate(activeDate) + ' · 오늘 일정'`) |
| `src/shared/location/LocationPreprompt.tsx` | **전체화면**(레이더 히어로·denied 전용 레이아웃 — 카드형은 폐기됐고 내부 마크업만 전면 교체, props/testID 시그니처 무변경). `default`/`permission-denied` 2상태. `expo-location`을 import조차 안 함(구조적으로 OS 다이얼로그 못 부름). **라우트 미등록**(실사용처 0, 프리뷰 전용) |
| `src/shared/location/LocationGlyphs.tsx` | 인라인 SVG — 위치 화면 글리프(레이더 히어로·오프 타일). stroke/fill 색은 `locationColors.ts` 상수 경유(`shared/location/**` 는 F2 raw-hex 가드 대상) |
| `src/shared/location/lib/locationColors.ts` | 위치 글리프 색 상수(raw hex 분리 — `gradients.ts` 패턴 재사용). 토큰 색과 수동 동기화 필요(03b 참고-2) |
| `src/shared/ui/StateNotice.tsx` | **승격(TRIP-222, `git mv` — 내용 무변경)**, 원래 위치는 features/stay/ui 아래(TRIP-182 신규, 지금은 없음). `empty`·`filter-zero`·`error` 3상태 공용 안내 블록(props 6개로 완전 파라미터화 — `testID`·`icon`·`title`·`description`·`actions[]`·`dashed?`). 호출부는 `stay`(`StaySearchScreen.tsx`, import 경로 1줄만 변경)·`explore`(`PlaceExploreScreen.tsx`, 신규 소비) 둘. **이 파일 전용 ESLint `no-restricted-imports` 블록**(`eslint.config.js`, `files: ['src/shared/ui/StateNotice.tsx']`)이 `useState`/`useReducer`(named import)·`expo-router`·`@tanstack/react-query`·`axios`를 막는다 — 승격으로 `staySearchStructure.test.ts`의 `FORBIDDEN_IN_STAY_UI` 검사 5항목을 잃은 것(code-critic W-3)을 jest가 아니라 lint로 대신 메운 것. `src/__tests__/sharedUiStructure.test.ts`(아래 테스트 인프라 절)가 `shared/ui` 디렉토리 재귀 스캔(경계·INV-3·URL·zustand)을 신설해 승격 파일이 토큰·경계 심판 사정거리 밖으로 안 나가게 잠근다. `SkeletonList.tsx`는 **승격하지 않았다**(prop 0개, 형상 불일치 — 위 `features/stay/ui` 절 참고) |
| `src/shared/ui/BottomTabBar.tsx` | 순수 뷰 탭바(TRIP-170) — 5탭 아이콘 자체 보유(인라인 SVG), 네비게이션을 모른다(`activeKey`·`onPressTab` 두 prop뿐). testID `shell-tabbar-*`. **TRIP-173 FSD 완결 3/4**에서 비주얼을 Figma 마스터(`1236:1177`)에 정합 — 풀폭 직각 바(74px·`bg-canvas`·`border-t`) → 투명 84px 밴드 안에 334×64 알약(`rounded-pill`·`bg-surface-soft`, 좌우 28px 여백). 탭 폭 `flex-1`(가변) → `w-[62px]`(고정). 아이콘 좌표계 `0 0 24 24` → `0 0 27 27` + path 10종 전량 교체. prop 계약·testID·접근성 전부 불변. 알약 배경은 1차 판정(Figma raw CSS `rgba(255,255,255,0.68)`)에서 프로덕션 렌더 실측(`#F7F7F7`)으로 **되정정**돼 `bg-surface-soft` 토큰이 됐다(raw 선언값과 렌더 합성값은 다른 질문 — 메커니즘은 미확정). **SWT PRO Figma 실물 재정합(2026-08-14, TRIP-201 티켓값 되정정)**: TRIP-201이 티켓 실측 신 치수(px-lg·h-46·w-67.6·아이콘 22)를 채택했으나 라이브 Figma(`1236:1177`)와 안 맞았다 — 사용자 확인 후 **Figma 실물로 정합**. 밴드 `h-[84px]→h-[96px]`·`px-lg→px-[30px]`, 알약 고정높이 제거(내용 맞춤 ~60)·탭 `w-[67.6px]→flex-1`(≈62)·`gap-xs pt-xs pb-[2px]→gap-[3px] py-[6px]`, 아이콘 렌더 크기 `22→27`(좌표계 viewBox 27 불변). **알약을 `expo-blur` BlurView 프로스티드 글래스로 교체**(Figma 실측 `rgba(255,255,255,0.68)+backdrop-blur-14`를 실 블러로 재현 — 오버레이라 아래 콘텐츠가 비쳐야 하므로 불투명 #F7F7F7 되정정을 다시 뒤집음). 그림자는 overflow-hidden 밖 래퍼(`PILL_SHADOW_STYLE`), 테두리 raw 50% 흰색은 BlurView 안쪽(`PILL_BORDER_STYLE`). ⚠️ BlurView는 네이티브 모듈 — `pnpm expo prebuild`+재빌드 필요. **TRIP-201 기반: 루트 `bg-canvas` 제거(투명 밴드, M-2 역전) + `absolute inset-x-0 bottom-0`**(5-a 근본수정 — `(tabs)/_layout.tsx`의 `tabBarStyle:{position:'absolute'}`는 커스텀 `tabBar` 렌더프롭에 react-navigation이 적용하지 않는 죽은 문자열이라, 오버레이는 이 루트가 스스로 진다. RN 7.18.9 `BottomTabView` 컬럼 플로우 실측 — absolute 자식은 플로우 제외돼 씬(`MaybeScreenContainer flex:1`)이 안 줄고, 이 탭바가 그 바닥 84px를 덮는다. 개념 [[가드의 사정거리]] 실측 12). 루트에 **`pointerEvents="box-none"`**(5-c N1 대응 — 투명 View도 기본 `pointerEvents:'auto'`라 하단 84px 터치를 흡수해 미래에 그 구역에 인터랙티브 요소를 놓는 화면을 막는다, 현재 화면은 전부 무해). AC-O2(`_layout.tsx`의 `tabBarStyle` 문자열 존재)를 잠그는 승인 심판은 이제 무효 문자열만 보므로, 오버레이 회귀를 실제로 잡는 심판은 `tabbarOverlay.test.ts`의 루트 `absolute`+`bottom-0` 단언 쪽으로 옮겨졌다(게이트①-2) |

### `src/shared/map/` — 카카오 지도 WebView 브리지 (TRIP-197 신설 → TRIP-199로 핀·역지오코딩 확장 → TRIP-297로 다중 핀·폴리라인 확장 → TRIP-301로 핀 탭·실패 통지·줌 상한 additive 확장)

**화면이 아니다** — 지도 렌더 표면만 증명하는 칸(부모 TRIP-75·U1). 카카오가 RN 네이티브 지도 래퍼를 안 만든다(`@react-native-kakao/map`은 저자가 2025-03-19 커밋으로 map 패키지 자체를 삭제) — 그래서 `react-native-webview` 안에 카카오 지도 **JavaScript** SDK를 얹는 우회로 갔다.

| 파일 | 역할 |
|---|---|
| `src/shared/map/mapHtml.ts` | `MapCenter` 타입 · `REGISTERED_DOMAIN` · `MAP_LOAD_FAILED_MESSAGE` · **신규(TRIP-199)** `KakaoMapMessage` 판별 유니온(`PIN_DROP`\|`GEOCODE_OK`\|`GEOCODE_FAIL` — `postMessage`가 문자열 하나만 보낼 수 있어 종류 꼬리표가 필요) · `buildMapHtml(center, jsKey, enablePin = true)` — SDK를 `libraries=services`(역지오코딩 모듈)까지 실어 조립. `enablePin` 3번째 인자로 롱프레스·마커·`coord2Address` 대본을 실을지 고른다(핀을 안 받는 검색 미리보기·확정 시트는 `KakaoMapView`가 `onMapMessage` 없이 불릴 때 자동으로 `false`). 롱프레스는 SDK 내장 이벤트가 없어 `mousedown`+600ms 타이머로 흉내(`dragstart`가 취소) · 응답은 `pinSeq` 일련번호로 늦게 온 이전 핀의 응답을 버림 · 빈 주소는 성공이 아니라 `GEOCODE_FAIL` · **`coord2Address(lng, lat, cb)`는 경도가 먼저다**(리포 전체 `{lat,lng}` 순서와 반대 — 문자열 안 JS라 tsc가 못 잡음, 뒤바뀌면 에러 없이 엉뚱한 주소가 나옴). **TRIP-297 확장**: `buildMapHtml`이 다중 번호 핀(1~n)·분홍 폴리라인·`LatLngBounds`+`setBounds`(핀 전부를 화면에 맞춤)를 추가로 대본에 싣는다. 대본 안 raw hex 2종(핀·폴리라인 색)이 **어느 색 가드 모집단에도 없다**(후속 티켓 후보 → TRIP-339가 `itineraryMapSurfaceStructure.test.ts`의 팔레트 부분집합 가드로 이 사각지대를 메웠다). **TRIP-339 확장(핀 마커·연결선·지도 고정)**: 핀을 원 `<div>`에서 물방울 `<svg>`(`yAnchor: 1`, 흰 2px 테두리)로 교체 · 폴리라인을 겹당 2개(흰 6px 케이싱 + 분홍 2.8px 본선, 개수 대신 선분 비교로 잰다 — `segmentsOf`) · **5번째 옵셔널 인자 `viewOnly`**(기본 `false` — 켜면 지도 생성 옵션에 `draggable:false`·`disableDoubleClickZoom:true` + `setBounds` 뒤에 `map.setZoomable(false)`, 안 켜면 빈 문자열이라 대본이 기능 도입 전과 바이트 단위로 같음) · **6번째 옵셔널 인자 `connectPins`**(기본 `true` — h05만 `false`로 꺼서 연결선을 안 그림, `multiplePins`/`linkPins` 두 변수로 "화면 맞춤"과 "선 그리기"를 분리 — 원래 한 조건이 둘 다 물고 있었다). 화면 계약(`MapCenter`·`MapPin`)은 무변경. **TRIP-301 확장**: `KakaoMapMessage`에 `PIN_TAP` 유니온 멤버 추가(`{type:'PIN_TAP', index}` — index는 `pins` 배열의 0-기반 위치, 개념 [[핀 결번 역참조]]) · CustomOverlay에 `onclick="window.__pinTapOverlay(index)"`+`clickable:true` 대본을 얹어 핀 탭을 postMessage로 브리지(실제 클릭 발화는 jest 사정거리 밖 — 6-b 실기 전용, `03_implementer_notes.md` §1-2) · **7번째 옵셔널 인자 `maxLevel`**(`setMaxLevel` 호출, jest 잠금 없음 — 6-b 조정 대상 `ponytail:` 주석) |
| `src/shared/map/KakaoMapView.tsx` | `KakaoMapView`(`center` + **신규(TRIP-199) 옵셔널 `onMapMessage?: (message: KakaoMapMessage) => void`** + **신규(TRIP-297) 옵셔널 `pins?: MapPin[]`**(`mapHtml.ts`의 `MapPin{number,lat,lng}` — `features/itinerary/model/draftView.ts`의 `DraftPin`과 이름은 다르지만 구조가 같아 별도 변환 없이 그대로 통과됨, `DraftScreen.tsx`가 호출부) + **신규(TRIP-339) 옵셔널 `viewOnly?: boolean`·`connectPins?: boolean`**(`buildMapHtml`의 5·6번째 인자로 그대로 전달 — 새 export 0건, 기존 `KakaoMapViewProps`에 필드만 추가)) · `KakaoMapViewProps`. `onMessage`로 받은 JSON을 `isFiniteNumber`(NaN·문자열·null 방어, TRIP-197 이월 N5 판정)로 거른 뒤 `onMapMessage`로 전달. **`source.html`은 `useState`의 lazy initializer로 마운트 시점 `center`·`pins`·`viewOnly`·`connectPins`에 한 번만 고정되고 이후 바뀌어도 다시 조립되지 않는다**(TRIP-199 5-a B-1 수정 — WebView는 `source`가 바뀌면 문서를 통째로 재로드해 진행 중이던 역지오코딩 콜백을 죽인다. 좌표·핀을 바꿔 다시 그려야 하는 호출부는 `key`로 이 컴포넌트를 remount한다 — `DraftScreen.tsx`가 `key={날짜|JSON.stringify(pins)}`로 이 규약을 따름). **TRIP-339 뮤테이션 실측(M1~M8, 게이트①-2)이 이 조립부를 소스·렌더·실행 세 층으로 나눠 검증하는 구조를 확립**했다(가운데 렌더 층이 비어 있으면 `connectPins`를 조립부가 무시해도 전수 green이었다). **TRIP-301 확장**: 옵셔널 `onLoadFailed?: () => void`·`maxLevel?: number` prop 추가 + PIN_TAP 브리지(유한수 index만 통과, `isFiniteNumber` 재사용) · `handleLoadError`(=`onError`/`onHttpError`) 처리를 **위임-또는-폴백**으로 비대칭화: `onLoadFailed`가 있으면 그것만 호출(내부 `loadFailed` state는 안 켬 — 부모가 폴백 UI를 소유), 없으면 기존대로 `setLoadFailed(true)`(무회귀). `MAP_LOAD_FAILED_MESSAGE`(SDK 서브리소스 실패) 경로는 **항상 둘 다**(`setLoadFailed(true)` + `onLoadFailed?.()`) — 이 비대칭이 두 동결 테스트(`KakaoMapView.test.tsx` A-7 무수정 회귀·`KakaoMapView.extend.test.tsx` KE3 신규 승인)를 동시에 통과시키는 유일한 형태(03 §1-1, 03b 감사 통과) |
| `src/shared/map/index.ts` | 배럴 — `KakaoMapView`·`KakaoMapViewProps`·`REGISTERED_DOMAIN`·`MapCenter`·**`KakaoMapMessage`(신규 TRIP-199, 타입 전용 재수출)** |

**소비처 있음(TRIP-198부터)** — `StayRegisterScreen.tsx`(e05)가 검색 미리보기·좌표 확정 시트(`MapSheet`)·**핀 지정 탭(`PinPanel`, TRIP-199 신규)** 셋에서 재사용한다. 시트가 열려 있는 동안엔 핀 지도를 언마운트하지 않고 배경막과 함께 계속 그려(TRIP-199 5-a 2차 B-2 — "지도 마운트는 실질 1개" 규율을 이 자리에서 의도적으로 깸, `ponytail:` 주석에 천장 명시) WebView 2개가 동시에 존재한다.

### `src/shared/api/generated/` — 도구 생성물 (orval, TRIP-179)

`pnpm codegen`(`orval.config.ts` — `filters: { mode: 'include', tags: ['stays', 'saved-stays', 'trips', 'preferences', 'places'] }`(**TRIP-183으로 `saved-stays` 추가, TRIP-203으로 `trips`·`preferences` 추가, TRIP-220으로 `places` 추가**) + `httpClient: 'axios'` + `override.mutator`)이 `backend/docs/design/openapi.yaml`의 다섯 태그 경로만 읽어 생성(17→49→56→67→75→**84**파일, TRIP-294로 itinerary 4오퍼레이션·스키마 11개 추가 → **사이클 `20260808-contract-refresh`로 change-log 경로 + `candidatesSummary` + 슬롯 10필드 흡수, 신규 스키마 8개** → **사이클 `20260809-trip295-contract-refresh`(직전 사이클의 재판, 티켓 없음)로 `generationMode` 필수 승격 + 요청 enum `MANUAL` 추가 + 슬롯 `violationReason`(선택) + `revisions`·`slot-candidates` 경로·스키마 4종 흡수, 신규 9파일** — `trips` 태그는 이미 있어 `filters.tags` 배열 수정 없이 재생성만). **사람이 손댄 줄 0건** — 재생성하면 통째로 덮인다. `pnpm codegen`은 리포 prettier를 거치지 않는다 — 재생성 직후 포매터(`prettier --write`)를 돌리지 않으면 순수 포맷 차이로 큰 diff가 난다(codegen → prettier 순서 고정). 이 절 전체가 `docs/structure.md` 유지 규약의 "파일 목록" 기계 담당분에 해당하지만, 코드젠 필터가 없으면 전 태그가 생성돼 여기 목록이 통째로 낡는다는 것 자체가 경고다(아래 "지금 작업하려면" 참조). orval은 **태그 단위로만** 필터링해 오퍼레이션 하나만 못 고른다 — `trips` 태그 12개·`preferences` 태그 2개 오퍼레이션 중 이번에 배선된 것은 `POST /trips`·`GET /me/preferences` 둘뿐이고, 나머지는 `saved-stays` CRUD 5종과 같은 소비자 0 상태다.

| 파일 | 역할 |
|---|---|
| `src/shared/api/generated/stays/stays.ts` | `getStaysSearch`·`useGetStaysSearch`(TanStack Query 훅) · `getStaysGeocode`·`useGetStaysGeocode`(같은 태그라 동반 생성, 소비자 없음) |
| `src/shared/api/generated/schemas/staySearchResponse.ts` | `StaySearchResponse` 타입(`items`·`degraded`·`filterZeroReasons`) |
| `src/shared/api/generated/schemas/stayItem.ts` | `StayItem` 타입(`price?: StayPrice \| null` 포함) |
| `src/shared/api/generated/schemas/stayPrice.ts` | `StayPrice` 타입 |
| `src/shared/api/generated/schemas/getStaysSearchParams.ts` | `GetStaysSearchParams` 타입(`region?`·`amenity?`·`stayType?` — 날짜·인원·정렬 없음, BR-U1-10/15) |
| `src/shared/api/generated/schemas/getStaysGeocodeParams.ts` | `GetStaysGeocodeParams` 타입(지오코딩, 소비자 없음) |
| `src/shared/api/generated/schemas/geocodeCandidate.ts` | `GeocodeCandidate` 타입(지오코딩, 소비자 없음) |
| `src/shared/api/generated/schemas/index.ts` | 위 스키마 배럴 재수출(export 없음 — re-export만) |
| `src/shared/api/generated/saved-stays/saved-stays.ts` | **신규(TRIP-183, 이번 사이클 문서 소급 반영) — ⚠️ 소비자 0.** `postSavedStays`·`usePostSavedStays`(등록) · `getSavedStays`·`useGetSavedStays`(목록) · `getSavedStaysSavedStayId`·`useGetSavedStaysSavedStayId`(단건) · `patchSavedStaysSavedStayId`·`usePatchSavedStaysSavedStayId`(수정) · `deleteSavedStaysSavedStayId`·`useDeleteSavedStaysSavedStayId`(삭제) — CRUD 5종 전부 코드젠만 되고 화면 배선은 아직 없다 |
| `src/shared/api/generated/schemas/registerRoute.ts` | `RegisterRoute` — `MAP_SEARCH`\|`LINK_PASTE`\|`PIN` 3값(BR-U1-21 숙소 등록 경로 3종의 타입 표현) |
| `src/shared/api/generated/schemas/registerSavedStayRequest.ts` | `RegisterSavedStayRequest` — `name`·`registerRoute`·`lat?`·`lng?`·`coordConfirmed?`·`checkIn?`·`checkOut?`·`externalSource?`·`externalId?`·`memo?` |
| `src/shared/api/generated/schemas/editSavedStayRequest.ts` | `EditSavedStayRequest` — `RegisterSavedStayRequest`에서 `registerRoute`·외부 출처 필드가 빠진 부분집합(경로 재변경 불가 암시) |
| `src/shared/api/generated/schemas/savedStay.ts` | `SavedStay` — 응답 전체 형태. `coordConfirmed: boolean`에 "false면 거점 배정 불가(INV-U1-08)" 주석이 스키마 자체에 있음(orval이 openapi description을 그대로 옮김) |
| `src/shared/api/generated/schemas/errorResponse.ts` | 공용 에러 응답 포락선 `{error: ErrorResponseError}` — saved-stays 전용이 아니라 U1 공용 형태 |
| `src/shared/api/generated/schemas/errorResponseError.ts` | `ErrorResponseError` — `{code,message,traceId?,fields?,existingProvider?}`(`existingProvider`는 TRIP-211 — SOCIAL_EMAIL_CONFLICT 409 전용, 이번 사이클은 그 재생성 빚만 선행 커밋 `df43082`로 분리 반영) |
| `src/shared/api/generated/schemas/errorResponseErrorExistingProvider.ts` | **TRIP-211 재생성분(선행 커밋 `df43082`)** — `'google'\|'apple'\|'kakao'\|'naver'` |
| `src/shared/api/generated/schemas/errorResponseErrorFieldsItem.ts` | `ErrorResponseErrorFieldsItem` — `{field?,reason?}`(필드별 검증 오류) |
| `src/shared/api/generated/schemas/validationErrorResponse.ts` | `ValidationErrorResponse = ErrorResponse`(입력 검증 실패 — 별도 필드 없이 의미만 다른 타입 별칭) |
| `src/shared/api/generated/trips/trips.ts` | **신규(TRIP-203)** — `postTrips`·`usePostTrips`(생성, 유일한 소비처) · `getTrips`·`useGetTrips`·`getGetTripsQueryKey`(목록, `useCreateTrip`의 무효화 키 출처) + `getTripsTripId`·`patchTripsTripId`·`deleteTripsTripId`·`postTripsTripIdBases`·`getTripsTripIdBases`·`deleteTripsTripIdBasesBaseAssignmentId`·`getTripsTripIdCoverage`·`postTripsTripIdMustVisits`·`getTripsTripIdMustVisits`·`deleteTripsTripIdMustVisitsMustVisitId` — **⚠️ 소비자 0**(bases·coverage·must-visits, TRIP-84·미티켓 US-TRIP-06·TRIP-209 소관. openapi에 `operationId` 0건이라 orval이 method+path로 이름을 지었다 — 티켓 표기 `createTrip`이 아니라 `postTrips`. **TRIP-294로 itinerary 4오퍼레이션 추가**(`getTripsTripIdItinerary`·`postTripsTripIdItinerary`·`putTripsTripIdItinerary`·`postTripsTripIdItineraryConfirm` + 각 `use*` 훅·옵션 빌더 — 23심볼, **소비자 0**·화면 배선은 밴드 h 칸 몫). **사이클 `20260808-contract-refresh`로 change-log 오퍼레이션 1개 추가** — `getTripsTripIdChangeLog`+`useGetTripsTripIdChangeLog`+쿼리키/옵션 헬퍼+에러 타입(6심볼, 고유 export 92→98). **소비자 0**(D4 확정 — 계약 앵커도 세우지 않음, 화면 h36 미착수). **사이클 `20260809-trip295-contract-refresh`로 오퍼레이션 3개 추가** — `postTripsTripIdItinerarySlotCandidates`(+훅), `getTripsTripIdItineraryRevisions`(+훅), `postTripsTripIdItineraryRevisionsRevisionIdRestore`(+훅) — 각 쿼리키/옵션 헬퍼·타입 동반(export 라인 수 119→139, `grep -c '^export '` 기준). **셋 다 소비자 0**(계약 앵커도 안 세움 — 아래 `revision.ts`·`slotCandidates.ts` 행 참조, D4와 같은 판단) |
| `src/shared/api/generated/schemas/itinerary.ts` | **신규(TRIP-294)** — `Itinerary` 응답 타입, required 7필드(`itineraryId`·`tripId`·`status`·`solveMode`·`isFallback`·`generationState`·`days`). **소비자 0**. **사이클 `20260808-contract-refresh`로 `candidatesSummary?: ItineraryCandidatesSummary` 추가**(선택, 실제 타입은 `{level,...} | null | undefined` 삼중 옵셔널 — 개념 [[삼중 옵셔널 (T 또는 null 또는 undefined)]]). **사이클 `20260809-trip295-contract-refresh`로 `generationMode: ItineraryGenerationMode` 필수 승격**(required 8필드로, `solveMode` 바로 뒤 — solveMode="AI가 어떻게 풀었나"와 다른 축인 "사용자가 무엇을 골랐나"). 여전히 **소비자 0** |
| `src/shared/api/generated/schemas/itineraryDaysItem.ts` · `src/shared/api/generated/schemas/itineraryDaysItemSlotsItem.ts` | **신규(TRIP-294)** — 일자·슬롯 응답 타입. **사이클 `20260808-contract-refresh`로 슬롯 required 7필드**(`poiId`·`startAt`·`endAt`·`isFixed`·`endsNextDay`·`hasViolation`·`tags`, `tags` 신규 필수) **+ 선택 9필드 추가**(`placementReason`·`distanceRange`·`nameKo`·`lat`·`lng`·`category`·`imageUrl`·`openingHours`·`openingHoursKnown`, 전부 nullable — BR-U3-09 POI 표면. `openingHoursKnown`은 확정 일정(`status=CONFIRMED`)에서 null, `distanceRange`는 편집 직후 null). **사이클 `20260809-trip295-contract-refresh`로 선택 필드 10번째 `violationReason?: string \| null` 추가**(BR-U3-13 — 위반 사유, 저장 후 재조회에도 남아 위반 블록을 지속 가시화. `hasViolation=false`면 항상 null). **소비자 0** |
| `src/shared/api/generated/schemas/itineraryGenerationState.ts` · `src/shared/api/generated/schemas/itineraryStatus.ts` · `src/shared/api/generated/schemas/itinerarySolveMode.ts` | **신규(TRIP-294)** — enum 3종(`PARTIAL\|COMPLETE\|FAILED` · `PLANNED\|CONFIRMED` · `FULL_AI\|DETERMINISTIC\|MINIMAL`). **소비자 0** |
| `src/shared/api/generated/schemas/generateItineraryRequest.ts` · `src/shared/api/generated/schemas/generateItineraryRequestGenerationMode.ts` | **신규(TRIP-294)** — 생성 요청 바디 + `generationMode` enum(`FULLY_AI\|CO_PLAN` 2종 — u3 `domain-entities.md`의 3종과 불일치, 정본 반영 A 참조). **소비자 0** |
| `src/shared/api/generated/schemas/editItineraryRequest.ts` · `src/shared/api/generated/schemas/editItineraryRequestDaysItem.ts` · `src/shared/api/generated/schemas/editItineraryRequestDaysItemSlotsItem.ts` | **신규(TRIP-294)** — 편집 요청 바디(대조군: 슬롯 `endsNextDay?` 옵셔널·`hasViolation` 없음). **소비자 0** |
| `src/shared/api/generated/schemas/changeLog.ts` · `src/shared/api/generated/schemas/changeLogEntry.ts` · `src/shared/api/generated/schemas/changeLogEntrySourceType.ts` | **신규(사이클 `20260808-contract-refresh`, `GET /trips/{tripId}/change-log` 응답)** — `ChangeLog{entries: ChangeLogEntry[]}` · `ChangeLogEntry{actor,sourceType,at,before,after,reason?}` · `sourceType` enum `PLAN_B\|MANUAL\|COEDIT\|ASSISTANT`(현재 생성 출처는 `MANUAL`뿐). **소비자 0**(화면 h36 미착수, D4로 계약 앵커도 세우지 않음) |
| `src/shared/api/generated/schemas/getTripsTripIdChangeLogParams.ts` | **신규(사이클 `20260808-contract-refresh`)** — `limit?`(선택, default 100, min 1 max 500). **소비자 0** |
| `src/shared/api/generated/schemas/itineraryCandidatesSummary.ts` | **신규(사이클 `20260808-contract-refresh`)** — `ItineraryCandidatesSummary = {level: string; poolSize?; shortfallCategories?} \| null`. `level`은 **enum이 아니라 string**(판정 소유자가 AI라 열거를 고정하지 않음 — `switch`에 tsc가 침묵). **소비자 0** |
| `src/shared/api/generated/schemas/itinerarySnapshot.ts` · `src/shared/api/generated/schemas/itinerarySnapshotDaysItem.ts` · `src/shared/api/generated/schemas/itinerarySnapshotDaysItemSlotsItem.ts` | **신규(사이클 `20260808-contract-refresh`, change-log `before`/`after` 스냅숏)** — ⚠️ 슬롯 모양이 `ItineraryDaysItemSlotsItem`과 다르다, **5필수뿐**(`hasViolation`·`tags` 없음). 리포에 슬롯 모양이 셋(응답·요청·스냅숏)이 됐다 — 이름 유사로 자동완성 오선택 위험. **소비자 0** |
| `src/shared/api/generated/schemas/itineraryGenerationMode.ts` | **신규(사이클 `20260809-trip295-contract-refresh`)** — `ItineraryGenerationMode` 응답 enum `FULLY_AI\|CO_PLAN\|MANUAL`. `itinerary.ts`의 신규 필수 `generationMode` 필드가 참조. **요청 쪽 enum(`generateItineraryRequestGenerationMode.ts`)과 값 목록이 같아야 하는 게 계약 의도지만 파일이 갈라져 있어 한쪽만 늘어도 `tsc`가 안 깨진다** — 이번 사이클이 `staySearchGenerated.test.ts`에 이 파일을 응답 쪽 앵커로 신규 등록해 값 목록 불일치를 잡는다(게이트 원장 J-7). **소비자 0** |
| `src/shared/api/generated/schemas/revision.ts` | **신규(사이클 `20260809-trip295-contract-refresh`)** — `Revision` 응답 타입: `revisionId`·`seq`(여행 안에서 유일·단조, INV-U3-06)·`actor: RevisionActor`(화면 `나`/`AI` 배지 원천)·`kind: RevisionKind`·`summary`·`detail?`·`createdAt`. **내용 무심판**(계약 앵커 없음 — h-대역 되돌리기 UI 티켓이 착수 시 세운다, D4·직전 사이클 C-2와 같은 판단). **소비자 0** |
| `src/shared/api/generated/schemas/revisionActor.ts` | **신규(사이클 `20260809-trip295-contract-refresh`)** — `RevisionActor` enum `USER\|AI`. `revision.ts`의 `actor` 필드가 참조(되돌리기 UI 배지 원천). **소비자 0** |
| `src/shared/api/generated/schemas/revisionKind.ts` | **신규(사이클 `20260809-trip295-contract-refresh`)** — `RevisionKind` enum `BASELINE\|GENERATE\|EDIT\|RESTORE`(`BASELINE`="AI가 처음 짠 일정·기준 버전", BR-U3-31). `revision.ts`의 `kind` 필드가 참조. **소비자 0** |
| `src/shared/api/generated/schemas/revisionList.ts` | **신규(사이클 `20260809-trip295-contract-refresh`)** — `RevisionList = {revisions: Revision[]}`. `GET /trips/{tripId}/itinerary/revisions` 응답 봉투. **소비자 0** |
| `src/shared/api/generated/schemas/getTripsTripIdItineraryRevisionsParams.ts` | **신규(사이클 `20260809-trip295-contract-refresh`)** — `limit?`(선택, default 100, max 500). 이력은 append-only로 절대 줄지 않는다는 설명이 스키마 주석에 그대로 있음(orval이 openapi description을 옮김, `getTripsTripIdChangeLogParams.ts`와 동형). **소비자 0** |
| `src/shared/api/generated/schemas/slotCandidates.ts` | **신규(사이클 `20260809-trip295-contract-refresh`)** — `SlotCandidates`: `candidates: SlotCandidatesCandidatesItem[]`(빈 배열=후보 0건→반경 확대·컨셉 변경 제안, BR-U3-25) + `radiusMUsed: number`(실제 사용 반경 — AI가 자동 확대했을 수 있어 **그대로 표시**하는 게 계약 지시, h15 "반경 넓힘"의 근거). `POST /trips/{tripId}/itinerary/slot-candidates` 응답. **소비자 0** |
| `src/shared/api/generated/schemas/slotCandidatesCandidatesItem.ts` | **신규(사이클 `20260809-trip295-contract-refresh`)** — `{poiId, distanceRange, rationale}`. `distanceRange`는 거리만(INV-3, 소요시간 없음) — `rationale`은 closed-set 근거 문구(BR-U2-09, 시각·소요시간 언급 없음). **소비자 0** |
| `src/shared/api/generated/schemas/slotCandidatesRequest.ts` | **신규(사이클 `20260809-trip295-contract-refresh`)** — `POST /trips/{tripId}/itinerary/slot-candidates` 요청 바디: `slotKey`(required, `"{date}#{poiId}"` 규약 BR-U2-04) · `radiusM?`(1~50000, 상한 없으면 탐색 범위가 전 지구를 덮는다) · `concept?`(h13 테마, max 40자). **소비자 0** |
| `src/shared/api/generated/preferences/preferences.ts` | **신규(TRIP-203)** — `getMePreferences`·`useGetMePreferences`(조회, `usePreferencePrefill`이 재수출) + `putMePreferences`·`usePutMePreferences` — **⚠️ PUT은 소비자 0**(이번 칸은 조회만 배선, 계정 취향 수정 화면은 이 스토리 밖) |
| `src/shared/api/generated/schemas/createTripRequest.ts` | `CreateTripRequest` — required `startDate`·`endDate`·`destinations`, 옵셔널 `title`·`party`·`companionType`·`budgetTotal`·`preferenceSnapshot`(AC-3·AC-6이 이 필수/옵셔널 경계를 잠근다) |
| `src/shared/api/generated/schemas/trip.ts` | `Trip` — required 10필드(`tripId`·`title`·`startDate`·`endDate`·`party`·`preferenceSnapshot`·`destinations`·`status`·`createdAt`·`updatedAt`). `preferenceSnapshot`이 **응답에는 required**인 것이 AC-6("클라이언트는 안 보낸다")의 근거(채우는 주인은 서버) |
| `src/shared/api/generated/schemas/tripDestination.ts` | `TripDestination` — `{seq,region,nights}` 셋 다 필수 |
| `src/shared/api/generated/schemas/companionType.ts` | `CompanionType` — `'혼자'\|'친구'\|'연인'\|'가족'` 4값. **`'커플'` 없음**(온보딩 축과 다른 목록, BR-U1-39 — `커플→연인` 매핑 함수는 TRIP-204 소관) |
| `src/shared/api/generated/schemas/tripStatus.ts` | `TripStatus` — `'PLANNED'\|'CONFIRMED'\|'ACTIVE'\|'ENDED'`(BR-U1-42 단방향) |
| `src/shared/api/generated/schemas/tripPreferenceSnapshot.ts` | `Trip.preferenceSnapshot`의 타입(응답 쪽, required — 취향 동결 스냅숏. 만드는 자리는 서버, 클라이언트는 절대 안 만듦) |
| `src/shared/api/generated/schemas/createTripRequestPreferenceSnapshot.ts` | `CreateTripRequest.preferenceSnapshot`의 타입(요청 쪽, 옵셔널 — `buildCreateTripRequest`가 이 키를 결과에서 걷어내 AC-6을 지킨다) |
| `src/shared/api/generated/schemas/preferenceView.ts` | `PreferenceView` — 7축(`pace`·`budget`·`companion`·`styles`·`activities`·`foodTastes`·`transportModes`) **전부 옵셔널**. `usePreferencePrefill`의 반환 타입 |
| `src/shared/api/generated/schemas/preferenceViewBudget.ts` | `PreferenceView.budget` — `{tier?,rawAmount?,isNeutralDefault?}`. `rawAmount`가 숫자일 때만 여행 생성 요청의 `budgetTotal`로 실린다 |
| `src/shared/api/generated/schemas/preferenceViewCompanion.ts` | `PreferenceView.companion` — 동행 축 취향(이번 칸은 소비 안 함) |
| `src/shared/api/generated/schemas/addMustVisitRequest.ts` · `src/shared/api/generated/schemas/assignBaseRequest.ts` · `src/shared/api/generated/schemas/baseAssignment.ts` · `src/shared/api/generated/schemas/coverage.ts` · `src/shared/api/generated/schemas/dayCoverage.ts` · `src/shared/api/generated/schemas/dayCoverageStatus.ts` · `src/shared/api/generated/schemas/editTripRequest.ts` · `src/shared/api/generated/schemas/mustVisit.ts` · `src/shared/api/generated/schemas/mustVisitType.ts` | **`trips` 태그가 통째로 딸려오며 생긴 소비자 0 스키마 9종**(bases·coverage·must-visits 도메인) — TRIP-84·TRIP-209 소관, 이번 사이클은 배선하지 않음 |
| `src/shared/api/generated/schemas/preferenceInput.ts` · `src/shared/api/generated/schemas/preferenceInputActivitiesItem.ts` · `src/shared/api/generated/schemas/preferenceInputBudgetTier.ts` · `src/shared/api/generated/schemas/preferenceInputCompanionTypesItem.ts` · `src/shared/api/generated/schemas/preferenceInputFoodTastesItem.ts` · `src/shared/api/generated/schemas/preferenceInputPace.ts` · `src/shared/api/generated/schemas/preferenceInputStylesItem.ts` · `src/shared/api/generated/schemas/preferenceInputTransportModesItem.ts` · `src/shared/api/generated/schemas/prefArrayAxis.ts` · `src/shared/api/generated/schemas/prefScalarAxis.ts` | **`PUT /me/preferences` 요청 바디 스키마 10종 — ⚠️ 소비자 0**(이번 칸은 `GET`만 배선. 계정 취향 수정 화면이 생길 때 소비) |
| `src/shared/api/generated/places/places.ts` | **신규(TRIP-220)** — `getPlaces`·`useGetPlaces`·`getGetPlacesQueryKey`(장소 탐색) · `postSavedPlaces`·`usePostSavedPlaces`(담기) · `getSavedPlaces`·`useGetSavedPlaces`·`getGetSavedPlacesQueryKey`(담은 목록) · `deleteSavedPlacesSavedPlaceId`·`useDeleteSavedPlacesSavedPlaceId`(해제) — 4 오퍼레이션 전부 `features/explore/model/savedPlaces.ts`가 소비(**소비자 0 없음** — 이 태그의 다른 신규 스키마들과 다르다). openapi에 `operationId` 0건이라 orval이 method+path로 이름을 지었다(`trips` 선례와 동일 사정) |
| `src/shared/api/generated/schemas/getPlacesParams.ts` | `GetPlacesParams` — `region?`·`category?` 둘뿐(정렬·좌표 파라미터 없음) |
| `src/shared/api/generated/schemas/place.ts` | `Place` — required `poiId`·`nameKo`·`category`·`lat`·`lng`·`savedCount`·`dataStatus`·**`tags`**(TRIP-219 신규, `string[]`) + optional·nullable `region`·`openingHours`·**`imageUrl`**(TRIP-219 신규) |
| `src/shared/api/generated/schemas/placeDataStatus.ts` | `PlaceDataStatus` — `ACTIVE`\|`UNVERIFIED`\|`LOST`\|`CLOSED`. 탐색(`GET /places`)은 항상 `ACTIVE`(BR-U1-01), 담기 목록(`GET /saved-places`)은 나머지도 포함(클라 뱃지 몫) |
| `src/shared/api/generated/schemas/poiCategory.ts` | `PoiCategory` — 7종(명소·맛집·카페·야경·자연·쇼핑·문화). Figma d04 칩(6종)·카드 라벨(`바다`, enum 밖)과 3원 불일치 — `frontend-components.md` §2-2 F-4가 이미 기록, **enum이 정본** |
| `src/shared/api/generated/schemas/savePlaceRequest.ts` | `SavePlaceRequest` — `{poiId}`(required) |
| `src/shared/api/generated/schemas/savedPlace.ts` | `SavedPlace` — `{savedPlaceId,savedAt,place}` 셋 다 required |
| `src/shared/bootstrap/bootstrapReeval.ts` | 온보딩 완료 → 부트스트랩 재평가 pub/sub 신호(값 없는 순수 이벤트, `subscribeAccessToken` 형태) — TRIP-353 |
