# U4 In-trip & Plan-B — Functional Design Plan

> **유닛**: U4 In-trip & Plan-B — **C9 Plan-B Detection**(트리거 판정) · **C10 Itinerary Recalculation**(잔여 재최적화) · **C11 Weather & Context**(날씨·맥락 공급)
> **스토리**: **US-PLANB-01~13 (13개) + US-ONTRIP-01~03 (3개) = 16개**
> **범위 주의(SCOPE.md)**: 설계 문서까지. 코드는 팀이 `backend/`·`frontend/`에서 직접 개발 (2026-08-08 사용자 재확인: "구현은 각자 할거고 문서 작성만")
> **선행**: U0(2026-07-17) · U1(2026-07-23) · U2·U3(2026-08-07) 설계 종료. U2 경계 계약(BR-U2-01~16)과 U3 결정(DEC-U3-1~9·BR-U3-01~34)이 이 유닛의 전제다

---

## Step 1 결과 — 기존 자산

| 자산 | 실재 | U4 관련성 |
|---|---|---|
| `backend/modules/` | auth · profile · place-data · accommodation-search · saved-accommodation · trip · itinerary-generation · change-log · moderation **(10)** | **planb-detection · itinerary-recalculation · weather-context 3모듈 전무** — U4 백엔드는 통째로 신규 |
| `backend/modules/change-log/` + `V2.11__change_log.sql` | `ChangeLogEntry(tripId, actor, source, reason, before, after, at)` · `ChangeSource{PLAN_B, MANUAL, COEDIT, ASSISTANT}` · `ItinerarySnapshot`(시각·순서만, INV-3) · append-only(앱 롤 UPDATE/DELETE 회수) · `findByTrip(limit)` 타임라인 | **US-PLANB-09가 이미 절반 구현됨** — `PLAN_B` 출처값·`reason` 칸이 선재. U4는 *무엇을 채울지*만 정하면 됨 |
| `ScheduleAgentPort.kt`(itinerary-generation/domain) | **3메서드**: `generate` · `validate` · `repair`(Plan-B 재정렬 — 시각·순서만 최소 조정, POI 불변) | `repair`는 **위반 수리**용이지 대안 산출용이 아님. **`recalculate` 없음** — U2가 U4로 남겨둔 미개통 계약(Q6) |
| ⚠️ 같은 파일 | U3가 확정한 **`proposeSlotCandidates`(4번째 메서드)가 코드에 없다** | 설계-코드 드리프트. U4의 슬롯 후보 교체(Q1=A일 때)가 같은 계약을 재사용하므로 **개통 시점이 U4로 앞당겨짐** |
| `ai/src/trippilot/c2/` | `repair.py` · `ortools_solver.py` · `llm_solver.py` · `fallback_solver.py` · `constraints.py` | 솔버 본체 실재. **warm-start 재계산 경로 없음** |
| `ai/src/trippilot/ports/` | llm · places · poi_db · travel · cache · embedding · vector_store · solver · trace **(9)** | **날씨 포트 없음** — C11은 backend 소유로 설계 |
| `frontend/src/app/` | 온보딩·탐색·숙소·여행생성·(tabs) — **밴드 `i` 라우트 0** | **화면 19코드가 통째로 미착수** — U3와 같은 형국, 프런트 설계가 본체 |
| U2·U3 산출물 | 경계 계약 · BR-U2-01~16 · BR-U3-01~34 | **전제** — 재논의 대상 아님 |

> **U3와 동형·U2와 반대**: 백엔드는 change-log 하나만 선재, 화면은 0. 다만 U3와 달리 **백엔드 모듈 3개가 신규**라 도메인 설계 비중이 U3보다 크다.

## 라이브 Figma 밴드 `i` 대조 (2026-08-08 관측)

관측: 캔버스 `1228:1045` 행 `y=12485`, **프레임 22개 = 화면 코드 19 + `i01` 변형 2 + 이름 없는 프레임 1**.
`i01`(통합·변수 감지·수정 알약 열림) · `i02~i05` · `i07~i10` · `i12·i13` · `i15~i22`. **결번: i06·i11·i14.**
시각 확인(스크린샷): `i01-변수감지` · `i03` · `i04` · `i09` · `i10` · `i13` · `i16` · `i18` **8장**. 나머지 11코드는 **프레임 이름 수준 매핑**.
밴드 전체가 `· 통합` 접미사 — 상태 변형이 한 프레임에 합쳐진 세대다(밴드 e·g의 변형 분리 방식과 다름).

| ID | 드리프트 | 문서 쪽 근거 | 라이브 |
|---|---|---|---|
| **D-U4-1** ★ | **"대안 후보 2~3개"가 라이브에 없다** | US-PLANB-04: "대안 후보 2~3개를 받는다" · `RecalculationFacade.proposeAlternatives → List<AlternativeOption>`(2~3개) | `i13 AI 재계획안`은 **단일 안**이다 — 지도 1 + 슬롯 5 리스트 + `[직접 수정] [이대로 적용]`. 대안성은 **슬롯 단위**로 이동: 변경된 슬롯에 `변경됨` 배지 + **`다른 후보 4 >`**. 일정 단위 후보 목록 화면은 **결번(i11?)** |
| **D-U4-2** ★ | **재계획 지시 입력이 스토리보다 3배 넓다** | US-PLANB-01: 사유 6종 선택만(날씨·휴무·지연·취소·체력·사유 없음) | `i10` 바텀시트에 **① 바꿀 범위 3종**(`지금 이후`·`오늘 전체`·`내일까지`) **② 왜 6종**(임시 휴무·이동 지연·체력 저하·예약 마감·날씨·그냥 바꾸고 싶어요) **③ 어떻게 7종**(여유 있게·더 채워서·실내로·가까운 곳으로·맛집 추가·야경 코스·이동 줄이기) **④ 자유 텍스트**(`예: 저녁은 광안리 야경 보이는 곳으로`). ②만 스토리와 대응(취소→`예약 마감`) |
| **D-U4-3** | **자동 트리거 감시 항목이 스토리와 어긋남** | US-PLANB-02: (a)강수 60%+·특보 (b)당일 임시 휴무 (c)이동시간 임계(기본 15분) (d)**체류 초과** | `i09 감지된 변화`의 감시 항목 4종 = 날씨 · 이동 지연 · 영업·휴무 · **교통**. **체류 초과가 없고 `교통`이 새로 있다**(데이터원 미정 — `TravelEstimatePort`로 대체 가능한지 불명). 항목별 `활성/정상` 배지 + `[이 변화로 대안 보기]` |
| **D-U4-4** ★ | **여행 중 일정 화면에 U5(기록) 요소가 섞임** | 방문 기록·사진·메모는 **C12 Travel Archive = U5** 소유. `VisitChecked` 이벤트도 C12 | `i01`이 한 화면에 타임라인 + **사진 2장·메모 본문·`전체｜기록만` 필터** + `[방문 완료] [사진] [메모]` 버튼. `i04`엔 **"'기록' 탭에 자동으로 쌓여요"** 문구 |
| **D-U4-5** | **예상 도착 시각을 화면이 표시한다** | INV-2: 사용자 노출 시각은 **솔버 검증값만** | `i01` `예상 15:00 도착 · 11:00~22:00 영업` · `i04` 다음 일정 **`15:12 · 850m`**. 실시간 지연을 반영한 재추정치로 보이는데, 그 값이 어디서 나오는지(클라 추정 vs 서버 재계산) 정의된 곳이 없다 |
| **D-U4-6** | **걸음 수 표시 = 새 센서 의존** | US-PLANB-13: "누적 실제 이동 거리·걸음 수를 추정으로 표시" — 산출 방법 미정 | `i03` 하단 **`실제 1.4km · 12,400보`**. 만보계는 GPS로 안 나온다 — HealthKit/Activity Recognition 권한이 붙는 **신규 외부 포트**거나, 보폭 환산 추정이거나 |
| **D-U4-7** | 추천 강도 세그먼트가 **결과 화면**에 (U3 D-U3-5 재발) | 경계 계약은 `recommendationStrength`를 **요청 입력**으로 정의 | `i13` 상단 `최소｜균형｜많이` — h11과 동일한 위치·동일한 모호성(재생성 트리거인지 클라 필터인지) |
| **D-U4-8** | 정합 ✅ | US-PLANB-04 예외 · US-PLANB-08 · US-PLANB-10·11 · INV-3 | `i16 대안 없음`이 **정확히 3옵션**(`남은 방문지 1개 건너뛰기`·`휴식 모드로 전환`·`수동으로 일정 수정`) + 사유 한 줄 · `i18 변경 전/후 비교`가 **영향 지표 3종**(`이동 +1.2km`·`방문지 3→3`·`숙소 복귀 17:30→17:45`) + 추가/삭제/이동/**고정** 4배지 + 점선(전)·실선(후) 지도 + `[취소][확정]` · `i20 수동 위치 입력`·`i21 위치 권한 거부`·`i22 수동 일정 수정·error` 폴백 3종 실재 · **소요시간 표기 0건**(거리만: `도보 600m`·`차량 8km`·`850m`) |
| **D-U4-9** | 트리거 억제 위치가 다름 | US-PLANB-02: "동일 사유는 동일 방문지에 1회, 전역 빈도 상한·민감도(적게/보통/많이)" — 억제는 **감지 단계** | `i10`의 감지 배너에 **`끄기`** 버튼 — 억제가 *대안 화면에 들어온 뒤*에 있다. 민감도 3단 설정 UI는 밴드 `i`·`l`(설정) 어디에도 안 보임 |
| **D-U4-10** | 잔재·결번 | — | 결번 **i06·i11·i14** · `y=12485` 행 `x=358`에 **이름 없는 프레임**(`2335:2231`) 1개 |

---

## 실행 계획

- [x] 1. 유닛 컨텍스트 — `unit-of-work.md`(U4) · `story-map`(16 스토리) · `stories.md` US-PLANB-01~13·US-ONTRIP-01~03 · `components.md` C9·C10·C11 · `component-methods.md`(`PlanBDetectionFacade`·`RecalculationFacade`·`RecalculateCommand`)
- [x] 1b. **기존 자산 조사** — backend 10모듈(U4 3모듈 전무) · `change-log`+V2.11 선재 · `ScheduleAgentPort` 3메서드 · ai `c2/repair.py` · frontend 밴드 i 라우트 0
- [x] 1c. **라이브 Figma 밴드 i 대조** — 22프레임 관측, 스크린샷 8장, 드리프트 **D-U4-1~10** (위 표)
- [x] 1d. **디자인 반영 요청 → 라이브 수정 → 재관측**(2026-08-09) — 아래 §반영 결과
- [x] 2. 질문 Q1~Q8 답변 수집 — **Q1=A · Q2=A · Q3·Q4=지오펜스 조합 · Q5=C · Q6=ai 폴더(develop) 정본 · Q7=A(버튼까지만) · Q8=C**. Q3·Q4는 초기 답변이 되물음("꼼수가 없나")이라 기술 선택지를 제시하고 재확인 → **지오펜스 조합** 확정. 모호성 그 외 0 → 명확화 파일 없음
- [x] 3. `business-logic-model.md` — **DEC-U4-1~11** · 트리거 판정 파이프라인 · 재계획 플로우 · **`replan` 계약(§3.1)** · 슬롯 후보 재사용(§3.2) · 기준점 사다리 · 폴백 표 · 갭 **G-U4-1~8**
- [x] 4. `domain-entities.md` — 신설 6종(`plan_b_trigger`·`plan_b_suppression`·`visit_check`·`replan_session`·`actual_route_point`·`weather_snapshot`) + 기존 재사용 5종 + **INV-U4-01~09** + 이벤트 3 + 소유 경계표
- [x] 5. `business-rules.md` — **BR-U4-01~46** + **PBT-U4-1~5** + 미결 **O-U4-1~5**
- [x] 6. `frontend-components.md` — 라우트 12 · 컴포넌트 30여 · testID · **PBT-U4-F1~F3**
- [x] 7. 정합 검증 — **U2 사후 정정 2건 반영**: `business-logic-model` §7 행 종결 + **§7.2 신설**(`recalculate` → `replan` 개통, `ScheduleAgentPort` **5메서드**) · `business-rules` O-U2-3 종결 문구 갱신. U3 `proposeSlotCandidates`는 **계약 변경 없이 재사용**(U4 §3.2)
- [ ] 8. 완료 메시지 → 승인 게이트 → `audit.md`·`aidlc-state.md`

## 디자인 반영 결과 — 밴드 `i` 재관측 (2026-08-09 · 27프레임)

드리프트 D-U4-1~10 중 **설계로 흡수한 것과 디자인이 고친 것**을 갈랐다. 아래는 **라이브가 바뀐 것**만이다.

| 드리프트 | 반영 | 재관측 확인 |
|---|---|---|
| D-U4-3 | `i09` 감시 항목 `교통` 삭제 → 3항목 | ✅ 날씨·이동 지연·영업·휴무 |
| D-U4-2 | `i10` 바꿀 범위 `내일까지` 삭제 → 2칩 | ✅ ai `ReplanScope` 2종과 일치 |
| D-U4-6 | `i03` 걸음 수 삭제 | ✅ `실제 1.4km` |
| D-U4-5 | `i01`·`i04` 재추정 시각 → 계획 시각 | ✅ `15:00 도착 예정` · `15:00 · 850m` |
| D-U4-7 | `i13` 추천 강도 세그먼트 삭제 + 이동 수단 병기 | ✅ `#실내 · 도보 1.3km` |
| — | `i05` 혼잡도 행 삭제(데이터원 부재) | ✅ 영업시간·위치·다음 일정까지 3행 |
| D-U4-4 | **`i01 · 기록 없음` 변형 신설** | ✅ 사진·메모 없는 타임라인, `전체｜기록만` 필터도 빠짐 = **U4 기본 렌더** |
| D-U4-3 | **`i01 · 변수 감지(이동 지연)`·`(영업·휴무)` 변형 신설** | ✅ `TriggerKind` 3종 × 배너 |
| D-U4-2 | **`i10 · 범위 밖` 변형 신설** | ✅ INV-1 게이트 표면(`파리로 바꿔줘` 거절) |
| D-U4-1 | **`i14 슬롯 후보 교체 (Plan-B)` 신설** ★ | ✅ US-PLANB-05 4정보 완비 — 이유 / `지금 위치서 차량 6.4km` / 수단 / `17:00 고정까지 여유 1시간 20분` |
| D-U4-10 | 이름 없던 프레임 명명 | ✅ `i01 · 되돌리기 토스트` |

**미반영(설계에서 흡수)**: D-U4-8(정합 확인분) · D-U4-9(억제 위치 — `i10 [끄기]`를 억제 레코드 생성으로 규정, BR-U4-15) · 결번 `i06`·`i11`(DEC-U4-1로 불필요).

**Q3·Q4 확정 후 추가 요청분(디자인 진행 중)**: `c08` 항상-허용 변형 · `i03` 각주 · `l02` 민감도 행 · `l06` 상태 행. 설계는 이 4건을 **전제로** 쓰였다(`frontend-components.md` §8 · G-U4-6).

---

**전제(질문 아님)** — 이견 있으면 알려주세요:
- 산출물 **4종 풀세트**(business-logic-model · domain-entities · business-rules · frontend-components). 백엔드 3모듈 신규 + 화면 19코드라 U2식 축약본은 맞지 않음
- **C11 Weather & Context**는 `WeatherPort` 계약 + 기상청(공공데이터포털) 어댑터 규약까지 문서로 정의하되, 실호출·키 발급은 U0·U1 콘솔 설정과 같이 **개발 중 처리**로 이연
- **Infrastructure Design**은 U0·U1·U3와 동형으로 SKIP 예정(배포·클라우드 계획 부재)

---

## 질문 (모두 `[Answer]:` 에 답해 주세요)

마지막 선택지는 항상 "Other". "추천해줘"라고 적으셔도 됩니다.

## Question 1 — 대안의 단위 ★ (D-U4-1)

스토리·컴포넌트 계약은 **"일정 단위 대안 2~3안"**(`proposeAlternatives → List<AlternativeOption>`)인데, 라이브 `i13`은 **단일 재계획안 하나**를 주고 대안성을 **슬롯별 `다른 후보 4`**로 옮겨 놨습니다. 이 선택이 C10 퍼사드 계약·AI 경계·화면 수를 전부 가릅니다.

- **A) 라이브대로 — 단일 재계획안 + 슬롯별 후보 교체** *(추천)*
  `proposeAlternatives`를 "1안 산출"로 재해석하고, "2~3개"는 **슬롯 후보 목록**으로 충족. U3가 확정한 `proposeSlotCandidates` 계약을 그대로 재사용해 신규 계약이 줄어듭니다. 대신 US-PLANB-04 문구와 어긋나므로 인셉션 스토리에 정정 기록이 남습니다.
- **B) 스토리대로 — 일정 단위 대안 2~3안 카드**
  결번 `i11`이 후보 목록 화면이었다고 보고 화면을 신설. US-PLANB-05(대안별 이유·이동 거리·체류·여유 시간)가 자연스럽게 붙지만, **라이브에 없는 화면을 설계가 만들어내는** 형태가 됩니다.
- **C) 둘 다** — 1안 즉시 제시 + "다른 안 보기"로 2~3안 목록 진입
- **D) Other**:

[Answer]: A

## Question 2 — 재계획 지시 입력의 범위 ★ (D-U4-2)

`i10`은 **범위 3종 + 사유 6종 + 방향 지시어 7종 + 자유 텍스트** 4단 입력입니다. 스토리는 사유 하나뿐입니다. 자유 텍스트는 LLM 해석 입력이라 **INV-1(폐쇄집합 후보 풀) 게이트**를 반드시 통과시켜야 합니다.

- **A) 라이브 전부 수용(범위+사유+지시어+자유 텍스트)** *(추천)*
  `ai/`에 인텐트 라우터·워커가 이미 있어 자유 텍스트 해석 경로는 신규 아님. BR로 "자유 텍스트는 선호 가중치에만 반영, 후보는 폐쇄집합에서만"을 못 박습니다.
- **B) 자유 텍스트 제외** — 범위+사유+지시어 3단까지만. 게이트 부담·환각 표면을 줄이고 입력을 전부 열거형으로 유지
- **C) 스토리대로 사유만** — 나머지는 후속 티켓
- **D) Other**:

[Answer]: A

## Question 3 — 자동 트리거를 어디서 돌리나 ★

`i07 푸시 알림` 화면이 있다는 건 **앱이 닫혀 있어도 트리거가 뜬다**는 뜻인데, 지금은 배포 계획이 없어 서버 스케줄러를 검증할 수단이 없습니다(U0·U1·U3에서 Infra Design을 스킵한 이유와 같은 상황).

- **A) 클라이언트 포그라운드 판정만** *(추천)*
  앱이 켜져 있을 때 주기 폴링으로 감지 → 인앱 배너·칩(`i01`·`i08`). 로컬 개발과 정합하고 서버 스케줄러·푸시 인프라가 필요 없습니다. `i07`은 U6(알림) 유닛으로 이연.
- **B) 서버 스케줄러 + 푸시** — 활성 여행을 서버가 주기 평가하고 FCM 발송. 배포 전엔 검증 불가
- **C) 하이브리드** — 포그라운드는 클라, 백그라운드는 서버(둘 다 설계)
- **D) Other**:

[Answer]: 항상 떠있게 하는 꼼수가 없나요?

## Question 4 — 위치 신호를 어디까지 쓰나 ★

`i03`의 실제 경로(GPS 발자취)와 `i04`의 자동 도착 감지는 둘 다 **백그라운드 위치**를 함의합니다. 백그라운드 위치는 배터리·스토어 심사·위치정보법 신고(출시 선결과제)와 직결됩니다.

- **A) 포그라운드 단발 조회만** *(추천)*
  앱이 켜져 있을 때만 위치를 찍어 실제 경로를 성기게 기록하고, 도착은 **수동 체크인 우선**(자동 감지는 포그라운드일 때 보조). US-ONTRIP-01 예외절(권한 없으면 수동 체크인)과 이미 정합.
- **B) 백그라운드 추적** — 실제 경로·자동 도착 감지 완전 구현
- **C) 전면 수동** — 위치는 재계획 기준점 입력에만 쓰고 발자취·자동 감지 없음(`i03` 실제 경로 레이어 삭제)
- **D) Other**:

[Answer]: 이것도 꼼수가 없나

## Question 5 — 걸음 수 (D-U4-6)

`i03`의 `12,400보`는 GPS로는 안 나옵니다.

- **A) GPS 이동 거리에서 보폭 환산 추정** *(추천)*
  센서 권한 0개, 라이브 문구("추정")와 정합. 정확도는 낮지만 표시 목적엔 충분.
- **B) 만보계 센서 도입**(`expo-sensors` Pedometer / HealthKit·Activity Recognition 권한) — 정확하지만 권한·심사 표면 추가
- **C) 걸음 수 표시 제거** — 거리만. 디자인 협의 필요(갭으로 기록)
- **D) Other**:

[Answer]:  C

## Question 6 — AI 경계 확장 ★

현 `ScheduleAgentPort`는 `generate`·`validate`·`repair` 3메서드고, `repair`는 **위반 수리(시각·순서 최소 조정, POI 불변)** 전용입니다. Plan-B는 POI를 바꾸는 재최적화라 `repair`로는 안 됩니다. U2가 `recalculate`를 U4 몫으로 남겨 뒀습니다.

- **A) `recalculate` 신설** *(추천)*
  warm-start 입력(현재 위치·시각·완료 슬롯·고정 블록·트리거 사유·지시어)을 받는 5번째 메서드. U2 경계 계약 문서에 사후 정정으로 반영.
- **B) 기존 `generate` 재사용** — `excludedPoiIds` + 축소된 `timeWindows`로 남은 구간만 재생성(U3 DEC-U3-2와 같은 수법). 신규 계약 0이지만 "완료 슬롯 보존·warm-start"를 입력으로 표현할 방법이 마땅치 않음
- **C) 신설하되 `repair`를 흡수 통합**
- **D) Other**:

[Answer]: AI는 ai 폴더를 전적으로 따른다 지금 develop 브랜치에 최신 상황을 따르는걸로

## Question 7 — 여행 중 화면의 기록 요소 소유 ★ (D-U4-4)

`i01`·`i04`에 사진·메모·"기록 탭에 쌓여요"가 들어 있는데, 이건 **C12 Travel Archive = U5** 소유입니다. 다만 방문 완료(`VisitChecked`)는 **Plan-B의 체류 초과 트리거 입력**이라 U4가 안 쓸 수 없습니다.

- **A) 방문 체크·실제 체류까지 U4, 사진·메모는 U5로 이연** *(추천)*
  U4는 `VisitChecked` 생성·소비와 진행 상태만 설계하고, 화면의 사진·메모 버튼은 **진입점만** 그리고 계약은 U5에 남김.
- **B) 전부 U5로 이연** — U4는 Plan-B 트리거가 소비할 이벤트 계약만 선언(체크 UI도 U5)
- **C) 전부 U4에서 설계** — 사진·메모까지 U4가 정의하고 U5는 회고·요약만
- **D) Other**:

[Answer]: A 실제 기능은 없는 버튼까지만

## Question 8 — 진행 중 시각 표시 (D-U4-5 · INV-2)

`i01`의 `예상 15:00 도착`, `i04`의 `15:12`는 실시간 지연을 반영한 값으로 보입니다. INV-2는 "사용자 노출 시각 = 솔버 검증값만"입니다.

- **A) "예상" 라벨을 붙인 클라이언트 추정 허용** *(추천)*
  솔버 확정 시각(계획)과 시각적으로 구분(`09:30` vs `예상 15:00`)하고, 확정 시각을 덮어쓰지 않음. BR로 "추정치는 확정 일정에 저장되지 않는다"를 명시.
- **B) 서버 재계산 결과만 표시** — 지연이 감지되면 재계획을 거쳐야만 시각이 갱신됨(INV-2 엄격). 표시가 자주 굳어 보일 수 있음
- **C) 재추정 없음** — 원 계획 시각만 표시하고 지연은 배너 문구로만
- **D) Other**:

[Answer]: C
